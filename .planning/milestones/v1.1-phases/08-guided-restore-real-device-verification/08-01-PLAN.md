---
phase: 08-guided-restore-real-device-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/i18n.ts
  - "web/src/lib/locales/*.ts (40 locale modules, one same-commit pass)"
  - web/src/pages/Recovery.tsx
  - web/src/pages/Recovery.mobile.dom.test.tsx (new)
  - web/e2e/guided-restore.spec.ts (new)
  - web/e2e/desktop-untouched.spec.ts
autonomous: true
requirements: [SCRN-06, VERIFY-03, VERIFY-04]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Below 48rem, /recovery renders the mobile step flow behind !isDesktop while the desktop stepper stays mounted-hidden via max-md:hidden; at >=48rem the desktop layout is byte-identical (extended /recovery battery, both directions)."
    - "Every mobile step fires the SAME guarded handler the desktop StepCard fires (checkReadable, connectPreview, discover, downloadRecoveryKit); one fire path, no new API surface (D-02, D-12)."
    - "Steps gate in desktop order: readable -> config (optional) -> attached -> discovered -> restored -> kit; the kit step is reachable when restoreAllResult is set OR no targets were discovered (zero-target users are never stranded)."
    - "The page-flat hue counter is untouched by the mobile block: the desktop-first-8 hue indices remain 0..7, dom-asserted (six StepCards + CloudCredsDisclosure's two calls)."
    - "Empty/loading/error/overflow/long-text/unavailable-data states per 08-UI-SPEC: empty = pre-check idle StepState badge (step 1), the unfilled attach form IS the designed state (RevealInput blank + Set badge from has*), recovery.foundNone with remedy copy (discover); error = backend text verbatim + appKeyRemedy/notReachable/vmSshNote/enc* family, failed actions STAY on-step; overflow = step body scrolls in the page scroller, StickyActionBar last direct child of the PAGE_SHELL column; long-text = paths/URLs break-all + title, the stepOf chip renders short single-line tabular-nums; unavailable-data = the flow renders only what discover*/readSources return, encryption five-state family honest (unknown is not unencrypted)."
    - "Loading states per 08-UI-SPEC: recovery.encChecking (step 1), connect/discover busy (steps 3/4), kit download busy (step 6); step-body busy renders inside the step body while the chrome (chip/Back/bar) stays stable."
    - statement: "Touch-target geometry on the new chrome (Back, action rows, chip) is backstopped by Playwright bounding-box assertions; device-session feel is the D-11 checkpoint."
      verification: backstop
    - statement: "Desktop identity is backstopped by the extended desktop-untouched /recovery battery plus the dom hue-sequence assertion; desktop hue order is identity even when no desktop pixel moved."
      verification: backstop
  artifacts:
    - path: web/src/lib/i18n.ts + web/src/lib/locales/*.ts
      provides: "recovery.mobile.stepOf + common.continue bound in all 42 tables, one commit"
    - path: web/src/pages/Recovery.tsx
      provides: "D-01 double gate, MobileRecoveryFlow page-local block, chrome + gating + steps 1/3/4/6 real, step 2 skip path, step 5 empty branch"
    - path: web/src/pages/Recovery.mobile.dom.test.tsx
      provides: "hue-sequence, gating, chip-text, Back-preserves-state assertions"
    - path: web/e2e/guided-restore.spec.ts
      provides: "staged Go-JSON route fixtures, choreography + API-call parity, desktop leakage needles"
    - path: web/e2e/desktop-untouched.spec.ts
      provides: "/recovery in the dual-direction battery"
  key_links:
    - from: mobile step bodies (Recovery.tsx mobile block)
      to: existing handlers checkReadable / connectPreview / discover / downloadRecoveryKit
      via: shared component state and handlers (D-02)
      why: "Breakage here forks the restore path - the phase's core prohibition."
    - from: mobile block JSX position
      to: page-flat hueSeq/nextHue counter
      via: "mobile block calls NO nextHue(); why-comment at the site"
      why: "A stray nextHue() renumbers desktop hues - a desktop identity regression."
    - from: guided-restore.spec.ts fixtures
      to: frozen web/src/lib/api.ts request/response shapes
      via: Go-JSON field-for-field route staging (destination-vms-flash.spec.ts:55-165 template)
      why: "Parity assertions are only meaningful against the real frozen shapes."
    - from: new i18n keys
      to: 42 locale tables + parity/orphans/quality/preseed quartet
      via: single-writer pre-seed commit (07-02 pattern)
      why: "One partial table fails the quartet and orphans the key."
---

<objective>
Land the i18n pre-seed and the mobile guided-restore tracer: the D-01 double gate in Recovery.tsx with full step chrome and gating, steps 1 (readability), 3 (attach), 4 (discover) and 6 (kit) real on the existing handlers, step 2 with its sanctioned skip path, step 5 with its empty-state branch only; plus the dom test, the guided-restore tracer e2e and the extended desktop-untouched battery.

Purpose: This is the phase's tracer slice - one vertical path (phone viewport -> Recovery.tsx mobile block -> existing guarded handlers -> staged routes -> assertions) proving the double-gate architecture before the config and restore steps expand into it.
Output: Pre-seeded i18n keys, the mobile flow skeleton with four real steps, Recovery.mobile.dom.test.tsx, guided-restore.spec.ts, /recovery in the desktop-untouched battery.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-guided-restore-real-device-verification/08-CONTEXT.md
@.planning/phases/08-guided-restore-real-device-verification/08-RESEARCH.md
@.planning/phases/08-guided-restore-real-device-verification/08-PATTERNS.md
@.planning/phases/08-guided-restore-real-device-verification/08-UI-SPEC.md
@web/src/pages/Recovery.tsx
@web/src/pages/Config.tsx
@web/src/lib/i18n.ts
@web/e2e/destination-vms-flash.spec.ts
@web/e2e/desktop-untouched.spec.ts
</context>

## Constraints (binding)

- **D-10 (locked, verbatim):** "During task verification, run only the e2e specs affected by your changes (filtered `npx playwright test <spec>`); the full e2e suite is run once at the phase tail gate by the orchestrator — do not re-run it per plan." Vitest stays full-suite per plan.
- **D-12 frozen surfaces:** `web/src/app/router.tsx`, `web/src/lib/api.ts`, `web/src/lib/progress.ts`, `internal/**` are read-only. Mapping helpers (`progressKeyFor`, `verifyDomainFor` style) are copied into the consumer, never added to frozen files.
- **Zero npm installs.** Package Legitimacy Audit: none required for this phase (audit table empty per 08-RESEARCH.md); no legitimacy checkpoint.
- **Repo hygiene:** never embed real IPs, hostnames, credentials or app keys in any repo file; every non-obvious choice carries a load-bearing why-comment (house convention); every user-visible string through `t()`; no em dashes in user text; semantic tokens only in mobile regions, never raw hex on controls.
- **Windows Playwright discipline (STATE.md):** never pipe Playwright output through `tail`; on webServer teardown hang, kill orphaned node workers before re-running.

## Artifacts this phase produces

`recovery.mobile.stepOf` and `common.continue` in 42 locale tables; `MobileRecoveryFlow` (page-local function in Recovery.tsx) with step-chrome subcomponents (position chip, Back, StickyActionBar wiring), the gating predicate chain, and the step bodies for readability/attach/discover/kit; `Recovery.mobile.dom.test.tsx` (hue-sequence, gating, chip); `guided-restore.spec.ts` (staged fixtures, choreography, API parity, leakage needles); the `/recovery` entry in `desktop-untouched.spec.ts`.

<tasks>

<task type="auto">
  <name>Task 1: Pre-seed recovery.mobile.stepOf + common.continue across all 42 locale tables</name>
  <files>web/src/lib/i18n.ts, web/src/lib/locales/*.ts (all 40 modules)</files>
  <read_first>
    - web/src/lib/i18n.ts:1542-1612 (the en recovery.* block this phase reuses verbatim) and :1219-1246 (common.done/back/cancel placement pattern)
    - web/src/lib/i18n.ts:26 (localeChunks glob - the 40 modules under web/src/lib/locales/ are loaded from here)
    - web/src/lib/locales/fr.ts (module shape for the 40 files)
    - The i18n quartet tests (parity, orphans, quality, preseed) under web/src/lib/ - they fence this sweep
  </read_first>
  <action>
    Add two keys in ONE commit across all 42 tables (D-07 same-commit parity; per the 08-UI-SPEC Copywriting Contract census: exactly these two new keys for the whole phase):
    1. `recovery.mobile.stepOf` - en "Step {n} of {total}", de "Schritt {n} von {total}". Keep the placeholder form invariant ({n}, {total}) in every language; the chip renders it tabular-nums and the narrow-viewport battery asserts single-line, so no locale may reorder to more than two placeholders or add words that cannot fit 320px.
    2. `common.continue` - en "Continue", de "Weiter" - placed next to the existing common.done/back/cancel block.
    For the 40 locale modules: translate both keys using the house vocabulary (follow each module's existing renderings of "Step"/"Schritt" and "Continue"-class verbs from sibling keys); where a locale has no established term, use the standard translation. No em dashes in any user text. En and de live inline in web/src/lib/i18n.ts (en block near the recovery.* section, de block near its recovery.* mirror); the 40 modules each gain both keys. All 42 tables in the SAME commit - the parity quartet fails on any straggler. This is the 07-02 single-writer pre-seed pattern: no consumer code references the keys yet.
  </action>
  <verify>
    <automated>cd web && npx vitest run</automated>
  </verify>
  <done>Both keys resolve in en and de; the parity/orphans/quality/preseed quartet is green; no consumer code changed.</done>
  <acceptance_criteria>
    - "recovery.mobile.stepOf" and "common.continue" appear in web/src/lib/i18n.ts (en + de blocks) and in every module under web/src/lib/locales/ (40 files)
    - npx vitest run exits 0 (quartet included)
    - git diff shows only i18n.ts + locale modules touched in this task's commit
  </acceptance_criteria>
</task>

<task type="tracer">
  <name>Task 2 (tracer): Double gate + mobile step flow chrome, gating, and steps 1/3/4/6 in Recovery.tsx</name>
  <files>web/src/pages/Recovery.tsx</files>
  <read_first>
    - web/src/pages/Config.tsx:452-459 (double-gate shape + the load-bearing always-rendered comment), :1100-1235 (MobileRestoreSheet: BottomSheet, guard-chain ol at :1187-1197, configPhase states, StickyActionBar), :345 (useConfirm usage)
    - web/src/pages/Recovery.tsx:1497-1520 (default export, component state, useConfirm at :1499), :1563-1961 (handlers: checkReadable path, connectPreview, discover, restoreAll, kit download), :1973-1985 (hueSeq/nextHue trap), :1987-2014 (PAGE_SHELL root + h1 text-lg), :2022/:2093/:2236/:2395/:2431/:2563 (the six StepCards - the bodies being re-hosted), :2601-2602 (ForeignRestoreCard + confirmDialog placement)
    - web/src/lib/useMediaQuery.ts:42 (DESKTOP_QUERY - the only width authority, D-09)
    - web/src/lib/pageShell.ts:88 (PAGE_SHELL)
    - 08-UI-SPEC.md Screen Contracts section (the step structure ASCII is the layout contract)
    - 08-PATTERNS.md (Config double-gate + hue-counter + one-useConfirm-two-presentations patterns)
  </read_first>
  <action>
    Implement the D-01 double gate exactly as Config.tsx does (per D-01, D-02):
    1. Wrap the existing desktop stepper JSX in a max-md:hidden div. The desktop half stays ALWAYS rendered - do not convert to a conditional return, do not change a single desktop byte, do not reorder the desktop JSX (the hue counter depends on evaluation order). Existing effects keep firing identically because there is still exactly one component instance.
    2. Add a page-local `function MobileRecoveryFlow(...)` INSIDE Recovery.tsx (not a new component file - the mobileShellSource guard's mixed-file tailAnchor pattern keys on the in-file function), rendered under `{!isDesktop && (...)}` inside the same PAGE_SHELL root. It receives the existing component state and handlers as props or closes over them - it must NOT re-declare any state, never call api.ts directly, never add a second fire path (D-02, D-12).
    3. Chrome per the 08-UI-SPEC Screen Contract: the page h1 (recovery.pageTitle, text-lg) stays above both halves; the step header renders the position chip (t("recovery.mobile.stepOf") with n/total, accentSoft/accentText tonal chip per accent reservation 12, tabular-nums, single-line) and a Back text button (t("common.back"), minimum 44px hit area below md via padding or the Toggle max-md:after bleed pattern); the step body re-hosts the desktop card's fields scrollable in the page scroller; StickyActionBar is the last direct child of the PAGE_SHELL column holding the SAFE action: the step's primary key while incomplete (recovery.recheck / recovery.connectPreview / recovery.discover / recovery.kitDownload), t("common.continue") to advance a completed/optional step without re-firing, t("common.done") on the final step.
    4. Step gating mirrors the desktop disclosures, desktop order: 1 readable -> 2 config (optional - always skippable) -> 3 attached -> 4 discovered -> 5 restored -> 6 kit. The kit gate is `restoreAllResult != null || (containers.length === 0 && vms.length === 0)` so zero-target users reach the kit. Back navigates presentation-only: completed state is preserved (shared state is the single source). A Continue past an unmet prerequisite cannot render (gating decides, not the button).
    5. Step bodies for this tracer: step 1 re-hosts the readability check (checkReadable handler, status Badge with the StepState TEXT label + status hue, recovery.encChecking/enc* detection family); step 2 renders title, optional-badge, and the skip resolution (t("recovery.configSkip") advancing the flow, setting the same state the desktop skip sets) - the full config restore body lands in Plan 02 (a functionality gap a later step fills with no architectural change); step 3 re-hosts the attach card (SourceToggle, local/off-site input, cloud-creds cards with the existing RevealInput write-only contract: blank + Set badge from has*, blank-keeps, Clear-only-removes per D-05) and fires connectPreview; step 4 re-hosts discover (discover handler, recovery.foundCounts / recovery.foundNone); step 5 renders its empty-state branch only: foundCounts plus the zero-target empty copy (t("recovery.noneDiscovered")) - the populated restore body is Plan 03; step 6 re-hosts the kit card (recovery.kitHint + the existing kit download handler).
    6. Hue discipline (contract): the mobile block calls NO nextHue() anywhere - mobile step accents take fixed semantic tokens, and a why-comment at the mobile block's opening states that the page-flat hue counter is desktop-only by evaluation order and why the mobile block must not call it.
    7. ForeignRestoreCard stays inside the max-md:hidden wrapper (it already does) - add the why-comment recording the desktop-only decision (separate read-only-session card outside the numbered wizard path; SCRN-06 never names it; listed as a known surface in 08-UAT.md).
    8. Status rendering (D-08): StepState renders as a Badge with the statusLabel TEXT + status hue - never the bare dot, never color alone (VERIFY-05); every control below md clears the 44px floor and every affordance keeps its non-hover tap path. Backend error text verbatim; recovery.appKeyRemedy for the key-mismatch path; failed actions keep the user ON the step with the error visible in the step body (chrome stays stable). Every new string goes through t(); semantic tokens only.
    Route the "restored" gate on the existing restoreAllResult state even though Plan 03 fills the restore body - the predicate compiles now because the state already exists.
  </action>
  <verify>
    <automated>cd web && npx vitest run</automated>
  </verify>
  <done>Below md the step flow renders with chrome, gating and steps 1/3/4/6 firing the existing handlers; desktop JSX bytes are unchanged outside the max-md:hidden wrapper; the guard suite and full vitest suite pass.</done>
  <acceptance_criteria>
    - Recovery.tsx contains exactly one `{!isDesktop && (` mount for the mobile block, inside the existing PAGE_SHELL root, with the desktop half wrapped in max-md:hidden
    - No nextHue() call exists inside the mobile block (guarded by the Task 3 dom test)
    - Recovery.tsx contains no direct api.ts import usage inside MobileRecoveryFlow beyond the handlers the desktop already binds (no new fetch/putSettings call sites)
    - npx vitest run exits 0 (mobileShellSource guard + uiConventions included)
    - git diff shows desktop JSX outside the wrapper untouched
  </acceptance_criteria>
  <reversibility rating="reversible">Additive mobile block behind the existing double-gate pattern; the desktop half is untouched, so reverting is deleting the mobile block and the wrapper.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Dom test + guided-restore tracer e2e + desktop-untouched /recovery battery</name>
  <files>web/src/pages/Recovery.mobile.dom.test.tsx (new), web/e2e/guided-restore.spec.ts (new), web/e2e/desktop-untouched.spec.ts</files>
  <read_first>
    - web/e2e/destination-vms-flash.spec.ts:55-165 (Go-JSON field-for-field route fixture template: vmPayload/settingsBody shapes)
    - web/e2e/destination-settings.spec.ts:202-209 (the includeHidden:true rule - mandatory here because the desktop half is always mounted-hidden)
    - web/e2e/desktop-untouched.spec.ts:39-113 (ROUTES battery + MAQUETTE_ROUTES leakage needles), :163 (mobile-presents-at assertion)
    - web/src/lib/useMediaQuery.test.ts (jsdom matchMedia desktop-default stub pattern)
    - web/e2e/narrow-viewport.spec.ts:45-72 (bootSeededPage for locale seeding if the e2e needs a non-default locale - default en needs none)
  </read_first>
  <action>
    1. Recovery.mobile.dom.test.tsx: render Recovery under the jsdom desktop-default stub (matchMedia answers desktop, so the desktop half asserts as today) and, in separate cases, stub matchMedia to answer mobile for the flow assertions. Assert: (a) the desktop-first-8 hue sequence survives the mobile block - six StepCards + CloudCredsDisclosure's two nextHue() calls still receive indices 0..7 in order (read the computed accent class or the counter's observable output the way existing dom tests assert classes); (b) the gating chain - a fresh mount renders step 1 only; the kit step is unreachable until its gate holds; the zero-target path (staged empty discover result) reaches the kit step; (c) the position chip renders t("recovery.mobile.stepOf") with n of 6 for each position; (d) Back preserves completed step state; (e) the StickyActionBar action label per state (primary key vs common.continue vs common.done).
    2. guided-restore.spec.ts (mobile projects only for flow assertions): stage Go-JSON route fixtures field-for-field after the destination-vms-flash template for the domains the flow reads (readSources/discover/settings/runs shapes from web/src/lib/api.ts - frozen). Drive the choreography: step 1 checkReadable passes -> Continue; step 2 skip; step 3 attach + connectPreview -> Continue; step 4 discover returns the fixture set -> Continue; step 5 empty branch renders (zero-target fixture); step 6 kit visible. Assert API-call parity: the intercepted requests match the exact methods/paths/bodies the desktop flow fires (same endpoints, no new calls). Every locator uses includeHidden:true semantics per the destination-settings rule. Never assert restore outcomes - the harness has no Docker; the e2e asserts choreography and call parity only.
    3. Desktop leakage needles (desktop-1280/desktop-768 projects in the same spec): at >=48rem the mobile chrome is absent - assert the stepOf chip text and the mobile flow's unique class signature do not resolve, while the desktop stepper markers do resolve.
    4. desktop-untouched.spec.ts: add /recovery to the dual-direction battery - desktop projects assert the desktop markers present AND the mobile leakage needles absent; mobile projects assert the inverse ("mobile presents at" pattern at :163).
    5. Windows discipline: run specs filtered per D-10, never pipe output through tail.
  </action>
  <verify>
    <automated>cd web && npx vitest run && npx playwright test e2e/guided-restore.spec.ts e2e/desktop-untouched.spec.ts</automated>
  </verify>
  <done>The dom test proves hue identity, gating, chip and Back; the tracer e2e proves the zero-target path end-to-end on staged routes with API parity; the /recovery battery proves desktop identity both directions.</done>
  <acceptance_criteria>
    - Recovery.mobile.dom.test.tsx contains an explicit desktop-first-8 hue-index assertion (0..7)
    - guided-restore.spec.ts contains includeHidden:true on its page locators, staged route fixtures for readSources/discover/settings/runs shapes, and leakage-needle assertions for both desktop projects
    - desktop-untouched.spec.ts battery includes /recovery in both directions
    - npx playwright test e2e/guided-restore.spec.ts e2e/desktop-untouched.spec.ts exits 0
    - npx vitest run exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPA -> Go API | The mobile flow consumes the same frozen api.ts client; staged e2e fixtures mimic Go-JSON responses but ship no server changes |
| staged e2e routes -> assertions | Fixtures are test-local; parity assertions keep them honest against frozen shapes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | restore controls (chrome hosts their placement in steps 5; body lands in Plan 03) | high | mitigate | Server Confirmed guard chain is frozen and authoritative; client-side, restore-class controls render secondary-styled mid-screen away from the thumb-default sticky action (D-03); the ConfirmSheet gate lands with the bodies in Plan 03 |
| T-08-03 | Information Disclosure | step error rendering | medium | mitigate | Backend error text shown verbatim (pre-scrubbed server-side per house rule); never render raw client-side exceptions into the step body |
| T-08-04 | Information Disclosure | attach-step secrets (cloud creds RevealInput) | high | mitigate | Write-only contract per D-05: blank input + Set badge from has* flag, blank-on-save keeps, Clear-only-removes; never prefill or echo |
| T-08-05 | Repudiation | status Badges (StepState) | medium | mitigate | Four-status always by TEXT label + hue (WCAG 1.4.1 G14), never the bare color dot |
| T-08-SC | Tampering | npm installs | high | accept | Zero installs this phase (D-12); Package Legitimacy Audit table is empty per 08-RESEARCH.md, so no legitimacy checkpoint applies |
</threat_model>

<verification>
- `cd web && npx vitest run` green after each task (guard suite included).
- `cd web && npx playwright test e2e/guided-restore.spec.ts e2e/desktop-untouched.spec.ts` green (filtered per D-10).
- Desktop JSX outside the max-md:hidden wrapper byte-identical (git diff review).
</verification>

<success_criteria>
- The mobile step flow renders below 48rem with chrome, gating and steps 1/3/4/6 firing existing handlers; step 2 skip and step 5 empty branch behave per contract.
- Desktop identity proven both directions on /recovery; desktop hue sequence 0..7 asserted.
- Both new i18n keys live in 42 tables in one commit; quartet green.
</success_criteria>

<output>
Create .planning/phases/08-guided-restore-real-device-verification/08-01-SUMMARY.md when done
</output>
