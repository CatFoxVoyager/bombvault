---
phase: 05-mobile-shell-navigation-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/package-lock.json
  - .gitignore
  - web/playwright.config.ts
  - web/e2e/health.spec.ts
autonomous: false
requirements: [VERIFY-01]
user_setup: []

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`npx playwright test` runs green locally on all four projects (desktop-1280, desktop-768, mobile-iphone, mobile-android), each booting the compiled Go binary via webServer and passing the /api/health smoke"
    - "The @playwright/test version in web/package.json is an EXACT pin (no ^ or ~ range) so browser binaries and the npm package move in lockstep under Renovate"
    - "Playwright harness artifacts (test-results, playwright-report, .playwright-data, the built binary) can never be committed: they are gitignored"
    - "No login bypass, seeded password, or auth-weakening helper exists in the harness: a fresh database has auth disabled and the harness relies on exactly that"
  artifacts:
    - path: "web/playwright.config.ts"
      provides: "4-project Playwright config; webServer spawns the compiled Go binary (APP_KEY 64-hex, temp DATA_DIR, HTTP_ONLY=true) and polls /api/health"
      contains: "webServer"
    - path: "web/e2e/health.spec.ts"
      provides: "Per-project smoke: the server under test answers GET /api/health with ok"
      contains: "api/health"
    - path: ".gitignore"
      provides: "Ignored: web/test-results/, web/playwright-report/, .playwright-data/, /bombvault and /bombville-free binary names"
      contains: "playwright-report"
  key_links:
    - from: "web/playwright.config.ts"
      to: "cmd/bombvault (compiled binary at repo root)"
      via: "webServer.command spawns ../bombvault(.exe) with cwd=repo root, env APP_KEY/DATA_DIR/HTTP_ONLY/PORT"
      pattern: "bombvault"
    - from: "web/playwright.config.ts"
      to: "/api/health"
      via: "webServer.url poll — same endpoint the CI Docker boot smoke uses"
      pattern: "api/health"
---

<objective>
Install the ONE sanctioned new package (`@playwright/test`, devDependency, exact-pinned) and stand up the Playwright harness foundation for VERIFY-01: a 4-project config (desktop-1280, desktop-768 at the 48rem boundary, iPhone-class WebKit, Android-class Chromium) whose `webServer` boots the compiled Go binary and polls `/api/health`, proven end-to-end by a per-project health smoke test.

Purpose: VERIFY-01 says responsive regressions fail CI "from the first phase" — the harness must exist and be provably bootable before any shell code lands, so later plans extend a working gate instead of debugging plumbing and layout at once.
Output: exact-pinned devDependency + lockfile, `web/playwright.config.ts`, `web/e2e/health.spec.ts` (green on all 4 projects locally), gitignore coverage for harness artifacts.
</objective>

<artifacts_this_phase>
Symbols this phase (05) creates, for source-grounding drift exclusion:
- `web/src/lib/navModel.ts` — `NavDestination`, `destinations(settings)`, `barDestinations(settings)`, `moreDestinations(settings)`
- `web/src/lib/navModel.test.ts`, `web/src/lib/useMediaQuery.ts` (`DESKTOP_QUERY`, `useIsDesktop`), `web/src/lib/useMediaQuery.test.ts`, `web/src/lib/testSetup/matchMedia.ts`
- `web/src/components/mobile/BottomSheet.tsx` (+ `.dom.test.tsx`), `web/src/components/mobile/MoreSheet.tsx` (+ `.dom.test.tsx`), `web/src/components/mobile/BottomNav.tsx`
- `web/src/components/navGlyphs.tsx` — new `IconEllipsis` glyph; `web/src/lib/i18n.ts` + `web/src/lib/locales/*.ts` — new `nav.more` key
- `web/src/app/mobileShellSource.test.ts`; edits to `web/src/app/Layout.tsx`, `web/src/components/Sidebar.tsx`, `web/src/pages/Login.tsx`, `web/index.html`, `web/src/index.css`, `web/src/lib/theme.ts`
- testids: `desktop-sidebar`, `bottom-nav`, `more-sheet`
- `web/playwright.config.ts`, `web/e2e/health.spec.ts`, `web/e2e/desktop-untouched.spec.ts`, `web/e2e/mobile-shell.spec.ts`, `web/e2e/narrow-viewport.spec.ts`; new `playwright` job in `.github/workflows/lint.yml`
</artifacts_this_phase>

<execution_context>
@C:/Users/jerem/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/jerem/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mobile-shell-navigation-foundation/05-CONTEXT.md
@.planning/phases/05-mobile-shell-navigation-foundation/05-RESEARCH.md
@.planning/phases/05-mobile-shell-navigation-foundation/05-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package-legitimacy gate for @playwright/test</name>
  <what-built>
Research-phase Package Legitimacy Audit (05-RESEARCH.md §Package Legitimacy Audit) flags `@playwright/test` as [SUS] on the too-new-publish heuristic (weekly Tuesday release train). Verified mitigating signals, all from this session: package name is the one named by official docs (playwright.dev); source repo github.com/microsoft/playwright; ~45.3M weekly npm downloads; not deprecated; NO postinstall script (`npm view` returned none); latest publish 1.63.0 (2026-09-04). The approved UI-SPEC mandates this exact package as the milestone's single sanctioned new package (tooling-only devDependency).
  </what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/@playwright/test — confirm repository points to microsoft/playwright, weekly downloads in the tens of millions, no install-script warning.
    2. Run `npm view @playwright/test version` (or read the npm page) — note the current latest version; the install task pins THAT version exactly.
    3. Confirm you accept adding this single devDependency (zero new runtime dependencies — milestone constraint).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the exact-pinned install, or state concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install @playwright/test exact-pinned + harness config + health smoke</name>
  <files>web/package.json, web/package-lock.json, .gitignore, web/playwright.config.ts, web/e2e/health.spec.ts</files>
  <read_first>
    - web/package.json (current devDependencies + scripts — `build` is `tsc --noEmit && vite build`, `test` is `vitest run`; add nothing to scripts yet)
    - .gitignore (current tail — append a new harness section; existing entries must stay untouched)
    - .github/workflows/build.yml lines 49-64 (the CI Docker boot smoke — the verified env recipe: fresh APP_KEY, HTTP_ONLY=true, poll /api/health)
    - 05-RESEARCH.md "Pattern 3: Playwright webServer over the compiled Go binary" (the complete verified config draft this task instantiates)
    - internal/config/config.go lines 73-77 (APP_KEY must be exactly 64 lowercase hex) and cmd/bombvault/main.go line 178 (DATA_DIR writability is the only fail-fast boot check)
  </read_first>
  <action>
    1. Run `cd web && npm install -D --save-exact @playwright/test` (the `--save-exact` flag writes the version with no range — per the user's exact-pin decision; Renovate PRs future bumps so browser binaries and the npm package move in one commit). Then `npx playwright install chromium webkit` (local dev browsers; Windows is supported).
    2. Append to .gitignore a "# Playwright harness (never commit artifacts or the built binary)" section with: web/test-results/, web/playwright-report/, .playwright-data/, /bombvault, /bombvault.exe.
    3. Create web/playwright.config.ts instantiating the researched design: `testDir: "./e2e"`; global `use.baseURL = "http://127.0.0.1:3000"`; `webServer` with command `../bombvault` + (process.platform === "win32" ? ".exe" : ""), cwd = repo root (resolve via `fileURLToPath(new URL("..", import.meta.url))` — Node 24-safe, no import.meta.dirname dependency), url `http://127.0.0.1:3000/api/health`, timeout 120000, `reuseExistingServer: !process.env.CI`, gracefulShutdown default; webServer env: APP_KEY = 64 lowercase zeros (dev-only throwaway constant with a comment saying so — never a real secret), DATA_DIR = "./.playwright-data" (gitignored), HTTP_ONLY = "true" (plain HTTP instead of any TLS-bypass flag), PORT = "3000". Four projects exactly: `desktop-1280` (Desktop Chrome device, viewport 1280x720), `desktop-768` (Desktop Chrome device, viewport 768x900 — the 48rem boundary), `mobile-iphone` (`devices["iPhone 13"]` — WebKit 390x844), `mobile-android` (`devices["Pixel 5"]` spread with viewport overridden to 360x800 so isMobile/hasTouch survive). Open the file with a narrative why-comment citing the locked decisions (compiled binary over vite-preview/MSW; Chromium+WebKit; health-poll per the build.yml smoke precedent).
    4. Create web/e2e/health.spec.ts: one test, "server under test answers /api/health", using `page.request.get("/api/health")` and asserting the response is ok (HTTP 200). A header comment states this spec proves the webServer boot path on every project and doubles as the no-tests-found guard.
    5. Do NOT touch any web/src file, vitest.config.ts, or workflow files in this task — those belong to other plans.
  </action>
  <verify>
    <automated>cd web && npx playwright test</automated>
  </verify>
  <done>
    All four projects boot the compiled Go binary via webServer, the health smoke passes on each, and the run exits 0. `node -e "console.log(require('./web/package.json').devDependencies['@playwright/test'])"` prints a bare version number (no ^ or ~). Local browsers (chromium, webkit) are installed. Harness artifact paths and the built binary are gitignored.
  </done>
  <acceptance_criteria>
    - `cd web && npx playwright test` exits 0 with 4 projects x 1 passing health test
    - web/package.json devDependencies contains @playwright/test with an exact version (no caret, no tilde) and web/package-lock.json is updated in the same change
    - web/playwright.config.ts contains all four project names: desktop-1280, desktop-768, mobile-iphone, mobile-android
    - web/playwright.config.ts webServer env contains APP_KEY (64 hex chars), DATA_DIR, HTTP_ONLY="true", PORT="3000", and url ends with /api/health
    - .gitignore contains web/test-results/, web/playwright-report/, .playwright-data/, /bombvault, /bombvault.exe
    - `git status --porcelain` shows no playwright-report, test-results, .playwright-data, or bombvault binary staged or untracked-after-add
    - No file under web/src, web/vitest.config.ts, or .github/workflows is modified by this task
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/dev machine -> npm registry | `@playwright/test` (+ transitive playwright-core) enters the repo through the package manager |
| Harness -> compiled Go binary | The test harness boots the real server process with dev env (HTTP_ONLY, throwaway APP_KEY, empty data dir) |
| Harness artifacts -> public repo | Traces/videos/screenshots/data dirs could leak host context into a public repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering | npm install of @playwright/test + playwright-core | high | mitigate | Package-legitimacy `blocking-human` checkpoint before install (Task 1); exact version pin; no postinstall script (verified); Renovate manages bumps in lockstep with browsers |
| T-05-01 | Information Disclosure | web/test-results/, web/playwright-report/, .playwright-data/ | medium | mitigate | All harness artifact dirs gitignored in Task 2; fresh empty-state DB means nothing to leak; never commit them (repo rule: no real user data) |
| T-05-02 | Elevation of Privilege | harness auth handling | high | mitigate | No login bypass, no seeded password, no auto-login helper: fresh DB = auth disabled by default (internal/store/settings.go:60); harness asserts chrome/layout only |
| T-05-04 | Repudiation | TLS handling under test | low | accept | Harness uses HTTP_ONLY=true rather than ignoreHTTPSErrors; no TLS-disabling enters app code — accepted because it weakens nothing in production |
</threat_model>

<prohibitions>
Every plan in this phase carries the full recalled prohibition set (descriptor-less, flagged-unverified — never auto-dismissed, never silently dropped). Plan 01 is harness-only (no web/src, no chrome, no shell CSS): statements 1-4 and 6-8 are vacuously satisfied by this plan's file scope (package.json, lockfile, .gitignore, playwright.config.ts, e2e/health.spec.ts only); statement 5 is operative — this plan performs the phase's single sanctioned package addition and no other.
- statement: "The bottom bar is never taken out of normal document flow — it is a flex sibling of the scroller, not a fixed-position overlay" (SHELL-02)
  status: active
  verification: flagged-unverified
- statement: "The viewport-height trap unit 100vh never appears in the mobile shell; the viewport contract uses dvh/svh exclusively" (SHELL-05)
  status: active
  verification: flagged-unverified
- statement: "The FOUC inline script bytes in web/index.html are never modified" (SHELL-06)
  status: active
  verification: flagged-unverified
- statement: "The route tree is never forked — no second page tree, no route-level mobile/desktop split; web/src/app/router.tsx stays untouched" (locked v1.1 decision)
  status: active
  verification: flagged-unverified
- statement: "No UI kit, no state library, and no new runtime npm dependency; the exact-pinned @playwright/test devDependency is the sole sanctioned addition" (milestone constraint)
  status: active
  verification: flagged-unverified
- statement: "Frozen files are never modified: web/src/app/router.tsx, web/src/lib/api.ts, web/src/lib/progress.ts, and everything under internal/**" (v1.1 scope)
  status: active
  verification: flagged-unverified
- statement: "Raw hex colors are never reintroduced where semantic tokens exist (carbon-*, accent*, status*)" (house convention)
  status: active
  verification: flagged-unverified
- statement: "A settings-gated tab never appears in any mobile chrome surface when the desktop Sidebar hides it" (SHELL-03 + T-05-05)
  status: active
  verification: flagged-unverified
</prohibitions>

<verification>
- `cd web && npx playwright test` exits 0 (4 projects, health smoke green)
- `cd web && npm test` still exits 0 (vitest untouched by this plan)
- Exact pin verified in package.json; lockfile committed
- `git check-ignore web/playwright-report .playwright-data bombvault.exe` reports each ignored
</verification>

<success_criteria>
The Playwright harness is installed, configured, and PROVEN: every project boots the real Go binary and passes the health smoke locally. The legitimacy checkpoint recorded human approval for the milestone's one sanctioned package. CI wiring lands in plan 05-06 once the real specs exist (an empty suite would fail Playwright's no-tests-found guard).
</success_criteria>

<output>
Create `.planning/phases/05-mobile-shell-navigation-foundation/05-01-SUMMARY.md` when done
</output>
