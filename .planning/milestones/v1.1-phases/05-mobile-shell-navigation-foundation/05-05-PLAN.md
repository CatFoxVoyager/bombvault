---
phase: 05-mobile-shell-navigation-foundation
plan: 05
type: execute
wave: 2
depends_on: ["05-01", "05-02", "05-03", "05-04"]
files_modified:
  - web/src/components/mobile/BottomNav.tsx
  - web/src/components/mobile/MoreSheet.tsx
  - web/src/components/mobile/MoreSheet.dom.test.tsx
  - web/src/app/Layout.tsx
  - web/src/app/mobileShellSource.test.ts
  - web/e2e/mobile-shell.spec.ts
autonomous: true
requirements: [SHELL-01, SHELL-02, SHELL-03, SHELL-05, VERIFY-01]
user_setup: []

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Exactly ONE chrome surface renders at a time, switched at the single Layout switch point by useIsDesktop/DESKTOP_QUERY: at >=48rem the desktop Sidebar renders and the bottom nav appears zero times in the DOM; below it the Sidebar is absent (not CSS-hidden — not rendered, so its subscriptions and dialogs never run) and the bottom nav renders as a flex sibling"
    - "The bottom bar (SHELL-02) is a normal-flow flex sibling with data-testid=bottom-nav: exactly 5 slots (the enabled bar destinations from the registry + the More trigger), each flex-1 with a >=44px touch target, h-14 static height (svh semantics — it never resizes with the dynamic viewport) over a top hairline with bottom safe-area padding; the active slot is accent-tinted (accentText label + accentSoft icon backdrop) and resting slots use the muted text token"
    - "Tapping the ALREADY-ACTIVE bar slot scrolls the main scroller (bv-main) back to the top — tap-on-active returns to view top per SHELL-02"
    - "MoreSheet (SHELL-03) opens from the More trigger inside the BottomSheet primitive: title row (nav.more + close button with t(common.close) accessible name), destination rows derived from moreDestinations(settings) in desktop Sidebar order with 52px minimum row height, active row highlighted accentSoft+accentText, data-testid=more-sheet, every string through t()"
    - "Sign-out lives at the BOTTOM of the More sheet, visually muted (hairline separated, muted IconPower), with NO confirmation, using the exact Sidebar sign-out mechanism (logout().catch(() => undefined) then location reload) — and only renders when authEnabled, matching the desktop footer gating"
    - "Sheet behaviors hold through the mobile surface: focus trapped inside and restored to the trigger on close; closes via scrim, Escape, and close button; panel max-h-[85dvh] with internal overscroll-contain scroll and bottom safe-area padding (PRIM-01 through real content)"
    - "EMPTINESS rule: the More trigger renders only when the sheet would have content (at least one enabled non-bar destination OR sign-out availability) — an empty sheet never exists; in the shipped IA Recovery is always-on so the sheet always has at least one row, and the hidden-More case degrades the bar to 4 items"
    - "The iOS keyboard mechanism is ONE Layout-level listener set (focusin/focusout + window.visualViewport resize, guarded for environments lacking visualViewport): after a resize while an input is focused, the focused field is scrolled back into view — no per-component listeners exist anywhere"
    - "The shell root sizes with h-dvh and the landscape contract holds: the bar stays bottom-docked in both orientations (no side rail), asserted by e2e in a landscape viewport"
    - "LOADING (UI Considerations): the chrome renders the moment Layout renders — the registry is a pure synchronous function of already-loaded Settings, so no loading state exists"
    - "ERROR (UI Considerations): the chrome performs zero fetches; the settings fetch stays non-fatal in Layout (catch keeps default-gated tabs) and mobile inherits that identical degradation"
    - "POPULATED + ZERO-ONE-MANY (UI Considerations): fixed 4+1 bar grid; the variable-count surface is the sheet list (0-6 rows in sidebar order) with active states per the Interaction Contract"
    - "First e2e smoke is green on all four Playwright projects: mobile projects see the bar and zero sidebars; desktop projects see the Sidebar and zero bars"
  artifacts:
    - path: "web/src/app/Layout.tsx"
      provides: "The ONE chrome switch point: h-dvh root, useIsDesktop branch, Sidebar OR BottomNav, bv-main scroller id preserved on main, the guarded Layout-level keyboard mechanism"
      contains: "useIsDesktop"
    - path: "web/src/components/mobile/BottomNav.tsx"
      provides: "SHELL-02 bottom bar: registry-derived slots + More trigger, accent active states, tap-on-active scroll, data-testid=bottom-nav"
      contains: "bottom-nav"
    - path: "web/src/components/mobile/MoreSheet.tsx"
      provides: "SHELL-03 More sheet: registry-ordered destination rows + bottom-docked muted no-confirm sign-out, data-testid=more-sheet"
      contains: "more-sheet"
    - path: "web/src/components/mobile/MoreSheet.dom.test.tsx"
      provides: "jsdom tests: rows from the registry, authEnabled gating of the sign-out row, active-row accent, row navigation"
      contains: "MoreSheet"
    - path: "web/e2e/mobile-shell.spec.ts"
      provides: "VERIFY-01 mobile assertions: bar visible/Sidebar absent on mobile projects (inverse on desktop), sheet open/close/trap, landscape dock, expanded across Tasks 1-2"
      contains: "mobile-shell"
  key_links:
    - from: "web/src/app/Layout.tsx"
      to: "web/src/lib/useMediaQuery.ts"
      via: "useIsDesktop() drives the single chrome switch — the only breakpoint consumer"
      pattern: "useIsDesktop"
    - from: "web/src/components/mobile/BottomNav.tsx"
      to: "web/src/lib/navModel.ts"
      via: "barDestinations(settings) fills the destination slots — zero hand-typed routes"
      pattern: "barDestinations"
    - from: "web/src/components/mobile/MoreSheet.tsx"
      to: "web/src/lib/navModel.ts + web/src/components/mobile/BottomSheet.tsx"
      via: "moreDestinations(settings) rows inside the BottomSheet primitive — both Wave-1 contracts consumed"
      pattern: "moreDestinations"
    - from: "web/e2e/mobile-shell.spec.ts"
      to: "compiled Go binary via webServer (plan 01)"
      via: "Real server, real SPA build, testid-located chrome assertions"
      pattern: "playwright"
---

<objective>
The phase's tracer slice, expanded: switch Layout to the ONE responsive chrome point (h-dvh root, Sidebar-or-BottomNav), build the SHELL-02 bottom bar and the SHELL-03 More sheet on the Wave-1 contracts (registry, BottomSheet, safe areas), add the guarded Layout-level iOS keyboard mechanism — and prove the whole thing end-to-end with the first mobile e2e smoke against the real compiled binary.

Purpose: this is the first commit where a phone-width viewport gets real chrome. It composes ONLY proven pieces (plans 01-04), so the risk here is composition, not invention — and the e2e smoke proves the composition on all four projects before plan 06 hardens it into the CI gate.
Output: BottomNav.tsx, MoreSheet.tsx (+ dom tests), rewired Layout.tsx, extended mobileShellSource.test.ts, mobile-shell.spec.ts green on 4 projects.

**Flagged assumptions (spec-less edge report — surfaced, not dropped):** the 12-item fallback report resolves as follows: 3 generic SHELL-02 probes (empty/ordering/adjacency) were resolved by analysis onto the nav registry and live as truths in plan 02; the concurrency probe resolves as not-applicable (pure synchronous registry — plan 02); the remaining 9 items are generic unclassified probes ("review manually") produced against prose UI requirements — reviewed and judged non-applicable to a pure-UI phase with no data transforms, no persistence, and no API surface. They are recorded here as explicit flagged assumptions, not silently dropped.
</objective>

<artifacts_this_phase>
Symbols this phase (05) creates, for source-grounding drift exclusion:
- `web/src/lib/navModel.ts` — `NavDestination`, `destinations(settings)`, `barDestinations(settings)`, `moreDestinations(settings)`
- `web/src/lib/navModel.test.ts`, `web/src/lib/useMediaQuery.ts` (`DESKTOP_QUERY`, `useIsDesktop`), `web/src/lib/useMediaQuery.test.ts`, `web/src/lib/testSetup/matchMedia.ts`
- `web/src/components/mobile/BottomSheet.tsx` (+ `.dom.test.tsx`), `web/src/components/mobile/MoreSheet.tsx` (+ `.dom.test.tsx`), `web/src/components/mobile/BottomNav.tsx`
- `web/src/components/navGlyphs.tsx` — new `IconEllipsis` glyph; `web/src/lib/i18n.ts` + `web/src/lib/locales/*.ts` — new `nav.more` key
- `web/src/app/mobileShellSource.test.ts`; edits to `web/src/app/Layout.tsx`, `web/src/components/Sidebar.tsx`, `web/src/pages/Login.tsx`, `web/index.html`, `web/src/index.css`, `web/src/lib/theme.ts`
- testids: `desktop-sidebar`, `bottom-nav`, `more-sheet`
- `web/playwright.config.ts`, `web/e2e/health.spec.ts`, `web/e2e/desktop-untouched.spec.ts`, `web/e2e/mobile-shell.spec.ts`, `web/e2e/narrow-viewport.spec.ts`; new `playwright` job in `.github/workflows/lint.yml`
</artifacts_this_phase>

<execution_context>
@C:/Users/jerem/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/jerem/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mobile-shell-navigation-foundation/05-UI-SPEC.md
@.planning/phases/05-mobile-shell-navigation-foundation/05-PATTERNS.md
@.planning/phases/05-mobile-shell-navigation-foundation/05-CONTEXT.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Mobile shell end-to-end spine — Layout switch + BottomNav + first e2e smoke</name>
  <files>web/src/app/Layout.tsx, web/src/components/mobile/BottomNav.tsx, web/src/components/mobile/MoreSheet.tsx, web/e2e/mobile-shell.spec.ts</files>
  <read_first>
    - web/src/app/Layout.tsx lines 160-209 (the blocked LoginPage branch at 171-173; the shell root at 176 with the screen-height class; Sidebar at 177; main scroller at 190 with overflow-y-auto p-6 min-w-0; the long why-comments at 178-199 — PRESERVE them, extend where the mobile branch needs its own why)
    - web/src/lib/useMediaQuery.ts (useIsDesktop from plan 02) and web/src/lib/navModel.ts (barDestinations/moreDestinations)
    - web/src/components/mobile/BottomSheet.tsx (the primitive from plan 03) and web/src/components/navGlyphs.tsx (IconEllipsis, IconClose)
    - 05-UI-SPEC.md component inventory (BottomNav/BottomNav slots, bar geometry: h-14, flex-1 slots, hairline, safe-area padding, 24px icon over 11px caption) + Interaction Contract (active/rest states, tap-on-active)
    - web/e2e/health.spec.ts (the spec file form from plan 01) and web/playwright.config.ts (the four project names to branch on)
  </read_first>
  <action>
    Wire ONE path end-to-end: settings load -> registry -> chrome -> DOM -> e2e assertion. In Layout.tsx: swap the root's screen-height class (line 176) for h-dvh; call useIsDesktop(); when true render EXACTLY today's desktop shell (Sidebar + main, unchanged classes, comments intact); when false render the mobile shell — a flex-col variant where main (now carrying id="bv-main") keeps its flex-1 overflow-y-auto p-6 min-w-0 contract and BottomNav sits as the flex sibling BELOW it (shrink-0, normal flow). The blocked LoginPage branch stays untouched before both. Give main the bv-main id in BOTH branches (the scroll target).
    Create BottomNav.tsx (named export, banner comment citing SHELL-02 + the flex-sibling prohibition): reads barDestinations(settings) passed as a prop from Layout (Layout already holds settings and authEnabled — the chrome performs no fetches); renders a nav element with data-testid="bottom-nav" containing one flex-1 slot per destination plus the More trigger slot (IconEllipsis over the t(nav.more) caption) — five slots total; each slot is a >=44px target (h-14 bar geometry per UI-SPEC) with the 24px icon over the 11px caption; NavLink className-by-isActive drives the accent language: active = accentText label + accentSoft icon backdrop, rest = the muted text token (no status colors on controls); the top hairline uses the border token; bottom padding from var(--safe-area-bottom). Clicking an ALREADY-ACTIVE destination calls document.getElementById("bv-main")?.scrollTo with top 0 (tap-on-active-to-top). The More trigger toggles local open state rendering MoreSheet inside the BottomSheet primitive.
    Create MoreSheet.tsx in its spine form (completed in Task 2): rendered inside BottomSheet with title t(nav.more); destination rows mapped from moreDestinations(settings) — NavLink rows in sidebar order, each closing the sheet on navigate; data-testid="more-sheet" on the sheet content root; every string through t(); semantic tokens only.
    Create web/e2e/mobile-shell.spec.ts: branch assertions on test.info().project.name — for the two mobile projects: goto /dashboard, expect the element with data-testid="bottom-nav" visible, expect desktop-sidebar to have zero matches, expect bv-main present; for the two desktop projects: expect desktop-sidebar visible and bottom-nav to have zero matches. Header comment states this spec is the mobile-chrome smoke and will be expanded (sheet behaviors, landscape) in Task 2.
    This is a production-quality slice, not a prototype: real error handling on the path (the sheet state machine, guard clauses on the scroll target) and no stubs anywhere — every layer it touches is the final layer.
  </action>
  <verify>
    <automated>cd web && npm run build && npx vitest run && cd .. && go build -o bombvault ./cmd/bombvault && cd web && npx playwright test e2e/mobile-shell.spec.ts</automated>
  </verify>
  <done>
    A phone-width viewport gets the bar + sheet chrome and a >=48rem viewport gets the unchanged desktop Sidebar, proven end-to-end on all four Playwright projects against the real compiled binary; the SPA builds, the full vitest suite stays green.
  </done>
  <acceptance_criteria>
    - `npx playwright test e2e/mobile-shell.spec.ts` exits 0 across all four projects
    - on mobile projects the DOM contains exactly one element with data-testid="bottom-nav" and zero with data-testid="desktop-sidebar"; on desktop projects the inverse
    - Layout.tsx root class list contains h-dvh and no longer contains the screen-height root class it had at line 176
    - the bottom-nav element is a flex sibling of main inside the shell root (parent-child relationship in the rendered tree — normal flow, no fixed-position bar)
    - clicking the active bar slot invokes a scroll on the bv-main element (code path present; behavior exercised in Task 2 expansion)
    - BottomNav contains zero hand-typed route literals (slots come from the barDestinations prop)
    - `cd web && npx vitest run` and `cd web && npm run build` exit 0 (desktop suites unaffected)
  </acceptance_criteria>
  <reversibility rating="costly">The chrome-switch contract (testids, single switch point, bv-main id, bar-as-flex-sibling) is load-bearing for every later milestone phase (6-8 build touch surfaces on it) and for the e2e suite — changing it after this slice lands rewrites plans and specs, so the contract is set deliberately here.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MoreSheet completion — sign-out, active accents, sheet e2e expansion</name>
  <files>web/src/components/mobile/MoreSheet.tsx, web/src/components/mobile/MoreSheet.dom.test.tsx, web/e2e/mobile-shell.spec.ts</files>
  <behavior>
    - MoreSheet rows equal moreDestinations(settings) for the passed settings, in registry order (mock settings in the dom test)
    - the row whose route matches the current location carries the active accent classes (accentSoft backdrop + accentText label); resting rows use the muted token
    - the sign-out row renders only when authEnabled is true; when false it is absent (matching the desktop footer gating)
    - clicking the sign-out row calls logout from ../lib/api (mocked) and follows with a page reload — no confirmation dialog anywhere in the flow
    - clicking a destination row calls navigate and closes the sheet (onClose invoked)
    - rows have 52px minimum height (min-h class) and the sheet content root carries data-testid="more-sheet"
  </behavior>
  <read_first>
    - web/src/components/Sidebar.tsx lines 269-324 (NavItem active language: navActive + glim-active) and lines 353-357 (the signOut function to copy verbatim: logout().catch(() => undefined); then location reload — NO confirm) and the footer gating at 784 (authEnabled && SidebarSignOut)
    - web/src/components/mobile/BottomSheet.tsx (the primitive shell from plan 03: header, close, trap — MoreSheet only fills the body)
    - 05-UI-SPEC.md spacing scale (52px sheet rows) + Interaction Contract (active row accentSoft + accentText) + the sign-out rows (bottom of sheet, muted, no confirm)
    - web/e2e/mobile-shell.spec.ts from Task 1 (the file being expanded)
    - 05-CONTEXT.md > Implementation Decisions (the locked decisions on sign-out placement/muting/no-confirm and the More sheet's sidebar-order rows)
  </read_first>
  <action>
    Write MoreSheet.dom.test.tsx FIRST (jsdom docblock, RTL, vi.mock ../lib/api for logout, mock useT/i18n as in existing dom tests, react-router MemoryRoutes or the app's router wrapper pattern from existing page tests), covering the behavior list above. Then complete MoreSheet.tsx: style rows per the Interaction Contract (active detection via NavLink isActive); add the sign-out row at the BOTTOM of the row list, visually separated by a hairline and muted (muted text token + IconPower at small size), wired with the Sidebar sign-out mechanism copied verbatim (best-effort logout, then location reload — NO confirmation); gate it on the authEnabled prop threaded from Layout. Keep the sheet's destination rows exactly the moreDestinations(settings) order from Task 1.
    Expand web/e2e/mobile-shell.spec.ts for mobile projects: tap the More trigger and expect more-sheet visible; assert the visible destination rows correspond to the enabled non-bar registry entries reachable through the UI (default fresh-DB settings); close via Escape, via scrim click, and via the header close button (each path re-opens the sheet); focus-trap engagement: open the sheet, move focus into it, press Tab repeatedly and assert focus never leaves the sheet; SIGN-OUT PARITY: the harness database has auth disabled, so assert the conditional honestly — the sign-out row is absent from the More sheet AND absent from the desktop Sidebar footer on the same fresh-DB environment (parity of the authEnabled gate on both surfaces; the row's presence/behavior with auth enabled is proven by the dom test mocks, and real-device/real-auth flows are Phase 8 VERIFY-02/04 scope). Add the landscape assertion: set a landscape viewport (e.g. 844x390) and assert the bottom-nav's bounding box sits at the bottom of the page (bottom-docked in both orientations).
  </action>
  <verify>
    <automated>cd web && npx vitest run src/components/mobile/MoreSheet.dom.test.tsx && npm run build && cd .. && go build -o bombvault ./cmd/bombvault && cd web && npx playwright test e2e/mobile-shell.spec.ts</automated>
  </verify>
  <done>
    MoreSheet dom tests prove registry rows, active accents, authEnabled gating, and no-confirm sign-out; the e2e suite proves open/close (3 paths), trap engagement, landscape bottom-dock, and auth-gate parity against the real binary.
  </done>
  <acceptance_criteria>
    - `npx vitest run src/components/mobile/MoreSheet.dom.test.tsx` exits 0 with the five behavior groups present
    - `npx playwright test e2e/mobile-shell.spec.ts` exits 0 including the sheet, trap, landscape, and sign-out-parity assertions
    - MoreSheet.tsx contains the verbatim Sidebar sign-out mechanism (best-effort logout then reload) and contains no confirmation dialog
    - the sign-out row renders in a container that is the LAST row group of the sheet, separated by a hairline using the border token, with the muted text token
    - on the fresh-DB e2e environment the sign-out row is absent from both the More sheet and the desktop Sidebar footer (parity assert green)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: iOS keyboard mechanism at Layout level + source-guard extension</name>
  <files>web/src/app/Layout.tsx, web/src/app/mobileShellSource.test.ts</files>
  <read_first>
    - web/src/app/Layout.tsx (post-Task-1 state — the mobile branch and bv-main)
    - web/src/lib/theme.ts lines 78-87 (the house add/remove listener shape to imitate) and web/src/lib/testSetup/matchMedia.ts (why guards for missing APIs are the jsdom discipline — visualViewport is absent in jsdom exactly like matchMedia)
    - web/src/app/mobileShellSource.test.ts from plan 04 (the guard suite being extended — its banner and self-guard idiom)
    - 05-UI-SPEC.md Viewport & Shell Contract (interactive-widget=resizes-content from plan 04 is the precondition this mechanism complements; ONE mechanism at Layout level is the locked decision)
  </read_first>
  <action>
    Add the single keyboard-visibility mechanism INSIDE Layout (the locked decision forbids per-component listeners): in a useEffect active only while the mobile branch is rendered, attach focusin/focusout listeners on the shell root to track the currently focused text field, and — guarded by typeof window.visualViewport !== "undefined" so jsdom and old browsers no-op — a resize listener on window.visualViewport that, when a field is focused, scrolls the focused element back into view within the bv-main scroller (scrollIntoView with block nearest, deferred to the next frame so the resize has settled). Remove every listener on unmount and on branch switch (desktop never pays the cost). Document with a why-comment: this complements plan 04's interactive-widget=resizes-content + h-dvh root — the meta makes the layout viewport shrink; this mechanism guarantees the focused field remains visible inside the shrunken scroller.
    Extend web/src/app/mobileShellSource.test.ts (the file from plan 04 — same banner, same style) with the Layout-scoped asserts deferred from Wave 1: Layout.tsx contains h-dvh; contains no occurrence of the banned literals `100vh` or `min-h-screen`; contains the bv-main id; contains the visualViewport presence guard and the focusin wiring; and the chrome testids (bottom-nav, more-sheet) appear in the mobile component sources. Each assert carries the self-guard and an explanatory why-message.
    <!-- planner-discipline-allow: 100vh -->
    <!-- planner-discipline-allow: min-h-screen -->
  </action>
  <verify>
    <automated>cd web && npx vitest run src/app/mobileShellSource.test.ts && npx vitest run && npm run build</automated>
  </verify>
  <done>
    One guarded Layout-level mechanism keeps focused inputs visible under the resizes-content keyboard; jsdom suites unaffected (guard no-ops); the source-guard suite now covers the full mobile-shell contract including the Layout asserts deferred from plan 04.
  </done>
  <acceptance_criteria>
    - `npx vitest run src/app/mobileShellSource.test.ts` exits 0 with the new Layout asserts live
    - `npx vitest run` (full) exits 0 — the visualViewport guard no-ops under jsdom (no crash in Layout-rendering dom tests)
    - Layout.tsx contains exactly one focusin mechanism (grep count of focusin wiring is 1) and zero visualViewport listeners outside Layout.tsx
    - all listeners added in the effect are removed on cleanup (teardown present in the effect's return)
    - Layout.tsx contains no occurrence of `100vh` and no occurrence of `min-h-screen`
    - the mechanism is attached only while the mobile branch renders (desktop path adds no listeners)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Viewport width -> rendered chrome | The breakpoint decides which chrome renders; a switch regression alters the DESKTOP experience (the phase's hard invariant) or leaks chrome across surfaces |
| More sheet -> auth session | The sign-out row destroys the session on click; a mis-wired trigger (or a confirm-removal done wrong) could log users out unexpectedly or leave auth states inconsistent |
| Keyboard mechanism -> layout | A viewport/scroll manipulation bug can hide focused inputs — an availability (DoS-class UX) boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-10 | Tampering | Layout chrome switch | medium | mitigate | Desktop branch renders the identical pre-change tree (classes/comments preserved); e2e asserts Sidebar visible + bar absent on BOTH desktop projects from Task 1 onward, and plan 06 hardens it into a 10-route loop |
| T-05-02 | Elevation of Privilege | sign-out + auth gating | high | mitigate | No login bypass, no seeded credentials anywhere; sign-out copied verbatim from Sidebar (proven mechanism), gated by the same authEnabled flag as the desktop footer, and parity-asserted in e2e on the fresh-DB environment |
| T-05-01 | Information Disclosure | e2e traces/reports | medium | mitigate | (carried from plan 01) Harness artifacts gitignored; fresh empty-state DB has nothing to leak |
| T-05-11 | Denial of Service | keyboard scroll mechanism | low | mitigate | Mechanism only scrolls (never mutates layout or focus), is frame-deferred, guarded for missing visualViewport, and torn down on unmount/branch switch |
</threat_model>

<prohibitions>
Every plan in this phase carries the full recalled prohibition set (descriptor-less, flagged-unverified — never auto-dismissed, never silently dropped):
- statement: "The bottom bar is never taken out of normal document flow — it is a flex sibling of the scroller, not a fixed-position overlay" (SHELL-02)
  status: active
  verification: flagged-unverified
- statement: "The viewport-height trap unit 100vh never appears in the mobile shell; the viewport contract uses dvh/svh exclusively" (SHELL-05)
  status: active
  verification: flagged-unverified
- statement: "The FOUC inline script bytes in web/index.html are never modified" (SHELL-06)
  status: active
  verification: flagged-unverified
- statement: "The route tree is never forked — no second page tree, no route-level mobile/desktop split; web/src/app/router.tsx stays untouched" (locked v1.1 decision)
  status: active
  verification: flagged-unverified
- statement: "No UI kit, no state library, and no new runtime npm dependency; the exact-pinned @playwright/test devDependency is the sole sanctioned addition" (milestone constraint)
  status: active
  verification: flagged-unverified
- statement: "Frozen files are never modified: web/src/app/router.tsx, web/src/lib/api.ts, web/src/lib/progress.ts, and everything under internal/**" (v1.1 scope)
  status: active
  verification: flagged-unverified
- statement: "Raw hex colors are never reintroduced where semantic tokens exist (carbon-*, accent*, status*)" (house convention)
  status: active
  verification: flagged-unverified
- statement: "A settings-gated tab never appears in any mobile chrome surface when the desktop Sidebar hides it" (SHELL-03 + T-05-05)
  status: active
  verification: flagged-unverified
</prohibitions>

<verification>
- `cd web && npm run build && npx vitest run` exit 0
- `go build -o bombvault ./cmd/bombvault` succeeds; `cd web && npx playwright test e2e/mobile-shell.spec.ts` exits 0 on all four projects
- mobileShellSource.test.ts covers the full shell contract (root class, banned literals absent, bv-main, keyboard mechanism, testids)
</verification>

<success_criteria>
A phone-width viewport is a first-class citizen: bar + sheet chrome composed entirely from Wave-1 proven pieces, desktop provably untouched, keyboard and landscape contracts implemented at the ONE sanctioned place, and the whole spine green end-to-end against the real binary. Plan 06 only hardens and extends what this plan proved.
</success_criteria>

<output>
Create `.planning/phases/05-mobile-shell-navigation-foundation/05-05-SUMMARY.md` when done
</output>
