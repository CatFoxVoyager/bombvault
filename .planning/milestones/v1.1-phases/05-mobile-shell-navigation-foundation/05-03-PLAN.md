---
phase: 05-mobile-shell-navigation-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/i18n.ts
  - web/src/lib/locales/
  - web/src/components/navGlyphs.tsx
  - web/src/components/mobile/BottomSheet.tsx
  - web/src/components/mobile/BottomSheet.dom.test.tsx
autonomous: true
requirements: [PRIM-01, SHELL-03]
user_setup: []

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The nav.more key exists in the en and de blocks of i18n.ts AND in all 40 locale modules with exact key parity, gated by the existing i18n parity pipeline; the More trigger button and the sheet title share this single key"
    - "The ellipsis More glyph (IconEllipsis) joins the existing navGlyphs.tsx Streamline family with matching stroke style and sizing — no icon library or new asset dependency is introduced"
    - "BottomSheet portals to document.body (escaping ancestor CSS transforms), and closes via three paths: scrim click (event target === currentTarget), a document-level Escape keydown, and its header close button whose accessible name is t(common.close)"
    - "Focus is trapped inside the open sheet — document-level Tab/Shift+Tab cycling over the FOCUSABLE_SELECTOR candidate set lifted from useConfirm.tsx — and focus is restored to the element captured at open time when the sheet closes or unmounts"
    - "The sheet is a mobile primitive per the viewport contract: max-h-[85dvh] panel with internal overflow-y-auto overscroll-contain scrolling, bottom padding from the safe-area custom property, slide-up entrance behind motion-safe:, and the React 19 boolean inert attribute on the backdrop; there is no drag-to-dismiss gesture and no grabber handle"
    - "Every user-visible string in BottomSheet flows through t() from useT(); all styling uses semantic tokens (bg-carbon-*, rounded-card/rounded-control) with zero raw hex and zero hard-coded radii"
  artifacts:
    - path: "web/src/components/mobile/BottomSheet.tsx"
      provides: "PRIM-01 bottom sheet primitive: portal + scrim + focus trap/restore + Escape + safe-area/max-h contract + motion-safe slide-up; the reusable shell MoreSheet (plan 05) renders inside"
      contains: "createPortal"
    - path: "web/src/components/mobile/BottomSheet.dom.test.tsx"
      provides: "jsdom behavioral tests: portal target, all three close paths, Tab containment wrap, focus restore, close-button accessible name"
      contains: "BottomSheet"
    - path: "web/src/lib/i18n.ts"
      provides: "nav.more key in en and de blocks (the only sanctioned new user-visible string this phase)"
      contains: "nav.more"
    - path: "web/src/components/navGlyphs.tsx"
      provides: "IconEllipsis glyph in the existing Streamline family style"
      contains: "IconEllipsis"
  key_links:
    - from: "web/src/components/mobile/BottomSheet.tsx"
      to: "web/src/lib/useConfirm.tsx"
      via: "Document-level keydown + FOCUSABLE_SELECTOR trap + focus restore mechanism lifted from useConfirm.tsx:78-149 (never re-invented, never inline onKeyDown — documented broken at useConfirm.tsx:25-33)"
      pattern: "FOCUSABLE_SELECTOR"
    - from: "web/src/lib/i18n.ts"
      to: "web/src/lib/locales/*.ts"
      via: "nav.more parity across all 42 locale surfaces enforced by the existing parity test"
      pattern: "nav.more"
    - from: "plan 05 MoreSheet"
      to: "web/src/components/mobile/BottomSheet.tsx"
      via: "MoreSheet consumes the BottomSheet prop API as its shell — the API set here is the contract"
      pattern: "BottomSheet"
---

<objective>
Build PRIM-01: the bottom sheet primitive (`BottomSheet.tsx`) that lifts — not re-invents — the modal mechanism the repo already trusts (useConfirm.tsx portal/Escape/Tab-trap/focus-restore), adapted to the mobile viewport contract; plus the two shared atoms the mobile chrome needs: the `nav.more` i18n key across all 42 locale surfaces and the `IconEllipsis` glyph in the existing navGlyphs family.

Purpose: MoreSheet (plan 05) and later milestone touch-sheets (Phase 6) all render inside this primitive; a third modal family with its own trap implementation is a documented house anti-pattern. Landing the primitive in Wave 1 means plan 05 composes proven pieces instead of debugging focus management mid-slice.
Output: BottomSheet.tsx + dom tests, nav.more in en/de + 40 locales (parity-gated), IconEllipsis glyph.

Note on UI Considerations distribution: the six lifted UI-SPEC consideration rows bind to the chrome surfaces (bar slots, sheet CONTENT) and land in plans 05/06 where those surfaces are built; this plan's scope (the empty primitive shell) has no binding row from that table.
</objective>

<artifacts_this_phase>
Symbols this phase (05) creates, for source-grounding drift exclusion:
- `web/src/lib/navModel.ts` — `NavDestination`, `destinations(settings)`, `barDestinations(settings)`, `moreDestinations(settings)`
- `web/src/lib/navModel.test.ts`, `web/src/lib/useMediaQuery.ts` (`DESKTOP_QUERY`, `useIsDesktop`), `web/src/lib/useMediaQuery.test.ts`, `web/src/lib/testSetup/matchMedia.ts`
- `web/src/components/mobile/BottomSheet.tsx` (+ `.dom.test.tsx`), `web/src/components/mobile/MoreSheet.tsx` (+ `.dom.test.tsx`), `web/src/components/mobile/BottomNav.tsx`
- `web/src/components/navGlyphs.tsx` — new `IconEllipsis` glyph; `web/src/lib/i18n.ts` + `web/src/lib/locales/*.ts` — new `nav.more` key
- `web/src/app/mobileShellSource.test.ts`; edits to `web/src/app/Layout.tsx`, `web/src/components/Sidebar.tsx`, `web/src/pages/Login.tsx`, `web/index.html`, `web/src/index.css`, `web/src/lib/theme.ts`
- testids: `desktop-sidebar`, `bottom-nav`, `more-sheet`
- `web/playwright.config.ts`, `web/e2e/health.spec.ts`, `web/e2e/desktop-untouched.spec.ts`, `web/e2e/mobile-shell.spec.ts`, `web/e2e/narrow-viewport.spec.ts`; new `playwright` job in `.github/workflows/lint.yml`
</artifacts_this_phase>

<execution_context>
@C:/Users/jerem/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/jerem/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mobile-shell-navigation-foundation/05-PATTERNS.md
@.planning/phases/05-mobile-shell-navigation-foundation/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: nav.more key across all locales + IconEllipsis glyph</name>
  <files>web/src/lib/i18n.ts, web/src/lib/locales/, web/src/components/navGlyphs.tsx</files>
  <read_first>
    - web/src/lib/i18n.ts lines 60-70 (en nav.* block — where nav.more joins) and the de nav.* block (~lines 1898-1905); note nav.files is "Folders" in en and is REUSED verbatim, never re-authored
    - one representative locale module from web/src/lib/locales/ (structure + default-export convention — locale modules are the sanctioned default exports)
    - web/src/lib/i18n.parity.test.ts (the existing gate that fails on key imbalance — the proof this task is complete)
    - web/src/components/navGlyphs.tsx lines 1-56 (banner: family/style contract) and lines 129-201 (compact glyph shapes to imitate — IconPower, IconSync, IconDashboard)
  </read_first>
  <action>
    Add the nav.more key to the en block of web/src/lib/i18n.ts with value "More" and to the de block with the natural German equivalent, placed adjacent to the existing nav.* keys. Then add the same key to every module in web/src/lib/locales/ (40 files), translating "More" naturally per language (standard UI convention for a navigation overflow trigger — e.g. Mehr, Plus, Más; keep it short since the label sits under a bar icon at 11px). Match each module's existing formatting and keep every other key untouched. No em dashes in any user-visible string (lint-enforced, non-configurable). The en and de values must also serve the sheet title, so pick words that read as a section title, not just a button.
    Then add IconEllipsis to web/src/components/navGlyphs.tsx: a horizontal three-dot glyph matching the family's established stroke conventions exactly (same stroke width, viewBox, sizing pattern as its neighbors — copy the structural pattern from an existing compact glyph; it must read clearly at 24px and survive the 11px-caption bar slot). Named export, placed with the other navigation glyphs, with a one-line comment noting it serves the mobile More trigger (SHELL-02).
    Verify with the existing parity pipeline — it fails loudly if any locale missed the key.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/lib/i18n.parity.test.ts && npm run lint</automated>
  </verify>
  <done>
    nav.more resolves in en, de, and all 40 locales (parity test green); IconEllipsis exported from navGlyphs.tsx in family style; lint clean (translated-text and em-dash rules satisfied).
  </done>
  <acceptance_criteria>
    - `cd web && npx vitest run src/lib/i18n.parity.test.ts` exits 0
    - `cd web && npm run lint` exits 0
    - web/src/lib/i18n.ts contains nav.more in BOTH the en and de blocks
    - a grep across web/src/lib/locales/*.ts finds the nav.more key in every locale module (40 files)
    - web/src/components/navGlyphs.tsx exports IconEllipsis as a named export with the same structural svg attributes (viewBox, stroke convention) as its neighbor glyphs
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: BottomSheet primitive (PRIM-01)</name>
  <files>web/src/components/mobile/BottomSheet.tsx</files>
  <read_first>
    - web/src/lib/useConfirm.tsx lines 6-64 (the two-half doctrine banner: stateful mechanism lives in the hook, presentation in the component), lines 78-99 (FOCUSABLE_SELECTOR + focusableElements + focus capture/restore), lines 110-149 (document-level keydown: Escape closes, Tab/Shift+Tab cycles), lines 155-170 (createPortal to document.body and why — escapes .glim-page-enter ancestor transforms), lines 25-33 (why inline onKeyDown Escape on the panel root is documented-broken — never copy it)
    - web/src/components/ConfirmDialog.tsx lines 56-62 (strict-mode/accessible-name discipline: two identically-named controls broke Playwright strict matching — reason the sheet needs distinct naming), lines 110-121 (backdrop fixed inset-0 z-50 bg-black/60 + onClick target check; card rounded-card bg-carbon-surface max-h pattern)
    - web/src/components/Sidebar.tsx line 222 (motion-safe: house precedent)
    - 05-UI-SPEC.md component inventory + spacing scale (52px sheet rows land in plan 05; the primitive owns max-h-[85dvh], overscroll-contain, safe-area bottom padding) + Interaction Contract rows for the sheet
  </read_first>
  <action>
    Create web/src/components/mobile/BottomSheet.tsx as a named-export component opening with a narrative banner comment: it is the stateful mechanism of useConfirm.tsx re-expressed as a bottom sheet (per the repo's two-half doctrine, this is the sanctioned way modal logic propagates — a THIRD trap implementation is the anti-pattern); cite the deltas and their reasons (85dvh not vh — mobile viewport contract SHELL-05; safe-area padding; motion-safe slide-up).
    Props: open (boolean), onClose (callback), title (string, already-translated), and children (the sheet body). When open transitions to true: capture document.activeElement into a ref (the trigger); render via createPortal into document.body a fixed inset-0 z-50 scrim (bg-black/60 carrying the glim-modal-backdrop engine class per house convention) whose onClick closes only when event target === currentTarget, plus a bottom-anchored panel: bg-carbon-surface, rounded-card with top corners only, max-h-[85dvh], flex column, a header row (title + IconClose button whose aria-label is t(common.close) — distinct accessible name per the ConfirmDialog strict-mode discipline), and a body region with overflow-y-auto overscroll-contain and padding-bottom from var(--safe-area-bottom). Entrance animation: slide-up applied under motion-safe: only. React 19 boolean inert attribute on the scrim (it is non-interactive decoration).
    Focus management (document-level, lifted from useConfirm.tsx:110-149): while open, a document keydown listener closes on Escape and cycles focus on Tab/Shift+Tab across focusableElements gathered with the same FOCUSABLE_SELECTOR from the panel; on close and on unmount, restore focus to the captured trigger element. No drag-to-dismiss, no grabber handle, no body-scroll-lock (the sheet's internal scroll contains interaction; background content is unreachable via the trap). Every user-visible string through t(); semantic tokens only; named export only.
  </action>
  <verify>
    <automated>cd web && npm run build && npx tsc --noEmit</automated>
  </verify>
  <done>
    BottomSheet compiles clean with the full contract: portal, three close paths, trapped + restored focus, 85dvh/overscroll/safe-area viewport behavior, motion-safe slide-up, token-only styling. Behavioral proof lands in Task 3.
  </done>
  <acceptance_criteria>
    - `cd web && npm run build` exits 0
    - web/src/components/mobile/BottomSheet.tsx uses createPortal into document.body
    - the Escape handling is a document-level keydown listener (no inline onKeyDown Escape on the panel root — the documented-broken pattern is absent)
    - the FOCUSABLE_SELECTOR-based candidate gathering is lifted (imported or copied with a citation comment) from useConfirm.tsx, not re-derived
    - panel classes include the 85dvh max-height, overscroll-contain, and the var(--safe-area-bottom) padding; the slide-up utility is prefixed with the motion-safe variant
    - the close button's aria-label is t(common.close) and the file contains no raw hex colors and no default export
  </acceptance_criteria>
  <reversibility rating="costly">The BottomSheet prop API is the shared shell for MoreSheet (plan 05) and the Phase 6 touch surfaces — changing the signature after consumers land ripples across milestone phases, so the API is fixed deliberately here.</reversibility>
</task>

<task type="auto">
  <name>Task 3: BottomSheet.dom.test.tsx behavioral tests</name>
  <files>web/src/components/mobile/BottomSheet.dom.test.tsx</files>
  <read_first>
    - web/src/components/BottomSheet.dom.test.tsx does not exist yet — model the file on an existing .dom.test.tsx (e.g. web/src/components/Button.dom.test.tsx or ConfirmDialog.test.ts): literal line-1 jsdom environment docblock, RTL render/screen/cleanup discipline, vi.mock of ../lib/api for boundary fakes where needed
    - web/src/lib/useConfirm.tsx lines 78-149 (the behaviors being asserted: trap candidate set, Escape, Tab wrap, focus restore)
    - web/src/lib/i18n.ts (common.close key for the accessible-name assertion)
  </read_first>
  <action>
    Create web/src/components/mobile/BottomSheet.dom.test.tsx (jsdom docblock, RTL, cleanup between tests) covering: (a) when open, the panel renders in document.body via portal (not inside the test container); when closed, nothing renders; (b) Escape keydown on document calls onClose; (c) scrim mousedown/click with target === currentTarget calls onClose, while a click originating inside the panel does not; (d) the header close button is found by its accessible name (common.close via t) and clicking it calls onClose; (e) Tab containment: with two focusable elements in the body, Tab from the last wraps focus to the first, Shift+Tab from the first wraps to the last; (f) focus restore: focus a trigger element, open, close via Escape, and assert the trigger has focus again. Keep the suite self-sufficient with no dependency on plan 02's vitest setupFiles wiring: BottomSheet touches no matchMedia/visualViewport API (it renders no useMediaQuery consumer), so these tests must pass whether or not the stub module is installed — do not rely on its presence. Keep assertions behavioral (queries + events), not class-string snapshots.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/components/mobile/BottomSheet.dom.test.tsx</automated>
  </verify>
  <done>
    All PRIM-01 behaviors proven under jsdom: portal target, three close paths, Tab wrap in both directions, and focus restore to the captured trigger; suite green.
  </done>
  <acceptance_criteria>
    - `cd web && npx vitest run src/components/mobile/BottomSheet.dom.test.tsx` exits 0
    - the file's first line is the jsdom environment docblock
    - tests cover all of: portal-to-body, Escape close, scrim-vs-panel click discrimination, close-button accessible name, Tab wrap both directions, focus restore on close
    - `cd web && npx vitest run` still exits 0 (no cross-suite interference: cleanup between tests, portal nodes removed)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sheet content -> focus scope | A focus-trap regression lets keyboard/switch-control users interact with invisible background content — a correctness and accessibility boundary |
| Design tokens -> new component | A new primitive can silently reintroduce raw styling that the token system and lint rules exist to prevent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Tampering | BottomSheet styling | low | mitigate | Semantic tokens only (carbon-*/accent*, rounded-card/control); acceptance criteria grep for raw hex absence; house lint rules run in Task 1 verify |
| T-05-08 | Denial of Service (a11y) | focus trap | medium | mitigate | Trap mechanism lifted verbatim from useConfirm.tsx (battle-tested), proven bidirectionally (Tab + Shift+Tab wrap) and focus restore proven in dom tests; no inline onKeyDown (documented broken) |
| T-05-02 | Elevation of Privilege | harness auth handling | high | mitigate | (carried from plan 01) This plan adds no fetches, no auth surface, no API calls — presentation only |
</threat_model>

<prohibitions>
Every plan in this phase carries the full recalled prohibition set (descriptor-less, flagged-unverified — never auto-dismissed, never silently dropped):
- statement: "The bottom bar is never taken out of normal document flow — it is a flex sibling of the scroller, not a fixed-position overlay" (SHELL-02)
  status: active
  verification: flagged-unverified
- statement: "The viewport-height trap unit 100vh never appears in the mobile shell; the viewport contract uses dvh/svh exclusively" (SHELL-05)
  status: active
  verification: flagged-unverified
- statement: "The FOUC inline script bytes in web/index.html are never modified" (SHELL-06)
  status: active
  verification: flagged-unverified
- statement: "The route tree is never forked — no second page tree, no route-level mobile/desktop split; web/src/app/router.tsx stays untouched" (locked v1.1 decision)
  status: active
  verification: flagged-unverified
- statement: "No UI kit, no state library, and no new runtime npm dependency; the exact-pinned @playwright/test devDependency is the sole sanctioned addition" (milestone constraint)
  status: active
  verification: flagged-unverified
- statement: "Frozen files are never modified: web/src/app/router.tsx, web/src/lib/api.ts, web/src/lib/progress.ts, and everything under internal/**" (v1.1 scope)
  status: active
  verification: flagged-unverified
- statement: "Raw hex colors are never reintroduced where semantic tokens exist (carbon-*, accent*, status*)" (house convention)
  status: active
  verification: flagged-unverified
- statement: "A settings-gated tab never appears in any mobile chrome surface when the desktop Sidebar hides it" (SHELL-03 + T-05-05)
  status: active
  verification: flagged-unverified
</prohibitions>

<verification>
- `cd web && npx vitest run src/components/mobile/BottomSheet.dom.test.tsx` exits 0 (all PRIM-01 behaviors)
- `cd web && npx vitest run src/lib/i18n.parity.test.ts` exits 0 (nav.more parity across 42 surfaces)
- `cd web && npm run build && npm run lint` exit 0
</verification>

<success_criteria>
A proven, token-clean bottom-sheet primitive exists with lifted (not re-invented) focus mechanics; the More label and glyph exist everywhere they are needed; plan 05 can compose MoreSheet purely from finished pieces.
</success_criteria>

<output>
Create `.planning/phases/05-mobile-shell-navigation-foundation/05-03-SUMMARY.md` when done
</output>
