---
phase: 05-mobile-shell-navigation-foundation
plan: 06
type: execute
wave: 3
depends_on: ["05-05"]
files_modified:
  - web/e2e/desktop-untouched.spec.ts
  - web/e2e/mobile-shell.spec.ts
  - web/e2e/narrow-viewport.spec.ts
  - .github/workflows/lint.yml
  - web/dist
autonomous: true
requirements: [SHELL-02, SHELL-03, VERIFY-01]
user_setup: []

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "DESKTOP-UNTOUCHED (VERIFY-01 success criterion 4, per-page): for EVERY one of the 10 routed destinations, on BOTH desktop projects (768px boundary and 1280px), the desktop Sidebar is visible, the bottom nav has zero matches in the DOM, and the main scroller (bv-main) is present — parameterized as a 10-route loop, not spot checks"
    - "Mobile exactness: on both mobile projects the bar renders with EXACTLY 5 slots (the enabled bar destinations + More) and the desktop Sidebar has zero matches"
    - "The narrow-viewport backstop is operationalized: de AND fr at BOTH 320px and 360px, every bar slot label stays on a single line and never clips (the min-w-0 + truncate CSS contract), with no horizontal overflow of the bar"
    - "CI gate: a new playwright job in .github/workflows/lint.yml runs the full e2e suite (4 projects, Chromium + WebKit) on every push/PR alongside the go/web/test jobs — npm run build BEFORE go build (the binary embeds web/dist; a stale build embeds a stale SPA), npx playwright install --with-deps chromium webkit, and the same digest-pinned actions as its sibling jobs"
    - "The regression gate is armed from this phase on: any desktop-layout or mobile-shell assertion failure fails CI (VERIFY-01)"
    - "web/dist is rebuilt and committed at phase close (the house embed gate: commit web/dist after web changes) — closing the phase gate"
  truths_backstop:
    - statement: "Long-text labels (de/fr) at 320/360px never wrap to two lines and never clip, in the bar and in the sheet, with overflow handled by the min-w-0 + truncate CSS contract"
      verification: backstop
  artifacts:
    - path: "web/e2e/desktop-untouched.spec.ts"
      provides: "The 10-route parameterized desktop-invariance loop across both desktop projects — VERIFY-01's core deliverable"
      contains: "desktop-sidebar"
    - path: "web/e2e/narrow-viewport.spec.ts"
      provides: "de/fr x 320/360px single-line label assertions — the UI-SPEC long-text backstop made executable"
      contains: "narrow-viewport"
    - path: ".github/workflows/lint.yml"
      provides: "The playwright CI job (build-first, --with-deps browsers, 4 projects) — the armed regression gate"
      contains: "playwright"
    - path: "web/e2e/mobile-shell.spec.ts"
      provides: "Expanded with the exactly-5-slots assertion completing the mobile contract"
      contains: "exactly"
  key_links:
    - from: ".github/workflows/lint.yml playwright job"
      to: "web/playwright.config.ts webServer (plan 01)"
      via: "CI sets CI=true so reuseExistingServer is false and the job boots its own compiled binary"
      pattern: "reuseExistingServer"
    - from: "lint.yml build step order"
      to: "web/dist embed"
      via: "npm run build strictly before go build — the stale-embed pitfall documented in CLAUDE.md"
      pattern: "npm run build"
    - from: "web/e2e/narrow-viewport.spec.ts"
      to: "web/src/lib/i18n.ts locale persistence"
      via: "addInitScript seeds the persisted-locale key to de/fr (the chrome has no language control this phase — SidebarControls is desktop-only)"
      pattern: "addInitScript"
---

<objective>
Complete VERIFY-01 and arm the phase's regression gate: the parameterized 10-route desktop-untouched loop (the phase's headline guarantee that desktop is untouched, per route, on both desktop projects), the exactly-5-slots mobile exactness check, the de/fr narrow-viewport backstop made executable, the `playwright` CI job inside the existing lint.yml — and the phase-closing web/dist rebuild + commit.

Purpose: VERIFY-01 requires responsive regressions to fail CI from the first phase. Everything before this plan was capability; this plan is the gate.
Output: desktop-untouched.spec.ts, expanded mobile-shell.spec.ts, narrow-viewport.spec.ts, lint.yml playwright job, committed web/dist.

Note on UI Considerations: this plan operationalizes the single backstop row (de/fr long-text at 320/360px) as a `{verification: backstop}` truth; the five covered rows are lifted in plan 05's must_haves where the chrome surfaces live.
</objective>

<artifacts_this_phase>
Symbols this phase (05) creates, for source-grounding drift exclusion:
- `web/src/lib/navModel.ts` — `NavDestination`, `destinations(settings)`, `barDestinations(settings)`, `moreDestinations(settings)`
- `web/src/lib/navModel.test.ts`, `web/src/lib/useMediaQuery.ts` (`DESKTOP_QUERY`, `useIsDesktop`), `web/src/lib/useMediaQuery.test.ts`, `web/src/lib/testSetup/matchMedia.ts`
- `web/src/components/mobile/BottomSheet.tsx` (+ `.dom.test.tsx`), `web/src/components/mobile/MoreSheet.tsx` (+ `.dom.test.tsx`), `web/src/components/mobile/BottomNav.tsx`
- `web/src/components/navGlyphs.tsx` — new `IconEllipsis` glyph; `web/src/lib/i18n.ts` + `web/src/lib/locales/*.ts` — new `nav.more` key
- `web/src/app/mobileShellSource.test.ts`; edits to `web/src/app/Layout.tsx`, `web/src/components/Sidebar.tsx`, `web/src/pages/Login.tsx`, `web/index.html`, `web/src/index.css`, `web/src/lib/theme.ts`
- testids: `desktop-sidebar`, `bottom-nav`, `more-sheet`
- `web/playwright.config.ts`, `web/e2e/health.spec.ts`, `web/e2e/desktop-untouched.spec.ts`, `web/e2e/mobile-shell.spec.ts`, `web/e2e/narrow-viewport.spec.ts`; new `playwright` job in `.github/workflows/lint.yml`
</artifacts_this_phase>

<execution_context>
@C:/Users/jerem/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/jerem/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mobile-shell-navigation-foundation/05-UI-SPEC.md
@.planning/phases/05-mobile-shell-navigation-foundation/05-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: desktop-untouched.spec.ts (10-route loop) + exactly-5-slots mobile assertion</name>
  <files>web/e2e/desktop-untouched.spec.ts, web/e2e/mobile-shell.spec.ts</files>
  <read_first>
    - web/src/app/router.tsx (READ ONLY — frozen: the 10 canonical route paths to parameterize: dashboard, recovery, containers, vms, flash, files, config, receiver, fleet, settings)
    - web/e2e/mobile-shell.spec.ts (Task 1/2 of plan 05 — the project-branching pattern and existing assertions; desktop-untouched reuses the locators)
    - .planning/phases/05-mobile-shell-navigation-foundation/05-UI-SPEC.md Verification Harness table (the desktop-untouched contract row: per-route Sidebar visible + BottomNav absent + main scroller present)
    - internal/api/spa.go (READ ONLY — confirms the SPA fallback serves index.html for client routes, so direct page.goto to any route renders the app)
  </read_first>
  <action>
    Create web/e2e/desktop-untouched.spec.ts with a parameterized loop over all 10 route paths, running on BOTH desktop projects (restrict via the project name check as in mobile-shell.spec.ts). For each route x project: page.goto the route (the Go SPA fallback serves the app for client routes), then assert data-testid="desktop-sidebar" is visible, data-testid="bottom-nav" has zero matches, and the bv-main element is present. Header comment cites VERIFY-01 success criterion 4 (per-page desktop invariance) and notes the assertions deliberately run on fresh-DB empty states — chrome/layout only, never data. Add the mobile exactness assertion to web/e2e/mobile-shell.spec.ts: on both mobile projects, the bar element with data-testid="bottom-nav" contains exactly 5 slot elements (the count of top-level slots), tightening plan 05's "bar visible" smoke into the SHELL-02 contract.
  </action>
  <verify>
    <automated>cd web && npx playwright test e2e/desktop-untouched.spec.ts && npx playwright test e2e/mobile-shell.spec.ts</automated>
  </verify>
  <done>
    20 route-project combinations prove desktop invariance; mobile projects prove exactly 5 slots; both specs green against the real binary.
  </done>
  <acceptance_criteria>
    - `npx playwright test e2e/desktop-untouched.spec.ts` exits 0 with 10 routes x 2 desktop projects reported
    - desktop-untouched.spec.ts contains the loop over all 10 route paths and three assertions per combination (sidebar visible, bar absent, scroller present)
    - `npx playwright test e2e/mobile-shell.spec.ts` exits 0 including the exactly-5-slots assertion
    - web/src/app/router.tsx shows no modifications in git status (frozen file)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: narrow-viewport.spec.ts — de/fr labels at 320/360px (the backstop)</name>
  <files>web/e2e/narrow-viewport.spec.ts</files>
  <read_first>
    - web/src/lib/i18n.ts (how the locale is persisted — the localStorage key and valid locale codes; the narrow spec seeds this key because the mobile chrome has no language control this phase)
    - 05-UI-SPEC.md UI Considerations long-text row (the backstop contract: never wrap to two lines, never clip, min-w-0 + truncate CSS contract) and typography rows (11px captions)
    - web/e2e/mobile-shell.spec.ts (project selection + testid locators to reuse)
  </read_first>
  <action>
    Create web/e2e/narrow-viewport.spec.ts running on the mobile projects only, parameterized over locales de and fr and viewport widths 320 and 360 (four combinations minimum): seed the persisted-locale key via a fresh context's addInitScript BEFORE page load, set the viewport, goto /dashboard, then assert the long-text contract: for every slot caption inside data-testid="bottom-nav", the caption element's rendered height stays within a single line box (no two-line wrap — de compound words and fr accented long labels are the stress cases) and the bar container shows no horizontal overflow (scrollWidth within clientWidth); additionally open the More sheet and assert its rows stay single-line at the same widths. Use CSS-contract-respecting assertions (geometry via bounding boxes, not screenshot diffs) so the test stays fast and non-flaky; a header comment marks this spec as the operationalized UI-SPEC backstop (de/fr at 320/360px). Keep the assertions tolerant of subpixel rounding but strict about the two-line failure mode.
  </action>
  <verify>
    <automated>cd web && npx playwright test e2e/narrow-viewport.spec.ts</automated>
  </verify>
  <done>
    All de/fr x 320/360 combinations pass: labels single-line, no clipping, no bar overflow, sheet rows single-line — the backstop is now executable evidence.
  </done>
  <acceptance_criteria>
    - `npx playwright test e2e/narrow-viewport.spec.ts` exits 0 covering de and fr at 320px and 360px
    - the spec seeds the locale via addInitScript before load (no UI-driven language switch — the mobile chrome has none this phase)
    - assertions are geometric (bounding box / scroll width), each targeting the single-line and no-overflow contract, with the two-line failure mode explicitly asserted against
    - the More sheet rows are included in the single-line assertion set
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: lint.yml playwright CI job + full local gate + web/dist phase-close commit</name>
  <files>.github/workflows/lint.yml, web/dist</files>
  <read_first>
    - .github/workflows/lint.yml lines 15-41 (triggers block; the go job's setup-go pin at 24-25; the web job skeleton at 30-41: digest-pinned checkout/setup-node with # v7 comments, defaults working-directory web, npm ci sequence — the new job clones this skeleton)
    - .github/workflows/build.yml lines 49-64 (the health-poll env recipe the webServer config already instantiates)
    - web/playwright.config.ts (webServer command ../bombvault at repo root; reuseExistingServer: !process.env.CI — CI boots its own binary)
    - CLAUDE.md build section (npm ci && npm run build BEFORE go build after any web/ change — the stale-embed pitfall; commit web/dist)
  </read_first>
  <action>
    Add a playwright job to .github/workflows/lint.yml, parallel to the go/web/test jobs: same trigger context; ubuntu runner; timeout-minutes 20; the SAME digest-pinned actions as its siblings (checkout, setup-go with go-version '1.26', setup-node with node-version '24' — copy the pins verbatim from the existing jobs); defaults working-directory web; steps in this exact order: npm ci; npm run build (BEFORE go build — the stale-embed pitfall); build the binary at the repo root with output name bombvault (a step-level working-directory override or cd, matching the webServer command's ../bombvault expectation); npx playwright install --with-deps chromium webkit; npx playwright test. Header comment: this is the VERIFY-01 regression gate armed in phase 5; the job intentionally lands AFTER the specs exist because a zero-spec Playwright run fails its no-tests-found guard. Do not modify the existing jobs or the triggers block.
    Then close the phase gate locally: run the FULL suite in order (cd web && npm test && npm run build; go build -o bombvault ./cmd/bombvault at repo root; cd web && npx playwright test) and confirm everything is green; finally commit the rebuilt web/dist per the house rule (the SPA is embedded in the binary; dist must never go stale relative to src). The harness artifacts (test-results, playwright-report, .playwright-data, the built binary) stay uncommitted — they are gitignored since plan 01.
  </action>
  <verify>
    <automated>cd web && npm test && npm run build && cd .. && go build -o bombvault ./cmd/bombvault && cd web && npx playwright test</automated>
  </verify>
  <done>
    lint.yml carries the playwright job with sibling-identical pins and build-before-go ordering; the full local gate (vitest + tsc/vite build + go build + all e2e specs on 4 projects) is green; web/dist rebuilt and committed; no harness artifacts staged.
  </done>
  <acceptance_criteria>
    - `cd web && npx playwright test` exits 0 (health, desktop-untouched, mobile-shell, narrow-viewport across 4 projects)
    - .github/workflows/lint.yml contains a playwright job whose steps run npm run build BEFORE the go build step, and whose actions use the same digests as the existing jobs in the same file
    - the playwright job installs browsers with --with-deps chromium webkit and ends with npx playwright test
    - the existing go/web/test jobs and the triggers block are unchanged (diff confined to the new job)
    - web/dist is rebuilt (content matches the current src build) and included in the phase-close commit
    - `git status --porcelain` shows no test-results, playwright-report, .playwright-data, or bombvault binary entries
    - YAML parses cleanly (the workflow loads — verified by a YAML parse check or the CI syntax view)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner -> GitHub Actions | New CI job executes on every push/PR; action references must be digest-pinned like their siblings (supply-chain boundary) |
| CI job -> npm/Playwright browsers | `npx playwright install --with-deps` downloads browser builds at run time |
| Harness env -> logs | The webServer env (throwaway APP_KEY, DATA_DIR) must never leak real secrets into CI logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering | new CI job's action references | high | mitigate | Digest pins copied verbatim from the sibling jobs in the same file (never @version tags); acceptance criterion asserts digest parity; Renovate manages future bumps |
| T-05-01 | Information Disclosure | CI artifacts + harness env | medium | mitigate | Reports/traces stay gitignored and are not uploaded anywhere; the webServer APP_KEY is the documented throwaway dev constant — no real secret ever enters CI env or logs |
| T-05-04 | Repudiation | TLS handling under test | low | accept | (carried from plan 01) HTTP_ONLY=true instead of any TLS-bypass flag; weakens nothing in production |
| T-05-02 | Elevation of Privilege | harness auth handling | high | mitigate | (carried) The gate asserts chrome/layout only on a fresh auth-disabled DB; no bypass, no seeded credentials anywhere in CI or specs |
</threat_model>

<prohibitions>
Every plan in this phase carries the full recalled prohibition set (descriptor-less, flagged-unverified — never auto-dismissed, never silently dropped):
- statement: "The bottom bar is never taken out of normal document flow — it is a flex sibling of the scroller, not a fixed-position overlay" (SHELL-02)
  status: active
  verification: flagged-unverified
- statement: "The viewport-height trap unit 100vh never appears in the mobile shell; the viewport contract uses dvh/svh exclusively" (SHELL-05)
  status: active
  verification: flagged-unverified
- statement: "The FOUC inline script bytes in web/index.html are never modified" (SHELL-06)
  status: active
  verification: flagged-unverified
- statement: "The route tree is never forked — no second page tree, no route-level mobile/desktop split; web/src/app/router.tsx stays untouched" (locked v1.1 decision)
  status: active
  verification: flagged-unverified
- statement: "No UI kit, no state library, and no new runtime npm dependency; the exact-pinned @playwright/test devDependency is the sole sanctioned addition" (milestone constraint)
  status: active
  verification: flagged-unverified
- statement: "Frozen files are never modified: web/src/app/router.tsx, web/src/lib/api.ts, web/src/lib/progress.ts, and everything under internal/**" (v1.1 scope)
  status: active
  verification: flagged-unverified
- statement: "Raw hex colors are never reintroduced where semantic tokens exist (carbon-*, accent*, status*)" (house convention)
  status: active
  verification: flagged-unverified
- statement: "A settings-gated tab never appears in any mobile chrome surface when the desktop Sidebar hides it" (SHELL-03 + T-05-05)
  status: active
  verification: flagged-unverified
</prohibitions>

<verification>
- `cd web && npx playwright test` exits 0 — all four specs on all four projects (the VERIFY-01 gate, locally proven before CI arms it)
- `cd web && npm test && npm run build` and `go build ./cmd/bombvault` exit 0
- lint.yml diff confined to the new playwright job; digest parity with siblings holds
- web/dist committed at phase close; zero harness artifacts tracked
</verification>

<success_criteria>
VERIFY-01 is fully armed: desktop untouched is proven per-route on both desktop projects, mobile exactness is proven, the de/fr backstop is executable, and every future push/PR runs the whole gate in CI from this phase on. The phase closes with a fresh committed web/dist and a green full-suite run.
</success_criteria>

<output>
Create `.planning/phases/05-mobile-shell-navigation-foundation/05-06-SUMMARY.md` when done
</output>
