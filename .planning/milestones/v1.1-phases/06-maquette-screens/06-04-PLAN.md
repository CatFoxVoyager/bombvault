---
phase: 06-maquette-screens
plan: 04
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - web/src/lib/runDisplay.ts
  - web/src/pages/Dashboard.tsx
  - web/src/components/mobile/RunDetailSheet.tsx
  - web/src/components/mobile/RunDetailSheet.dom.test.tsx
  - web/src/lib/useVisibilityGate.ts
  - web/src/lib/useVisibilityGate.test.ts
  - web/src/lib/backupWatch.ts
  - web/src/lib/i18n.ts
  - web/src/lib/locales/
  - web/e2e/run-detail-visibility.spec.ts
autonomous: true
requirements: [SCRN-05, PRIM-04]
user_setup: []

estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The run presentation helpers (runKindLabel, runDomainLabel, isDomainOpRunKind, runTargetText, statusTone, statusLabel) move verbatim from Dashboard.tsx (lines 49-112, 340, 387-406) into the new web/src/lib/runDisplay.ts; Dashboard imports them from there — a behavior-identical refactor respecting the pages->components->lib layering (components never import from pages), with all existing tests green"
    - "RunDetailSheet renders inside the 06-02 BottomSheet fullHeight variant: status headline via statusLabel/statusTone + completion time formatTs(finishedAt); three tabular-numeral stat tiles fed ONLY by fields that exist on the frozen Run record — Data volume humanBytes(run.bytes), Duration formatDuration(finishedAt - startedAt) (reuse the existing dashboard.duration key), Snapshot run.snapshotId.slice(0, 8) in font-mono with the full id as title"
    - "The activity log renders the EXISTING buildLogLines output for the run with formatLogDate mono timestamps (the ActivityLog.tsx:385-390 font-mono text-xs tabular-nums pattern verbatim — never a second log style); the per-file new/changed/unchanged triad and per-file exclusion-reason lines are NOT built (no source on the frozen Run record) — the UI-SPEC Frozen-API Data Adaptations substitution is the binding contract and is restated as the recorded deviation vs SCRN-05's verbatim wording"
    - "Verify integrity renders as a >=44px tonal touch row backed by the existing checkDomain (api.ts:1576); browse snapshot files renders as a >=44px touch row hosting the existing SnapshotFileTree; the restore entry point is reachable in <=2 taps, rendered secondary/tonal (never accent), positioned above the footer rows"
    - "The sheet's 06-02 footer slot carries the browse + verify action rows (bg-carbon-sidebar chrome language, safe-area padded); the title composes from EXISTING keys via runKindLabel + runTargetText — no new title key"
    - "useVisibilityGate() (web/src/lib/useVisibilityGate.ts) returns a visibilitychange-driven boolean with a windowless-safe default; while hidden, live-progress consumers unmount (the frozen progress.ts ref-count drops the shared EventSource to zero, so the reconnect replays the server snapshot) and useBackupWatch's POLL_INTERVAL_MS setTimeout chain stops; on visible, listRuns refetch happens FIRST and the baseline-run-id correlation (backupWatch.ts:226-236, seeded before firing) reconciles a run that finished in the background — never the client clock"
    - "web/src/lib/progress.ts is untouched (frozen); the gate is a consumer-side hook around it (D-10)"
  artifacts:
    - path: "web/src/lib/runDisplay.ts"
      provides: "shared run presentation helpers extracted from Dashboard.tsx — consumed by Dashboard AND RunDetailSheet (no duplication, D-05)"
      contains: "runKindLabel"
    - path: "web/src/components/mobile/RunDetailSheet.tsx"
      provides: "SCRN-05 full-screen run detail: headline, stat triad, activity log, verify/browse rows, restore entry, footer slot, live section"
      contains: "RunDetailSheet"
    - path: "web/src/components/mobile/RunDetailSheet.dom.test.tsx"
      provides: "jsdom tests: tiles render humanBytes/duration/mono snapshot slice, log lines via buildLogLines, footer rows, restore entry secondary styling"
      contains: "RunDetailSheet"
    - path: "web/src/lib/useVisibilityGate.ts"
      provides: "PRIM-04 visibility gate hook (D-10) — hidden pauses SSE consumers + timers; visible refetches then resubscribes"
      contains: "useVisibilityGate"
    - path: "web/src/lib/useVisibilityGate.test.ts"
      provides: "jsdom visibilitychange tests: hidden flips the gate, visible flips it back, windowless default is visible"
      contains: "visibilitychange"
    - path: "web/src/lib/backupWatch.ts"
      provides: "visibility-gated poll timers: the POLL_INTERVAL_MS chain pauses while hidden and resumes on visible with a fresh listRuns reconcile"
      contains: "POLL_INTERVAL_MS"
    - path: "web/src/lib/i18n.ts"
      provides: "run.statVolume + run.statSnapshot keys in en + de + all 40 locale modules (Duration reuses dashboard.duration); parity + orphans green"
      contains: "run.statVolume"
    - path: "web/e2e/run-detail-visibility.spec.ts"
      provides: "Playwright: run-detail sheet opens from a run row; backgrounded-page reconcile via shadowed visibilityState + dispatched visibilitychange"
      contains: "visibilitychange"
  key_links:
    - from: "web/src/pages/Dashboard.tsx"
      to: "web/src/lib/runDisplay.ts"
      via: "verbatim helper extraction — Dashboard and RunDetailSheet share ONE implementation (D-05 no-duplication mandate)"
      pattern: "runDisplay"
    - from: "web/src/components/mobile/RunDetailSheet.tsx"
      to: "web/src/components/mobile/BottomSheet.tsx (fullHeight + footer from plan 06-02)"
      via: "full-screen sheet shell with the footer action slot; three close paths + focus trap inherited"
      pattern: "fullHeight"
    - from: "web/src/lib/useVisibilityGate.ts"
      to: "web/src/lib/progress.ts (FROZEN) + web/src/lib/backupWatch.ts"
      via: "conditional render drops the ref-counted EventSource to zero at the frozen singleton; the gate ALSO stops backupWatch poll timers; reconcile = listRuns refetch + baseline-id match"
      pattern: "useVisibilityGate"
    - from: "web/src/lib/backupWatch.ts"
      to: "web/src/lib/api.ts listRuns (FROZEN api)"
      via: "reconciliation correctness from server state refetch, never client clock (house rule)"
      pattern: "listRuns"
  prohibitions:
    - statement: "web/src/lib/progress.ts, web/src/lib/api.ts, web/src/app/router.tsx, and internal/** are FROZEN — zero modifications (milestone contract)"
      status: enforced
      verification: explicit
    - statement: "No fabricated data: per-file new/changed/unchanged counts and per-file exclusion lines are never invented — the sanctioned substitutes are the only rendering (repo honesty culture)"
      status: enforced
      verification: backstop
    - statement: "Backup outcomes are never read from the POST response — reconciliation only via useBackupWatch baseline-id correlation over listRuns server state"
      status: enforced
      verification: backstop
    - statement: "No new npm dependencies and no second log style or second formatter (humanBytes/formatDuration/buildLogLines/formatLogDate reused verbatim)"
      status: enforced
      verification: backstop

---

<objective>
Ship SCRN-05 as the D-05 full-screen run-detail BottomSheet — extracting the run presentation helpers from Dashboard into a shared lib (no duplication), rendering the honest Frozen-API substitutes (Data volume / Duration / Snapshot tiles + buildLogLines activity log), with verify-integrity, browse-snapshot-files, and a reachable secondary restore entry — and ship PRIM-04 as the useVisibilityGate hook that pauses SSE consumers and poll timers when the page hides and reconciles from server state on return (D-10).

Purpose: the run sheet is the deep-link target of every backup trigger in the phase (06-05 container/file-set surfaces, 06-06 Home), so it must exist before those plans run; the visibility gate closes the background-tab staleness pitfall the milestone research flagged (throttled timers, stale SSE) without touching the frozen progress singleton.

Output: lib/runDisplay.ts, RunDetailSheet + dom tests, useVisibilityGate + tests, gated backupWatch timers, visibility e2e, run.statVolume/run.statSnapshot i18n keys.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-maquette-screens/06-CONTEXT.md
@.planning/phases/06-maquette-screens/06-RESEARCH.md
@.planning/phases/06-maquette-screens/06-PATTERNS.md
@.planning/phases/06-maquette-screens/06-UI-SPEC.md
@web/src/pages/Dashboard.tsx
@web/src/lib/activityLog.ts
@web/src/lib/backupWatch.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract run presentation helpers into lib/runDisplay.ts (pure refactor)</name>
  <files>web/src/lib/runDisplay.ts, web/src/pages/Dashboard.tsx</files>
  <read_first>
    web/src/pages/Dashboard.tsx lines 49-112 (runDomainLabel 49-64, runKindLabel 66-92, isDomainOpRunKind 97-99, runTargetText 107-112), line 340 (statusTone), lines 387-406 (statusLabel), and the import block at lines 14-24 (reltime + forecast formatters)
    web/src/components/Badge.tsx line 445 (BadgeTone type — statusTone returns it)
    web/src/lib/reltime.ts + web/src/lib/forecast.ts (the formatters the helpers take as parameters or that stay in Dashboard)
    .planning/phases/06-maquette-screens/06-PATTERNS.md section 5 (the extraction boundary — OQ-1 resolution)
  </read_first>
  <action>
    Move the six helpers VERBATIM into web/src/lib/runDisplay.ts as named exports, preserving their t-based signatures (standard alias type T = ReturnType<typeof useT>["t"] for the translate parameter), their comments, and their exact key usage (run.kindBackup / run.kindRestore / etc. — no new key). statusTone/statusLabel move too (Dashboard-local today; Badge exports the BadgeTone type they return/consume). Dashboard.tsx then imports them from ../lib/runDisplay and deletes the local copies — zero behavior change, zero copy change. This resolves research OQ-1: desktop has NO dedicated run-detail component today (RunsCard rows + ActivityLog own the content), so the extraction seam is the helper layer, and RunDetailSheet (Task 2) becomes the shared-content consumer. Keep the layering law: lib imports nothing from pages or components.
  </action>
  <verify>
    <automated>cd web && npx vitest run && npx eslint src/lib/runDisplay.ts src/pages/Dashboard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - web/src/lib/runDisplay.ts exports runKindLabel, runDomainLabel, isDomainOpRunKind, runTargetText, statusTone, statusLabel
    - Dashboard.tsx no longer defines them locally and imports from ../lib/runDisplay
    - Full vitest suite green (pure refactor — no test edits needed)
    - No string or key changes (existing run.* keys only)
  </acceptance_criteria>
  <done>Run presentation lives in one shared lib module; Dashboard renders identically; the sheet can consume the same helpers without a page-layer import.</done>
</task>

<task type="auto">
  <name>Task 2: RunDetailSheet — full-screen sheet with honest stat tiles, activity log, verify/browse/restore rows</name>
  <files>web/src/components/mobile/RunDetailSheet.tsx, web/src/components/mobile/RunDetailSheet.dom.test.tsx, web/src/lib/i18n.ts, web/src/lib/locales/</files>
  <read_first>
    web/src/components/mobile/BottomSheet.tsx (fullHeight + footer props from plan 06-02; title Badge heading treatment; three close paths)
    web/src/lib/api.ts lines 341-358 (the frozen Run record: id, targetId, kind, status, startedAt, finishedAt, snapshotId, bytes, error, acknowledged, target, domain, groupId) and line 1576 (checkDomain) and line 1923 (listRuns)
    web/src/lib/reltime.ts (formatTs, formatDuration), web/src/lib/forecast.ts (humanBytes)
    web/src/lib/activityLog.ts (buildLogLines signature + LogLine shape; formatLogDate at 704) and web/src/components/ActivityLog.tsx lines 385-390 (the mono timestamp rendering pattern to reuse)
    web/src/components/SnapshotFileTree.tsx line 155 (props) + its use in Recovery.tsx:606 / Files.tsx:345
    web/src/components/RestorePanel.tsx line 926 (the restore entry surface)
    .planning/phases/06-maquette-screens/06-UI-SPEC.md SCRN-05 section + Frozen-API Data Adaptations (the binding substitutes table)
    .planning/phases/06-maquette-screens/06-RESEARCH.md Pattern 4
  </read_first>
  <action>
    Per D-05: RunDetailSheet takes a Run record (and an onClose), renders inside BottomSheet fullHeight with the footer slot. Title = runKindLabel(t, run.kind) + target via runTargetText (existing keys — no new title key). Body top: status headline — Badge tone from statusTone(run.status) with statusLabel text + formatTs(run.finishedAt) completion time (a failed run renders the fail path of the same sheet with run.error via runReason, backend text verbatim). Stat triad per the Frozen-API Data Adaptations (the recorded deviation vs SCRN-05 verbatim wording — restate it in a load-bearing comment at the component header): tile 1 Data volume = humanBytes(run.bytes) under new key run.statVolume; tile 2 Duration = formatDuration(run.finishedAt - run.startedAt) under the EXISTING dashboard.duration key; tile 3 Snapshot = run.snapshotId.slice(0, 8) in font-mono with title={run.snapshotId} under new key run.statSnapshot. All tile values carry tabular-nums; empty finishedAt renders the duration tile muted (never blank — degraded meta treatment). Activity log: buildLogLines for the run rendered with the ActivityLog mono timestamp pattern (font-mono text-xs tabular-nums + formatLogDate) — reuse, never re-style. Footer slot rows: Verify integrity (checkDomain, >=44px tonal row, existing Settings integrity label keys) and Browse snapshot files (>=44px tonal row that expands/mounts SnapshotFileTree inside the sheet body, Recovery.tsx:606 precedent). Restore entry: a secondary/tonal row ABOVE the footer rows in the scroll body — never accent-colored, reachable in <=2 taps (full guided restore flow is SCRN-06 / Phase 8; this is the entry point only). Add run.statVolume + run.statSnapshot to en + de inline blocks and all 40 locale modules in the same commit. While the run is in flight (status running), render the ProgressBar + live activity tail section (Task 3 gates it). Export a small render helper or the component such that plans 06-05/06-06 can host it with local open state (component-local <RunDetailSheet run={...} open onClose/> — no router change).

    RunDetailSheet.dom.test.tsx: given a fixture Run — tiles show humanBytes/formatDuration/slice(0,8) values with tabular classes and the mono snapshot title; failed run shows the scrubbed error via runReason; footer rows exist with >=44px sizing; restore entry renders secondary (no accent classes); title composes kind + target.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/components/mobile/RunDetailSheet.dom.test.tsx src/lib/i18n.parity.test.ts src/lib/i18n.orphans.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - RunDetailSheet renders inside BottomSheet fullHeight with the footer slot carrying verify + browse rows
    - Stat tiles show ONLY: humanBytes(run.bytes), formatDuration(finishedAt - startedAt), snapshotId.slice(0, 8) with full id as title — no invented per-file numbers anywhere in the component
    - Activity log lines come from buildLogLines with formatLogDate mono timestamps (ActivityLog pattern classes present)
    - Restore entry is secondary/tonal and positioned above the footer rows
    - run.statVolume + run.statSnapshot exist in en, de, and all 40 locale modules; parity + orphans green
    - Dom tests pass for populated + failed run fixtures
  </acceptance_criteria>
  <done>A finished run reads completion time, duration, mono snapshot id, honest stat tiles, and the activity log on a full-screen touch sheet — with verify, browse, and a secondary restore entry reachable.</done>
</task>

<task type="auto">
  <name>Task 3: useVisibilityGate + consumer wiring + reconcile e2e (PRIM-04 / D-10)</name>
  <files>web/src/lib/useVisibilityGate.ts, web/src/lib/useVisibilityGate.test.ts, web/src/lib/backupWatch.ts, web/src/components/mobile/RunDetailSheet.tsx, web/e2e/run-detail-visibility.spec.ts</files>
  <read_first>
    web/src/lib/progress.ts (FROZEN — read-only: refCount increment on subscribe, closeSource on last unsubscribe clears cached state so reconnect replays the server snapshot)
    web/src/lib/backupWatch.ts lines 218-289 (fire() seeding baselineIds BEFORE firing at 226-236; resolveFromRuns; POLL_INTERVAL_MS = 2000 setTimeout chain at ~285)
    web/src/lib/useMediaQuery.ts (the windowless-safe hook convention to mirror)
    .planning/phases/06-maquette-screens/06-RESEARCH.md Pattern 7 + Pitfall 7 (stale SSE after tab return)
    .planning/phases/06-maquette-screens/06-UI-SPEC.md PRIM-04 section (consumers: Home recent runs, RunDetailSheet live section, trigger surfaces, tree save busy state — this plan wires the sheet + backupWatch; the surface plans wire their own consumers)
  </read_first>
  <action>
    Per D-10: create useVisibilityGate() returning the visible boolean — visibilitychange listener, initial state from document.visibilityState with a windowless-safe default (visible), mirroring the useMediaQuery convention. Wire TWO consumers in this plan: (1) RunDetailSheet's live section renders conditionally on the gate — {visible && <progress consumer/>} so hiding the page drops the frozen singleton's ref-count to zero (EventSource closes; reconnect replays) and showing it remounts + resubscribes; the terminal content re-render on completion comes from the refetched Run/listRuns state, never from extrapolation; (2) backupWatch's poll loop — gate the POLL_INTERVAL_MS setTimeout chain on visibility: while hidden, stop scheduling; on visible, immediately listRuns refetch FIRST (reconcile via the existing baseline-id match in resolveFromRuns) then resume polling. The baseline seeding order (before firing) is untouched. Never touch progress.ts. Never read visibility from client clocks or timestamps.

    useVisibilityGate.test.ts: jsdom tests — stub document.visibilityState via defineProperty + dispatch visibilitychange: hidden flips false, visible flips true, initial hidden state is respected, windowless default visible.

    e2e/run-detail-visibility.spec.ts (mobile projects): start/choose a run, open the sheet; then page.evaluate to shadow document.visibilityState = "hidden" and dispatch visibilitychange; assert the SSE consumer unmounted (progress surface absent or connection state per the app's visible affordances); restore "visible" + dispatch; assert the sheet converges to the run's server state (completed run shows terminal content). Keep assertions tolerant of timing via the existing expect polling helpers.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/lib/useVisibilityGate.test.ts src/lib/backupWatch.test.ts && npx playwright test e2e/run-detail-visibility.spec.ts --project=mobile-android</automated>
  </verify>
  <acceptance_criteria>
    - useVisibilityGate exists with jsdom tests proving hidden/visible transitions and a windowless default
    - RunDetailSheet's live progress consumer is conditionally rendered on the gate; backupWatch stops its poll chain while hidden and refetches listRuns before resuming on visible
    - backupWatch.test.ts still green (baseline-id correlation semantics unchanged)
    - The e2e background/return reconcile passes on mobile-android
    - progress.ts, api.ts, router.tsx appear in no diff
  </acceptance_criteria>
  <done>Live progress pauses when the page hides and a run that finished in the background reconciles from server state on return — never silently stale, never from the client clock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE / listRuns → sheet rendering | Server-derived run state renders into the sheet; staleness or fabrication would misrepresent backup truth |
| backgrounded page → reconnect | The visibility gate decides when the app holds live connections |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering (stale-state misrepresentation) | Live progress + sheet content after background | high | mitigate | D-10 gate: hidden unsubscribes (ref-count closes the EventSource) + stops timers; visible refetches listRuns FIRST and reconciles via baseline-id correlation — never silently stale, never client clock; e2e pins the reconcile |
| T-06-04 | Information disclosure | Sheet focus trap + content | low | accept | Content is operational run metadata (non-sensitive); the BottomSheet focus trap + scrubbed error text (runReason over backend text) carry the discipline |
| T-06-05 | Tampering (XSS) | Log lines + labels | medium | mitigate | React escaping + t()/runReason pipeline; no dangerouslySetInnerHTML; backend error text shown verbatim is the already-scrubbed upstream text |
| T-06-SC | Tampering (supply chain) | npm installs | low | accept | Zero package installs this phase (locked) |
</threat_model>

<verification>
- `cd web && npx vitest run src/lib/useVisibilityGate.test.ts src/lib/backupWatch.test.ts src/components/mobile/RunDetailSheet.dom.test.tsx src/lib/i18n.parity.test.ts src/lib/i18n.orphans.test.ts` green
- `cd web && npx vitest run` (full suite — Task 1's refactor must not regress anything) green
- `cd web && npx playwright test e2e/run-detail-visibility.spec.ts --project=mobile-android` green
- `git diff --name-only` over the plan shows no progress.ts / api.ts / router.tsx / internal/** entries
</verification>

<success_criteria>
- A finished run on a phone reads: completion time, duration, mono snapshot id, honest stat tiles, activity log with mono timestamps, verify + browse touch rows, and a reachable secondary restore entry
- Hiding the page mid-run pauses SSE + timers; returning reconciles the run from server state
- Run presentation helpers live in ONE shared lib module consumed by both the desktop dashboard and the mobile sheet
</success_criteria>

<specless_note>
Planner resolution of the fallback coverage report: SCRN-05's concurrency probe ("run finishes while sheet open/backgrounded") resolves to the PRIM-04 reconcile truths above (server refetch + baseline-id). The recorded DEVIATION vs SCRN-05's verbatim wording (per-file triad + exclusion-reason log lines) is the UI-SPEC Frozen-API Data Adaptations contract — restated in Task 2's action and in the component-header comment; flagged as a v2 data candidate by the spec, explicitly not built here. PRIM-04's "unclassified" probe row resolves to the visibility truths. Nothing dropped.
</specless_note>

<output>
Create `.planning/phases/06-maquette-screens/06-04-SUMMARY.md` when done
</output>
