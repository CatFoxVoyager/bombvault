---
phase: 06-maquette-screens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/useMediaQuery.ts
  - web/src/lib/useMediaQuery.test.ts
  - web/src/components/SelectionTree.tsx
  - web/src/pages/Containers.tsx
  - web/src/components/SelectionTree.touch.dom.test.tsx
  - web/src/lib/i18n.ts
  - web/src/lib/locales/
  - web/e2e/touch-tree.spec.ts
autonomous: true
requirements: [SCRN-03]
user_setup: []

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "useIsCoarsePointer() is exported from web/src/lib/useMediaQuery.ts mirroring the useIsDesktop shape verbatim (lazy module-level MediaQueryList, useSyncExternalStore, Safari<14 fallback) over POINTER_COARSE_QUERY = \"(pointer: coarse)\" with a windowless/jsdom snapshot default of false (pointer mode); DESKTOP_QUERY (line 29) remains the only WIDTH literal in the file (D-11)"
    - "SelectionTree accepts an additive interactionMode?: \"pointer\" | \"touch\" prop defaulting to \"pointer\"; the APG state model (role=tree/treeitem/group, aria-checked true/mixed/false, roving tabindex computation, Space-through-onToggle, cascade semantics, save-queue wiring) is byte-identical between modes (D-01)"
    - "In touch mode, tapping anywhere on a row toggles the check through the ONE onToggle pipeline using the exact Space guard (!spec.unreachable && !busyPaths?.has(spec.path), SelectionTree.tsx:423-430); the visual checkbox becomes purely presentational (pointer-events-none, still aria-hidden) so the dead-zone/double-fire hazard (research Pitfall 1) cannot occur"
    - "In touch mode the chevron is a real <button type=button> with stopPropagation and an aria-label built from new i18n keys common.expand / common.collapse, sized >=44x44px and right-aligned; the toggle and expand gestures never share a hit area (D-02)"
    - "Tap moves the roving tabindex: activating a row that is not the current tab target calls focusNode(spec.path) (SelectionTree.tsx:356-361) before toggling, so Space after tapping row B acts on row B (research Pitfall 2)"
    - "Touch rows render min-h-[2.75rem] (>=44px full-row target) with touch-action: manipulation (double-tap-zoom killed, scroll preserved) and no hover-dependent styling; unreachable/excluded rows keep the existing muted tone branch (SelectionTree.tsx:455-460)"
    - "The FoldersEditor SelectionTree mount in Containers.tsx derives interactionMode from useIsCoarsePointer — never from useIsDesktop; the Files.tsx FileSetFoldersEditor mount is deliberately left on the pointer default until plan 06-05 restructures that page"
    - "Every existing SelectionTree behavior suite (SelectionTree.dom.test.tsx, SelectionTree.keyboard.dom.test.tsx, src/lib/selectionTree.test.ts) passes UNCHANGED — the touch work is additive to the render/handler layer only"
  artifacts:
    - path: "web/src/lib/useMediaQuery.ts"
      provides: "POINTER_COARSE_QUERY constant + useIsCoarsePointer() hook (additive twin of useIsDesktop); width authority untouched"
      contains: "useIsCoarsePointer"
    - path: "web/src/components/SelectionTree.tsx"
      provides: "interactionMode prop with the touch interaction layer: row-tap=toggle, presentational checkbox, >=44x44 chevron button, roving-on-tap, min-h-[2.75rem] rows"
      contains: "interactionMode"
    - path: "web/src/components/SelectionTree.touch.dom.test.tsx"
      provides: "touch-mode twins: tap toggles (twice-tap round-trips, not zero-change), chevron expands without toggling, roving target follows tap, pointer default untouched"
      contains: "interactionMode"
    - path: "web/e2e/touch-tree.spec.ts"
      provides: "Playwright touch geometry: row bounding box >=44px, chevron box >=44x44, hit separation, tap toggles aria-checked (mobile projects only)"
      contains: "boundingBox"
    - path: "web/src/lib/i18n.ts"
      provides: "common.expand and common.collapse keys in en + de inline blocks; same keys in all 40 locale modules (parity + orphans green — consumers land in the same commit)"
      contains: "common.expand"
  key_links:
    - from: "web/src/lib/useMediaQuery.ts (useIsCoarsePointer)"
      to: "web/src/components/SelectionTree.tsx (interactionMode)"
      via: "FoldersEditor's tree mount derives the prop from pointer capability, never width — landscape phones (>=48rem, desktop chrome) still get the touch tree (D-11)"
      pattern: "useIsCoarsePointer"
    - from: "SelectionTree.tsx touch row onClick"
      to: "SelectionTree.tsx onToggle via the Space guard (lines 423-430)"
      via: "Row tap reuses the EXACT guard expression Space uses — one toggle pipeline, no second semantics"
      pattern: "onToggle"
    - from: "SelectionTree.tsx touch chevron button"
      to: "SelectionTree.tsx toggleExpansion"
      via: "stopPropagation + dedicated >=44x44 zone — expand never toggles the check (D-02)"
      pattern: "toggleExpansion"
  prohibitions:
    - statement: "interactionMode is never derived from useIsDesktop or any width media query — width is the chrome axis only (D-11; research Pitfall 6)"
      status: enforced
      verification: backstop
    - statement: "No preventDefault on touchstart in tree rows — activation belongs on click; preventDefault kills scrolling (research Pitfall 2 / PITFALLS.md milestone rule)"
      status: enforced
      verification: backstop
    - statement: "SelectionTree is never forked or wrapped into a second tree component (D-01); the touch adaptation lives inside the ONE component as an interaction-mode prop"
      status: enforced
      verification: backstop
    - statement: "The APG state model logic is not modified — verified by the existing SelectionTree dom + keyboard + selectionTree unit suites passing without edits to their assertions"
      status: enforced
      verification: explicit
    - statement: "No new npm dependencies (locked presentation-only milestone)"
      status: enforced
      verification: backstop

---

<objective>
Give the ONE `SelectionTree` a touch interaction mode: an additive `interactionMode` prop (default "pointer") that inverts the desktop hit semantics below the breakpoint — row tap toggles the check, a dedicated >=44x44px chevron zone expands — while the APG state model stays byte-identical. Plus the `useIsCoarsePointer` capability hook that drives it (D-11), and the touch e2e geometry gate.

Purpose: SCRN-03's interaction layer is the milestone's hardest work (STATE.md research flag); landing it in Wave 1 means the Containers (06-05) and Files screens compose a proven tree instead of debugging pointer semantics mid-slice. Desktop rendering is unchanged by construction (pointer is the untouched default).

Output: `useIsCoarsePointer` hook, `interactionMode` prop on SelectionTree wired into the FoldersEditor mount, touch dom twins, touch geometry e2e, `common.expand`/`common.collapse` i18n keys across all 42 locale surfaces.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-maquette-screens/06-CONTEXT.md
@.planning/phases/06-maquette-screens/06-RESEARCH.md
@.planning/phases/06-maquette-screens/06-PATTERNS.md
@.planning/phases/06-maquette-screens/06-UI-SPEC.md
@web/src/lib/useMediaQuery.ts
@web/src/components/SelectionTree.tsx
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end touch path — coarse-pointer hook, interactionMode prop, live FoldersEditor mount</name>
  <files>web/src/lib/useMediaQuery.ts, web/src/lib/useMediaQuery.test.ts, web/src/components/SelectionTree.tsx, web/src/pages/Containers.tsx, web/src/components/SelectionTree.touch.dom.test.tsx, web/src/lib/i18n.ts, web/src/lib/locales/</files>
  <read_first>
    web/src/lib/useMediaQuery.ts (WHOLE file — copy the useIsDesktop shape verbatim: lazy module-level MediaQueryList, currentMql/subscribe/getSnapshot, useSyncExternalStore, Safari<14 fallback, load-bearing header comment, and the source-assert guard style in useMediaQuery.test.ts that pins DESKTOP_QUERY)
    web/src/components/SelectionTree.tsx lines 340-540 (focusNode 356-361, tone classes 455-460, ariaChecked 461, roving tabindex 477, row onClick 485, chevron span 487-501, checkbox 511-521) and lines 590-710 (notice rows, tree viewport)
    web/src/pages/Containers.tsx — the FoldersEditor component's SelectionTree mount (search for `<SelectionTree` inside FoldersEditor)
    .planning/phases/06-maquette-screens/06-PATTERNS.md sections 1-2 (the seam, the hook twin, the jsdom-default-false note)
    .planning/phases/06-maquette-screens/06-RESEARCH.md Pattern 1 + Pitfalls 1, 2, 6 + the touch-mode row skeleton
  </read_first>
  <action>
    Per D-11: add POINTER_COARSE_QUERY = "(pointer: coarse)" and useIsCoarsePointer() to useMediaQuery.ts as an additive twin of useIsDesktop — same useSyncExternalStore subscribe/snapshot mechanics, windowless-safe, jsdom/test snapshot default FALSE (pointer mode) so every existing dom test stays on the pointer tree. Extend useMediaQuery.test.ts in the existing source-assert style to pin the new literal. DESKTOP_QUERY stays untouched as the only width literal.

    Per D-01/D-02: add `interactionMode?: "pointer" | "touch"` (default "pointer") to SelectionTreeProps and branch ONLY the row render/handler layer when `interactionMode === "touch"`: (a) row onClick becomes the toggle — `() => { focusNode(spec.path); onToggle(spec.path); }` guarded by the exact Space guard expression from lines 423-430 (`!spec.unreachable && !busyPaths?.has(spec.path)`), replacing the desktop expand-on-row-click; (b) the checkbox input gets pointer-events-none in touch mode and NO onChange (purely presentational, still aria-hidden tabIndex -1) — this is the double-fire/dead-zone fix (Pitfall 1); (c) the chevron span (lines 487-501) becomes a real button in touch mode: type button, stopPropagation, calls toggleExpansion(spec.path), className includes h-11 w-11 (44px) shrink-0 ml-auto, aria-label = t(common.expand) / t(common.collapse) by expansion state; (d) row className in touch mode adds min-h-[2.75rem] touch-manipulation select-none. Desktop branch stays byte-identical to today's lines 485/511-521.

    Add i18n keys common.expand ("Expand") and common.collapse ("Collapse") to the en and de inline blocks in i18n.ts AND to all 40 modules under web/src/lib/locales/ in the same commit (the orphans test passes because the chevron consumes them).

    Wire ONE real mount as the tracer: FoldersEditor's SelectionTree in Containers.tsx passes interactionMode={coarse ? "touch" : "pointer"} from useIsCoarsePointer. Do NOT touch Files.tsx (plan 06-05 owns that page's restructure; the prop default keeps its mount safe on pointer mode).

    Write SelectionTree.touch.dom.test.tsx: render with interactionMode="touch" and assert (1) clicking a row calls onToggle for that path; (2) clicking the row twice round-trips the check (guards against the zero-change double-fire); (3) clicking the chevron button calls toggle-expansion without calling onToggle; (4) the chevron button exists with an accessible name; (5) the checkbox input has pointer-events-none class and no onChange handler wired in touch mode; (6) a second render with default props keeps desktop behavior (row click expands, not toggles).
  </action>
  <verify>
    <automated>cd web && npx vitest run src/components/SelectionTree.touch.dom.test.tsx src/components/SelectionTree.dom.test.tsx src/components/SelectionTree.keyboard.dom.test.tsx src/lib/selectionTree.test.ts src/lib/useMediaQuery.test.ts src/lib/i18n.parity.test.ts src/lib/i18n.orphans.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - useMediaQuery.ts exports useIsCoarsePointer and POINTER_COARSE_QUERY; the file still contains exactly one width query literal (DESKTOP_QUERY)
    - SelectionTreeProps carries interactionMode with default "pointer"; SelectionTree.touch.dom.test.tsx exists and passes
    - Touch twins prove: tap toggles, double-tap round-trips (not zero change), chevron expands without toggling, desktop default unchanged
    - Existing SelectionTree.dom.test.tsx + SelectionTree.keyboard.dom.test.tsx + selectionTree.test.ts pass with NO assertion edits
    - i18n.parity.test.ts and i18n.orphans.test.ts pass with common.expand/common.collapse present in en, de, and all 40 locale modules
    - FoldersEditor's tree mount reads useIsCoarsePointer; Files.tsx is unmodified by this task
  </acceptance_criteria>
  <done>The touch interaction path works end-to-end on the real FoldersEditor mount: capability hook drives the prop, tap toggles, chevron expands, state model untouched, all existing suites green.</done>
</task>

<task type="auto">
  <name>Task 2: Roving-on-tap, full-row targets, notice-row touch sizing</name>
  <files>web/src/components/SelectionTree.tsx, web/src/components/SelectionTree.touch.dom.test.tsx</files>
  <read_first>
    web/src/components/SelectionTree.tsx lines 350-365 (focusNode), 420-435 (Space handler + guard), 470-525 (row + roving tabindex + checkbox), 590-650 (notice rows: loading / error + retry / empty / truncated / blocked)
    .planning/phases/06-maquette-screens/06-RESEARCH.md Pitfall 2 and the byte-identical invariants list
    .planning/phases/06-maquette-screens/06-UI-SPEC.md SCRN-03 section (rows, notice rows, muted excluded rows)
  </read_first>
  <action>
    Roving-on-tap (Pitfall 2): in touch mode the row's activation handler already calls focusNode(spec.path) (Task 1); extend the touch twins to prove it — render with a ref-forwarded tree, tap row B while row A holds tabindex="0", assert B now carries tabindex="0" and A carries -1, and that Space (keyboard keydown through the existing handler) now toggles B. Reuse focusNode — never re-implement focus/scroll logic.

    Full-row target: touch rows keep the 14px body register (text-sm may replace text-xs for the label in touch mode per UI-SPEC typography — touch mode may hold 14px where desktop renders 12px) with min-h-[2.75rem]; keep the depth-0/depth-n desktop min-h classes only on the pointer branch.

    touch-action: manipulation on the row element in touch mode (class touch-manipulation) plus select-none — never a touchstart preventDefault (that kills scroll).

    Notice rows (SelectionTree.tsx ~603-648): in touch mode the retry Button and any interactive notice control renders >=44px tall (min-h-[2.75rem] / py bump); notice copy and shapes stay verbatim (folders.couldNotRead, retry label unchanged).

    Muted excluded/unreachable tone branch (455-460) stays as-is — it is already touch-ready per the pattern map.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/components/SelectionTree.touch.dom.test.tsx src/components/SelectionTree.dom.test.tsx src/components/SelectionTree.keyboard.dom.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Touch twins assert: tap on a non-tab-target row moves roving tabindex to it; subsequent Space toggles that row
    - Touch twins assert: rows carry min-h-[2.75rem] and touch-manipulation classes in touch mode; pointer-mode rows keep min-h-8/min-h-7
    - Notice-row retry control renders >=44px in touch mode (asserted by class/height in the twins)
    - Existing dom + keyboard suites still pass unchanged
  </acceptance_criteria>
  <done>Tap both toggles AND moves the keyboard roving target; every touch row is a >=44px target; the APG keyboard model is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Touch geometry e2e on the mobile projects</name>
  <files>web/e2e/touch-tree.spec.ts</files>
  <read_first>
    web/e2e/mobile-shell.spec.ts (MOBILE_PROJECTS pattern, bootWithoutServerLook route-abort for localized boots, fresh-DB boot conventions)
    web/playwright.config.ts (4 projects; tap() requires hasTouch — mobile-iphone + mobile-android have it; reuseExistingServer: !CI and the Windows manual-webServer caveat from STATE.md)
    .planning/phases/06-maquette-screens/06-RESEARCH.md the touch e2e geometry snippet
  </read_first>
  <action>
    New spec scoped to the two MOBILE_PROJECTS only (never desktop-1280/desktop-768 — mouse projects have no touch context). Open /containers, open a container's folders editor through the affordance that exists TODAY (pre-06-05 the page layout is still the desktop structure; the assertions here are interaction geometry, not layout). Assert with locator.tap() + boundingBox(): (1) tapping a treeitem row flips aria-checked to "true" and tapping again returns it; (2) row boundingBox height >= 44; (3) the chevron control (getByRole button with expand/collapse name) has boundingBox >= 44x44; (4) hit separation — the chevron box's left edge does not overlap the row's label text box; (5) tapping the chevron expands the child group WITHOUT changing aria-checked. Guard boundingBox() null (assert visible first). Keep the spec independent of plan 06-05's stacked view — it must keep passing after that restructure, so target the tree by role, never by page-position selectors.
  </action>
  <verify>
    <automated>cd web && npx playwright test e2e/touch-tree.spec.ts --project=mobile-android --project=mobile-iphone</automated>
  </verify>
  <acceptance_criteria>
    - e2e/touch-tree.spec.ts exists, runs only on the two touch projects, and passes
    - Assertions cover: tap toggles aria-checked (both directions), row box >=44px, chevron box >=44x44, chevron/label hit separation, chevron expand does not toggle
  </acceptance_criteria>
  <done>Touch hit semantics are pinned by CI-runnable geometry assertions on WebKit and Chromium mobile projects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| touch input → selection state | Finger taps drive the backup-selection toggle pipeline; mis-hits mutate what restic will back up |
| row/label rendering | Container mount paths and folder names (host data) render into row labels |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02 | Tampering (accident) | SelectionTree touch hit zones | high | mitigate | Dedicated >=44x44 chevron zone disjoint from the toggle zone (D-02); tap uses the same guarded onToggle pipeline as Space (busy/unreachable rows cannot toggle); destructive reset-selection confirm inherits the fail-tone sheet via plan 06-02 — no new destructive surface here |
| T-06-05 | Tampering (XSS) | Row label + aria-label rendering | medium | mitigate | React escaping only; every user-visible string via t(); no dangerouslySetInnerHTML in any touch-layer change |
| T-06-SC | Tampering (supply chain) | npm installs | low | accept | Zero package installs this phase (locked presentation-only milestone); no registry surface |
</threat_model>

<verification>
- `cd web && npx vitest run src/components/SelectionTree.touch.dom.test.tsx src/components/SelectionTree.dom.test.tsx src/components/SelectionTree.keyboard.dom.test.tsx src/lib/selectionTree.test.ts` green
- `cd web && npx vitest run src/lib/i18n.parity.test.ts src/lib/i18n.orphans.test.ts` green
- `cd web && npx playwright test e2e/touch-tree.spec.ts --project=mobile-android --project=mobile-iphone` green
- `cd web && npx eslint src/components/SelectionTree.tsx src/lib/useMediaQuery.ts` clean
</verification>

<success_criteria>
- A coarse-pointer device (and the mobile e2e projects) toggles tree rows by full-row tap and expands via the dedicated chevron zone; both gestures are >=44px and never share a hit area
- The desktop pointer tree is byte-identical: all pre-existing SelectionTree suites pass without assertion edits
- interactionMode is derived from pointer capability, not viewport width (D-11) — the landscape-phone case is correct by construction
</success_criteria>

<specless_note>
Planner resolution of the fallback coverage report (no SPEC edge/prohibition sections existed): SCRN-03's boundary probe (row/threshold edges) resolves to the explicit truths above (guarded rows don't toggle; chevron absent semantics unchanged for non-expandable rows); SCRN-03 precision probe resolves to integer tick counts rendered via tabular numerals (no derived floats anywhere in the tree); SCRN-03 idempotency resolves to the serialized save queue coalescing rapid toggles (desktop-identical queue — consumers land in plan 06-05); SCRN-03 concurrency resolves to the queue's one-in-flight + dirty-flag re-save contract (unchanged). No item was dropped.
</specless_note>

<output>
Create `.planning/phases/06-maquette-screens/06-01-SUMMARY.md` when done
</output>
