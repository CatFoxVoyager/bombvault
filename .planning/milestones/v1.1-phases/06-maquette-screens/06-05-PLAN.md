---
phase: 06-maquette-screens
plan: 05
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03", "06-04"]
files_modified:
  - web/src/components/mobile/StickyActionBar.tsx
  - web/src/components/mobile/StickyActionBar.dom.test.tsx
  - web/src/pages/Containers.tsx
  - web/src/pages/Files.tsx
  - web/src/components/SelectionTree.tsx
  - web/src/lib/i18n.ts
  - web/src/lib/locales/
  - web/e2e/maquette-screens.spec.ts
autonomous: true
requirements: [SCRN-02, SCRN-03, SCRN-04, FLOW-03]
user_setup: []

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "StickyActionBar (web/src/components/mobile/StickyActionBar.tsx) is the ONE shared sticky-in-flow bar: sticky bottom-0 as a DIRECT child of the page column (never nested in a Card, never position:fixed), bg-carbon-sidebar + border-t border-carbon-border, padded by max(0.75rem, var(--safe-area-bottom)); sticky works because main#bv-main is the nearest scrolling ancestor and no overflow-hidden wrapper sits between bar and scroller"
    - "Container detail (D-04) is a LOCALLY STACKED VIEW inside /containers: component-local openContainer state in Containers(); the container LIST stays mounted beneath the detail (scroll position survives Back); a back row with chevron glyph + visible common.back label, >=44px, aria-label naming the container + Back; it is NOT a BottomSheet and NOT a route (router.tsx frozen)"
    - "The tree Save bar (D-03) row 1 carries the live count folders.handedToRestic ({n} folders handed to restic — invariant {n}, no plural fork) in 12px tabular numerals with an accentSoft ticked chip, plus the rowBusy spinner treatment while the queue is inFlight; row 2 is the Save button reusing the EXISTING folders.save key, filled accent, >=44px; row 2's press FLUSHES the existing serialized queue (drains any pending attempt, busy while the drain is in flight, folders.saved toast on success, failure toasts AND shakes) — the ONE desktop-identical save path, no mobile buffer, no second save mechanism"
    - "The Save bar's second row carries the per-root CACHEDIR.TAG plain-language line (folders.cachedirToggle + folders.cachedirScope InfoBubble) for the container tree; per-root sub-rows remain (state lives there); the file-set editor passes NO CACHEDIR props (RESTIC-01 deferral, SelectionTree.tsx:104-108)"
    - "The empty-deselect refusal surfaces where the truth lives: the existing zero-include client guard (Containers.tsx:1183-1188) routes to blockedPath and the existing folders.emptySelectionBlocked / files.emptySelectionBlocked warn line renders under the refused row exactly as desktop — copy reused verbatim"
    - "In the stacked container detail the tree renders full available height in touch mode via an additive SelectionTree viewportClassName-style prop (the h-[clamp(12rem,55vh,32rem)] default at SelectionTree.tsx:702 stays for every existing mount); the detail view owns the scroll, the tree scrolls within it"
    - "Files list (SCRN-04): one coverage card per set — icon avatar (accentSoft), name + path meta, the n-folders-ticked coverage line over the set's selection encoding, four-status Badge with text label, last-run line, plus a purely decorative aria-hidden 6px rounded coverage bar (track bg-carbon-surface2; the count line is the accessible statement); the files.emptyRule outline card renders in the list"
    - "The file-set editor below the breakpoint embeds the SAME touch tree + Save bar (SCRN-03's component, same mode prop, no fork); the fileSetEditorKey remount contract (Files.tsx:1125) and the FoldersEditor queueRef (Containers.tsx:836) are undisturbed"
    - "FLOW-03 on both surfaces: triggering uses the exact BackupButton semantics (async POST {ok:true,started:true}; useBackupWatch baseline ids seeded BEFORE firing; on correlation the 06-04 RunDetailSheet opens for the new run); the confirm inherits the 06-02 fail-tone sheet; ZERO new trigger code paths"
    - "Both page roots adopt the responsive rhythm gap-6 md:gap-10 (desktop values unchanged by construction); the existing FilterPopover stays, presenting through the 06-03 TapPopover below the breakpoint"
  artifacts:
    - path: "web/src/components/mobile/StickyActionBar.tsx"
      provides: "the shared sticky-in-flow action bar consumed by the tree Save bar (this plan) and the Home trigger zone (06-06)"
      contains: "sticky"
    - path: "web/src/pages/Containers.tsx"
      provides: "SCRN-02 mobile surface: card list + locally stacked detail (back row, full-height touch tree, Save bar, CACHEDIR line, FLOW-03 trigger)"
      contains: "openContainer"
    - path: "web/src/pages/Files.tsx"
      provides: "SCRN-04 mobile surface: coverage cards + files.emptyRule card + editor with the same touch tree and Save bar"
      contains: "files.emptyRule"
    - path: "web/src/components/SelectionTree.tsx"
      provides: "additive viewportClassName-style prop so the stacked detail can render the tree full-height (default clamp unchanged for all existing mounts)"
      contains: "viewportClassName"
    - path: "web/e2e/maquette-screens.spec.ts"
      provides: "mobile e2e: card tap -> stacked detail -> back preserves list scroll; tree tap toggles + live count updates; Save flush; coverage card; desktop project asserts no stacked view / no sticky bars at >=48rem"
      contains: "maquette"
    - path: "web/src/lib/i18n.ts"
      provides: "common.back, folders.handedToRestic, files.emptyRule keys in en + de + all 40 locale modules (parity + orphans green)"
      contains: "handedToRestic"
  key_links:
    - from: "web/src/pages/Containers.tsx (FoldersEditor queueRef, line 836)"
      to: "the Save bar flush action"
      via: "Save press drains the EXISTING serialized queue — SCRN-03 mandates the desktop-identical path; rowBusy spinner + shake/toast semantics unchanged"
      pattern: "queueRef"
    - from: "web/src/pages/Files.tsx (fileSetEditorKey, line 1125)"
      to: "the file-set editor tree mount"
      via: "remount contract id:anchor:selection-presence untouched — the editor seeds its mirror exactly as desktop"
      pattern: "fileSetEditorKey"
    - from: "web/src/pages/Containers.tsx + Files.tsx triggers"
      to: "web/src/lib/backupWatch.ts + web/src/components/mobile/RunDetailSheet.tsx"
      via: "FLOW-03: baseline seeded before firing; RunDetailSheet opens on correlation (deep-link) — no new trigger path, no outcome read from the POST"
      pattern: "useBackupWatch"
    - from: "StickyActionBar"
      to: "web/src/app/Layout.tsx main#bv-main"
      via: "sticky bottom-0 resolves against the scroller — the Phase 5 in-flow discipline (never fixed), safe-area via the Phase 5 custom properties"
      pattern: "bv-main"
  prohibitions:
    - statement: "No position:fixed chrome anywhere in this plan — sticky-in-flow only (locked Phase 5 discipline; fixed fights the visualViewport keyboard mechanism)"
      status: enforced
      verification: backstop
    - statement: "No second save path: toggles keep live-saving through the ONE serialized queue exactly as desktop; the Save press only flushes it"
      status: enforced
      verification: backstop
    - statement: "No new routes and no edits to router.tsx / api.ts / progress.ts / internal/** (frozen)"
      status: enforced
      verification: explicit
    - statement: "The zero-include guard and empty-deselect semantics are not weakened or bypassed — refusal routing to blockedPath stays exactly as desktop"
      status: enforced
      verification: explicit
    - statement: "No infinite scroll and no horizontal-scroll tables (Out of Scope); status colors on badges only — the filled accent Save button is the screen's primary action, never status-tinted"
      status: enforced
      verification: backstop

---

<objective>
Operationalize the two tree-bearing maquette surfaces: Containers gains a locally stacked detail view (D-04) hosting the 06-01 touch tree full-height with the D-03 sticky Save bar (live "handed to restic" count + queue flush + CACHEDIR line), and File sets gains the SCRN-04 coverage-card list with the same touch tree inside its editor — plus the shared StickyActionBar primitive and FLOW-03 backup triggering on both surfaces.

Purpose: SCRN-02/03/04 are the operational core of the phase — folder selection is the product's keystone interaction. Composition only: the tree (06-01), the sheet confirm (06-02), the run sheet (06-04), and the save queue (existing) are all proven pieces; this plan wires them into screens.

Output: StickyActionBar, restructured Containers + Files pages, tree viewport prop, maquette-screens e2e, common.back / folders.handedToRestic / files.emptyRule i18n keys.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-maquette-screens/06-CONTEXT.md
@.planning/phases/06-maquette-screens/06-RESEARCH.md
@.planning/phases/06-maquette-screens/06-PATTERNS.md
@.planning/phases/06-maquette-screens/06-UI-SPEC.md
@web/src/pages/Containers.tsx
@web/src/pages/Files.tsx
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end stacked container detail — StickyActionBar, back row, full-height touch tree, Save bar</name>
  <files>web/src/components/mobile/StickyActionBar.tsx, web/src/components/mobile/StickyActionBar.dom.test.tsx, web/src/pages/Containers.tsx, web/src/components/SelectionTree.tsx, web/src/lib/i18n.ts, web/src/lib/locales/</files>
  <read_first>
    web/src/pages/Containers.tsx — Containers() list structure, FoldersEditor (lines 760+), queueRef at 836, zero-include guard at 1169-1200 (blockedPath routing 1183-1188), the current SelectionTree mount inside FoldersEditor, and where the FoldersEditor is rendered today (~2435)
    web/src/components/SelectionTree.tsx line 702 (the viewport clamp class) + the props interface
    web/src/components/mobile/BottomNav.tsx line ~100 (the chrome styling precedent: border-t border-carbon-border bg-carbon-sidebar pb-[var(--safe-area-bottom)])
    web/src/app/Layout.tsx line 295 (main#bv-main scroller — the sticky containing scroller)
    web/src/lib/pageShell.ts line 88 (PAGE_SHELL = flex flex-col gap-10 max-w-6xl)
    .planning/phases/06-maquette-screens/06-RESEARCH.md Pattern 2 (sticky mechanics) + Pattern 3 (stacked view) + Pitfall 3 (bar not sticking)
    .planning/phases/06-maquette-screens/06-UI-SPEC.md SCRN-02 + SCRN-03 sections (detail anatomy, save-bar rows, copy table)
  </read_first>
  <action>
    StickyActionBar (new, web/src/components/mobile/): a sticky bottom-0 bar rendered as the LAST direct child of the page column — bg-carbon-sidebar, border-t border-carbon-border, pt-3, pb-[max(0.75rem,var(--safe-area-bottom))]; children slot for the action rows; NO position:fixed, NO negative margins (plain in-column form per the BottomNav precedent), and document in a header comment why nesting inside a Card would break sticking (Pitfall 3). Add i18n keys in the same commit: common.back ("Back"/"Zurück") and folders.handedToRestic ("{n} folders handed to restic" / de equivalent, invariant {n} — no plural fork, folders.previewPaths precedent) in en + de inline blocks and all 40 locale modules.

    Per D-04: in Containers() add component-local state (openContainer: Container | null). Below the breakpoint (max-md: restructure — the desktop list markup above md: stays byte-identical): the list shows a summary meta line (derived counts from the same source the desktop protection summary uses — no new endpoints) and one block card per container (monogram avatar bg-carbon-surface2, name 14px/600, meta line with mount count + selection summary from the existing folders.previewPaths counts, four-status Badge with text label); card tap sets openContainer (>=44px card target). When openContainer is set, render the stacked detail INSTEAD (list stays mounted beneath — hidden, not unmounted, so scroll survives Back): back row first (chevron glyph + visible t(common.back) label, >=44px, aria-label = "{container name}, Back" — never icon-only), container name (600) + host path meta (12px mono), then FoldersEditor with the tree rendered full-height: add an additive viewportClassName?: string prop to SelectionTree that appends to (and overrides the height of) the default clamp class at line 702 — pass a full-height class from the detail; every existing mount keeps the default clamp.

    Per D-03: inside the detail, render the Save bar via StickyActionBar with row 1 = the live folders.handedToRestic count (12px tabular) + accentSoft chip + the existing rowBusy spinner while queueRef.inFlight, and row 2 = Save (existing folders.save key, filled accent bg-accent text-accentContrast, >=44px) whose press FLUSHES the serialized queue: drain any pending/dirty attempt, show busy while in flight, folders.saved toast on success, failure toasts AND shakes (existing rowShake path). Second bar row: the CACHEDIR.TAG plain-language line — folders.cachedirToggle text + folders.cachedirScope InfoBubble (which is tap-openable via 06-03). The empty-deselect rule: keep the existing zero-include guard untouched; its blockedPath line (folders.emptySelectionBlocked) renders under the refused row as today.

    StickyActionBar.dom.test.tsx: renders children, carries the chrome classes + safe-area padding, is a normal-flow sticky element (assert class, not computed layout).
  </action>
  <verify>
    <automated>cd web && npx vitest run src/components/mobile/StickyActionBar.dom.test.tsx src/components/SelectionTree.dom.test.tsx src/components/SelectionTree.touch.dom.test.tsx src/lib/i18n.parity.test.ts src/lib/i18n.orphans.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - StickyActionBar exists with the chrome classes, safe-area padding, and dom tests; it is used as the last direct child of the Containers page column
    - Containers.tsx holds component-local openContainer state; the list is hidden-not-unmounted while the detail is open; back row carries visible common.back + a >=44px target with a container-naming aria-label
    - SelectionTree gains the additive viewport class prop; existing mounts (desktop FoldersEditor default, file-set editor) keep the clamp default
    - Save bar row 1 shows the folders.handedToRestic count + accentSoft chip + rowBusy spinner; row 2 Save flushes the existing queue with toast/shake semantics; CACHEDIR line + InfoBubble present
    - common.back + folders.handedToRestic exist in en, de, and all 40 locale modules; parity + orphans green
  </acceptance_criteria>
  <done>On a phone, tapping a container card stacks the full-height touch-tree editor with a pinned Save bar whose count updates live and whose Save flushes the desktop-identical queue; Back returns to the list with scroll intact.</done>
</task>

<task type="auto">
  <name>Task 2: File sets mobile surface — coverage cards, emptyRule card, same tree + Save bar in the editor</name>
  <files>web/src/pages/Files.tsx, web/src/lib/i18n.ts, web/src/lib/locales/</files>
  <read_first>
    web/src/pages/Files.tsx — FileSetRow (line 1373), FileSetFoldersEditor (line ~1149) + its SelectionTree mount (~1343), fileSetEditorKey (line 1125), the page list structure (~1619+), existing empty copy
    web/src/pages/Containers.tsx (the Task 1 detail/save-bar pattern to mirror — identical queue pattern per the pattern map)
    .planning/phases/06-maquette-screens/06-UI-SPEC.md SCRN-04 section + Copywriting Contract (files.emptyRule wording with {action} interpolation)
    .planning/phases/06-maquette-screens/06-RESEARCH.md Pattern 3 note (file sets keep their existing page structure — the same tree, not the same navigation)
  </read_first>
  <action>
    Add files.emptyRule to en + de + all 40 locale modules in this commit: en "A set needs at least one folder. The last tick cannot be removed. Use {action} to remove it entirely." with {action} interpolated from the EXISTING files.deleteSet value at render time (never re-authored, never a second key).

    Below the breakpoint (max-md:; desktop markup above md: byte-identical): restructure the set list into coverage cards — icon avatar (accentSoft), set name (600) + path meta (mono, break-all + title), the n-folders-ticked coverage line derived from the set's selection encoding (same count derivation family as folders.handedToRestic), four-status Badge with text label, last-run line, and a purely decorative aria-hidden 6px rounded coverage bar (track bg-carbon-surface2, fill per set identity; the count line is the accessible statement — the bar carries no information alone). The files.emptyRule outline card renders in the list. Card tap opens the set editor (the existing FileSetRow expansion content) — below the breakpoint the editor presents the touch tree (pass interactionMode from useIsCoarsePointer — wire this mount now that the page is restructured) + the same StickyActionBar Save bar from Task 1 (no CACHEDIR props — file sets pass none per SelectionTree.tsx:104-108; the blocked line uses the already-parameterized files.emptySelectionBlocked at SelectionTree.tsx:116-121). The fileSetEditorKey remount contract stays untouched. Page root rhythm: gap-6 md:gap-10.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/lib/i18n.parity.test.ts src/lib/i18n.orphans.test.ts src/components/SelectionTree.touch.dom.test.tsx && npx eslint src/pages/Files.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Coverage cards render avatar, name + mono path meta, ticked-count line, four-status text-label Badge, last-run line, and an aria-hidden decorative bar
    - files.emptyRule exists in all 42 locale surfaces with the {action} interpolation from files.deleteSet; the outline card renders in the list
    - The file-set editor mount passes interactionMode from useIsCoarsePointer and embeds the same Save bar; fileSetEditorKey contract untouched
    - Desktop (>=48rem) Files markup unchanged (max-md: only)
  </acceptance_criteria>
  <done>File sets read as coverage cards on a phone and open the same touch tree + Save bar — one tree, zero forks, desktop untouched.</done>
</task>

<task type="auto">
  <name>Task 3: FLOW-03 triggers on both surfaces + maquette-screens e2e</name>
  <files>web/src/pages/Containers.tsx, web/src/pages/Files.tsx, web/e2e/maquette-screens.spec.ts</files>
  <read_first>
    web/src/components/BackupButton.tsx (the semantics to reuse: async POST {ok:true,started:true}, STOP_ACK_KEY ack contract)
    web/src/lib/backupWatch.ts lines 218-289 (baseline seeding before firing; resolveFromRuns correlation)
    web/src/components/mobile/RunDetailSheet.tsx (the 06-04 component to open on correlation)
    web/e2e/mobile-shell.spec.ts (MOBILE_PROJECTS + bootWithoutServerLook route-abort patterns)
    .planning/phases/06-maquette-screens/06-RESEARCH.md the FLOW-03 rows + the async-job house rule
  </read_first>
  <action>
    Per D-08: wire backup triggering from the container detail and the file-set editor using the EXACT BackupButton component/semantics as desktop — no new trigger code path. On correlation (useBackupWatch baseline-id match), open the 06-04 RunDetailSheet for the new run with local state (deep-link into the live run); the confirmation inherits the 06-02 fail-tone sheet (containers stop/restart consequence copy already exists); outcomes never read from the POST response.

    e2e/maquette-screens.spec.ts (mobile projects): (1) container card tap stacks the detail; Back returns with the list scroll position intact (scroll, open, back, assert scroll restoration); (2) tree row tap toggles and the Save-bar count updates; (3) Save press completes with the success toast path (fresh-DB boot conventions from Phase 5); (4) a file-set coverage card shows the ticked-count line and opens the editor tree; (5) desktop-1280 project: /containers and /files assert NO stacked view, NO sticky action bar, NO filled full-width trigger at >=48rem (max-md leakage guard).
  </action>
  <verify>
    <automated>cd web && npx playwright test e2e/maquette-screens.spec.ts --project=mobile-android --project=mobile-iphone --project=desktop-1280 && npx vitest run src/components/BackupButton.dom.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Container + file-set surfaces trigger backups through BackupButton with useBackupWatch correlation opening RunDetailSheet; BackupButton.dom.test.tsx stays green (semantics unchanged)
    - e2e passes: stacked detail + scroll-preserving back, tap-toggle + live count, Save flush, coverage card, desktop no-leakage assertions
  </acceptance_criteria>
  <done>Backup triggering from both tree surfaces is desktop-identical in semantics and mobile in presentation; the screens are pinned by CI e2e.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selection edits → serialized save queue | Touch edits change what restic backs up; the queue is the only write path |
| backup trigger → API | Existing async endpoints; CSRF/cross-site refusal unchanged server-side |
| stacked-view navigation state | Component-local state must not leak stale editor state across containers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02 | Tampering (accidental destructive tap) | Tree save/reset surfaces | high | mitigate | >=44px separated hit zones from 06-01; reset-selection + remove-set confirms inherit the 06-02 fail-tone sheet (consequence copy + safe-default focus); the zero-include guard stays untouched so a deselect-everything cannot silently save |
| T-06-06a | Tampering (stale editor state) | openContainer local state | medium | mitigate | Detail state keys off the container identity; the existing fileSetEditorKey/queue contracts govern remounts; editor state resets on target switch exactly as the desktop editor does today |
| T-06-05 | Tampering (XSS) | Card meta lines + paths | medium | mitigate | React escaping; mono break-all + title for paths; every string via t() |
| T-06-SC | Tampering (supply chain) | npm installs | low | accept | Zero package installs (locked) |
</threat_model>

<verification>
- `cd web && npx vitest run` green (existing Containers/Files suites + queue behavior tests unchanged and passing)
- `cd web && npx vitest run src/lib/i18n.parity.test.ts src/lib/i18n.orphans.test.ts` green
- `cd web && npx playwright test e2e/maquette-screens.spec.ts --project=mobile-android --project=mobile-iphone --project=desktop-1280` green
- `git diff --name-only` shows no router.tsx / api.ts / progress.ts / internal/** entries
</verification>

<success_criteria>
- On a phone: container cards open a stacked detail with the full-height touch tree and a pinned Save bar (live count, queue-flush Save, CACHEDIR line); Back preserves list scroll
- File sets read as coverage cards with the honest ticked-count line and embed the SAME tree + Save bar
- Triggering from either surface is byte-identical BackupButton semantics with a deep-link into the 06-04 run sheet
- Desktop >=48rem renders both pages exactly as today
</success_criteria>

<specless_note>
Planner resolution of the fallback coverage report: SCRN-02 boundary probe resolves to the explicit truths (guard respected on unreachable/busy rows; count statement at 0 and all-ticked; back-row semantics) — the zero tick case reads "0 folders handed to restic" via the invariant-{n} key; SCRN-02/03 precision probe resolves to integer counts in tabular numerals (no percentages/floats); SCRN-03 idempotency + concurrency probes resolve to the serialized queue's one-in-flight + dirty-flag contract (byte-identical to desktop); SCRN-04 adjacency probe resolves to the fileSetEditorKey remount contract (equal key = no spurious remount); SCRN-04 empty probe resolves to files.emptyRule + existing empty copy; SCRN-04 ordering probe resolves to the existing server list order with no re-sort introduced (backstop: e2e asserts stable card order); SCRN-04 idempotency/concurrency resolve to the same queue contract. UI-SPEC "UI Considerations" E2/E3/E4/E5 rows (empty/loading/error/populated explicit; partial + overflow backstops) lift into these truths and the e2e backstops. Nothing dropped.
</specless_note>

<output>
Create `.planning/phases/06-maquette-screens/06-05-SUMMARY.md` when done
</output>
