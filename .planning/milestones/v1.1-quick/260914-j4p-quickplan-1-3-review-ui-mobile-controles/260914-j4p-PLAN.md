---
phase: quick
plan: 260914-j4p
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/Selector.tsx
  - web/src/components/Selector.dom.test.tsx
  - web/src/components/Selector.mobile.dom.test.tsx
  - web/src/components/mobile/ListToolbar.tsx
  - web/src/pages/Settings.tsx
  - web/src/pages/settings/LanguageCard.tsx
autonomous: true
requirements: [] # quickplan: UI-review fix batch, no ROADMAP requirement IDs
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - At 390px, Settings > General renders the Theme/Corners/Animations/Labels pickers as content-hugging segments in 1-2 rows (no 200px-pinned stack, no ~2400px page scroll)
    - At 390px, the Containers and VMs toolbar filter chips wrap onto multiple rows instead of clipping mid-word behind a native scrollbar
    - At 390px, all 7 Settings section chips are visible/reachable via a wrapped strip, no signifier-less horizontal scroll
    - At 390px the Language trigger spans the Card body width; at >=48rem it stays the 192px flush trigger
    - At >=48rem the rendered DOM of every touched surface is unchanged: only max-md:-scoped classes and a JS branch keyed on the existing DESKTOP_QUERY were added
  artifacts:
    - web/src/components/Selector.tsx (pinWidth gated on useIsDesktop + documented header paragraph)
    - web/src/components/mobile/ListToolbar.tsx (chips row wraps below md)
    - web/src/pages/Settings.tsx (mobile section chip strip wraps below md)
    - web/src/pages/settings/LanguageCard.tsx (full-width trigger below md)
  key_links:
    - Selector pinWidth -> useIsDesktop (DESKTOP_QUERY in web/src/lib/useMediaQuery.ts is the ONLY breakpoint authority; no new width literal anywhere)
    - LanguageCard max-md:w-full button -> max-md:block on its inline-block wrapper (an inline-block parent collapses a full-width child back to content width)
---

<objective>
Fix the four mobile-overflow findings from the 390px UI review (lot 1): P1-9 (Selector equalWidth pinning stacks 200px blocks on phones), P1-2 (ListToolbar chips clip mid-word), P1-3 (Settings section nav hides 5 of 7 pills), P2-10 (Language button's orphaned fixed width). All four are layout-only fixes below the 48rem breakpoint; desktop rendering stays byte-identical by construction.

Purpose: the v1.1 mobile shell must never present controls that overflow, clip, or hide behind an unsignalled horizontal scroll on a 390px phone. The mechanisms were already diagnosed by DOM measurement; this plan implements them, it does not re-diagnose.

Output: 4 modified source files + 1 new mobile-half dom test file, all existing vitest suites green, web build green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@web/src/lib/useMediaQuery.ts
@web/src/components/Selector.tsx
@web/src/components/Selector.dom.test.tsx
@web/src/components/mobile/ListToolbar.tsx
@web/src/pages/settings/LanguageCard.tsx

Hard constraints (all non-negotiable, from the review-fix brief):
- DESKTOP_QUERY `(min-width: 48rem)` in web/src/lib/useMediaQuery.ts is the ONLY breakpoint authority. Mobile-only styling is written `max-md:` (Tailwind md == 48rem) — the codebase already uses this in 25 files (Button, Toggle, Settings, Flash, Fleet, Recovery, ...).
- Desktop byte-identical: NEVER change a class that renders above 48rem. Every added class is exclusively `max-md:`-scoped. The one JS behavior change (Selector pinning) keys on useIsDesktop, which reads the same DESKTOP_QUERY.
- D-06 touch floor 44px: no fix may reduce any control's height. All fixes here are width/wrap only; heights are untouched.
- Zero npm dependencies, zero backend, nothing outside web/src. Frozen surfaces: router.tsx, lib/api.ts, lib/progress.ts, internal/** — do not touch.
- No new locale keys; no user-visible strings are added by any of these fixes.
- The 8 bombvault/* lint rules apply (no em dashes anywhere, controls read engine tokens). These fixes add only layout utilities, so nothing new can trip them.
- House comment discipline: Selector.tsx, ListToolbar.tsx and Settings.tsx carry long load-bearing "why" paragraphs. Where a fix REVERSES a documented decision, the paragraph is rewritten in the same narrative voice — never deleted, never contradicted by leaving the old rationale in place.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Selector — suppress equalWidth pinning below 48rem (P1-9)</name>
  <files>web/src/components/Selector.tsx, web/src/components/Selector.dom.test.tsx, web/src/components/Selector.mobile.dom.test.tsx</files>
  <behavior>
    - New file Selector.mobile.dom.test.tsx (whole file stubs window.matchMedia via vi.stubGlobal in beforeAll, restored in afterAll; the mock returns matches:false for every query — key it on the imported DESKTOP_QUERY constant, never a literal string — with no-op addEventListener/removeEventListener/addListener/removeListener and onchange:null):
      - Test "equalWidth chip strip does not pin below 48rem": render Selector with equalWidth and the file's existing getBoundingClientRect mock pattern (natural widths 220/180/260 per segment, copied from the desktop pinning test in Selector.dom.test.tsx around line 304) -> every segment button has style.width === "" (no inline width, no 260px, no MIN_PINNED_WIDTH floor applied).
      - Test "equalWidth well strip does not pin below 48rem": same mock, variant="well" equalWidth -> every segment style.width === ""; rerender with the same items -> still "" (the measurement pass stays suppressed, matchedWidth never set).
      - Test "content-hugging fallback keeps the equalWidth scale classes": variant="well" equalWidth -> segment className still contains the fixed-height and centring classes from the pinned branch (below md only the inline width disappears, not the branch classes).
    - Existing Selector.dom.test.tsx pinning assertions (style.width "260px", MIN_PINNED_WIDTH floor, flex-wrap-not-nowrap) MUST PASS UNMODIFIED: that file defines no matchMedia, so the guarded desktop-default stub from the vitest setupFiles answers desktop and pinWidth stays true. Do not weaken or rewrite any existing assertion.
  </behavior>
  <action>In Selector.tsx: import useIsDesktop from ../lib/useMediaQuery (the single DESKTOP_QUERY authority; introducing any new width literal or media query string is forbidden). Inside the Selector component body, above the pinWidth computation at line 774, call the hook and conjoin it into the existing expression so pinning requires the desktop query to match: equalWidth AND isDesktop AND the existing labelsOffScreen exemption, keeping the exemption's logic and operator precedence exactly as-is. Change nothing downstream: the two-pass measurement effects, the inline width merge at lines 1130-1135, and the segment class branches at lines 1112-1118 (keyed on the equalWidth PROP, not pinWidth) all stay untouched — below 48rem segments simply lose the inline width and fall back to content-hugging inside the track's existing flex-wrap, which is the file's own documented "wraps, never scrolls" presentation for an unpinned well. Above 48rem pinWidth evaluates identically to today, so the desktop DOM (classes AND inline widths) is byte-identical.
    Then add one narrative paragraph to the file header comment block, placed directly after item 5c (MIN_PINNED_WIDTH, ends ~line 154) as a new lettered sub-item, in the file's existing voice: state that the 200px pin is a desktop-scale decision; cite the measured finding (a ~303px phone column fits ONE 200px segment per row, so the Settings General tab stacked 12+ pinned 200x43 blocks into ~2400px of page scroll at 390px); state that below 48rem the pinning measurement is suppressed so these strips render the content-hugging presentation, which already wraps per the file's "wraps, never scrolls" rule; state that desktop >=48rem is byte-identical because the gate rides useIsDesktop on the same DESKTOP_QUERY. Cross-reference item 5b/5c rather than restating them.
    Also extend the short comment directly above the pinWidth line (the block ending "whether to pin is a decision about the whole row") with one or two sentences naming the mobile suppression and pointing at the new header sub-item, so the line-level reader is not surprised by the hook.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/components/Selector.test.ts src/components/Selector.dom.test.tsx src/components/Selector.mobile.dom.test.tsx</automated>
  </verify>
  <done>Selector.mobile.dom.test.tsx proves no inline width below 48rem for both variants; every pre-existing Selector assertion passes unmodified (jsdom answers desktop); no class or inline-style change exists on the desktop render path; header comment documents the mobile decision with the "wraps, never scrolls" citation.</done>
</task>

<task type="auto">
  <name>Task 2: ListToolbar — chips row wraps below md (P1-2)</name>
  <files>web/src/components/mobile/ListToolbar.tsx</files>
  <action>In the chips row div (line 63), append max-md:flex-wrap to the existing className and keep every existing class exactly as-is (flex items-center gap-2 overflow-x-auto and the nowrap utility all stay; the max-md variant overrides below 48rem, and no consumer renders this component at >=48rem anyway — Containers.tsx:4244, VMs.tsx and Files.tsx all mount it behind !isDesktop gates, so the desktop DOM cannot change either way). Removing the base nowrap utility is forbidden: the constraint is that added classes are exclusively max-md:-scoped, and keeping the base untouched keeps any hypothetical wide render identical.
    Rewrite the two-line comment above that div (lines 60-62), which currently rules "never wrapping — a second row of chips would eat the card list's first row on 320-360px phones". Replace it with the documented reversal, in the file's narrative voice: cite the UI review finding (on Containers at 390px, 3 filter columns + Sort measured 451px inside 343px available, chips clipped mid-word behind a signifier-less native scrollbar); state that chips now wrap below md per the house "wraps, never scrolls" rule documented in Selector.tsx; name the consumer split (Containers passes FilterControl + ChipFilter chips, VMs passes ChipFilter chips, Files passes no children so the row does not render there) and note the row growing taller is accepted because the toolbar is sticky-in-flow, not fixed (the phase 5 no-fixed contract is unaffected).
  </action>
  <verify>
    <automated>cd web && npx vitest run src/app/mobileShellSource.test.ts && grep -c "max-md:flex-wrap" src/components/mobile/ListToolbar.tsx</automated>
  </verify>
  <done>Chips row wraps below 48rem on Containers and VMs; base class list otherwise unchanged; mobileShellSource.test.ts (which holds ListToolbar.tsx's whole text to the mobile type-scale asserts) stays green; rewritten comment documents the reversal with the consumer split.</done>
</task>

<task type="auto">
  <name>Task 3: Settings mobile section nav wraps + Language button full-width (P1-3, P2-10)</name>
  <files>web/src/pages/Settings.tsx, web/src/pages/settings/LanguageCard.tsx</files>
  <action>Settings.tsx: in the mobile chip strip nav (line 2658, mounted behind !isDesktop), append max-md:flex-wrap to the existing className (-mx-1 flex gap-2 overflow-x-auto px-1 py-1 all stay). Keep the per-chip classes (min-h-11 shrink-0 whitespace-nowrap) untouched — each chip stays one line at its 44px floor, the ROW wraps, so D-06 is untouched. Rewrite the "Scroll, never wrap" paragraph inside the block comment above (lines 2646-2654): keep the #178 history but document the reversal — cite the UI review measurement (scrollWidth 617 vs clientWidth 351 at 390px, 5 of 7 sections invisible with no scroll signifier) and rule that mobile discoverability beats the single-row aesthetic, noting #178's ragged-wrap regression was about the desktop strip, while this nav is the mobile-only half that now deliberately wraps.
    LanguageCard.tsx: append max-md:w-full to the trigger button className (line 90) — the w-48 desktop width stays untouched. ALSO append max-md:block to the wrapper div className (line 58, currently inline-block): an inline-block parent shrink-wraps its content, so a full-width button inside it would collapse straight back to label width and silently no-op the fix — the wrapper must become a block below md for the button to actually span the Card body. Extend the existing w-48 comment block (lines 59-73) with a short closing passage: below md a deliberate 192px reads as an orphaned fixed width inside a full-width card (UI review P2-10), so the trigger goes full-width under the same breakpoint; desktop keeps the flush trigger-over-listbox footprint, and DropdownListbox already sizes its portalled panel to the trigger's own measured width, so the mobile panel follows automatically — no second width literal, no new key.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/pages/Settings.languageCard.dom.test.tsx src/pages/Settings.settingsWrites.dom.test.tsx src/pages/Settings.themeCard.dom.test.tsx && grep -c "max-md:w-full" src/pages/settings/LanguageCard.tsx && grep -c "max-md:block" src/pages/settings/LanguageCard.tsx && grep -c "max-md:flex-wrap" src/pages/Settings.tsx</automated>
  </verify>
  <done>All 7 Settings section chips reachable via a wrapped strip below md; Language trigger spans the Card body below md while staying 192px at >=48rem; every added class is max-md:-scoped; existing Settings dom suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None crossed. All four fixes are CSS class additions and one client-side media-query branch in presentation components; no data flow, no API, no subprocess, no dependency change.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-QP-01 | Tampering | class-string edits in shared components (Selector, ListToolbar) | low | mitigate | Desktop identity asserted by construction (max-md:-only additions, useIsDesktop gate on DESKTOP_QUERY) plus the existing desktop-untouched e2e at the next full gate; existing dom suites pin the desktop class strings |
| T-QP-02 | Information Disclosure | none — no new rendering of user data | low | accept | Layout-only change; no strings, no keys, no data paths touched |
</threat_model>

<verification>
- Targeted suites: cd web && npx vitest run src/components/Selector.test.ts src/components/Selector.dom.test.tsx src/components/Selector.mobile.dom.test.tsx src/app/mobileShellSource.test.ts src/pages/Settings.languageCard.dom.test.tsx src/pages/Settings.settingsWrites.dom.test.tsx src/pages/Settings.themeCard.dom.test.tsx — all green.
- Type + bundle gate: cd web && npm run build (tsc --noEmit && vite build) — green; commit the regenerated tracked web/dist files per the repo rule (the SPA is embedded). Do NOT git add any pre-existing untracked artifact (ui-*.png, uat-*.png, .playwright-mcp/, web/pw-*.log, .gsd/, agent-history.json, Recovery.mobile.dom.test.tsx's own pre-existing copy is untracked — only commit files this plan created/modified).
- desktop-untouched.spec.ts is NOT runnable in this quick task (needs a built binary + served harness); desktop identity holds by construction — zero non-max-md class changes, zero changes to the desktop render path of Selector's pinning (pinWidth identical when DESKTOP_QUERY matches) — and is re-proven at the next full e2e gate.
- jsdom limitation, by design: media queries do not apply in jsdom, so no test asserts max-md class BEHAVIOR; the new Selector.mobile.dom.test.tsx asserts the JS half (pinWidth suppression) which IS testable, and the CSS halves are proven by the next real-device/e2e sweep.
</verification>

<success_criteria>
- P1-9: below 48rem no Selector segment carries an inline pinned width; Settings General pickers fit 1-2 wrapped rows at 390px; at >=48rem pinning behaves exactly as before.
- P1-2: ListToolbar chips wrap below md on Containers and VMs; no chip text clipped mid-word by a horizontal scroller.
- P1-3: the Settings mobile section strip wraps; all 7 pills reachable at 390px without a signifier-less scroll.
- P2-10: Language trigger spans the Card body below md (wrapper included in the fix), stays w-48 on desktop.
- All four documented decisions live in rewritten/extended load-bearing comments, house narrative style, no contradiction left behind.
- Zero changes outside web/src; zero new deps; zero new locale keys; zero desktop-rendered class changes.
</success_criteria>

<output>
Create `.planning/quick/260914-j4p-quickplan-1-3-review-ui-mobile-controles/260914-j4p-SUMMARY.md` when done
</output>
