---
phase: quick
plan: 260914-l7c
subsystem: web-spa-mobile-layout
tags: [mobile, touch-targets, folderbrowser, fresh-install-nudge, humanbytes, max-md, ui-review-lot3]
requires:
  - web/src/pages/Containers.tsx (row cibles/Add ~1515-1554, bloc commentaire du contrôle Add ~1524-1546)
  - web/src/components/FolderBrowser.tsx (rangée champ+déclencheur ~414-505, 19 call sites partagés)
  - web/src/pages/Dashboard.tsx (FreshInstallNudge ~1739-1787 : CTA Link ~1771-1776, chip close ~1778-1784)
  - web/src/components/Button.tsx (MOBILE_BLEED ~127, exclusion chip documentée ~122-126, injection className ~371)
  - web/src/index.css (.glim-btn-chip ~283-301 : boîte engine 18x18px, padding-inline:0, pas d'overflow clip)
  - web/src/lib/forecast.ts (humanBytes ~23-33, le formateur partagé) + web/src/lib/forecast.test.ts (describe humanBytes ~106-112)
  - web/src/lib/activityLog.ts (formatBytesShort ~178-188, miroir documenté) + web/src/components/RestorePanel.tsx (humanBytes ~25-35, miroir documenté)
  - web/src/pages/Config.tsx ~884 et web/src/pages/Flash.tsx ~671 (le precedent Link-contrôle de la maison : min-h-[2.75rem])
provides:
  - P0-1 — panel cibles containers à 390px : champ FolderBrowser pleine largeur, Browse emballé dessous, Add sur sa propre ligne — zéro chevauchement (desktop horizontale inchangée)
  - P1-4 — le × du FreshInstallNudge gagne un pavé tactile mobile ≥44px au call site, Button.tsx intact
  - P2-5 — le CTA Recovery du nudge atteint 44px sous md via le visage Link-contrôle de la maison
  - P2-6 — les trois formateurs d'octets miroirs promeuvent à 95% de chaque frontière ("1022.7 KB" lit "1.0 MB")
affects:
  - Containers > panel cibles à 390px (plus de chevauchement champ/Add)
  - TOUS les call sites FolderBrowser sur mobile (même débordement latente corrigée partout — RestorePanel, dialog Files, Settings, Recovery) ; desktop ≥48rem identique par construction
  - Dashboard > FreshInstallNudge à 390px (× et CTA au plancher D-06)
  - Tout affichage d'octets près d'une frontière d'unité (Dashboard, activity log, RestorePanel) — cas limites ~971-1023 B lisent désormais "1.0 KB"
tech-stack:
  added: []
  patterns:
    - max-md:-scoped-only class additions (rendu desktop identique par construction)
    - repetition call-site du pattern de bleed D-06 (classes MOBILE_BLEED passées via className, recalculées pour la boîte 18px du chip)
    - visage Link-contrôle de la maison (min-h-[2.75rem], precedent Config/Flash destinations-gate)
    - seuil de promotion à 95% de frontière dans trois formateurs miroirs tenus en lockstep
key-files:
  created: []
  modified:
    - web/src/pages/Containers.tsx
    - web/src/components/FolderBrowser.tsx
    - web/src/pages/Dashboard.tsx
    - web/src/lib/forecast.ts
    - web/src/lib/forecast.test.ts
    - web/src/lib/activityLog.ts
    - web/src/components/RestorePanel.tsx
    - web/dist/index.html (rebuild commité dans chaque commit de code)
decisions:
  - "P0-1 vérifié à la source 2026-09-14 : la rangée du brief existe EXACTEMENT (Containers.tsx:1515 `flex items-end gap-2 pt-1`, wrapper FolderBrowser `flex-1 min-w-0`, Add = Button glyph-only). MAIS l'empilement du seul call site ne suffit pas : la rangée interne de FolderBrowser (input flex-1 SANS min-w-0, min-content intrinsèque ~192px + Browse shrink-0 ~90-150px en mode texte) ne tient jamais dans ~223px — l'option 'input+Browse wrapping' du brief est donc OBLIGATOIRE, pas optionnelle. Fix en deux fichiers : la rangée externe emballe (max-md:flex-wrap + wrapper max-md:min-w-full), FolderBrowser emballe sa rangée interne (max-md:flex-wrap + input max-md:min-w-full) — le trick min-width:100% force le wrap d'un item flex-1 sans dépendre de l'ordre de cascade basis-vs-shorthand, et le pattern same-property max-md-overrides-base est déjà prouvé dans ce repo (LanguageCard w-48/max-md:w-full, lot 1)."
  - "P1-4 : le chip est EXCLU de MOBILE_BLEED volontairement (Button.tsx ~122-126 : un bleed de 12px avalerait les taps du pill hôte) — la doc du chip dit 'rides inside its host row's own touch floor'. ICI le chip n'est PAS dans un pill : il est le seul contrôle de fermeture d'une flex row, donc le call site doit le plancher D-06 lui-même. Le call site repasse les classes du bleed via className (le mécanisme exact du D-06 fix 1, pseudo-élément ::after = les clics du bleed portent le bouton ; un wrapper paddé ne marcherait PAS — le padding du wrapper est une zone morte qui ne déclenche pas l'enfant). Inset recalculé : boîte engine 18px + 2×12px = 42px < 44 → 14px (échelle plain -inset-3.5) → 46px ≥ plancher. Button.tsx NON touché."
  - "P2-5 : PAS de conversion vers Button (rend un <button>, ne peut pas naviguer ; aucun composant LinkButton engine n'existe — en créer un sort du quick). Le CTA garde son traitement accent rempli documenté (bloc commentaire 1761-1770, toujours vrai) et gagne la hauteur Link-contrôle de la maison SOUS md uniquement : max-md:min-h-[2.75rem], la valeur exacte du precedent Config.tsx:884 / Flash.tsx:671. Le desktop garde la hauteur contrôle engine 32px."
  - "P2-6 : les TROIS formateurs (forecast.ts humanBytes partagé, activityLog.ts formatBytesShort, RestorePanel.tsx humanBytes) changent EN LOCKSTEP — chacun porte un commentaire miroir ('mirrors Dashboard's humanBytes' / 'reads the same everywhere') qui deviendrait faux si un seul bouge. Nouvelle boucle : promouvoir dès 95% de la frontière (v >= 1024 * 0.95) — un valeur qui s'arrondit au '1.0' de l'unité supérieure ne s'affiche jamais dans l'unité inférieure. Toutes les attentes de tests existantes vérifiées inchangées (512 B, 1.5 KB, 2.0 GB, 1.0 MB, 2.0 MB, 1.0 KB, 2.0 KB) ; seuls les cas 971-1023 B → '1.0 KB' et 972.8+ KB → '1.0 MB' bougent, par la règle elle-même."
  - "ZÉRO nouvelle clé de locale : P0-1/P2-6 ne touchent aucun texte ; P1-4 réutilise common.close, P2-5 réutilise recovery.freshNudgeCta — déjà traduits dans les 42 tables. i18n.preseed.test.ts vert = porte. Si jamais une édition exigeait une clé : STOP, pré-semer les 42 tables d'abord (convention single-commit), sinon ne rien commiter."
metrics:
  estimated_duration: 30-40min
---

# Quick Task 260914-l7c: Review UI mobile lot 3/3 — cibles tactiles + polish (P0-1, P1-4, P2-5, P2-6)

## Objectif

Corriger le dernier lot des findings du review UI mobile 390px — fichiers disjoints des lots 1-2 :

- **P0-1** (le seul P0) : dans le panel cibles containers, le FolderBrowser (~350px de contenu min) déborde de la colonne mobile (~223px à 390px) et CHEVAUCHE le bouton Add (prouvé elementFromPoint). Fix sous md : rangée externe emballe (champ pleine largeur, Add dessous) ET la rangée interne champ+Browse de FolderBrowser emballe (champ pleine largeur, Browse dessous). Desktop ≥48rem : rangée horizontale inchangée.
- **P1-4** : le × close du FreshInstallNudge utilise variant="chip", délibérément exclu du bleed mobile de Button (~24px, sous le plancher D-06 44px). Fix au call site, sans toucher Button.tsx.
- **P2-5** : le CTA Recovery du nudge est un `<Link>` brut ~32px — gagne la hauteur Link-contrôle maison (44px) sous md.
- **P2-6** : "1022.7 KB" n'est pas promu — les trois formateurs d'octets miroirs promeuvent à 95% de chaque frontière d'unité.

Purpose: fermer le lot 3/3 avec le seul P0 du review corrigé sans régression desktop, le chip D-06 réglé à SA place (le call site, pas le composant), et les trois formateurs miroirs alignés pour que leurs commentaires restent vrais.
Output: 2 commits atomiques (code + web/dist ensemble, UN trailer), SUMMARY avec les dispositions.

## Contexte de réconciliation (LIRE AVANT TOUT — vérifié à la source le 2026-09-14, ré-ancore-toi sur le CONTENU, pas les numéros de ligne)

- **Containers.tsx:1515-1554 (P0-1)** — la rangée existe exactement : `<div className="flex items-end gap-2 pt-1">` contenant le wrapper `<div className="flex-1 min-w-0">` du FolderBrowser (label `t("folders.addCustom")`) puis le Button Add (labelKey `folders.add`, glyph IconAdd, tone accent). Le contrôle Add est un **Button** (glyph-only, ton accent) — le gros bloc commentaire au-dessus (~1524-1546) raconte l'historique Badge/`size="icon"` d'avant sa conversion ; ses passages encore vrais restent, la phrase « shares an items-end row with a FolderBrowser field » doit être complétée (voir Task 1).
- **FolderBrowser.tsx:414-505 (P0-1, la cause racine)** — rangée interne `<div className="flex items-center gap-2">` : input `flex-1 rounded-control ... text-sm font-mono px-3 py-1.5 glim-field-focus` (PAS de min-w-0 → ne rétrécit pas sous son min-content intrinsèque ~192px) + Button Browse `className="shrink-0"` (~90-150px selon la langue en mode texte). En mode texte la rangée interne vaut ~290-350px : elle déborde de N'IMPORTELLE colonne mobile étroite, même une fois le wrapper externe passé pleine largeur. Composant partagé (~19 call sites : Containers, RestorePanel, dialog add-folder-set Files, Settings, Recovery, PathModeSwitch) — le fix max-md interne le corrige partout, desktop identique partout. Les immenses blocs commentaires existants du composant sont INTACTS (encore vrais).
- **Button.tsx (P1-4)** — `MOBILE_BLEED` (~127) = `max-md:relative max-md:after:absolute max-md:after:-inset-3 max-md:after:content-['']`, injecté SEULEMENT hors chip (~371 : `${chip ? "" : ...}`) ; l'exclusion est documentée ~122-126 (un bleed 12px avalerait les taps du pill hôte ; « the chip keeps its engine-deliberate 18px box and rides inside its host row's own touch floor instead »). `.glim-btn-chip` (index.css:283-301) = boîte 18x18px, `padding-inline: 0`, PAS d'overflow clip → le pseudo-élément ::after fonctionne identiquement au variant default. ARITHMÉTIQUE : 18 + 2×12 = 42px < 44 → il faut 14px par côté (`-inset-3.5`, échelle plain Tailwind) → 46px ≥ plancher.
- **Dashboard.tsx:1739-1787 (P1-4 + P2-5)** — FreshInstallNudge : carte `flex items-center gap-4` p-5 ; CTA `<Link to="/recovery">` ~1771-1776 (`self-start inline-flex items-center gap-1 rounded-control bg-accent px-4 py-1.5 text-sm font-medium text-accentContrast hover:opacity-90 transition-opacity` — tokens engine déjà propres pour la règle lint, le bloc commentaire 1761-1770 qui justifie le traitement rempli reste VRAI, on l'étend) ; chip close ~1778-1784 (`labelKey="common.close"`, `className="shrink-0"`).
- **Precedent Link-contrôle** — Config.tsx:884 et Flash.tsx:671 (blocs destinations-gate) : `className="flex min-h-[2.75rem] items-center rounded-control bg-carbon-surface2 px-4 text-sm text-carbon-text"`. `min-h-[2.75rem]` (= 44px, forme bracket maison) EST la hauteur Link-contrôle de la maison — on la reprend scoppée max-md.
- **Formateurs (P2-6)** — trois copies identiques et documentées-comme-miroirs : `forecast.ts:23-33` (partagé : Dashboard 1599/2209/2324, Containers 2442/2451), `activityLog.ts:178-188`, `RestorePanel.tsx:25-35`. Boucle actuelle `while (v >= 1024 && i < units.length - 1)` → "1022.7 KB" est un affichage CORRECT du seuil 1024 exact ; le finding demande la promotion conventionnelle. ATTENDUS DE TESTS EXISTANTS TOUS INCHANGÉS avec le nouveau seuil : forecast.test 512 B / 1.5 KB / 2.0 GB ; excludesAssistant.dom "1.0 MB"/"2.0 MB"/"1.0 KB" (fixtures sizeBytes 1024 et 1 MB/2 MB) ; activityLog.test "2.0 KB". Vérifié par calcul, pas supposé.
- **Clés de locale** — aucune nouvelle : `common.close` et `recovery.freshNudge*` existent dans les 42 tables (vérifié par grep). Porte : `i18n.preseed.test.ts` vert à chaque tâche. Si une édition exigeait quand même une clé : STOP, pré-semer les 42 tables dans UN commit dédié avant tout autre commit.

Contraintes non négociables : DESKTOP_QUERY seule autorité ; classes d'ajout exclusivement `max-md:` (base intacte) ; rendu desktop >48rem identique ; zéro dep npm ; zéro backend ; D-06 plancher 44px sous md (aucun contrôle rétrécit) ; commentaires why réécrits/complétés là où le changement les contredit, en PARAPHRASANT (jamais la littérale qu'un gate grep compte) ; em-dashes interdits dans le texte utilisateur (aucun texte touché de toute façon).

Environnement Windows (leçons lots 1-2) : JAMAIS `npm run build` ni `npx vitest` — invoque node directement depuis `web/` :
- `node node_modules/typescript/bin/tsc --noEmit`
- `node node_modules/vite/bin/vite.js build`
- `node node_modules/vitest/vitest.mjs run <fichiers>`
- `node node_modules/eslint/bin/eslint.js <fichiers>`

Avant chaque commit de code : build (il régénère web/dist/index.html — convention lots 1-2 : committer le index.html rebuilt dans le MÊME commit ; seuls index.html est tracké sous web/dist), UN SEUL trailer `Co-Authored-By: Claude Code <noreply@anthropic.com>`. Ne touche à AUCUN artifact non tracké préexistant (screenshots ui-*/uat-*, .playwright-mcp/, .gsd/, web/pw-*.log, caches research, web/e2e/guided-restore.spec.ts, web/src/pages/Recovery.mobile.dom.test.tsx) et laisse la modification locale de web/e2e/desktop-untouched.spec.ts NON commitée.

<tasks>

<task type="auto">
  <name>Task 1: P0-1 — panel cibles containers : empilement mobile de la rangée Add + wrap interne FolderBrowser</name>
  <files>web/src/pages/Containers.tsx, web/src/components/FolderBrowser.tsx, web/dist/index.html</files>
  <action>
1. **Containers.tsx, rangée externe (~1515)** — ancre de contenu : `<div className="flex items-end gap-2 pt-1">` suivie du wrapper `<div className="flex-1 min-w-0">` du FolderBrowser. Ajoute ` max-md:flex-wrap` au className de la rangée et ` max-md:min-w-full` au className du wrapper (base `flex-1 min-w-0` intacte). Sous md le wrapper (min-width:100%) prend sa propre ligne et l'Add emballe sur la ligne suivante, bordée à gauche ; ≥48rem rien ne change (les max-md: ne s'appliquent pas, la rangée ne tient que si le contenu tient — ample sur desktop).
2. **Containers.tsx, commentaire why** — au-dessus de la rangée, court bloc maison : sous 48rem la colonne du panel (~223px à 390px) est plus étroite que le min-content du champ + Browse, la rangée horizontale faisait chevaucher le champ et le bouton Add (P0 du review 390px du 2026-09-14, prouvé elementFromPoint) ; la rangée emballe donc : champ pleine largeur, action d'ajout dessous ; le desktop garde la rangée horizontale items-end. PARAPHRASE — la littérale de classes ne doit PAS apparaître dans le commentaire (hygiène des gates grep).
3. **Containers.tsx, bloc commentaire du contrôle Add (~1524-1546)** — la phrase « this badge shares an `items-end` row with a FolderBrowser field » décrit une géométrie désormais width-conditionnelle. Complète-la d'une phrase : l'alignement items-end est la présentation desktop ; sous 48rem la rangée emballe et le voisinage vertical remplace le voisinage horizontal. Ne réécris PAS l'historique encore vrai (récit Badge → Button, size icon 32px, intégration hue) ; garde le paragraphe intègre et ajoute la clause.
4. **FolderBrowser.tsx, rangée interne (~414)** — ancre de contenu : `<div className="flex items-center gap-2">` contenant l'input de valeur puis le Button Browse (`className={"shrink-0"}`, glyph IconFolder). Ajoute ` max-md:flex-wrap` à la rangée et ` max-md:min-w-full` au className de l'input (la longue liste base de l'input reste intacte). Sous md : champ pleine largeur sur sa ligne, Browse emballe dessous — la rangée tient dans toute colonne mobile ; ≥48rem inchangé. C'est l'option « input+Browse wrapping » du brief, OBLIGATOIRE : sans elle, même pleine largeur, le min-content ~192px de l'input + Browse déborderait encore de ~223px.
5. **FolderBrowser.tsx, commentaire why** — au-dessus de la rangée interne (ou en fin du commentaire existant la couvrant), court paragraphe : les colonnes mobiles étroites sont plus étroites que le min-content du champ ; sous 48rem le champ prend la ligne entière et le déclencheur emballe, pour tous les call sites partagés (même débordement latente au RestorePanel, au dialog add-folder-set de Files, à Settings, à Recovery — mesuré à 390px dans le panel cibles containers, P0 du review du 2026-09-14) ; le desktop garde la rangée une-ligne. PARAPHRASE.
6. Aucun autre changement : pas de texte, pas de clé, pas de prop nouvelle, pas de desktop class. Les immenses blocs commentaires existants du composant (dialog, listes, [469]/[478]/[497]/[511]...) restent intacts.
  </action>
  <verify>
    <automated>cd web && node node_modules/typescript/bin/tsc --noEmit && node node_modules/eslint/bin/eslint.js src/pages/Containers.tsx src/components/FolderBrowser.tsx && node node_modules/vite/bin/vite.js build && node node_modules/vitest/vitest.mjs run src/pages/Containers.tree.dom.test.tsx src/pages/Containers.excludesAssistant.dom.test.tsx src/components/Button.dom.test.tsx src/lib/i18n.preseed.test.ts && [ "$(grep -cF 'max-md:flex-wrap' src/pages/Containers.tsx)" = "1" ] && [ "$(grep -cF 'max-md:min-w-full' src/pages/Containers.tsx)" = "1" ] && [ "$(grep -cF 'max-md:flex-wrap' src/components/FolderBrowser.tsx)" = "1" ] && [ "$(grep -cF 'max-md:min-w-full' src/components/FolderBrowser.tsx)" = "1" ]</automated>
  </verify>
  <done>À 390px dans le panel cibles : champ pleine largeur, Browse dessous, Add sur sa propre ligne — plus aucun chevauchement (le chemin P0-1 fermé) ; ≥48rem rangée horizontale identique octet de rendu près ; suites vertes (le preseed vert confirme zéro nouvelle clé) ; index.html rebuilt commité avec le code, UN trailer.</done>
</task>

<task type="auto">
  <name>Task 2: P1-4 + P2-5 + P2-6 — nudge : pavé tactile du chip + hauteur du CTA ; promotion des octets à 95% de frontière dans les trois miroirs</name>
  <files>web/src/pages/Dashboard.tsx, web/src/lib/forecast.ts, web/src/lib/forecast.test.ts, web/src/lib/activityLog.ts, web/src/components/RestorePanel.tsx, web/dist/index.html</files>
  <action>
1. **P1-4 — Dashboard.tsx, chip close du FreshInstallNudge (~1778-1784)** — ancre de contenu : le Button `labelKey="common.close"` `variant="chip"` `className="shrink-0"`. Étends le className : `shrink-0 max-md:relative max-md:after:absolute max-md:after:-inset-3.5 max-md:after:content-['']` (les classes exactes du pattern D-06 fix 1 de Button.tsx, avec l'inset recalculé). Au-dessus du Button, bloc commentaire why : le variant chip exclut délibérément le bleed partagé (citer Button.tsx, bloc « Scoped to the DEFAULT variant only ») parce qu'un chip vit DANS un pill dont la rangée hôte porte le plancher — ICI le chip est le seul contrôle de fermeture de la carte, libre dans une flex row, donc le call site doit le plancher lui-même ; 14px par côté (pas les 12px partagés) parce que la boîte engine 18px + 2×12 = 42 reste sous le plancher de 44, 14px donne 46 ; un wrapper paddé ne marcherait pas (le padding du wrapper est une zone morte, le pseudo-élément appartient au bouton donc les clics du bleed le déclenchent) ; ≥48rem rien ne s'applique. PARAPHRASE — pas la littérale de classes dans le commentaire. Button.tsx INTACT.
2. **P2-5 — Dashboard.tsx, CTA Link (~1771-1776)** — ancre de contenu : `<Link to="/recovery" className="self-start inline-flex items-center gap-1 rounded-control bg-accent px-4 py-1.5 ...">`. Ajoute ` max-md:min-h-[2.75rem]` en fin de className (la valeur exacte du precedent Link-contrôle de Config.tsx/Flash.tsx ; inline-flex+items-center déjà présents centrent verticalement). Étends le bloc commentaire existant (1761-1770, toujours vrai) d'une phrase : sous 48rem le CTA porte la hauteur Link-contrôle de la maison (même valeur que les Links des blocs destinations-gate de Config/Flash) ; le desktop garde la hauteur contrôle engine de 32px. PAS de conversion vers Button (rend un button, ne navigue pas ; aucun LinkButton engine n'existe — hors scope quick).
3. **P2-6 — les TROIS formateurs, en lockstep** — `forecast.ts` (~28), `activityLog.ts` (~183), `RestorePanel.tsx` (~30) : la condition de boucle `v >= 1024` devient `v >= 1024 * 0.95` (mêmes trois fichiers, même une-ligne de changement). Étends le commentaire doc de chaque formateur : promeut à 95% de chaque frontière d'unité pour qu'une valeur qui s'arrondit au 1.0 de l'unité supérieure ne s'affiche jamais dans l'inférieure (le review du 2026-09-14 : un affichage en kilooctets juste sous 1 Mo lit désormais 1.0 MB) ; la règle vaut à TOUTES les frontières donc les octets ~971-1023 lisent 1.0 KB — delta voulu ; les miroirs restent en lockstep pour que chaque commentaire « mirrors » reste vrai. PARAPHRASE — l'expression du seuil ne doit PAS apparaître dans un commentaire.
4. **forecast.test.ts** — dans le describe `humanBytes` (~106-112), ajoute un it : promotion à 95% de frontière — `humanBytes(1047245)` (1022.7 KB, le cas du review) retourne "1.0 MB" ; `humanBytes(994304)` (971 KB, juste sous) retourne "971.0 KB" ; `humanBytes(1048576)` (1024 KB exact) retourne "1.0 MB". Les attentes existantes restent telles quelles (déjà vérifiées inchangées).
5. Aucune clé de locale, aucun texte utilisateur, aucun changement Dashboard hors du nudge.
  </action>
  <verify>
    <automated>cd web && node node_modules/typescript/bin/tsc --noEmit && node node_modules/eslint/bin/eslint.js src/pages/Dashboard.tsx src/lib/forecast.ts src/lib/forecast.test.ts src/lib/activityLog.ts src/components/RestorePanel.tsx && node node_modules/vite/bin/vite.js build && node node_modules/vitest/vitest.mjs run src/lib/forecast.test.ts src/lib/activityLog.test.ts src/pages/Dashboard.protectionCard.dom.test.tsx src/pages/Dashboard.ransomwareCard.dom.test.tsx src/pages/Containers.excludesAssistant.dom.test.tsx src/lib/i18n.preseed.test.ts && [ "$(grep -cF 'max-md:after:-inset-3.5' src/pages/Dashboard.tsx)" = "1" ] && [ "$(grep -cF 'max-md:min-h-[2.75rem]' src/pages/Dashboard.tsx)" = "1" ] && [ "$(grep -cF '1024 * 0.95' src/lib/forecast.ts)" = "1" ] && [ "$(grep -cF '1024 * 0.95' src/lib/activityLog.ts)" = "1" ] && [ "$(grep -cF '1024 * 0.95' src/components/RestorePanel.tsx)" = "1" ]</automated>
  </verify>
  <done>Le × du nudge a une boîte d'activation ≥44px sous md (46px), Button.tsx intact ; le CTA Recovery fait ≥44px de haut sous md en restant le traitement accent rempli documenté ; "1022.7 KB" s'affiche "1.0 MB" partout où les trois formateurs rendent, tests existants inchangés + nouveaux cas verts ; ≥48rem rendu identique ; suites vertes ; index.html rebuilt commité avec le code, UN trailer.</done>
</task>

</tasks>

<verification>
- Par tâche : tsc --noEmit + eslint (fichiers touchés) + vite build verts, invocation node directe ; index.html rebuilt commité dans chaque commit de code.
- Gates grep par tâche (comptes exacts ci-dessus) ; commentaires paraphrasés pour ne jamais gonfler un compte.
- Suites vitest ciblées vertes ; i18n.preseed.test.ts vert aux deux tâches = zéro nouvelle clé (vérifié en amont : common.close et recovery.freshNudge* existent dans les 42 tables).
- Identité desktop : toutes les classes ajoutées sont max-md:-scoped, bases intactes (construction identique lots 1-2) ; jsdom voit desktop via le stub matchMedia — les suites existantes passent SANS modification d'attente ; desktop-untouched.spec.ts hors scope quick (binaire + harnais), re-prouvé à la prochaine gate e2e complète. Limitation jsdom assumée : aucun test n'affirme le comportement max-md (jsdom n'applique pas les media queries) — la géométrie mobile est prouvée par les mesures 390px du review + construction.
- P2-6 : les attentes de tests préexistantes sur les octets (512 B, 1.5 KB, 2.0 GB, 1.0 MB, 2.0 MB, 1.0 KB, 2.0 KB) passent SANS modification — vérifié par calcul en planification ; seuls les nouveaux cas promeuvent.
- zéro modification hors les 7 fichiers listés + dist + SUMMARY ; web/e2e/desktop-untouched.spec.ts (mod locale) NON commitée ; aucun artifact non tracké préexistant commité.
</verification>

<success_criteria>
- P0-1 : plus de chevauchement champ/Add à 390px (empilement rangée externe + wrap interne FolderBrowser) ; desktop inchangé ; les autres call sites FolderBrowser gagnent le même fix mobile par le même composant.
- P1-4 : × du nudge ≥44px sous md au call site, Button.tsx intact, commentaire chip-exclusion cité et réconcilié.
- P2-5 : CTA ≥44px sous md, precedent Config/Flash suivi, desktop 32px inchangé.
- P2-6 : "1022.7 KB" → "1.0 MB" (cas de test) ; trois miroirs en lockstep, commentaires miroirs restés vrais.
- 2 commits atomiques (code + dist, UN trailer Co-Authored-By chacun), SUMMARY à la fin.
- Aucune clé de locale nouvelle, aucune dep, aucun fichier hors contrainte, aucun artifact non tracké commité.
</success_criteria>

<output>
Crée `.planning/quick/260914-l7c-quickplan-3-3-review-ui-mobile-cibles-ta/260914-l7c-SUMMARY.md` en fin de tâche (frontmatter `status: complete`, sections habituelles du template quick : Tasks Delivered avec commits, What Changed, Verification, Deviations, Constraints Audit, Known Stubs, Self-Check).
</output>
