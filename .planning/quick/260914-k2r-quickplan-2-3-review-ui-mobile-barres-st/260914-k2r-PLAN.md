---
phase: quick
plan: 260914-k2r
subsystem: web-spa-mobile-layout
tags: [mobile, layout, sticky-action-bar, files, dashboard, destructive-tone, max-md, ui-review-lot2]
requires:
  - web/src/pages/Files.tsx (FileSetRow ~1483-1757, FileSetFoldersEditor ~1151-1477, mobile list ~2291-2370, Save bar ~2372-2405)
  - web/src/pages/Dashboard.tsx (page root ~2768, New-backup bar ~3071-3094)
  - web/src/components/mobile/StickyActionBar.tsx (the WHY-last-child contract, header comment lines 10-17)
  - web/src/components/Button.tsx (tone table lines 97-103 — tone="danger" EXISTS, the ConfirmDialog pairing)
  - web/src/pages/Containers.tsx (reference sticky pattern ~4401-4445)
provides:
  - P1-13 — single set-name header on mobile Files (expanded row defers to the MobileFileSetCard above)
  - P2-14 — Remove set reads destructive below 48rem via the shared Button danger tone (desktop byte-identical)
  - P1-11/P1-12 — measured dispositions: both sticky bars re-verified against the Containers last-child pattern, evidence recorded (conditional move only if the probe contradicts source)
affects:
  - Files > Ordres de sauvegarde at 390px (expanded set: one name header; destructive action visually distinct from CTAs)
  - Desktop rendering of every touched surface (unchanged by construction — all class additions max-md:-scoped; tone gate keys on useIsDesktop)
tech-stack:
  added: []
  patterns:
    - max-md:-scoped-only class addition (desktop DOM/render byte-identical)
    - useIsDesktop-gated component prop (D-01 double-gate family) for a mobile-only tone
    - verify-first reconciliation of live-measured review findings against the source tree (both branches fully specified)
key-files:
  created: []
  modified:
    - web/src/pages/Files.tsx
    - web/src/pages/Dashboard.tsx (ONLY if the P1-12 probe contradicts source — expected: no change)
    - web/dist/* (build output, committed per code commit)
decisions:
  - "P1-11/P1-12 disposition: source-verified 2026-09-14 — both StickyActionBars are ALREADY the last direct child of their PAGE_SHELL_RESPONSIVE column (Files.tsx bar ~2379 inside root opened ~2098; Dashboard.tsx bar ~3079-3094 inside root opened ~2768), with the Containers.tsx:4401 comment's exact contract. The measured geometry (bar overlapping the STORAGE card at rest; cutting Back up now) is normal PINNED-sticky behavior (bar pinned at viewport bottom while scrollH > clientH), not mid-page nesting. Plan therefore verifies first and moves ONLY if the probe contradicts source; a no-op JSX shuffle would churn desktop-identity risk for zero effect."
  - "P2-14 uses Button.tsx's EXISTING tone='danger' (bg-statusFailSolid/text-carbon-background — the ConfirmDialog recipe adopted into the tone table), gated tone={isDesktop ? 'accent' : 'danger'} because desktop >48rem identity is a non-negotiable constraint; jdp's 'Keine Sonderfarbe' ruling stays true on desktop and is rewritten as a scoped decision, not deleted."
  - "No new locale keys, no npm dependencies, no backend, no Layout.tsx changes (scroll-padding on main#bv-main is out of scope by the two-file constraint)."
metrics:
  estimated_duration: 30-40min
---

# Quick Task 260914-k2r: Review UI mobile lot 2/3 — barres sticky flottantes + éditeur Files (P1-11..P2-14)

## Objectif

Corriger le lot 2/3 des findings du review UI mobile 390px dans `web/src/pages/Files.tsx` et `web/src/pages/Dashboard.tsx` :

- **P1-13** (réel) : double header quand un set est étendu — le nom du set apparaît dans le header de carte ET répété dans le panneau étendu. Fix : masquer la répétition sous md.
- **P2-14** (réel) : « Remove set » porte le même accent plein que les CTA primaires. Fix : ton danger partagé de Button, mobile uniquement.
- **P1-11 / P1-12** (à reconcilier — voir ci-dessous) : barres sticky « flottantes ». Le mécanisme revendiqué (nichée au milieu de la page, pas dernier enfant) est CONTRAINT par la source : les deux barres sont déjà des dernières enfants directs conformes au pattern Containers.tsx. La géométrie mesurée est le comportement normal d'une barre sticky épinglée en bas de viewport. Le plan impose une **vérification d'abord**, avec les deux branches entièrement spécifiées.

Purpose: fermer le lot 2/3 sans no-op silencieux ni remaniement JSX risqué — les deux findings actionables sont corrigés, les deux findings géométriques sont clos avec preuve (ou déplacés si la preuve contredit la source).
Output: commits atomiques par tâche (build web inclus), SUMMARY avec dispositions P1-11/P1-12 argumentées.

## Contexte de réconciliation (LIRE AVANT TOUT — évite un faux travail)

Mesure faite à la source le 2026-09-14 (re-vérifie au moment de l'exécution, ne fais pas confiance aux numéros de ligne — ancre-toi sur le contenu) :

- **Files.tsx** : la barre Save mobile (ancre : le `StickyActionBar` sous la gate `{!isDesktop && expandedVisible && expandedSet !== null && expandedSet.path !== ""}`) est un enfant DIRECT de la racine `<div className={PAGE_SHELL_RESPONSIVE}>` (ligne ~2098), placé APRÈS la fermeture du bloc liste mobile (~2370) et avant les portails (RunDetailSheet/FileSetDialog). Aucun wrapper entre elle et la racine.
- **Dashboard.tsx** : la barre New backup (ancre : `{!isDesktop && (<StickyActionBar className="md:hidden">` avec le Button `home.newBackup`) est un enfant DIRECT de la racine `PAGE_SHELL_RESPONSIVE` (~2768), dernier enfant de contenu avant `{confirmDialog}` (~3079-3094). Son commentaire documente déjà « LAST DIRECT CHILD of the page column — sticky resolves against main#bv-main, so nothing may wrap it ».
- Le contrat maison est `components/mobile/StickyActionBar.tsx` lignes 10-17 (WHY THE LAST DIRECT CHILD…) et `Containers.tsx` ~4401-4410 (Pitfall 3).
- Conséquence : à scrollTop=0 avec `main` scrollH > clientH, une barre conforme est ÉPINGLÉE en bas du viewport et recouvre le contenu qui s'y trouve (carte STORAGE, bouton Back up now). C'est exactement ce que les mesures des findings décrivent — pas un défaut de DOM. NE déplace PAS les barres sur la seule foi du brief ; passe par le protocole de vérification des tâches.

Contraintes non négociables (rappel du brief) : DESKTOP_QUERY `(min-width: 48rem)` seule autorité ; classes d'ajout exclusivement `max-md:`/`md:` existantes ; rendu desktop >48rem IDENTIQUE ; zéro dep npm, zéro backend, rien hors Files.tsx + Dashboard.tsx (+ dist) ; D-06 floor 44px (aucun contrôle ne rétrécit) ; zéro nouvelle clé de locale ; commentaires why réécrits si ils contredisent le code (jamais laisser un commentaire contredire le code).

Environnement Windows (leçon lot 1) : JAMAIS `npm run build` ni `npx vitest` (les shims cmd perdent node) — invoques node directement depuis `web/` :
- `node node_modules/typescript/bin/tsc --noEmit`
- `node node_modules/vite/bin/vite.js build`
- `node node_modules/vitest/vitest.mjs run <fichier>`

Avant chaque commit de code : build, restaurer le placeholder `web/dist/index.html` si le build l'a remplacé (convention phase 8), committer les changements dist dans le commit de code correspondant. Ne touche à AUCUN artifact non tracké préexistant (screenshots ui-*/uat-*, .playwright-mcp/, web/pw-*.log, etc.).

<tasks>

<task type="auto">
  <name>Task 1: Files.tsx — P1-13 un seul header de nom + disposition P1-11 (barre Save)</name>
  <files>web/src/pages/Files.tsx, web/dist/*</files>
  <action>
1. **P1-13** — Dans `FileSetRow` (commence ~1483), bloc « Top row: name + chips, path, last backup » (~1560-1609) : repère le span du nom, ancre de contenu `className="font-semibold text-carbon-text text-sm truncate"` rendant `{set.name}`. Ajoute ` max-md:hidden` à ce className (et seulement lui — ne touche ni les badges ni la ligne path ni la colonne Last backup). Ajoute un court commentaire why AU-DESSUS du span, voix maison : sous 48rem, la MobileFileSetCard divulgueur juste au-dessus rend DÉJÀ ce nom (la carte EST le header de divulgation — voir son bloc commentaire ~1759-1766) ; le panneau étendu le répétait donc deux fois sur téléphone (finding P1-13, mesuré à 390px : deux spans de nom visibles simultanément set étendu) ; le desktop >48rem garde ce span car c'est LE titre de la ligne desktop (la liste desktop rend FileSetRow sans carte). Le commentaire PARAPHRASE — il ne cite pas la littérale de classes (hygiène des gates grep).
2. **P1-11 disposition (verify-first)** — Vérifie l'ancrage source décrit dans « Contexte de réconciliation » : la barre Save est enfant direct de la racine `PAGE_SHELL_RESPONSIVE`, après tout le contenu du set étendu (la liste mobile contient la FileSetRow étendue ; la barre vient après la fermeture de la liste). Si conforme (attendu) : étends le bloc commentaire de la barre (~2372-2378) avec le paragraphe « pourquoi dernier enfant » — la barre doit rester le DERNIER enfant visible de la colonne pour que `sticky bottom-0` résolve contre `main#bv-main` (nichée dans un Card/wrapper elle ne collerait que dans sa propre boîte, Pitfall 3), en citant `Containers.tsx` (~4401) et `components/mobile/StickyActionBar.tsx` (lignes 10-17). Si (inattendu) la vérification montre un wrapper intermédiaire ou un placement non-dernier : DÉPLACE le bloc entier `{!isDesktop && (<StickyActionBar>…</StickyActionBar>)}` en dernier enfant de contenu de la colonne (avant les portails), en conservant la gate et `md:hidden`, et note le déplacement dans le SUMMARY.
3. Aucune autre classe, aucun texte, aucune clé de locale.
  </action>
  <verify>
    <automated>cd web && node node_modules/typescript/bin/tsc --noEmit && node node_modules/vite/bin/vite.js build && node node_modules/vitest/vitest.mjs run src/lib/i18n.preseed.test.ts && [ "$(grep -c 'text-sm truncate max-md:hidden' src/pages/Files.tsx)" = "1" ]</automated>
  </verify>
  <done>Set étendu à 390px : le nom du set n'apparaît qu'une fois (header de carte) ; la ligne desktop garde son titre ; la barre Save est vérifiée dernier enfant visible de la colonne (ou déplacée conformément) ; tsc + build verts ; dist committé avec le code.</done>
</task>

<task type="auto">
  <name>Task 2: Files.tsx — P2-14 le bouton Remove set porte le ton danger partagé sous 48rem</name>
  <files>web/src/pages/Files.tsx, web/dist/*</files>
  <action>
1. Dans `FileSetRow`, rangée d'actions (~1618-1716) : repère le bouton remove, ancre de contenu `label={t("files.deleteSet")}` + `glyph={<IconTrash />}` + `tone="accent"` (~1685-1694). Remplace `tone="accent"` par `tone={isDesktop ? "accent" : "danger"}` et ajoute `const isDesktop = useIsDesktop();` aux hooks de `FileSetRow` (l'import existe déjà, Files.tsx ~37). `danger` est une entrée de la table de tons de `Button.tsx` (~97-103 : `bg-statusFailSolid text-carbon-background hover:opacity-90` — la recette destructive de ConfirmDialog adoptée dans le composant) : aucune nouvelle couleur, aucune classe status écrite au call site, aucune exception eslint. Le mobile <48rem lit donc une action destructive ; le desktop >48rem reste à l'accent (contrainte d'identité desktop, jsdom compris — les suites existantes voient `accent`). Ne touche PAS aux autres boutons destructifs (snapshot rows ~670-679, RestorePanel) : leurs décisions documentées sont hors finding.
2. **Réécrit le commentaire contredit** : dans le bloc commentaire de la rangée d'actions, les passages déclarant que le badge remove ne reçoit AUCUN traitement de couleur spécial (« The remove badge gets NO special colour treatment… » et la citation « Keine Sonderfarbe fuer den Entfernen-Badge ») contredisent désormais le code mobile. Réécris-les en décision SCOPÉE : desktop >48rem garde l'accent intégré au hue (la règle jdp reste vraie là-haut) ; sous 48rem le bouton porte le ton danger partagé de Button (finding P2-14 du review 390px du 2026-09-14 : une action destructive irréversible ne doit pas porter la couleur du CTA primaire) ; le sens destructif reste porté par IconTrash, la bulle (t("files.deleteSet")) et la confirmation useConfirm (t("files.deleteSetConfirm")) — intactes. Garde intacts les paragraphes encore vrais (recette icon-badge, glyphes, gap-1.5, flex-wrap). Paraphrase — ne cite pas la littérale du ternaire dans le commentaire.
3. Le ton ne change aucune géométrie : MOBILE_BLEED et le floor 44px restent intacts.
  </action>
  <verify>
    <automated>cd web && node node_modules/typescript/bin/tsc --noEmit && node node_modules/vite/bin/vite.js build && node node_modules/eslint/bin/eslint.js src/pages/Files.tsx && [ "$(grep -c 'isDesktop ? "accent" : "danger"' src/pages/Files.tsx)" = "1" ]</automated>
  </verify>
  <done>À 390px « Remove set » se distingue des CTA primaires (ton danger shared) ; desktop >48rem identique octet par octet ; commentaire d'action réécrit sans contradiction ; tsc + eslint + build verts ; dist committé avec le code.</done>
</task>

<task type="auto">
  <name>Task 3: Dashboard.tsx — disposition P1-12 (barre New backup)</name>
  <files>web/src/pages/Dashboard.tsx (UNIQUEMENT si la vérification contredit la source — attendu : aucun changement de code), web/dist/* (si changement)</files>
  <action>
1. **Verify-first** — Reprends le protocole de la Task 1 sur la barre New backup (ancre : `{!isDesktop && (<StickyActionBar className="md:hidden">` avec le Button `home.newBackup`) : enfant direct de la racine `PAGE_SHELL_RESPONSIVE` (~2768), dernier enfant de contenu avant `{confirmDialog}`, aucun wrapper intermédiaire, paragraphe « LAST DIRECT CHILD » déjà présent dans son commentaire (~3071-3078 — le laisser, l'étendre seulement s'il manque quelque chose du contrat StickyActionBar.tsx:10-17).
2. **Si conforme (attendu)** : AUCUN changement de code sur Dashboard.tsx. La disposition va dans le SUMMARY (section « P1-11/P1-12 dispositions ») : mécanisme du finding re-mesuré contre l'arbre — la barre est déjà conforme au pattern ; le chevauchement mesuré avec la carte STORAGE (63px à scrollTop=0, main scrollH 1439 / clientH 787) est le comportement d'épinglage sticky normal, pas un défaut de DOM ; toute atténuation du recouvrement (scroll-padding sur main, position:fixed, masquage) est hors contrainte de fichiers et reste une décision séparée pour l'orchestrateur.
3. **Si non conforme** (wrapper intermédiaire ou placement non-dernier trouvé) : déplace le bloc entier en dernier enfant de contenu de la colonne (avant `{confirmDialog}`), gate et `md:hidden` conservés, pourquoi-dernier-enfant complété dans le commentaire, puis tsc + build + commit avec dist.
4. Ne modifie NI la visibilité de la barre, NI son contenu, NI Layout.tsx.
  </action>
  <verify>
    <automated>cd web && if git diff --quiet -- src/pages/Dashboard.tsx; then [ "$(grep -c 'LAST DIRECT CHILD' src/pages/Dashboard.tsx)" -ge 1 ]; else node node_modules/typescript/bin/tsc --noEmit && node node_modules/vite/bin/vite.js build; fi</automated>
  </verify>
  <done>P1-12 clos avec preuve dans le SUMMARY (barre déjà conforme au pattern, géométrie = épinglage sticky normal) — ou déplacée conformément au pattern si la preuve a contredit la source ; desktop inchangé dans les deux branches.</done>
</task>

</tasks>

<verification>
- Par tâche : tsc --noEmit + vite build verts (invocation node directe), dist commité dans le commit de code.
- Gates grep par tâche (comptes exacts ci-dessus, commentaires paraphrasés pour ne jamais gonfler un compte).
- eslint sur Files.tsx (Task 2) : aucune nouvelle exception, aucun disable comment.
- Suites vitest : Aucune suite dom Files/Dashboard n'existe (glob vérifié) ; i18n.preseed.test.ts vert confirme zéro nouvelle clé. desktop-untouched.spec.ts HORS scope (non exécutable en quick — besoins binaire + harnais), identité desktop tenue par construction (max-md:-scoping + gate isDesktop) et re-prouvée à la prochaine gate e2e complète.
- zéro modification hors Files.tsx / Dashboard.tsx / dist / SUMMARY.
</verification>

<success_criteria>
- P1-13 : un seul header de nom à 390px set étendu ; desktop inchangé.
- P2-14 : Remove set destructive-visible <48rem, accent inchangé ≥48rem.
- P1-11/P1-12 : dispositions argumentées dans le SUMMARY avec ancres source ; déplacement UNIQUEMENT si preuve.
- 3 commits atomiques (2 de code + dist, 1 docs/SUMMARY si Dashboard inchangé), trailer Co-Authored-By.
- Aucune clé de locale nouvelle, aucune dep, aucun fichier hors contrainte.
</success_criteria>

<output>
Crée `.planning/quick/260914-k2r-quickplan-2-3-review-ui-mobile-barres-st/260914-k2r-SUMMARY.md` en fin de tâche, avec une section « P1-11/P1-12 dispositions » (ancres source + reconciliation mesure vs arbre) et les sections habituelles du template quick.
</output>
