---
phase: 05-mobile-shell-navigation-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/navModel.ts
  - web/src/lib/navModel.test.ts
  - web/src/components/Sidebar.tsx
  - web/src/lib/useMediaQuery.ts
  - web/src/lib/useMediaQuery.test.ts
  - web/src/lib/testSetup/matchMedia.ts
  - web/vitest.config.ts
autonomous: true
requirements: [SHELL-01, SHELL-03]
user_setup: []

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "navModel.destinations(settings) is the ONE ordered nav registry: the desktop Sidebar and every mobile chrome surface derive their destinations from it, and no second hand-written destination ordering exists anywhere"
    - "destinations returns the FULL ordered list with an enabled flag per entry (gates never pre-filter the list), preserving Sidebar's nextHue() render-counter semantics exactly: a hidden tab never burns a hue slot"
    - "EMPTY probe (resolved by analysis): with every settings gate off, destinations still yields the always-on entries, barDestinations yields exactly 3 slots (Files gated off), and moreDestinations still yields Recovery — no caller can ever receive an empty navigation"
    - "ORDERING probe (resolved by analysis): bar and More derivations are filters of the one registry, so their relative order always equals the desktop Sidebar order (dashboard, containers, files, settings for the bar; Recovery-first for More) — asserted by unit test"
    - "ADJACENCY probe (resolved by analysis): toggling any single *Enabled setting changes only that entry's enabled flag; no other entry's position, adjacency, or identity changes — asserted by unit test"
    - "CONCURRENCY probe (resolved by analysis, not applicable): the registry is a pure synchronous function of already-loaded Settings — no fetch, timer, or shared mutable state exists, so there is no concurrency surface"
    - "useMediaQuery owns the single DESKTOP_QUERY literal for the Tailwind md breakpoint (min-width: 48rem), pinned by a node-env source-assert test; no second breakpoint literal exists in the codebase"
    - "jsdom suites keep passing in the same change that introduces useMediaQuery: a guarded desktop-default matchMedia stub is wired via vitest setupFiles (jsdom 30 has no window.matchMedia; Layout.displayPrefs.dom.test.tsx renders the real Layout)"
    - "Chrome renders the moment Layout renders (registry is synchronous — no loading state exists to design), and performs no fetches: the settings fetch stays non-fatal in Layout with its existing catch-to-default-gated degradation, which mobile inherits unchanged"
  artifacts:
    - path: "web/src/lib/navModel.ts"
      provides: "NavDestination type + destinations(settings) full ordered registry with enabled flags + barDestinations/moreDestinations filter derivations; never hue assignment"
      contains: "destinations"
    - path: "web/src/lib/navModel.test.ts"
      provides: "Node-env unit tests: order parity, gating, adjacency stability, bar/more filter derivations, registry shape (no hue fields)"
      contains: "barDestinations"
    - path: "web/src/lib/useMediaQuery.ts"
      provides: "DESKTOP_QUERY constant + useIsDesktop hook on useSyncExternalStore with the theme.ts subscribe/unsubscribe shape"
      contains: "DESKTOP_QUERY"
    - path: "web/src/lib/testSetup/matchMedia.ts"
      provides: "Guarded desktop-default matchMedia stub with addEventListener/removeEventListener, loaded via vitest setupFiles"
      contains: "matchMedia"
    - path: "web/src/components/Sidebar.tsx"
      provides: "Sidebar consuming navModel as its single destination source, nextHue() semantics preserved, data-testid=desktop-sidebar on its root"
      contains: "desktop-sidebar"
  key_links:
    - from: "web/src/components/Sidebar.tsx"
      to: "web/src/lib/navModel.ts"
      via: "Sidebar maps destinations(settings) instead of its inline JSX list — the drift SHELL-03 exists to kill"
      pattern: "destinations"
    - from: "web/src/lib/useMediaQuery.ts"
      to: "Tailwind md breakpoint"
      via: "DESKTOP_QUERY literal pinned by useMediaQuery.test.ts source assert to (min-width: 48rem)"
      pattern: "48rem"
    - from: "web/vitest.config.ts"
      to: "web/src/lib/testSetup/matchMedia.ts"
      via: "test.setupFiles entry landing in the SAME change as useMediaQuery (Pitfall-1 coupling)"
      pattern: "setupFiles"
---

<objective>
Extract the ONE pure nav registry (`web/src/lib/navModel.ts`) out of Sidebar's inline destination JSX, rewire Sidebar to consume it with byte-identical hue-counter semantics, and land the `useIsDesktop` media-query foundation with its mandatory jsdom stub — the shared substrate SHELL-01 (responsive switch) and SHELL-03 (one registry feeding Sidebar AND MoreSheet) both stand on.

Purpose: SHELL-03's core deliverable is a single registry so the mobile chrome can never drift from the desktop Sidebar; building the mobile chrome (plans 05) on anything else would reintroduce the exact drift this phase exists to kill. The media hook + stub must land together (Pitfall-1) or every jsdom suite that renders Layout goes red.
Output: navModel.ts + tests, Sidebar consuming the registry (+ `data-testid="desktop-sidebar"`), useMediaQuery.ts + source-assert test, guarded matchMedia stub + vitest setupFiles wiring.

**Open question resolved here (planner call, per phase instruction):** whether the bottom bar hardcodes a "Files" slot or hides it when `filesEnabled=false` is resolved BY the registry: bar slots are `destinations(settings)` filtered to enabled entries carrying `bar: true` (dashboard, containers, files, settings). With Files gated off the bar renders 3 destination slots + More — matching desktop Sidebar gating exactly, and prohibition 8 (a gated tab never appears in mobile chrome when the Sidebar hides it) holds by construction.
</objective>

<artifacts_this_phase>
Symbols this phase (05) creates, for source-grounding drift exclusion:
- `web/src/lib/navModel.ts` — `NavDestination`, `destinations(settings)`, `barDestinations(settings)`, `moreDestinations(settings)`
- `web/src/lib/navModel.test.ts`, `web/src/lib/useMediaQuery.ts` (`DESKTOP_QUERY`, `useIsDesktop`), `web/src/lib/useMediaQuery.test.ts`, `web/src/lib/testSetup/matchMedia.ts`
- `web/src/components/mobile/BottomSheet.tsx` (+ `.dom.test.tsx`), `web/src/components/mobile/MoreSheet.tsx` (+ `.dom.test.tsx`), `web/src/components/mobile/BottomNav.tsx`
- `web/src/components/navGlyphs.tsx` — new `IconEllipsis` glyph; `web/src/lib/i18n.ts` + `web/src/lib/locales/*.ts` — new `nav.more` key
- `web/src/app/mobileShellSource.test.ts`; edits to `web/src/app/Layout.tsx`, `web/src/components/Sidebar.tsx`, `web/src/pages/Login.tsx`, `web/index.html`, `web/src/index.css`, `web/src/lib/theme.ts`
- testids: `desktop-sidebar`, `bottom-nav`, `more-sheet`
- `web/playwright.config.ts`, `web/e2e/health.spec.ts`, `web/e2e/desktop-untouched.spec.ts`, `web/e2e/mobile-shell.spec.ts`, `web/e2e/narrow-viewport.spec.ts`; new `playwright` job in `.github/workflows/lint.yml`
</artifacts_this_phase>

<execution_context>
@C:/Users/jerem/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/jerem/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mobile-shell-navigation-foundation/05-PATTERNS.md
@.planning/phases/05-mobile-shell-navigation-foundation/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: navModel.ts registry + unit tests</name>
  <files>web/src/lib/navModel.ts, web/src/lib/navModel.test.ts</files>
  <read_first>
    - web/src/components/Sidebar.tsx lines 695-794 (the destination JSX being extracted: exact order dashboard, recovery, containers, then the six *Enabled-gated entries, then footer; the load-bearing comment at 703-722 explaining nextHue() render-order semantics — gates short-circuit BEFORE nextHue() so hidden tabs never burn a hue slot)
    - web/src/components/Sidebar.tsx lines 55-84 (SidebarProps: settings: Settings, authEnabled: boolean)
    - web/src/lib/pageShell.ts (module form model: narrative banner comment, named exports, one shared constant with its measured-reason essay)
    - web/src/app/routedPages.test.ts (node-env test style: readFileSync/it.each, self-guard idiom, failure messages that explain the why)
    - web/src/lib/i18n.ts lines 60-70 (the nav.* key namespace the registry's labelKey values must reuse verbatim)
    - 05-RESEARCH.md Pitfall 5 (never assign hues in navModel — Sidebar's nextHue() is a render counter)
  </read_first>
  <action>
    Create web/src/lib/navModel.ts as a pure, node-safe .ts module (no JSX, no DOM, no imports from components) opening with a narrative banner comment in the pageShell.ts style that cites SHELL-03, names Sidebar.tsx:695-794 as the extracted source, and states why the module must NEVER assign hues (Sidebar's nextHue() is a render-order counter; precomputing hues here would break the short-circuit semantics). Define and export: type NavDestination with fields to (route path), labelKey (existing nav.* i18n key), icon (a React ComponentType reference — a component reference needs no JSX, so the file stays .ts), bar (boolean: true only for the four bottom-bar destinations dashboard, containers, files, settings), and enabled (boolean). Export destinations(settings: Settings) returning the FULL ordered list of all ten destinations in desktop Sidebar order — dashboard, recovery, containers, then vms, flash, files, config, receiver, fleet (each gated by its settings field vmsEnabled/flashEnabled/filesEnabled/configEnabled/receiverEnabled/fleetEnabled), then settings (always enabled) — with enabled computed per entry, never pre-filtered. Settings is imported type-only from ../lib/api (reading the type is fine; api.ts itself is frozen). Export barDestinations(settings) = destinations filtered to bar && enabled, and moreDestinations(settings) = destinations filtered to !bar && enabled — both as one-line filter derivations of the one list so order parity is structural, not maintained. Named exports only.
    Then create web/src/lib/navModel.test.ts (node env — no jsdom docblock, routedPages.test.ts style) covering: (a) full-list order equals the documented Sidebar order with stable length 10 and unique routes; (b) each of the six gates flips exactly its own entry's enabled flag (the ADJACENCY probe: flip one gate, assert every other entry's position and identity unchanged); (c) barDestinations returns dashboard, containers, files, settings in registry order and drops files when filesEnabled is false (the resolved open question — document this case with a comment referencing the decision); (d) moreDestinations returns Recovery-first sidebar order, excludes all bar destinations, and with every gate off still contains recovery (the EMPTY probe — no empty navigation possible); (e) shape assert: no entry carries any hue or color field (guards Pitfall 5). Include the routedPages self-guard idiom so an accidental empty test file fails loudly.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/lib/navModel.test.ts</automated>
  </verify>
  <done>
    destinations(settings) returns the full 10-entry ordered registry with per-entry enabled flags; barDestinations/moreDestinations are structural filters of that one list; tests prove order parity, single-gate adjacency stability, Files-gated bar shrinkage, and a never-empty More list; no entry carries hue data.
  </done>
  <acceptance_criteria>
    - `cd web && npx vitest run src/lib/navModel.test.ts` exits 0
    - web/src/lib/navModel.ts exports NavDestination, destinations, barDestinations, moreDestinations as named exports and contains no default export
    - navModel.test.ts contains cases for: full order, each of the six settings gates, filesEnabled=false bar shrinkage, moreDestinations containing recovery when all gates are off, and absence of hue fields on entries
    - web/src/lib/navModel.ts imports Settings only as a type from ../lib/api and imports nothing from web/src/components
    - grep for hue assignment patterns in navModel.ts returns nothing (the module computes no colors)
  </acceptance_criteria>
  <reversibility rating="costly">The registry's shape (NavDestination fields, destinations/barDestinations/moreDestinations split) becomes the shared contract for Sidebar AND the phase-5 mobile chrome AND later milestone phases — changing it after plans 05 land ripples through three consumers, so the shape decision is worth getting right now.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Rewire Sidebar to consume the registry</name>
  <files>web/src/components/Sidebar.tsx</files>
  <read_first>
    - web/src/components/Sidebar.tsx lines 695-794 (current inline destination JSX + footer group + the nextHue() comment block at 703-722 — all comments preserved verbatim)
    - web/src/lib/navModel.ts from Task 1 (the contract now consumed)
    - web/src/components/Sidebar.tsx lines 269-324 (NavItem props: to, label, icon, hueIndex)
  </read_first>
  <action>
    Replace Sidebar's inline destination list with a map over navModel.destinations(settings), preserving EVERY existing behavior: iterate the registry in order; skip disabled entries BEFORE calling nextHue() so the render-counter semantics are byte-identical (a .map that calls nextHue() inside the enabled branch evaluates in the same JSX order as today); exclude the settings entry from the main scrolling list and keep the existing footer group structure (authEnabled-gated SidebarSignOut with nextHue(), SidebarControls with nextHue(), then the settings NavItem with nextHue()) — the footer's settings row takes its labelKey and icon FROM the registry entry looked up by route, so the registry is still the single source of label/icon data. Preserve all existing narrative comments (the 703-722 hue-semantics block stays, updated only where it references the now-extracted list). Add data-testid="desktop-sidebar" to the component's outermost root element (the e2e desktop-untouched spec locates the Sidebar by this testid). No visual changes of any kind: same classes, same order, same hue assignment sequence — the desktop Sidebar must be pixel-identical after this task.
  </action>
  <verify>
    <automated>cd web && npm run build && npx vitest run</automated>
  </verify>
  <done>
    Sidebar renders from navModel with identical order, gating, and hue sequence; footer structure untouched; data-testid="desktop-sidebar" present; full web build (tsc + vite) and the entire vitest suite pass.
  </done>
  <acceptance_criteria>
    - `cd web && npm run build` exits 0 (tsc --noEmit + vite build)
    - `cd web && npx vitest run` exits 0 (all pre-existing suites still green, including Layout.displayPrefs.dom.test.tsx)
    - web/src/components/Sidebar.tsx contains no hand-typed destination route literals in its render list (destinations come from the navModel import; the footer lookup by route path is the one sanctioned exception)
    - the rendered Sidebar element with data-testid="desktop-sidebar" exists (assertable via the built output referencing the testid)
    - nextHue() is invoked for enabled main-list entries in registry order followed by the footer entries, matching the pre-refactor call sequence (verified by reading the diff: no reordering of nextHue() call sites)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: useMediaQuery hook + source-assert guard + jsdom matchMedia stub (same-change coupling)</name>
  <files>web/src/lib/useMediaQuery.ts, web/src/lib/useMediaQuery.test.ts, web/src/lib/testSetup/matchMedia.ts, web/vitest.config.ts</files>
  <read_first>
    - web/src/lib/theme.ts lines 78-87 (onSystemThemeChange — the house matchMedia subscribe/unsubscribe shape including the Safari < 14 addListener fallback; also lines 1-13 for the banner-comment + keep-index.html-in-sync convention for fragile cross-file pairs)
    - web/vitest.config.ts (node default env, include globs, NO setupFiles yet)
    - web/src/components/ConfirmDialog.tsx lines 17-23 (the repo's own statement that node-env files have no window/document — why the stub must be guarded)
    - web/src/app/Layout.displayPrefs.dom.test.tsx lines 1-30 (the jsdom suite that renders real Layout and would crash without the stub)
    - web/src/app/routedPages.test.ts lines 60-75 (source-assert style to imitate)
  </read_first>
  <action>
    Create web/src/lib/useMediaQuery.ts: export const DESKTOP_QUERY with the exact Tailwind md breakpoint value (min-width: 48rem) — the ONLY place this literal may live; export function useIsDesktop(): boolean implemented with useSyncExternalStore, subscribing in the theme.ts:78-87 shape (addEventListener/removeEventListener with the deprecated addListener/removeListener fallback for older Safari), guarded for a missing window; the server snapshot returns true (desktop default, consistent with the stub's desktop-default answer). Open with a banner comment explaining the single-literal rule and that Layout's chrome switch (plan 05) will be the only consumer for now.
    Create web/src/lib/useMediaQuery.test.ts (node env, source-assert per routedPages style with self-guard): read useMediaQuery.ts source and assert it contains the exact DESKTOP_QUERY literal (min-width: 48rem), assert it uses useSyncExternalStore, and assert no OTHER file in web/src contains a second min-width breakpoint media-query literal (the one-literal rule, stated in the failure message with the why).
    Create web/src/lib/testSetup/matchMedia.ts: a GUARDED desktop-default stub — only when window exists AND window.matchMedia is not already a function, define it to return an MQL-like object whose matches is true for min-width desktop queries and false otherwise, carrying media, addEventListener, removeEventListener, addListener, removeListener, dispatchEvent, and an onchange property (useMediaQuery subscribes, so listener methods are mandatory). Banner comment states the desktop-default rationale (existing jsdom suites assert desktop layout) and the Pitfall-1 coupling.
    Edit web/vitest.config.ts to add the setupFiles entry pointing at the stub — this edit MUST land in the same commit as useMediaQuery.ts (the coupling is the point: Layout.displayPrefs.dom.test.tsx renders real Layout, jsdom 30 has no matchMedia, and useIsDesktop will be called during that render from plan 05 onward).
  </action>
  <verify>
    <automated>cd web && npx vitest run src/lib/useMediaQuery.test.ts && npx vitest run</automated>
  </verify>
  <done>
    DESKTOP_QUERY is pinned to Tailwind's md breakpoint by a source-assert test that also enforces the one-literal rule; the guarded stub is wired via setupFiles; the FULL vitest suite (node + jsdom) passes in the same change — proving Pitfall-1 coupling held.
  </done>
  <acceptance_criteria>
    - `cd web && npx vitest run` exits 0 including Layout.displayPrefs.dom.test.tsx
    - useMediaQuery.test.ts asserts the exact literal (min-width: 48rem) in useMediaQuery.ts and fails with an explanatory message when the literal is altered (self-guard present)
    - web/vitest.config.ts test config contains the setupFiles entry for src/lib/testSetup/matchMedia.ts
    - the stub in testSetup/matchMedia.ts is wrapped in a guard that no-ops when window is undefined or matchMedia already exists (node-env suites unaffected)
    - the returned MQL object from the stub exposes addEventListener and removeEventListener (subscription-compatible)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings store -> chrome gating | `*Enabled` flags from /api/settings decide which destinations exist in ANY chrome surface; a gating bug silently widens navigation |
| Test env -> production code | The matchMedia stub substitutes a browser API inside the test runtime; an unguarded stub leaks into node-env suites and can mask real behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Information Disclosure | navModel gating + Sidebar/mobile consumers | medium | mitigate | Registry computes `enabled` from the same settings fields the desktop Sidebar uses; unit tests assert each gate and that bar/more derivations never surface a disabled entry (prohibition 8 holds by construction); e2e chrome parity in plans 05/06 re-checks it against the real server |
| T-05-06 | Tampering | matchMedia stub in testSetup | low | mitigate | Stub is guarded (only defines matchMedia when missing), desktop-default, and lives in test setup only — never imported by production code; full-suite green run proves both envs coexist |
| T-05-02 | Elevation of Privilege | harness auth handling | high | mitigate | (carried from plan 01) No auth logic in this plan; the chrome adds no fetches and no login path |
</threat_model>

<prohibitions>
Every plan in this phase carries the full recalled prohibition set (descriptor-less, flagged-unverified — never auto-dismissed, never silently dropped):
- statement: "The bottom bar is never taken out of normal document flow — it is a flex sibling of the scroller, not a fixed-position overlay" (SHELL-02)
  status: active
  verification: flagged-unverified
- statement: "The viewport-height trap unit 100vh never appears in the mobile shell; the viewport contract uses dvh/svh exclusively" (SHELL-05)
  status: active
  verification: flagged-unverified
- statement: "The FOUC inline script bytes in web/index.html are never modified" (SHELL-06)
  status: active
  verification: flagged-unverified
- statement: "The route tree is never forked — no second page tree, no route-level mobile/desktop split; web/src/app/router.tsx stays untouched" (locked v1.1 decision)
  status: active
  verification: flagged-unverified
- statement: "No UI kit, no state library, and no new runtime npm dependency; the exact-pinned @playwright/test devDependency is the sole sanctioned addition" (milestone constraint)
  status: active
  verification: flagged-unverified
- statement: "Frozen files are never modified: web/src/app/router.tsx, web/src/lib/api.ts, web/src/lib/progress.ts, and everything under internal/**" (v1.1 scope)
  status: active
  verification: flagged-unverified
- statement: "Raw hex colors are never reintroduced where semantic tokens exist (carbon-*, accent*, status*)" (house convention)
  status: active
  verification: flagged-unverified
- statement: "A settings-gated tab never appears in any mobile chrome surface when the desktop Sidebar hides it" (SHELL-03 + T-05-05)
  status: active
  verification: flagged-unverified
</prohibitions>

<verification>
- `cd web && npx vitest run` exits 0 (navModel, useMediaQuery, stub wiring, and all pre-existing suites)
- `cd web && npm run build` exits 0 (desktop Sidebar still compiles pixel-identical)
- Source asserts prove the ONE registry, the pinned breakpoint literal, and the one-literal rule
</verification>

<success_criteria>
One registry feeds the desktop Sidebar (already) and is ready to feed the mobile chrome (plan 05) with zero drift possible; the breakpoint contract is pinned and guarded; the jsdom stub coupling landed in the same change so nothing downstream can crash the existing suites.
</success_criteria>

<output>
Create `.planning/phases/05-mobile-shell-navigation-foundation/05-02-SUMMARY.md` when done
</output>
