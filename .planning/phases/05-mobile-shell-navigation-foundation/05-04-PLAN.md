---
phase: 05-mobile-shell-navigation-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/index.css
  - web/index.html
  - web/src/lib/theme.ts
  - web/src/pages/Login.tsx
  - web/src/app/mobileShellSource.test.ts
autonomous: true
requirements: [SHELL-04, SHELL-05, SHELL-06, SHELL-07]
user_setup: []

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Safe-area insets are available to every component as :root custom properties derived from env(safe-area-inset-*) with a max(..., 0px) clamp, and web/index.html declares viewport-fit=cover so those env() values actually resolve on notched devices"
    - "The viewport meta declares interactive-widget=resizes-content so the visual keyboard resizes the layout viewport (the behavior the Layout-level keyboard mechanism in plan 05 builds on), and the FOUC inline script bytes in web/index.html are byte-identical before and after this plan"
    - "A static meta theme-color fallback (#161616) sits BELOW the FOUC script in web/index.html, and the live theme-color is mirrored by paint() in theme.ts — the single choke point all three theme-application paths funnel through — so browser chrome follows dark/light (#161616/#f4f4f4) on every theme switch"
    - "The login page (SHELL-07) sizes its root with the dynamic viewport height unit (min-h-dvh), keeps BOTH inputs (password and TOTP code) at a 16px effective font on narrow viewports via max-md:text-base (preventing iOS zoom-on-focus), pads for safe areas, and continues to render with NO mobile chrome because Layout returns LoginPage before any shell renders"
    - "Source-assert tests (node env) pin all of it: the safe-area block exists, the meta tokens sit below the byte-identical FOUC script, the login classes are present, and no banned viewport-height literal survives in Login.tsx or index.css"
  artifacts:
    - path: "web/src/index.css"
      provides: "--safe-area-top/right/bottom/left custom properties (max(env(...), 0px)) in :root with a banner comment naming their consumers"
      contains: "--safe-area-bottom"
    - path: "web/index.html"
      provides: "Extended viewport meta (viewport-fit=cover, interactive-widget=resizes-content) + static meta theme-color fallback below the untouched FOUC script"
      contains: "viewport-fit=cover"
    - path: "web/src/lib/theme.ts"
      provides: "paint() mirrors the live theme into the meta theme-color tag (#161616 dark / #f4f4f4 light) with the paired-change comment"
      contains: "theme-color"
    - path: "web/src/pages/Login.tsx"
      provides: "SHELL-07 mobile-correct login: min-h-dvh root, max-md:text-base on both inputs, safe-area padding, chrome-free by construction"
      contains: "min-h-dvh"
    - path: "web/src/app/mobileShellSource.test.ts"
      provides: "Node-env source asserts: safe-area block, FOUC byte-identity + meta ordering, login classes, banned-literal absence, LoginPage-before-shell order"
      contains: "mobileShellSource"
  key_links:
    - from: "web/src/index.css"
      to: "web/index.html viewport meta"
      via: "env(safe-area-inset-*) resolves only under viewport-fit=cover — the two edits are one contract"
      pattern: "safe-area-inset"
    - from: "web/src/lib/theme.ts paint()"
      to: "web/index.html meta theme-color"
      via: "JS paint() mirrors the live theme; the static meta is the first-paint fallback — theme.ts:10-12 keep-index.html-in-sync convention"
      pattern: "#161616"
    - from: "web/src/app/mobileShellSource.test.ts"
      to: "web/index.html FOUC script"
      via: "Byte-identity guard: the test copies the script text and fails on any mutation (SHELL-06 / T-05-03)"
      pattern: "FOUC"
---

<objective>
Land the viewport correctness contract for the whole mobile shell: safe-area custom properties + viewport-fit=cover (SHELL-04), the dvh-first viewport unit discipline at the places that exist TODAY (SHELL-05 — login now, Layout in plan 05), the extended viewport meta + theme-color mirroring with the FOUC script byte-untouched (SHELL-06), and the mobile-correct chrome-free login with 16px-effective inputs (SHELL-07) — all pinned by source-assert tests so the contract cannot silently rot.

Purpose: every later mobile surface consumes the safe-area custom properties and the dvh/svh discipline; and SHELL-06's FOUC guarantee is only credible if a test fails the moment anyone touches those bytes.
Output: index.css safe-area block, extended index.html meta + theme-color fallback, theme.ts paint() mirror, Login.tsx mobile fixes, mobileShellSource.test.ts guard suite.

Note on UI Considerations distribution: the six lifted UI-SPEC consideration rows bind to the nav chrome surfaces and land in plans 05/06; this plan's surfaces (login, meta, CSS custom properties) have no binding row from that table.
</objective>

<artifacts_this_phase>
Symbols this phase (05) creates, for source-grounding drift exclusion:
- `web/src/lib/navModel.ts` — `NavDestination`, `destinations(settings)`, `barDestinations(settings)`, `moreDestinations(settings)`
- `web/src/lib/navModel.test.ts`, `web/src/lib/useMediaQuery.ts` (`DESKTOP_QUERY`, `useIsDesktop`), `web/src/lib/useMediaQuery.test.ts`, `web/src/lib/testSetup/matchMedia.ts`
- `web/src/components/mobile/BottomSheet.tsx` (+ `.dom.test.tsx`), `web/src/components/mobile/MoreSheet.tsx` (+ `.dom.test.tsx`), `web/src/components/mobile/BottomNav.tsx`
- `web/src/components/navGlyphs.tsx` — new `IconEllipsis` glyph; `web/src/lib/i18n.ts` + `web/src/lib/locales/*.ts` — new `nav.more` key
- `web/src/app/mobileShellSource.test.ts`; edits to `web/src/app/Layout.tsx`, `web/src/components/Sidebar.tsx`, `web/src/pages/Login.tsx`, `web/index.html`, `web/src/index.css`, `web/src/lib/theme.ts`
- testids: `desktop-sidebar`, `bottom-nav`, `more-sheet`
- `web/playwright.config.ts`, `web/e2e/health.spec.ts`, `web/e2e/desktop-untouched.spec.ts`, `web/e2e/mobile-shell.spec.ts`, `web/e2e/narrow-viewport.spec.ts`; new `playwright` job in `.github/workflows/lint.yml`
</artifacts_this_phase>

<execution_context>
@C:/Users/jerem/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/jerem/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mobile-shell-navigation-foundation/05-UI-SPEC.md
@.planning/phases/05-mobile-shell-navigation-foundation/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Safe-area custom properties + viewport meta + theme-color mirror</name>
  <files>web/src/index.css, web/index.html, web/src/lib/theme.ts</files>
  <read_first>
    - web/index.html lines 16-34 (FOUC inline script ends at line 31 and must stay byte-identical; the meta viewport tag at line 34 is the extension target)
    - web/src/lib/theme.ts lines 1-100 (paint() at 45-47 is the single application choke point; lines 10-12 document the keep-index.html-in-sync convention and the banner-comment style for paired changes)
    - web/src/index.css :root region (where carbon-*/accent* token blocks live — the safe-area block joins the custom-property area; note the existing banner-comment style)
    - 05-UI-SPEC.md Viewport & Shell Contract table (the dvh/svh/safe-area rows and the theme-color values #161616 dark / #f4f4f4 light)
  </read_first>
  <action>
    In web/src/index.css, add to :root four custom properties — safe-area-top, safe-area-right, safe-area-bottom, safe-area-left — each defined as max(env(safe-area-inset-<side>), 0px) per the researched form, with a banner comment explaining: they resolve only because index.html declares viewport-fit=cover (the pairing is one contract), and their consumers are the mobile chrome surfaces landing in plan 05 (bar bottom padding, sheet bottom padding).
    In web/index.html, extend the existing meta viewport content attribute (line 34) with viewport-fit=cover and interactive-widget=resizes-content, keeping width=device-width and initial-scale intact — a single-line edit, nothing above it touched. Add a static meta theme-color tag with content #161616 positioned AFTER the FOUC script (at or below the current viewport meta line — first-paint fallback before JS runs), with an html comment naming it the static fallback mirrored by theme.ts paint().
    In web/src/lib/theme.ts, extend paint() to mirror the applied theme into the meta theme-color tag: dark resolves #161616, light resolves #f4f4f4 — look up the meta element with a null guard (jsdom suites may lack it) and set the content attribute. Add the paired-change comment in theme.ts's established style: the static index.html value and this mirror must change together (cite the 10-12 sync convention). Do NOT touch setTheme/applyStoredTheme/listener wiring — paint() is the choke point precisely so all three paths mirror for free.
  </action>
  <verify>
    <automated>cd web && npm run build && npx vitest run</automated>
  </verify>
  <done>
    Safe-area custom properties exist and resolve under the cover viewport; the meta carries both new directives plus the static theme-color fallback below the FOUC script; paint() mirrors live theme changes; build and full suite green.
  </done>
  <acceptance_criteria>
    - `cd web && npm run build` exits 0 and `cd web && npx vitest run` exits 0
    - web/src/index.css :root defines --safe-area-top, --safe-area-right, --safe-area-bottom, --safe-area-left using max(env(safe-area-inset-...), 0px)
    - web/index.html meta viewport content contains viewport-fit=cover AND interactive-widget=resizes-content AND the original width=device-width, initial-scale=1.0
    - web/index.html contains a meta theme-color tag whose position in the file is AFTER the FOUC script's closing tag
    - the FOUC script region of web/index.html is byte-identical to its pre-change content (git diff shows no changes above the viewport meta line)
    - web/src/lib/theme.ts paint() sets the meta theme-color content to #161616 for dark and #f4f4f4 for light, null-guarded
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Mobile-correct login page (SHELL-07)</name>
  <files>web/src/pages/Login.tsx</files>
  <read_first>
    - web/src/pages/Login.tsx lines 60-120 (root div with the screen-height class at line 67; the RevealInput password field className with text-sm; the needCode TOTP input with text-sm and tracking-[0.35em]; the max-w-sm card)
    - 05-UI-SPEC.md typography section (login inputs >= 16px effective font on narrow viewports — the iOS zoom-on-focus threshold) and the SHELL-07 row of the shell contract
  </read_first>
  <action>
    Three targeted edits in Login.tsx, nothing else: (1) the page root's height class (line 67) becomes the dynamic-viewport equivalent min-h-dvh so the login fills the visual viewport under the iOS keyboard; (2) BOTH field class strings — the RevealInput password className and the TOTP code input className — gain max-md:text-base so the effective font is 16px on narrow viewports (16px is the threshold below which iOS Safari zooms the viewport on focus); (3) the page container gains safe-area-aware padding using the custom properties from Task 1 (left/right/bottom) so the card clears notches and home-indicator edges in every orientation. Preserve the existing card classes, focus engine classes (glim-field-focus), and all copy exactly. There is no chrome change here and none needed: Layout's blocked branch already returns LoginPage before any shell renders (SHELL-07's no-chrome guarantee is structural — Task 3 pins it with a source assert).
  </action>
  <verify>
    <automated>cd web && npm run build && npx vitest run</automated>
  </verify>
  <done>
    Login root uses min-h-dvh; both inputs carry max-md:text-base; container pads with the safe-area custom properties; build + suite green with zero visual regression on desktop (text-base applies only under md).
  </done>
  <acceptance_criteria>
    - `cd web && npm run build` exits 0 and `cd web && npx vitest run` exits 0
    - Login.tsx root className contains min-h-dvh
    - Login.tsx contains at least two occurrences of max-md:text-base (password + TOTP inputs)
    - Login.tsx contains no occurrence of the banned viewport literal `100vh`
    - Login.tsx contains no occurrence of the static viewport root class `min-h-screen`
    - Login.tsx references var(--safe-area-bottom) (and horizontal safe-area properties) in its container padding
    - the TOTP input's tracking-[0.35em] and both fields' glim-field-focus classes are preserved
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: mobileShellSource.test.ts — the SHELL-04/05/06/07 source-assert guard suite</name>
  <files>web/src/app/mobileShellSource.test.ts</files>
  <read_first>
    - web/src/app/routedPages.test.ts lines 1-90 (THE model: node env, readFileSync + regex asserts, self-guard idiom, failure messages that explain the why, banner stating "reads source text, does not render")
    - web/index.html lines 16-36 (exact FOUC script text to copy into the test as the expected byte constant; the meta line position)
    - web/src/pages/Login.tsx and web/src/index.css (post-Task-1/2 state — the asserted content)
  </read_first>
  <action>
    Create web/src/app/mobileShellSource.test.ts in the routedPages.test.ts style (node env, no docblock, banner: "Node environment, no DOM: this reads source text, it does not render", self-guards so every regex provably matches something). Assert, each with an explanatory failure message: (1) index.css defines all four safe-area custom properties with the max(env(...), 0px) form; (2) index.html's viewport meta contains viewport-fit=cover and interactive-widget=resizes-content; (3) the FOUC script's inner text, copied verbatim into this test as the expected constant, equals what the file contains today — any future edit to those bytes fails the suite (SHELL-06/T-05-03, the byte-identity guard); (4) the theme-color meta's offset in the file is greater than the FOUC script's closing offset (fallback sits below the script); (5) Login.tsx contains min-h-dvh, exactly-two-or-more max-md:text-base occurrences, and zero occurrences of the banned literals `100vh` and `min-h-screen`; (6) Layout.tsx returns LoginPage (the blocked branch) BEFORE the shell root div appears in source order — the structural SHELL-07 no-chrome guarantee. SCOPE NOTE: do NOT assert anything about Layout.tsx viewport classes in this task — Layout still carries the desktop root class until plan 05 rewires it, and plan 05 EXTENDS this same test file with the Layout/mobile-shell asserts; asserting early would leave the suite red for all of Wave 1.
    <!-- planner-discipline-allow: 100vh -->
    <!-- planner-discipline-allow: min-h-screen -->
  </action>
  <verify>
    <automated>cd web && npx vitest run src/app/mobileShellSource.test.ts</automated>
  </verify>
  <done>
    The guard suite passes against the post-Task-1/2 tree and demonstrably fails on mutation (byte-identity and banned-literal asserts are live); Layout-scoped asserts correctly deferred to plan 05.
  </done>
  <acceptance_criteria>
    - `cd web && npx vitest run src/app/mobileShellSource.test.ts` exits 0
    - `cd web && npx vitest run` exits 0 (whole suite)
    - the test file contains the FOUC script text as an inline expected constant and compares it against the file content
    - the test file contains negative assertions for `100vh` and `min-h-screen` scoped to Login.tsx
    - the test file contains a source-order assertion placing the LoginPage return before the shell root in Layout.tsx
    - the test file contains no assertion on Layout.tsx viewport height classes (deferred to plan 05 per the scope note)
    - every regex in the file has a self-guard so a silent no-match cannot pass
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build tooling / humans -> web/index.html | The FOUC script is first-paint security-adjacent logic (theme flash prevention); any mutation is tampering with a byte-critical region |
| Theme application path -> document head | paint() writes into the live DOM head; a regression could fight the FOUC script or leak incorrect theme state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Tampering | web/index.html FOUC script (lines 16-31) | high | mitigate | Byte-identity source assert (Task 3, criterion 3) fails CI on ANY mutation of those bytes; Task 1 edits are confined to the meta line at :34 and below |
| T-05-09 | Tampering | theme-color mirror | low | mitigate | Mirror lives in paint() — the single choke point (setTheme/applyStoredTheme/system-flip all funnel through it) — with null guard; values pinned (#161616/#f4f4f4) in acceptance criteria; paired-change comment keeps index.html fallback in sync |
| T-05-02 | Elevation of Privilege | harness auth handling | high | mitigate | (carried from plan 01) Login changes are presentation-only: no auth logic, no endpoints, no bypass; SHELL-07's guarantee is structural (early return) and pinned by source order assert |
</threat_model>

<prohibitions>
Every plan in this phase carries the full recalled prohibition set (descriptor-less, flagged-unverified — never auto-dismissed, never silently dropped):
- statement: "The bottom bar is never taken out of normal document flow — it is a flex sibling of the scroller, not a fixed-position overlay" (SHELL-02)
  status: active
  verification: flagged-unverified
- statement: "The viewport-height trap unit 100vh never appears in the mobile shell; the viewport contract uses dvh/svh exclusively" (SHELL-05)
  status: active
  verification: flagged-unverified
- statement: "The FOUC inline script bytes in web/index.html are never modified" (SHELL-06)
  status: active
  verification: flagged-unverified
- statement: "The route tree is never forked — no second page tree, no route-level mobile/desktop split; web/src/app/router.tsx stays untouched" (locked v1.1 decision)
  status: active
  verification: flagged-unverified
- statement: "No UI kit, no state library, and no new runtime npm dependency; the exact-pinned @playwright/test devDependency is the sole sanctioned addition" (milestone constraint)
  status: active
  verification: flagged-unverified
- statement: "Frozen files are never modified: web/src/app/router.tsx, web/src/lib/api.ts, web/src/lib/progress.ts, and everything under internal/**" (v1.1 scope)
  status: active
  verification: flagged-unverified
- statement: "Raw hex colors are never reintroduced where semantic tokens exist (carbon-*, accent*, status*)" (house convention)
  status: active
  verification: flagged-unverified
- statement: "A settings-gated tab never appears in any mobile chrome surface when the desktop Sidebar hides it" (SHELL-03 + T-05-05)
  status: active
  verification: flagged-unverified
</prohibitions>

<verification>
- `cd web && npx vitest run src/app/mobileShellSource.test.ts` exits 0 (all guards live)
- `cd web && npm run build && npx vitest run` exit 0 (full suite, desktop untouched)
- `git diff web/index.html` shows changes only at/below the viewport meta line
</verification>

<success_criteria>
The viewport contract exists as code AND as guards: safe areas resolvable, meta correct, theme-color mirrored from the choke point, login mobile-correct and chrome-free, FOUC bytes provably untouched. Plan 05 consumes the custom properties and extends the guard suite instead of inventing its own.
</success_criteria>

<output>
Create `.planning/phases/05-mobile-shell-navigation-foundation/05-04-SUMMARY.md` when done
</output>
