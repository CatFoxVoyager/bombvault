---
phase: 08-guided-restore-real-device-verification
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - web/src/pages/Recovery.tsx
  - web/src/components/mobile/RunDetailSheet.tsx
  - web/src/app/mobileShellSource.test.ts
  - web/e2e/guided-restore.spec.ts
  - web/e2e/narrow-viewport.spec.ts
autonomous: true
requirements: [SCRN-06, VERIFY-03]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The mobile review/restore step (position 5) renders its populated body: restore-all as a secondary/tonal mid-screen row consuming the desktop restoreAll handler verbatim (sequential fireAndWaitRun, single-flight), per-target restore rows via the existing row components, and per-target four-status Badges."
    - "All destructive confirmations ride useConfirm -> ConfirmSheet: restore-all with t(containers.restoreSelectedConfirm), per-row restore via RestoreAction's confirmMessage path (t(recovery.restoreRowConfirm)); destructive control top, safe cancel thumb-default; zero new confirm UI (D-03)."
    - "The integrity verify row fires checkDomain through a locally-copied domain mapping; it shows integrity.checking busy then the result as status text + Badge; it never blocks restore and its copy never implies it does (the optional beat)."
    - "Live progress + log render from the frozen pipeline read-only (D-04): progress via useProgress/useBackupWatch, log lines via the RunDetailSheet LogList pattern class-for-class (the ONLY log style), visibility-gated with useVisibilityGate so hiding pauses the poll chain and returning reconciles a finished run."
    - "Completion renders t(recovery.restoreAllResult) with ok and fail counts side by side; a cancelled restore renders as a NEUTRAL terminal (distinct from failed, no failure tone); VM-without-SSH degrades to t(recovery.vmSshNote), never a silent skip."
    - "RunDetailSheet's stale SCRN-06/Phase 8 pointer comments (:64-68, :561-574) are updated in the same commit the flow ships - no stale pointer survives the phase."
    - "Populated/partial/zero-one-many/loading/error per 08-UI-SPEC: populated = per-target rows + four-status Badges; partial = ok/fail counts side by side and the SSH-note degradation; zero-one-many = zero targets keep the empty copy, one target renders the same row anatomy, restore-all loops sequentially over 1..n identically; loading = integrity.checking + RestoreProgress; error = backend text verbatim, failed actions stay on-step, cancelled = neutral terminal."
    - "The de/fr narrow-viewport battery covers the recovery flow at 320px and 360px: no horizontal pan, no clipped control, the stepOf chip single-line."
    - "The mobileShellSource guard knows the final Recovery.tsx geography: Recovery.tsx joins MIXED_FILES (tailAnchor on the in-file mobile function, isdMounts count, minChars floor) calibrated AFTER step 5 populates - this plan is the last writer of Recovery.tsx, so the anti-shrink floor lands against final geography and the guard rots loudly if the mobile block regresses."
    - statement: "Landscape rendering under the single 48rem authority is backstopped by the width-boundary proof: 740x360 (below 768px) renders mobile chrome, 844x390 (notched landscape, >=768px) renders desktop chrome and that is CORRECT (D-09), recorded per device in 08-UAT.md."
      verification: backstop
  artifacts:
    - path: web/src/pages/Recovery.tsx (step 5 populated)
      provides: "restore-all + per-row restore rows, ConfirmSheet beats, verify row, RestoreProgress + LogList, completion"
    - path: web/src/components/mobile/RunDetailSheet.tsx
      provides: "stale Phase-8 note updated, behavior unchanged"
    - path: web/src/app/mobileShellSource.test.ts
      provides: "Recovery.tsx MIXED_FILES entry (tailAnchor, isdMounts, minChars) calibrated against the final step-5 geography"
    - path: web/e2e/guided-restore.spec.ts (restore choreography)
      provides: "confirm beats, verify-row states, progress/log visibility, completion copy, sequential-fire parity"
    - path: web/e2e/narrow-viewport.spec.ts
      provides: "recovery flow in the de/fr 320/360 battery + the 48rem landscape boundary proof"
  key_links:
    - from: mobile restore-all row
      to: existing restoreAll handler (Recovery.tsx:1923-1961)
      via: shared state/handlers; sequential fireAndWaitRun
      why: "A parallel loop or second fire path is the phase's forbidden fork (D-02)."
    - from: progress/log presentation
      to: frozen web/src/lib/progress.ts + useBackupWatch
      via: useVisibilityGate mount-is-subscription; progressKeyFor mapping copied locally
      why: "Consuming the pipeline wrong breaks live progress for the WHOLE app, not just this page."
    - from: narrow-viewport recovery cases
      to: bootSeededPage + the display-prefs abort cut
      via: bv-lang localStorage seeding, route.abort on display-prefs
      why: "Without the abort cut, locale sweeps are nondeterministic across parallel workers."
    - from: the mobileShellSource Recovery.tsx MIXED_FILES entry
      to: the final Recovery.tsx mobile geography (Task 1's step-5 body)
      via: MIXED_FILES tailAnchor on the in-file mobile function + isdMounts + minChars floor
      why: "The anti-shrink floor is only meaningful against final geography; this plan is Recovery.tsx's last writer, so the entry belongs here rather than in the earlier-wave guard plan."
---

<assumption_delta_decision>
term: "optional" (from "optional dry-run/verify", SCRN-06 beat 3)
decision: no-change
rationale: "Optional" describes the skippability of the verify beat in the UI choreography (the integrity row never blocks restore), not an architectural required-to-optional transition of any noun. The verify surface remains the existing frozen `checkDomain(domain, source?)` endpoint consumed read-only; no new API noun, no entity re-identification, no plan changes.
</assumption_delta_decision>

<objective>
Fill the mobile review/restore step (position 5) with its populated body - restore-all and per-row restores behind ConfirmSheet gates, the optional checkDomain integrity row, live progress and log from the frozen pipeline, and the completion copy - update RunDetailSheet's stale Phase-8 pointer, and extend the narrow-viewport battery with the recovery flow plus the 48rem landscape boundary proof.

Purpose: This completes the SCRN-06 five-beat choreography (confirmation -> verify -> progress -> completion) on proven primitives; with it the guided restore flow is functionally whole on mobile.
Output: Populated step 5 in Recovery.tsx; RunDetailSheet note update; restore choreography in guided-restore.spec.ts; recovery flow + landscape proof in narrow-viewport.spec.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-guided-restore-real-device-verification/08-CONTEXT.md
@.planning/phases/08-guided-restore-real-device-verification/08-RESEARCH.md
@.planning/phases/08-guided-restore-real-device-verification/08-PATTERNS.md
@.planning/phases/08-guided-restore-real-device-verification/08-UI-SPEC.md
@web/src/pages/Recovery.tsx
@web/src/components/mobile/RunDetailSheet.tsx
@web/e2e/guided-restore.spec.ts
@web/e2e/narrow-viewport.spec.ts
</context>

## Constraints (binding)

- **D-10 (locked, verbatim):** "During task verification, run only the e2e specs affected by your changes (filtered `npx playwright test <spec>`); the full e2e suite is run once at the phase tail gate by the orchestrator — do not re-run it per plan." Vitest stays full-suite per plan.
- **D-12 frozen surfaces:** `web/src/app/router.tsx`, `web/src/lib/api.ts`, `web/src/lib/progress.ts`, `internal/**` are read-only. `progressKeyFor`/`verifyDomainFor`-style mappings are copied into Recovery.tsx, never added to frozen files. Zero npm installs (Package Legitimacy Audit: none required this phase).
- **Repo hygiene:** no real IPs/hosts/credentials/app keys in repo files; load-bearing why-comments; every user-visible string through `t()`; no em dashes in user text; semantic tokens only in mobile regions.
- **Windows Playwright discipline (STATE.md):** never pipe output through `tail`; kill orphaned workers on teardown hang.

## Artifacts this phase produces

Step-5 populated body inside `MobileRecoveryFlow` (restore-all secondary row, per-target RestoreRow/FileSetRecoveryRow reuse, ConfirmSheet confirmation beats, integrity verify row with locally-copied domain mapping, RestoreProgress + LogList presentation visibility-gated, restoreAllResult completion + four-status Badges, cancelled-as-neutral rendering); RunDetailSheet stale-note update; the mobileShellSource Recovery.tsx MIXED_FILES entry (tailAnchor/isdMounts/minChars) calibrated against the final geography; restore choreography + verify-row + progress-visibility assertions in `guided-restore.spec.ts`; recovery-flow cases and the 740x360/844x390 boundary proof in `narrow-viewport.spec.ts`.

<tasks>

<task type="auto">
  <name>Task 1: Step 5 populated - restore rows, ConfirmSheet beats, verify row, progress/log, completion</name>
  <files>web/src/pages/Recovery.tsx</files>
  <read_first>
    - web/src/pages/Recovery.tsx:1923-1961 (restoreAll handler - the ONLY restore-all path; sequential fireAndWaitRun), :2178-2220 (configPhase block for narration-state shape), :2431-2478 (desktop review StepCard: restore-all row, RestoreRow list, restoreAllResult rendering), :88-177 (RestoreRow + RestoreAction: requireConfirm=false + confirmMessage=t("recovery.restoreRowConfirm") path)
    - web/src/components/mobile/RunDetailSheet.tsx:120-153 (progressKeyFor + verifyDomainFor mappings to copy locally), :179-238 (LogList + LiveRunSection - the presentation being reused), :433-446 (the integrity verify-row precedent: integrity.verify/checking/ok)
    - web/src/components/mobile/ConfirmSheet.tsx:30-113 (anatomy already correct)
    - web/src/lib/i18n.ts:1600-1608 (recovery.restoreAll/restoreAllResult/vmSshNote/noneDiscovered), the integrity.* keys, containers.restoreSelectedConfirm
    - 08-PATTERNS.md (sequential restore-all, live progress + log, one-useConfirm-two-presentations)
  </read_first>
  <action>
    Replace the Plan-01 empty-branch placeholder of step 5 with the populated body (keep the zero-target empty copy for the no-targets case):
    1. Restore-all: a secondary/tonal mid-screen row labeled t("recovery.restoreAll") - NEVER the sticky bar (which keeps the safe action). Pressing awaits confirm(t("containers.restoreSelectedConfirm")) then calls the EXISTING restoreAll handler verbatim: sequential per-target fireAndWaitRun, terminal state before the next, single-flight honored. No parallel loop, no second fire path (D-02).
    2. Per-target rows: re-host the desktop row list with the existing row components (RestoreRow for containers/VMs, the FileSet row component the desktop card uses) - their RestoreAction confirmMessage path already produces the per-row ConfirmSheet below md (recovery.restoreRowConfirm with the target name substituted). Keep forceLeaveStopped semantics identical.
    3. Four-status: per-target outcome Badges with TEXT labels + hue (never color alone); partial results render ok and fail counts side by side; VM-without-SSH degrades to t("recovery.vmSshNote") on the affected row, never a silent skip.
    4. Integrity verify row (the optional beat): a secondary row labeled t("integrity.verify") firing checkDomain through a LOCALLY-copied domain-to-endpoint mapping (copy the RunDetailSheet verifyDomainFor/progressKeyFor mapping shape into Recovery.tsx - frozen files untouched, why-comment records the copy). Busy: t("integrity.checking"); result: status text + Badge per the RunDetailSheet verify-row precedent. The row never blocks restore and never renders copy implying it does; restore remains available throughout.
    5. Progress + log (D-04, read-only): during restores render the RestoreProgress presentation (ProgressBar + RestoreCancelButton + sticky outcome) fed by the existing useProgress/useBackupWatch wiring the desktop path uses, and log lines via the RunDetailSheet LogList pattern class-for-class (mono timestamps; LogList is the ONLY log style). Gate the section with useVisibilityGate so hiding pauses the poll chain and returning reconciles a run that finished in the background. Cancel via RestoreCancelButton; a cancelled restore renders as a NEUTRAL terminal (no failure tone), per the house rule.
    6. Completion: t("recovery.restoreAllResult") with {ok}/{fail} substituted, per-target Badges, and the kit step's gate (restoreAllResult set) satisfied. Failures keep the user on the step with backend text verbatim.
    7. Hue discipline holds: the populated body still calls NO nextHue(). Desktop bytes untouched; strings via t(); semantic tokens only; 44px row floors below md.
  </action>
  <verify>
    <automated>cd web && npx vitest run</automated>
  </verify>
  <done>Step 5 performs restore-all and per-row restores behind ConfirmSheet gates, offers the non-blocking integrity verify row, streams progress/log from the frozen pipeline with visibility gating, and completes with restoreAllResult + four-status badges; vitest green.</done>
  <acceptance_criteria>
    - MobileRecoveryFlow contains no direct api.ts restore call - restoreAll and the row components are the only fire paths
    - Restore-all and per-row restores each await confirm() before firing (ConfirmSheet below md)
    - The integrity row renders checking/ok states and stays non-blocking; the domain mapping is a local copy (frozen files diff-clean)
    - Progress/log section is visibility-gated; cancelled renders neutral
    - No nextHue() call added in the mobile block (Plan-01 dom test still passes)
    - npx vitest run exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: RunDetailSheet stale Phase-8 pointer comments + mobileShellSource Recovery.tsx guard entry</name>
  <files>web/src/components/mobile/RunDetailSheet.tsx, web/src/app/mobileShellSource.test.ts</files>
  <read_first>
    - web/src/components/mobile/RunDetailSheet.tsx:64-68 and :561-574 (the stale "full guided restore is SCRN-06 / Phase 8" comments; verified code comments only - no user-visible stale string)
    - 08-UI-SPEC.md Screen Contracts (RunDetailSheet entry stays reveal-only - no deep link into the flow)
    - web/src/app/mobileShellSource.test.ts:578-780 (SWEEP_NEEDLES, the wholly-swept files list, MIXED_FILES entries with {file, tailAnchor /function Mobile/, isdMounts, minChars} anti-shrink anchors, ISD_MOUNT = the !isDesktop && ( mount signature), stripComments/mountRegions mechanics - Recovery.tsx itself is already in context from Task 1)
  </read_first>
  <action>
    Update both stale comments (:64-68 and :561-574) to name the now-shipped mobile flow in Recovery.tsx instead of pointing at "Phase 8" as future work. Keep the restore entry reveal-only - do NOT add a deep link into the flow (recorded default, Open Question 2). Behavior unchanged; comment-only edit landed in the same series of commits as the shipping flow. If any user-visible note referencing the pending phase is found while editing, update it in the same commit.
    Then extend the source guard with the Recovery.tsx mixed-file entry - this task runs AFTER Task 1 populated step 5, and this plan is the LAST writer of Recovery.tsx, so the anti-shrink anchors are calibrated against final geography (the entry was moved here from the wave-2 guard plan so its minChars floor is never stale on arrival):
    1. Add Recovery.tsx as a MIXED_FILES entry modeled on the existing mixed-file shape: tailAnchor keyed on the in-file mobile function (the /function Mobile/ anchor pattern - use the actual function name Task 1 landed), isdMounts count matching the one {!isDesktop && ( mount, and a minChars floor generous enough to catch silent deletion of the mobile block - now including the step-5 body - while tolerating legitimate edits. If Plans 01-02 created any standalone component files for the flow that the wave-2 guard plan did not already sweep, add them to the wholly-swept files list; if none exist, note that in the SUMMARY.
    2. Verify the sweep needles pass against the FINAL Recovery.tsx regions (no banned stops, no font-medium in swept regions) and that the guard FAILS loudly if the mobile block is deleted or the mount count changes - this rot-loudly property is the guard's purpose; if the anchor does not fail on a simulated regression during development, tighten it before committing.
  </action>
  <verify>
    <automated>cd web && npx vitest run</automated>
  </verify>
  <done>No comment in RunDetailSheet describes the mobile guided restore as future work; the restore entry remains reveal-only; behavior unchanged. mobileShellSource.test.ts carries the Recovery.tsx MIXED_FILES entry (tailAnchor, isdMounts, minChars) calibrated against the final geography and demonstrably rots on regression.</done>
  <acceptance_criteria>
    - RunDetailSheet.tsx contains no "Phase 8"-as-future-work pointer for the restore flow (comments updated to reference the shipped Recovery.tsx flow)
    - No navigation/deep-link code added
    - mobileShellSource.test.ts contains a Recovery.tsx MIXED_FILES entry with a tailAnchor, an isdMounts count and a minChars floor
    - Temporarily deleting the mobile block (local dev check) makes the guard fail; restoring it makes it pass
    - npx vitest run exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Restore choreography e2e + narrow-viewport de/fr recovery battery + landscape boundary proof</name>
  <files>web/e2e/guided-restore.spec.ts, web/e2e/narrow-viewport.spec.ts</files>
  <read_first>
    - web/e2e/guided-restore.spec.ts (Plans 01-02 content to extend)
    - web/e2e/narrow-viewport.spec.ts:1-120 (MOBILE_PROJECTS gate, LOCALE_STORAGE_KEY bv-lang, LINE_BOX=20, bootSeededPage at :62-72, the locales x widths loop shape), :541 (the route battery to extend)
    - web/src/lib/useMediaQuery.ts:42 (48rem - the single width authority for the boundary proof)
    - web/e2e/destination-vms-flash.spec.ts:55-165 (runs-domain fixture shapes for staged restore runs)
  </read_first>
  <action>
    1. guided-restore.spec.ts (mobile projects), staged runs-domain fixtures after the template: drive step 5 populated - assert the restore-all row opens ConfirmSheet with the consequence-naming copy and destructive-on-top/cancel-bottom anatomy; cancel fires nothing; confirm issues the sequential restore requests in the desktop order (one at a time - assert request ordering parity, never fabricated outcomes); per-row confirmMessage copy names the target. Assert the verify row: checking state then the staged result rendered as text + Badge, with restore controls still enabled throughout. Assert progress/log render during a staged long run and that the section's visibility gating pauses/reconciles (hide + reshow pattern). Assert completion copy with the staged ok/fail counts and the neutral rendering of a staged cancelled run. Extend desktop leakage needles with the step-5 signature.
    2. narrow-viewport.spec.ts: add the recovery flow route to the :541 route battery and add recovery-flow cases to the {de, fr} x {320px, 360px} loop reusing bootSeededPage verbatim (D-07's narrow-viewport sweep) - assert no horizontal pan, no clipped control, the stepOf chip single-line (LINE_BOX pattern), de step titles wrap like the StepCard precedent. Never invent a new locale mechanism; keep the display-prefs abort cut.
    3. Landscape boundary proof (D-09) in the same spec: one mobile-project case at 740x360 (below 768px) asserting the mobile step flow renders; one mobile-project case at 844x390 (notched landscape, >=768px) asserting the DESKTOP chrome renders and the mobile flow is absent - recorded as the CORRECT render under the single authority, with a why-comment citing D-09 (the device matrix in 08-UAT.md records the same per device).
    4. Windows discipline: filtered runs per D-10; never pipe output through tail.
  </action>
  <verify>
    <automated>cd web && npx playwright test e2e/guided-restore.spec.ts e2e/narrow-viewport.spec.ts</automated>
  </verify>
  <done>The restore choreography, verify row, progress visibility and completion pass on staged routes; de/fr at 320/360 passes on the recovery flow; the 48rem landscape boundary renders the correct chrome on both sides.</done>
  <acceptance_criteria>
    - guided-restore.spec.ts asserts ConfirmSheet anatomy, cancel-no-op, sequential request ordering, verify-row non-blocking behavior, progress visibility gating, completion copy and cancelled-neutral
    - narrow-viewport.spec.ts includes recovery cases in the de/fr x 320/360 battery using bootSeededPage, plus 740x360 (mobile chrome) and 844x390 (desktop chrome) boundary cases
    - npx playwright test e2e/guided-restore.spec.ts e2e/narrow-viewport.spec.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPA -> Go API (restore + verify + progress SSE) | The restore fire path, guard chain and progress pipeline stay frozen server-side and client-lib-side; the mobile step is presentation over them |
| ConfirmSheet -> user intent | The last client-side backstop before a destructive restore fires |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | restore-all + per-row restore fires | high | mitigate | Frozen server Confirmed guard chain is authoritative; client-side: ConfirmSheet gates with consequence-naming existing copy, destructive control top, safe cancel thumb-default, controls secondary/tonal mid-screen away from the sticky safe action (D-03); sequential single-flight consumption only (D-02) |
| T-08-02 | (n/a - spoofing class avoided) | hover-only affordances | low | accept | No new hover-dependent control in this plan; Tailwind v4 compiles hover: behind (hover: hover) and the flow's rows are tap-first by construction |
| T-08-03 | Information Disclosure | restore/verify error + log rendering | medium | mitigate | Backend text verbatim (pre-scrubbed, capped server-side); LogList is the only log style and renders scrubbed run lines; never raw client exceptions |
| T-08-05 | Repudiation | four-status Badges + verify row result | medium | mitigate | Status always TEXT label + hue (WCAG 1.4.1 G14); cancelled renders as a distinct neutral terminal, not a failure |
| T-08-SC | Tampering | npm installs | high | accept | Zero installs this phase (D-12); Package Legitimacy Audit empty per 08-RESEARCH.md |
</threat_model>

<verification>
- `cd web && npx vitest run` green after Tasks 1 and 2 (full suite per D-10).
- `cd web && npx playwright test e2e/guided-restore.spec.ts e2e/narrow-viewport.spec.ts` green (filtered per D-10).
- `web/src/lib/progress.ts` and `web/src/lib/api.ts` diff-clean (mappings copied locally).
</verification>

<success_criteria>
- The five-beat choreography is complete on mobile: confirmation (ConfirmSheet beats) -> optional verify (checkDomain row) -> live progress with log (frozen pipeline, visibility-gated) -> completion (restoreAllResult + kit step).
- RunDetailSheet carries no stale Phase-8 pointer.
- The recovery flow passes the de/fr 320/360 sweep and the 48rem landscape boundary renders the correct chrome on both sides.
</success_criteria>

<output>
Create .planning/phases/08-guided-restore-real-device-verification/08-03-SUMMARY.md when done
</output>
