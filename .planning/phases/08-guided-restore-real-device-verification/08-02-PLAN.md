---
phase: 08-guided-restore-real-device-verification
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - web/src/pages/Recovery.tsx
  - web/e2e/guided-restore.spec.ts
autonomous: true
requirements: [SCRN-06]
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The mobile config step renders the real restore body: source picker (local path / off-site URL), the APP_KEY reminder, and the config.restoreChain numbered narration rendered read-only ABOVE the restore control (Config.tsx:1187-1197 precedent, D-03)."
    - "The config restore control is destructive-classed: secondary/tonal styling, mid-screen (never the sticky thumb-default slot), and gated by useConfirm so below md it presents as ConfirmSheet with the destructive control on top and the safe cancel at the thumb-default bottom."
    - "Skip remains a first-class resolution: no settings backup -> t(recovery.configSkip) advances without firing any restore call."
    - "Firing the restore routes through the EXISTING restoreOwnConfig handler (re-fetch + merge onto the fresh server baseline before the PUT); no direct putSettings with a captured object (Pitfall 6)."
    - "The configPhase narration states (recovery.configRestoring / configManualRestart / configReloadWhenBack / configReload) render exactly as Config's mobile sheet does; the mid-flow window reload is correct behavior and the flow re-enters at step 1 with restored settings live (Pitfall 7)."
    - "Config-step loading/error/empty per 08-UI-SPEC: loading = configRestoring + configPhase narration; error = backend text verbatim (+ the documented APP_KEY-mismatch remap to recovery.appKeyRemedy), failed restore STAYS on the step; empty = the configSkip path is the sanctioned empty resolution."
    - statement: "Desktop identity on /recovery is backstopped by the extended desktop-untouched battery from Plan 01; the config step adds mobile-only chrome, so desktop leakage needles extend to the narration block."
      verification: backstop
  artifacts:
    - path: web/src/pages/Recovery.tsx (config step body)
      provides: "source picker, chain narration ol, confirm-gated restore row, skip, configPhase states"
    - path: web/e2e/guided-restore.spec.ts (config choreography)
      provides: "narration-above-confirm DOM order, confirm gate, API parity for the config restore POST, reload re-entry"
  key_links:
    - from: mobile config step restore row
      to: existing restoreOwnConfig handler
      via: shared component state/handlers (D-02)
      why: "A second settings-write path is the phase's forbidden fork."
    - from: restore row press
      to: useConfirm -> ConfirmSheet presentation
      via: the shared confirm() promise (below md it renders ConfirmSheet automatically)
      why: "Destructive settings overwrite without a confirm gate violates D-03."
    - from: e2e config choreography
      to: staged settings/restore-own route fixtures
      via: Go-JSON field-for-field staging (Plan 01 template)
      why: "Parity assertions require the frozen request shapes."
---

<objective>
Fill the mobile config step (position 2) with the real restore body: source picker, guard-chain narration, confirm-gated destructive restore, skip path and the configPhase restart narration - completing the optional settings-restore beat of SCRN-06 on the same guarded calls the desktop fires.

Purpose: The tracer (Plan 01) shipped this step's skip path only; this plan makes the optional restore fully functional with D-03 destructive-action discipline, and extends the tracer e2e to the config choreography.
Output: Config step body in Recovery.tsx; config choreography assertions in guided-restore.spec.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-guided-restore-real-device-verification/08-CONTEXT.md
@.planning/phases/08-guided-restore-real-device-verification/08-RESEARCH.md
@.planning/phases/08-guided-restore-real-device-verification/08-PATTERNS.md
@.planning/phases/08-guided-restore-real-device-verification/08-UI-SPEC.md
@web/src/pages/Recovery.tsx
@web/src/pages/Config.tsx
@web/e2e/guided-restore.spec.ts
</context>

## Constraints (binding)

- **D-10 (locked, verbatim):** "During task verification, run only the e2e specs affected by your changes (filtered `npx playwright test <spec>`); the full e2e suite is run once at the phase tail gate by the orchestrator — do not re-run it per plan." Vitest stays full-suite per plan.
- **D-12 frozen surfaces:** `web/src/app/router.tsx`, `web/src/lib/api.ts`, `web/src/lib/progress.ts`, `internal/**` are read-only. Zero npm installs (Package Legitimacy Audit: none required this phase).
- **Repo hygiene:** no real IPs/hosts/credentials/app keys in repo files; load-bearing why-comments; every user-visible string through `t()`; no em dashes in user text; semantic tokens only in mobile regions.
- **Windows Playwright discipline (STATE.md):** never pipe output through `tail`; kill orphaned workers on teardown hang.

### Recorded design resolution: the config-step confirm gate

08-UI-SPEC's copywriting row cites "the existing desktop config-restore confirm for the config step". Verified against the codebase: no such dialog exists - the desktop Recovery fires `restoreOwnConfig()` directly with no confirm, and Config.tsx's own mobile restore sheet uses the sheet itself as the confirm surface (its useConfirm at :345 guards only snapshot deletion). This plan therefore implements the D-03-compliant reading: the `config.restoreChain` numbered narration renders read-only ABOVE the restore control (the MobileRestoreSheet precedent at Config.tsx:1187-1197), and the destructive restore row itself is gated by the shared `confirm()` promise with `t("config.restoreChain.title")` as the consequence-naming message - ConfirmSheet below md presents it with the destructive control on top and the safe cancel at the thumb-default bottom (ConfirmSheet.tsx:30-46 anatomy, already correct). Zero new i18n keys (the phase census locks exactly two, both landed in Plan 01).

## Artifacts this phase produces

The config step body inside `MobileRecoveryFlow` (source picker rows, chain-narration `<ol>`, confirm-gated secondary restore row, skip resolution, configPhase narration states with the restart->reload path); config-choreography assertions in `guided-restore.spec.ts` (narration-above-confirm DOM order, cancel/confirm behavior, config restore POST parity, reload re-entry at step 1).

<tasks>

<task type="auto">
  <name>Task 1: Config step body - narration, confirm-gated restore, skip, configPhase states</name>
  <files>web/src/pages/Recovery.tsx</files>
  <read_first>
    - web/src/pages/Config.tsx:1100-1235 (MobileRestoreSheet: the guard-chain ol at :1187-1197 rendered read-only above the confirm, runRestore phase machine at :1138-1180, configPhase narration states at :1200-1228, StickyActionBar at :1232)
    - web/src/pages/Recovery.tsx (the desktop config StepCard and the restoreOwnConfig handler it fires - the handler this step reuses; also the Plan-01 mobile block this body extends)
    - web/src/components/mobile/ConfirmSheet.tsx:30-113 (one promise API, destructive top, safe cancel bottom, no autoFocus)
    - web/src/lib/i18n.ts:149-154 (config.restoreChain.title + step1..step5 - reused verbatim), :1553-1566 (recovery.config* keys)
    - 08-PATTERNS.md (guard-chain narration + error/phase state machine patterns)
  </read_first>
  <action>
    Extend the Plan-01 mobile config step (currently title + skip) with the full body, re-hosting the desktop config card's fields (per D-02, same state, same handler):
    1. Source picker rows: the local-path / off-site-URL input the desktop card renders (SourceToggle equivalent), plus t("recovery.configSourceLabel"), t("recovery.configLocalPath") / t("recovery.configOffsiteUrl"), and the t("recovery.configAppKeyReminder") hint. Minimum 44px row heights below md; paths break-all + title for long text.
    2. Guard-chain narration per D-03: render t("config.restoreChain.title") + the five numbered list items (step1..step5) as a read-only ordered list directly ABOVE the restore control, copying the Config.tsx:1187-1197 structure class-for-class. Add a why-comment noting D-03 (narration = the consequence copy, per the Config MobileRestoreSheet precedent).
    3. Restore control: a secondary/tonal row labeled t("recovery.configRestore") (busy: t("recovery.configRestoring")), placed mid-screen - NEVER in the StickyActionBar (the sticky bar keeps the safe Continue action). Pressing it awaits the shared confirm(t("config.restoreChain.title")) before calling the existing restoreOwnConfig handler; below md this renders ConfirmSheet automatically (destructive top, safe cancel bottom). No second fire path, no direct settings PUT (Pitfall 6 - the handler re-fetches and merges onto the fresh server baseline).
    4. Skip path stays as landed in Plan 01 (t("recovery.configSkip") advances; a why-comment may record that skip is the sanctioned empty resolution).
    5. configPhase narration states: after restoreOwnConfig fires, present the same states Config's sheet presents (Config.tsx:1200-1228): t("recovery.configRestarting") while waiting, t("recovery.configManualRestart") when manual restart is needed, t("recovery.configReloadWhenBack") + a t("recovery.configReload") action on timeout, and the existing waitForAppBack -> window.location.reload() path (Pitfall 7: the reload is CORRECT; do not preserve step position across it - the flow re-enters at step 1 with restored settings live). Failure keeps the user ON the step with backend error text verbatim (the documented APP_KEY-mismatch remap to t("recovery.appKeyRemedy") included); the chrome stays stable.
    6. Every string through t(); semantic tokens only; desktop bytes untouched.
  </action>
  <verify>
    <automated>cd web && npx vitest run</automated>
  </verify>
  <done>The config step offers source input, narrates the chain read-only above a confirm-gated secondary restore row, keeps skip first-class, and walks the configPhase states through restart and reload; vitest (guards + conventions) green.</done>
  <acceptance_criteria>
    - The narration ordered list renders BEFORE the restore row in DOM order (e2e-asserted in Task 2)
    - The restore row awaits confirm() before the restore call (ConfirmSheet below md)
    - No new api.ts call sites in the mobile block; restoreOwnConfig is the only settings-write path for this step
    - No new i18n keys introduced (only the two Plan-01 keys exist as new keys this phase)
    - npx vitest run exits 0
    - Desktop JSX outside the max-md:hidden wrapper untouched
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Config choreography in guided-restore.spec.ts</name>
  <files>web/e2e/guided-restore.spec.ts</files>
  <read_first>
    - web/e2e/guided-restore.spec.ts (Plan-01 staged fixtures + choreography to extend)
    - web/e2e/destination-vms-flash.spec.ts:55-165 (fixture template; settings-domain shapes)
    - web/src/lib/api.ts (frozen request/response shapes for the settings read and the config restore call - parity targets)
  </read_first>
  <action>
    Extend the tracer e2e (mobile projects) with the config-step choreography on staged routes:
    1. Stage the settings read + the config restore endpoint field-for-field after the Plan-01 fixture template.
    2. Assert DOM order: the five narration list items precede the restore control (DOM order = reading order, per the Config precedent).
    3. Assert the confirm gate: pressing the restore row opens the ConfirmSheet presentation (destructive control on top, cancel at the bottom); cancel dismisses and stays on the step with zero restore calls intercepted; confirm issues exactly the request the desktop config restore fires (method/path/body parity).
    4. Assert the busy and narration states: configRestoring, then the restart narration; after the staged app-back, the page reloads and the flow re-enters at step 1 (step position not preserved - Pitfall 7 is the contract).
    5. Assert the skip path: skip advances to step 3 with no settings write intercepted.
    6. Extend the desktop-project leakage needles with the config narration's unique signature (absent at >=48rem). All locators keep includeHidden semantics. Filtered runs only (D-10); never pipe output through tail.
  </action>
  <verify>
    <automated>cd web && npx playwright test e2e/guided-restore.spec.ts</automated>
  </verify>
  <done>The config choreography passes on staged routes: narration order, confirm cancel/confirm behavior, POST parity, restart/reload re-entry, skip path, desktop leakage clean.</done>
  <acceptance_criteria>
    - The spec contains assertions for narration-before-restore DOM order, ConfirmSheet cancel and confirm paths, exactly-one restore request on confirm, and reload re-entry at step 1
    - Desktop projects assert the config narration signature absent
    - npx playwright test e2e/guided-restore.spec.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPA -> Go API (settings write) | The config restore PUTs BombVault's own settings; the guard chain and scrubbing stay server-side and frozen |
| ConfirmSheet -> user intent | The confirm presentation is the client-side backstop against an unintended settings overwrite |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | config restore row (settings overwrite) | high | mitigate | Server guard chain unchanged and authoritative; client-side: chain narration read-only above the control, confirm() gate presenting ConfirmSheet (destructive top, safe cancel thumb-default), secondary/tonal styling mid-screen away from the sticky safe action (D-03) |
| T-08-03 | Information Disclosure | config-step error rendering | medium | mitigate | Backend error text verbatim (pre-scrubbed server-side); APP_KEY-mismatch remaps to the recovery.appKeyRemedy copy; never raw client exceptions |
| T-08-SC | Tampering | npm installs | high | accept | Zero installs this phase (D-12); Package Legitimacy Audit empty per 08-RESEARCH.md |
</threat_model>

<verification>
- `cd web && npx vitest run` green after Task 1 (full suite per D-10).
- `cd web && npx playwright test e2e/guided-restore.spec.ts` green (filtered per D-10).
- Desktop bytes untouched outside the max-md:hidden wrapper.
</verification>

<success_criteria>
- The mobile config step restores BombVault settings through the desktop's own handler behind a ConfirmSheet gate with narration above; skip and restart/reload paths behave per contract.
- E2E proves the choreography and API parity; desktop identity needles stay clean.
</success_criteria>

<output>
Create .planning/phases/08-guided-restore-real-device-verification/08-02-SUMMARY.md when done
</output>
