---
phase: 08-guided-restore-real-device-verification
plan: 04
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - web/src/components/Button.tsx (the shared control named by 07-UI-REVIEW fix 1 - the max-md bleed lands here)
  - web/src/pages/Settings.tsx
  - web/src/pages/settings/LanguageCard.tsx (card-local raw <button> trigger, :74 - measured candidate)
  - web/src/pages/settings/NotifyCard.tsx (card-local raw mobile entry row, :892 - already min-h-[2.75rem], measured candidate)
  - web/src/pages/settings/shared.tsx (Card/ToggleRow scaffolding shared by the stacked cards - measured candidate)
  - web/src/components/ActivityLog.tsx
  - web/src/app/mobileShellSource.test.ts
  - web/e2e/destination-settings.spec.ts
autonomous: true
requirements: [VERIFY-04, VERIFY-05]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every interactive control below the breakpoint is >=44px: the Settings stacked-card 32px controls recorded by 07-UI-REVIEW fix 1 grow their hit area via the Toggle ::after bleed pattern applied at the shared control (web/src/components/Button.tsx, fix 1's recorded primary resolution) and at any measured sub-floor card-local raw button (visuals frozen, max-md:-scoped, desktop >=48rem byte-identical)."
    - "Every hover-dependent affordance keeps its non-hover path: Tailwind v4 compiles hover: behind (hover: hover); InfoBubble-class hints ride TapPopover below md; no new hover-only control is introduced."
    - "The ActivityLog day-filter chip (:465) is resolved per D-06 fix 2: tonal accentSoft/accentText (the contract default) OR a documented reservation at the site - the honest call is recorded in the SUMMARY; padding on the touched line normalizes to 4/8 stops; the desktop chip (:418) is untouched."
    - "The spacing/weight guard's binding comment carries the D-06 fix-3 scope: mobile regions only (max-md:hidden counterparts and !isDesktop blocks); desktop-half legacy 12px paddings (Dashboard.tsx:3029,3037) are NOT touched - desktop byte-identity wins."
    - "The mobileShellSource guard knows the phase-8 surfaces: the wholly-phase-8 files Plans 01-02 created join the swept list and the sweep's binding comment carries the D-06 fix-3 mobile-regions-only scope (this plan); the Recovery.tsx MIXED_FILES entry (tailAnchor on the in-file mobile function, isdMounts count, minChars floor) is calibrated by Plan 03 Task 2 - the last writer of Recovery.tsx - so the floor lands against final geography, and the guard rots loudly if the geography regresses."
    - "Touch-target geometry per 08-UI-SPEC (backstop row): Playwright bounding-box assertions cover the Settings bleed fixes and the new flow's controls; device-session feel is the D-11 checkpoint."
    - statement: "Four-status-by-text and semantic-token rules on the S8 sweep surfaces are backstopped by the source-level guard (token sweep + sweep needles); the device-session theme check is the D-11 checkpoint."
      verification: backstop
  artifacts:
    - path: web/src/components/Button.tsx + web/src/pages/Settings.tsx (+ measured card-local sites under web/src/pages/settings/)
      provides: "44px bleed hit areas on the recorded 32px stacked-card controls"
    - path: web/src/components/ActivityLog.tsx
      provides: "D-06 fix 2 resolution on the :465 chip"
    - path: web/src/app/mobileShellSource.test.ts
      provides: "phase-8 sweep-list entries for the wholly-phase-8 files, D-06 fix-3 scope comment (the Recovery.tsx mixed-file entry ships in Plan 03 Task 2)"
    - path: web/e2e/destination-settings.spec.ts
      provides: "touch-target backstop assertions on the fixed controls"
  key_links:
    - from: Settings bleed fixes
      to: Toggle.tsx:92-105 bleed pattern
      via: max-md:after:absolute max-md:after:-inset-3 max-md:after:content-[''] class-for-class
      why: "Growing the VISUAL control would break the frozen desktop and mobile design; only the hit area may bleed."
    - from: mobileShellSource sweep-list extension + fix-3 scope comment
      to: the wholly-phase-8 files Plans 01-02 created
      via: the guard's wholly-swept files list
      why: "Files this phase created must be swept or the guard is blind to them; the Recovery.tsx MIXED_FILES entry rides with Plan 03 - the last writer of Recovery.tsx - so its anti-shrink floor is not calibrated against a file that will still change."
    - from: destination-settings touch assertions
      to: the fixed stacked-card controls
      via: boundingBox >=44 assertions (list-ergonomics.spec.ts:369 pattern)
      why: "The bleed is invisible by design - only a bounding-box assertion can prove it."
---

<objective>
Land the carried D-06 review fixes and the automated half of the VERIFY-04/05 sweep (D-08): 44px bleed hit areas on the Settings stacked-card 32px controls (fix 1), the ActivityLog :465 chip resolution (fix 2), the mobileShellSource guard's D-06 fix-3 mobile-regions scope comment plus the phase-8 sweep-list extension (the Recovery.tsx mixed-file entry is calibrated by Plan 03, the last writer of Recovery.tsx), and the touch-target e2e backstop.

Purpose: These are the phase's contracted debts (D-06) plus the source-level theme/touch guards (VERIFY-04/05) - the automated half that makes the device session a confirmation rather than a discovery.
Output: Fixed Settings controls and ActivityLog chip; extended source guard; extended destination-settings touch battery.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-guided-restore-real-device-verification/08-CONTEXT.md
@.planning/phases/08-guided-restore-real-device-verification/08-RESEARCH.md
@.planning/phases/08-guided-restore-real-device-verification/08-PATTERNS.md
@.planning/phases/08-guided-restore-real-device-verification/08-UI-SPEC.md
@.planning/phases/07-remaining-destinations-operational-parity/07-UI-REVIEW.md
@web/src/components/Toggle.tsx
@web/src/components/ActivityLog.tsx
@web/src/app/mobileShellSource.test.ts
@web/e2e/destination-settings.spec.ts
</context>

## Constraints (binding)

- **D-10 (locked, verbatim):** "During task verification, run only the e2e specs affected by your changes (filtered `npx playwright test <spec>`); the full e2e suite is run once at the phase tail gate by the orchestrator — do not re-run it per plan." Vitest stays full-suite per plan.
- **D-12 frozen surfaces:** `web/src/app/router.tsx`, `web/src/lib/api.ts`, `web/src/lib/progress.ts`, `internal/**` are read-only. Zero npm installs (Package Legitimacy Audit: none required this phase).
- **D-06 fix 3 is binding:** the spacing/weight guard scope is mobile regions ONLY; desktop-half legacy paddings are not touched; desktop byte-identity wins on every touched file.
- **Repo hygiene:** load-bearing why-comments (the bleed pattern and the chip resolution each carry one); every user-visible string through `t()`; no em dashes in user text; semantic tokens only.
- **Windows Playwright discipline (STATE.md):** never pipe output through `tail`; kill orphaned workers on teardown hang.

## Artifacts this phase produces

44px `::after` bleed hit areas on the recorded Settings stacked-card controls (visuals unchanged; the shared Button.tsx bleed + measured card-local sites); the ActivityLog :465 chip resolution (tonal default or documented reservation) with 4/8 padding normalization on the touched line; mobileShellSource sweep-list extension for the wholly-phase-8 files + the D-06 fix-3 mobile-regions scope comment (the Recovery.tsx MIXED_FILES entry ships in Plan 03 Task 2, after step 5 lands); destination-settings bounding-box assertions proving the fixes.

<tasks>

<task type="auto">
  <name>Task 1: D-06 fixes 1+2 - Settings 44px bleed hit areas and ActivityLog :465 chip</name>
  <files>web/src/components/Button.tsx, web/src/pages/Settings.tsx, web/src/pages/settings/LanguageCard.tsx, web/src/pages/settings/NotifyCard.tsx, web/src/pages/settings/shared.tsx, web/src/components/ActivityLog.tsx</files>
  <read_first>
    - .planning/phases/07-remaining-destinations-operational-parity/07-UI-REVIEW.md:24-60 (fix 1: the recorded 32px stacked-card controls AND fix 1's own recorded primary resolution - extend the Toggle bleed to the shared Button below md; fix 2: the :465 chip; fix 3 scope note)
    - The recorded stacked-card surface, enumerated (the stacked cards ARE the desktop panels per 07-05's shared-panels decision): web/src/pages/Settings.tsx renders the card components it imports from web/src/pages/settings/ - AccentCard, TwoFactorCard, LanguageCard, ThemeCard, RestoreChecksSection, RcloneCard, CloudCard, NotifyCard, IntegrityCard, VMSSHCard, FleetSettingsCard, CloudCredSetsCard, SettingsPortabilityCard, AboutCard, DashboardWidgetCard - plus shared.tsx (Card/ToggleRow scaffolding). There is NO web/src/components/settings/ directory.
    - web/src/components/Button.tsx (the shared control: the bleed joins the className string on its <button> element the way Toggle.tsx joins it; confirm the element has a positioning context for the absolute ::after - via the glim-btn engine class or an added max-md:relative)
    - web/src/pages/settings/LanguageCard.tsx:74 (card-local raw <button> dropdown trigger, py-1.5 + text-sm - measured candidate, likely sub-floor) and :94 (listbox option rows, py-2 - measured candidate)
    - web/src/pages/settings/NotifyCard.tsx:892 (card-local raw mobile entry row - already min-h-[2.75rem] = 44px; measure, expect no-touch)
    - web/src/components/Toggle.tsx:92-105 (the bleed pattern verbatim: relative inline-flex control + max-md:after:absolute max-md:after:-inset-3 max-md:after:content-[''] + the why-comment)
    - web/src/components/ActivityLog.tsx:410-478 (desktop chip :418 - NOT touched; mobile chip :465 - the fix site, py-0.5 = 2px, off-scale)
    - web/src/app/mobileShellSource.test.ts:578-780 (the sweep needles this work must satisfy: no half stops, no font-medium in swept regions)
  </read_first>
  <action>
    Fix 1 (Settings stacked cards): the PRIMARY site is the shared control - fix 1's own recorded resolution is "extend the Toggle's documented bleed pattern to the shared Button below md", which lifts every stacked-card Button instance in one edit. In web/src/components/Button.tsx, add the Toggle bleed pattern class-for-class to the <button> className: the max-md:-scoped invisible ::after bleed (max-md:after:absolute max-md:after:-inset-3 max-md:after:content-[''] - -inset-3 yields a 44px+ hit area from a 32px control) with the why-comment copied in spirit (grow tap areas, never visual size inflation; desktop >=48rem byte-identical via the max-md: prefix), ensuring the element has a positioning context for the absolute ::after. Scope the bleed to the default variant only: variant="chip" keeps its engine-deliberate pill-embedded size (a 12px bleed inside a pill would swallow neighboring clicks); record that scoping in the SUMMARY. Then MEASURE the card-local raw buttons named in read_first: the LanguageCard.tsx:74 dropdown trigger gets the identical class-for-class bleed at its site; NotifyCard.tsx:892 already carries min-h-[2.75rem] - measure, record, expect no-touch; shared.tsx scaffolding gets the same treatment for any raw <button> it renders that measures sub-floor. Any OTHER recorded stacked-card control (full card list in read_first) that measures sub-floor and does NOT route through the shared Button gets the identical bleed at its own site and is listed in the SUMMARY. Do NOT restyle, resize or re-token the visuals; do NOT touch the desktop render. If a recorded control already clears 44px on measurement, note that in the SUMMARY instead of touching it.
    Fix 2 (ActivityLog :465): resolve the mobile day-filter chip per the contract default - switch the solid bg-accent/text-accentContrast to tonal accentSoft/accentText (accent stays reserved for active/primary state), OR keep solid accent with a documented reservation comment at the line (house exception-comment convention). Either way: normalize the touched line's padding to 4/8 stops (py-0.5 is off-scale - bring it to a scale stop while keeping the chip's visual height visually equivalent) and record the chosen resolution in the SUMMARY. The desktop chip at :418 is untouched - byte-identical.
    All strings through t(); no new keys; semantic tokens only.
  </action>
  <verify>
    <automated>cd web && npx vitest run</automated>
  </verify>
  <done>The recorded 32px controls bleed to >=44px hit areas below md with frozen visuals (shared Button.tsx bleed + measured card-local sites); the :465 chip is tonal (or carries a documented reservation) with on-scale padding; desktop renders byte-identical; vitest (guards + conventions) green.</done>
  <acceptance_criteria>
    - Each fixed control carries the max-md:after bleed classes and its visual classes are unchanged
    - ActivityLog.tsx :465 uses accentSoft/accentText or carries the documented reservation comment; :418 is untouched
    - The touched chip line's padding values are 4/8-scale stops
    - npx vitest run exits 0 (mobileShellSource sweep + uiConventions included)
    - SUMMARY records the fix-2 resolution choice and any control measured already-compliant
  </acceptance_criteria>
  <reversibility rating="reversible">max-md:-scoped additive classes and a one-line chip tone change; desktop is untouched, so a revert is class removal.</reversibility>
</task>

<task type="auto">
  <name>Task 2: mobileShellSource guard - phase-8 sweep-list extension + fix-3 scope comment</name>
  <files>web/src/app/mobileShellSource.test.ts</files>
  <read_first>
    - web/src/app/mobileShellSource.test.ts:578-780 (SWEEP_NEEDLES, the wholly-swept files list (WHOLLY_PHASE7_FILES shape), the MIXED_FILES entry shape {file, tailAnchor /function Mobile/, isdMounts, minChars} - for shape reference only; the Recovery.tsx MIXED_FILES entry itself is calibrated by Plan 03 Task 2), ISD_MOUNT = the !isDesktop && ( mount signature), stripComments/mountRegions mechanics)
    - 08-UI-SPEC.md Verification Sweep Contracts + Carried fixes table (fix 3 contracted resolution)
    - Plans 01-02 SUMMARYs in this phase directory if they exist, else their PLAN frontmatter files_modified: which standalone component files the flow actually created (expected: none beyond Recovery.tsx + its dom test - a test file is not a swept source file)
  </read_first>
  <action>
    Extend the source-assert guard so the phase-8 surfaces are swept and the fix-3 scope is binding:
    1. Sweep-list extension: add any WHOLLY-phase-8 standalone component files created by Plans 01-02 (e.g. web/src/components/recovery/) to the wholly-swept files list. Check Plans 01-02 outputs first: if the flow lives entirely inside Recovery.tsx plus its dom test, there is nothing to add - note that in the SUMMARY. Do NOT add the Recovery.tsx MIXED_FILES entry in this plan: Plan 03 Task 2 - the LAST writer of Recovery.tsx, running after step 5 populates - calibrates its tailAnchor (on the in-file mobile function), isdMounts count and minChars floor against the file's final geography, so the anti-shrink floor is never stale on arrival.
    2. Update the spacing/weight sweep's binding comment to the D-06 fix-3 scope: the sweep covers MOBILE regions only (max-md:hidden counterparts and !isDesktop blocks); desktop-half legacy paddings (Dashboard.tsx:3029,3037) are explicitly out of scope - desktop byte-identity wins. Cite D-06 fix 3 in the comment.
    3. Verify the sweep needles pass against the entries added here and that the guard FAILS loudly if a swept region is deleted or a mount count changes - this rot-loudly property is the guard's purpose; if an anchor does not fail on a simulated regression during development, tighten it before committing.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/app/mobileShellSource.test.ts</automated>
  </verify>
  <done>The guard sweeps the wholly-phase-8 files created so far (or the SUMMARY records none exist), carries the D-06 fix-3 mobile-regions-only scope comment, and demonstrably rots on regression; the Recovery.tsx mixed-file entry follows in Plan 03 Task 2 against final geography.</done>
  <acceptance_criteria>
    - mobileShellSource.test.ts sweeps the wholly-phase-8 standalone files created by Plans 01-02, or the SUMMARY records that none exist
    - The Recovery.tsx MIXED_FILES entry is NOT part of this plan's diff (it ships in Plan 03 Task 2)
    - The sweep comment states the mobile-regions-only scope citing D-06 fix 3
    - Temporarily deleting a swept region (local dev check) makes the guard fail; restoring it makes it pass
    - npx vitest run src/app/mobileShellSource.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: destination-settings touch-target backstop assertions</name>
  <files>web/e2e/destination-settings.spec.ts</files>
  <read_first>
    - web/e2e/destination-settings.spec.ts:202-209 (the includeHidden rule - the desktop half is always mounted-hidden, locators must be explicit) and the file's existing assertion conventions
    - web/e2e/list-ergonomics.spec.ts:369 (the bounding-box "every rendered card clears the 44px touch floor" pattern), :746 (log rows variant)
    - web/e2e/touch-tree.spec.ts:164-185 (bounding-box assertion shape on control collections)
  </read_first>
  <action>
    Extend the Settings touch-target battery (mobile projects) with bounding-box assertions covering the Task-1 fixed controls: iterate the recorded stacked-card controls on a mobile viewport and assert each rendered control's bounding box clears the 44px floor (following the list-ergonomics pattern). Use the includeHidden discipline where the always-mounted desktop half could satisfy a lazy locator. If the file already hosts a reusable floor-assertion helper, reuse it rather than duplicating the logic. Keep the run filtered (D-10) and never pipe output through tail (Windows wedge).
  </action>
  <verify>
    <automated>cd web && npx playwright test e2e/destination-settings.spec.ts</automated>
  </verify>
  <done>The Settings touch-target battery proves the fixed controls clear 44px on mobile viewports; the spec exits green.</done>
  <acceptance_criteria>
    - destination-settings.spec.ts contains bounding-box >=44 assertions over the fixed stacked-card controls on a mobile project
    - npx playwright test e2e/destination-settings.spec.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source guard -> shipped UI | mobileShellSource assertions gate the shipped class strings; e2e bounding boxes gate the rendered geometry |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-02 | Spoofing (affordance spoof) | hover-only affordances on sweep surfaces | medium | mitigate | Tailwind v4 compiles hover: behind (hover: hover); every swept affordance keeps its non-hover path (TapPopover below md); the guard's sweep needles catch regression in swept regions |
| T-08-05 | Repudiation | ActivityLog chip + sweep surfaces | medium | mitigate | Status/accent color never alone carries meaning; the chip change is tonal, not semantic; four-status text rules already guard the flow surfaces (Plans 01/03) |
| T-08-SC | Tampering | npm installs | high | accept | Zero installs this phase (D-12); Package Legitimacy Audit empty per 08-RESEARCH.md |
</threat_model>

<verification>
- `cd web && npx vitest run` green after each task (full suite per D-10; the guard task additionally runs its file filtered for fast feedback).
- `cd web && npx playwright test e2e/destination-settings.spec.ts` green (filtered per D-10).
- Desktop renders byte-identical on every touched file (git diff review: only max-md:-scoped additions).
</verification>

<success_criteria>
- D-06 fixes 1, 2 and 3 are landed exactly as contracted, with the fix-2 honest call recorded.
- The guard carries the D-06 fix-3 scope comment and this plan's phase-8 sweep entries; with Plan 03's Recovery.tsx entry the composed guard rots loudly on regression.
- The touch-target backstop proves >=44px on the fixed controls.
</success_criteria>

<output>
Create .planning/phases/08-guided-restore-real-device-verification/08-04-SUMMARY.md when done
</output>
