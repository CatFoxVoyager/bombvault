---
phase: 07-remaining-destinations-operational-parity
plan: 07
type: execute
wave: 3
depends_on: ["07-05"]
files_modified:
  - web/src/pages/settings/NotifyCard.tsx
  - web/src/pages/Settings.tsx
  - web/src/components/OffsiteTargetsSection.tsx
  - web/e2e/settings-editors.spec.ts
autonomous: true
requirements: [FLOW-02, FLOW-01]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Notification configuration is editable on mobile via the sheet-editor pattern (FLOW-02, D-05/D-07): the NotifyCard form re-hosts inside a fullHeight BottomSheet (title = the existing section key) opened from the stacked card's entry row — the SAME form JSX with the SAME setNotify write chain (api.ts:975), never a forked form and never a second write path"
    - "Off-site replication configuration is editable on mobile through the same pattern: target create/edit/test/delete ride the EXISTING createOffsiteTarget/updateOffsiteTarget/testOffsiteTarget/deleteOffsiteTarget functions, the wizard keys (offsite.wizard.*) render inside a fullHeight sheet, and per-domain target rows in OffsiteTargetsSection get a mobile presentation without new CRUD logic"
    - "Secret-bearing fields in both editors keep the write-only contract on mobile: blank input + the Set badge from the has* flag, blank-on-save keeps the stored value, explicit Clear removes — never prefilled, never echoed (the desktop contract carries over unchanged)"
    - "Every schedule-bearing invoking card across the phase shows its effective-schedule preview from the SHARED derivations (FLOW-01 completion sweep): the Settings schedule-adjacent sections render the same server-derived preview language as the destination cards — one derivation source, no per-card clock math"
    - "Editor sheets scroll in-panel (fullHeight h-dvh) with StickyActionBar apply/cancel where draft state exists; failed saves toast AND shake; destructive target removal rides useConfirm -> ConfirmSheet with the existing offsite.targets.* confirm copy; test-connection actions report through the existing result surface"
    - "Loading/busy states reuse the desktop form busy treatments while a write or test is in flight; backend error text renders verbatim; long values (URLs, remotes, paths) truncate with title / break-all for mono; and desktop >=48rem stays byte-identical — the sheets exist only behind !isDesktop and the desktop inline forms remain the desktop presentation"
  artifacts:
    - path: "web/src/pages/settings/NotifyCard.tsx"
      provides: "the notify form re-hosted as a fullHeight sheet below the breakpoint (same form JSX, setNotify chain, write-only secrets); desktop inline card unchanged"
      contains: "BottomSheet"
    - path: "web/src/pages/Settings.tsx"
      provides: "the stacked notify/offsite sections gain their sheet entry rows + the schedule-preview sweep alignment"
      contains: "isDesktop"
    - path: "web/src/components/OffsiteTargetsSection.tsx"
      provides: "mobile sheet presentation for target CRUD + wizard via the existing CRUD fns; desktop section unchanged"
      contains: "useConfirm"
    - path: "web/e2e/settings-editors.spec.ts"
      provides: "staged mobile e2e: notify sheet save via setNotify, offsite CRUD + wizard flow, write-only secret assertions, desktop dual-direction"
      contains: "display-prefs"
  key_links:
    - from: "web/src/pages/settings/NotifyCard.tsx mobile sheet"
      to: "web/src/lib/api.ts setNotify (:975)"
      via: "the ONE notify write chain — the sheet changes the shell, not the save"
      pattern: "setNotify"
    - from: "web/src/components/OffsiteTargetsSection.tsx mobile sheets"
      to: "web/src/lib/api.ts createOffsiteTarget/updateOffsiteTarget/deleteOffsiteTarget/testOffsiteTarget (:1666-1890)"
      via: "the existing CRUD functions — the mobile presentation adds zero new endpoints or write shapes"
      pattern: "createOffsiteTarget"
    - from: "both editor sheets"
      to: "web/src/components/mobile/BottomSheet.tsx (fullHeight) + StickyActionBar"
      via: "the D-05 editor shape: full screen sheet, pinned apply/cancel, in-panel scroll"
      pattern: "fullHeight"
    - from: "Settings schedule-adjacent sections"
      to: "web/src/lib/api.ts getScheduleNext + the shared preview derivations (07-03/07-04 pattern)"
      via: "the FLOW-01 completion sweep — every invoking card previews from the shared server-derived source"
      pattern: "getScheduleNext"
  prohibitions:
    - statement: "No mobile code path may capture, prefill, or echo a secret: editor sheets render secret fields blank with the Set badge from their has* flags; blank-on-save keeps; only explicit Clear removes; no sheet ever PUTs a captured secret value back"
      status: enforced
      verification: explicit
    - statement: "No second write path for notify or offsite configuration — setNotify and the four offsite CRUD functions are the only writers; the sheets re-host forms, they do not re-implement submission logic; no captured full-object PUT anywhere"
      status: enforced
      verification: explicit
    - statement: "No client-side schedule derivation in the completion sweep — previews come only from the shared /api/schedule/next derivations, and the sweep must not introduce a second derivation helper"
      status: enforced
      verification: explicit

---

<objective>
Close FLOW-02: notifications and off-site replication become fully editable on mobile via the sheet-editor pattern — the NotifyCard form and the offsite target CRUD/wizard re-hosted in fullHeight BottomSheets riding the EXISTING write chains (setNotify, the offsite CRUD functions), with write-only secrets preserved. Then sweep the phase for FLOW-01 completion: every schedule-bearing invoking card previews from the shared derivations.

Purpose: plans 07-05's stacked cards kept notify/offsite reachable through their inline forms; this plan gives them the contracted full-screen editor treatment (D-05/D-07 — no desktop-only settings), the last FLOW requirement the milestone owes.

Output: notify + offsite editor sheets, the FLOW-01/02 completion sweep, settings-editors.spec.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-remaining-destinations-operational-parity/07-CONTEXT.md
@.planning/phases/07-remaining-destinations-operational-parity/07-RESEARCH.md
@.planning/phases/07-remaining-destinations-operational-parity/07-PATTERNS.md
@.planning/phases/07-remaining-destinations-operational-parity/07-UI-SPEC.md
@web/src/pages/settings/NotifyCard.tsx
@web/src/components/OffsiteTargetsSection.tsx
@web/src/pages/Settings.tsx
@web/src/lib/api.ts
</context>

<artifacts_section>
## Artifacts this phase produces

See 07-01-PLAN.md for the phase-wide symbol inventory. This plan creates: the notify editor sheet (NotifyCard re-host below the breakpoint), the offsite target CRUD/wizard sheets (OffsiteTargetsSection mobile presentation), the FLOW-01/02 completion sweep edits, and `web/e2e/settings-editors.spec.ts`. No new shared components (BottomSheet/StickyActionBar/RevealInput compose), no new i18n keys (offsite.wizard.* and all editor strings exist), no api.ts changes.
</artifacts_section>

<flagged_assumptions>
## Flagged assumptions (edge-coverage fallback — never silently dropped)

- **FLOW-02 unclassified edge (orchestrator probe):** the engine classified no edge predicate for this requirement. The planner owns its coverage by derivation from the UI-SPEC Interaction Contract + the E7 note (sheet+form composite; state coverage rides the re-hosted desktop form rows): the edge behaviors are (a) a sheet open while its underlying settings change elsewhere must not write back stale sibling fields — the sheets write ONLY through the single-field semantics of setNotify / the targeted CRUD call (truths 1-2 + prohibition 2; the settings-editors e2e stages a concurrent-change fixture), (b) secret fields render blank with the Set badge even on a subsequent edit of the same target (truth 3; e2e re-opens an edited target and re-asserts the blank field), (c) an in-flight test/save navigating away must not double-fire — the existing fns' busy guards carry over (truth 5). Recorded here as an explicit planner assumption.
</flagged_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: NotifyCard sheet re-host — fullHeight editor on the existing setNotify chain</name>
  <files>web/src/pages/settings/NotifyCard.tsx, web/src/pages/Settings.tsx, web/e2e/settings-editors.spec.ts</files>
  <read_first>
    web/src/pages/settings/NotifyCard.tsx (WHOLE file — the form fields, the setNotify call shape, busy/shake treatments, any secret-bearing field and its desktop handling)
    web/src/lib/api.ts around :975 (setNotify signature + the NotifyConfig shape — which fields are write-only secrets and which has*-flagged reads feed the Set badges)
    .planning/phases/07-remaining-destinations-operational-parity/07-UI-SPEC.md (FLOW-01/02 editor-sheet pattern section; Copywriting Contract secrets + editor-sheet-titles rows; E7 note)
    .planning/phases/07-remaining-destinations-operational-parity/07-PATTERNS.md (Sheet editors section — the BottomSheet+CadenceBuilder skeleton to mirror for a form)
    web/src/pages/Settings.tsx (where NotifyCard mounts in the stacked presentation — the entry-row insertion point; the desktop presentation must stay the inline card)
    web/src/components/mobile/{BottomSheet,StickyActionBar}.tsx (the editor shell props)
    web/e2e/maquette-screens.spec.ts (staging half — this task CREATES settings-editors.spec.ts)
  </read_first>
  <action>
    In NotifyCard.tsx add the internal double gate (the ActivityLog.tsx pattern from 07-06): desktop keeps the inline card byte-identical; below the breakpoint the card renders a summary/entry row opening a fullHeight BottomSheet (title = the existing section key) hosting the SAME form JSX — the identical field components, validation, and busy/shake treatments, composed into the sheet with StickyActionBar apply/cancel where draft state exists. The apply path calls the EXISTING setNotify with the form's values — zero submission logic changes; secrets (if NotifyConfig carries any) keep the write-only contract verbatim: blank input + Set badge from the has* read, blank-on-save keeps, Clear-only removes, never prefilled. Failed saves toast AND shake (existing treatments carried into the sheet).

    In Settings.tsx, wire the stacked notify section's entry row to open the sheet state (lift the open state to where the card renders — keep it component-local if NotifyCard owns it).

    Create web/e2e/settings-editors.spec.ts with the notify scenarios (staged settings GET/notify-POST fixtures): the entry row opens the fullHeight sheet; editing + apply fires setNotify with the edited field (assert the POST body); a secret field renders blank with the Set badge (assert the value attribute is empty); blank-on-save sends no secret value (PUT/POST body assertion); cancel leaves the stored config untouched (re-fetch assertion); failed save toasts and shakes. Windows discipline: manual webServer, no output piping.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/pages/Settings && npx eslint src/pages/settings/NotifyCard.tsx src/pages/Settings.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - NotifyCard renders the inline card on desktop (unchanged, existing Settings dom tests pass) and the entry-row + fullHeight sheet below the breakpoint
    - The sheet hosts the SAME form JSX; apply calls setNotify with no re-implemented submission logic
    - Any secret field is write-only in the sheet (blank + Set badge; blank-keeps asserted by the e2e body assertion)
    - Failed saves toast AND shake inside the sheet; cancel writes nothing
    - tsc --noEmit, eslint, and the Settings dom suite pass
  </acceptance_criteria>
  <done>Notifications are fully editable on mobile in the contracted editor shape, riding the one setNotify chain — the tracer for the offsite work.</done>
</task>

<task type="auto">
  <name>Task 2: Offsite target CRUD + wizard sheets on the existing functions</name>
  <files>web/src/components/OffsiteTargetsSection.tsx, web/src/pages/Settings.tsx, web/e2e/settings-editors.spec.ts</files>
  <read_first>
    web/src/components/OffsiteTargetsSection.tsx (WHOLE file — the desktop per-domain target rows, add/edit entries, test buttons, delete confirms)
    web/src/lib/api.ts lines 1660-1890 (testOffsiteTarget :1679, OffsiteTarget :1825, listOffsiteTargets :1856, createOffsiteTarget :1864, updateOffsiteTarget :1874, deleteOffsiteTarget :1885 — the ONLY writers this plan may call)
    web/src/lib/i18n.ts offsite.* keys (offsite.wizard.* steps, offsite.targets.* confirms, secrets language)
    .planning/phases/07-remaining-destinations-operational-parity/07-UI-SPEC.md (FLOW-01/02 editor-sheet pattern; Copywriting Contract destructive-actions + secrets rows; Color anti-list)
    .planning/phases/07-remaining-destinations-operational-parity/07-PATTERNS.md (Sheet editors; Pattern 6 re-host discipline)
    web/src/components/RevealInput.tsx (write-only secret fields in the target forms)
    web/e2e/settings-editors.spec.ts (extend with the offsite scenarios)
  </read_first>
  <action>
    Give OffsiteTargetsSection an internal double gate (desktop section byte-identical; mobile presentation below the breakpoint): per-domain target rows re-presented as mobile rows (>=44px, name/remote summary + status language), each opening a fullHeight BottomSheet re-hosting the SAME target form the desktop renders — create and edit both ride createOffsiteTarget/updateOffsiteTarget unchanged; the backend picker + wizard steps (offsite.wizard.step1/step2/backends) render inside the fullHeight sheet with in-panel scroll; test connection rides testOffsiteTarget with the existing result surface; delete rides deleteOffsiteTarget behind useConfirm -> ConfirmSheet with the existing offsite.targets.* outcome-naming copy. Secret-bearing target fields (rclone/S3 credentials) keep the write-only contract: blank + Set badge from the has*-flagged reads, blank-on-save keeps, Clear-only removes, never prefilled or echoed. StickyActionBar apply/cancel; failed saves toast AND shake.

    Where Settings.tsx's per-domain offsite cards reference the section's add-entry (the "Ziel hinzufügen" seam at Settings.tsx:3892), keep the same entry working on mobile through the new sheet path — reachability without a second add flow.

    Extend settings-editors.spec.ts with the offsite scenarios (staged target fixtures): open a target, assert the sheet + wizard landmarks; create/update assert the CRUD function's body shape via the staged route (only the form's fields — no captured siblings); a secret field renders blank with the Set badge on FIRST open and STILL blank when the same target is re-opened after an edit (the flagged-assumption re-assertion); delete runs the ConfirmSheet with the outcome-naming copy; test-connection reports through the existing surface.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/pages/Settings && npx eslint src/components/OffsiteTargetsSection.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - OffsiteTargetsSection renders the mobile rows + fullHeight sheets below the breakpoint; the desktop section is unchanged (existing Settings dom tests pass)
    - Create/edit/delete/test call ONLY the four existing CRUD functions — no new submission logic, no new endpoints
    - Secret fields stay write-only across reopen cycles (e2e asserts blank twice); deletes ride useConfirm with outcome-naming copy
    - The wizard steps render in-panel without horizontal overflow; tsc --noEmit and eslint clean
  </acceptance_criteria>
  <done>Off-site replication is fully operable on mobile — CRUD, wizard, test, delete — through the existing functions with write-only secrets machine-asserted across reopen cycles.</done>
</task>

<task type="auto">
  <name>Task 3: FLOW-01/02 completion sweep + settings-editors desktop dual-direction</name>
  <files>web/src/pages/Settings.tsx, web/e2e/settings-editors.spec.ts</files>
  <read_first>
    web/src/pages/Settings.tsx (the schedule-bearing sections: IntegrityCard's ItemScheduleOverride presentation, FlashZipExportCard, any section rendering an effective-schedule line — grep EffectiveScheduleLine/schedule in the file and settings/)
    web/src/pages/VMs.tsx + web/src/pages/Config.tsx (the preview-chip pattern from 07-03/07-04 — the shared derivation usage the sweep aligns to)
    .planning/phases/07-remaining-destinations-operational-parity/07-UI-SPEC.md (FLOW-01/02 editor-sheet section — "the invoking card always shows the effective-schedule preview"; Interaction Contract schedule row)
    .planning/phases/07-remaining-destinations-operational-parity/07-CONTEXT.md (D-06 effective schedule preview)
    web/e2e/desktop-untouched.spec.ts (the dual-direction template)
    web/e2e/settings-editors.spec.ts (extend with the sweep + dual-direction scenarios)
  </read_first>
  <action>
    The completion sweep, in two passes: (1) FLOW-02 closure audit — walk every Settings section reachable from TAB_ORDER and confirm each is operable on mobile after the sheet work: any section that still renders a desktop-only control gets its entry row/sheet in this task (expected: none remain after Tasks 1-2; if one surfaces, fix it here and record it); write the per-section audit outcome (section -> operable / fixed-in-task) into this plan's SUMMARY.md — that record is the evidence trail the FLOW-02 no-desktop-only-setting truth stands on, since the interim state after 07-05 deliberately leaves notify/offsite as inline forms; (2) FLOW-01 completion — every schedule-bearing invoking card/section across the phase (Settings' schedule-adjacent sections included) renders its effective-schedule preview from the shared server-derived source: verify each uses the shared derivations (getScheduleNext / the existing EffectiveSchedule component for file-set effectiveSchedule) and align any section that renders its own clock math; no second derivation helper may exist after the sweep.

    Extend settings-editors.spec.ts: (a) the sweep scenario — on mobile, open each schedule-bearing Settings section and assert a preview is rendered from the staged schedule fixture (and absent when the fixture has no entry); (b) desktop dual-direction (/settings): the desktop inline forms render (NotifyCard inline, OffsiteTargetsSection desktop rows) and NO fullHeight sheets exist in the desktop DOM. Then run the full spec as this plan's closing verification. Windows discipline: manual webServer, no output piping, orphan cleanup on wedge.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/pages/Settings && npx playwright test e2e/settings-editors.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - The FLOW-02 audit records every TAB_ORDER section as mobile-operable (or fixes the straggler in-task); no desktop-only setting remains; the per-section audit table is written into 07-07-SUMMARY.md as the FLOW-02 evidence trail
    - Every schedule-bearing invoking surface renders previews from the shared derivations; no second derivation helper exists after the sweep
    - The sweep + dual-direction e2e scenarios pass; desktop renders the inline forms with no sheets
    - The full settings-editors spec passes end-to-end
  </acceptance_criteria>
  <done>FLOW-01 and FLOW-02 are closed: operational configuration is fully editable on mobile in the sheet-editor pattern, previews are server-derived everywhere, and the e2e proves both directions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GET notify/offsite reads -> sheet forms | Secrets arrive as "" + has* flags; a sheet that echoed a secret would leak it to anyone viewing the screen |
| sheet submits -> notify/offsite write endpoints | Editor submits cross into domain writes; a stale-capture sheet could overwrite concurrent edits or resurrect removed values |
| delete/test actions | Target removal is destructive; test fires a real connection attempt |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-07-24 | Information disclosure (secret echo) | notify + offsite secret fields in sheets | critical | mitigate | Write-only contract carried into both sheets (blank + Set badge from has* flags, blank-keeps, Clear-removes); e2e asserts the value attribute is empty on first open AND on reopen after edit (acceptance criteria Tasks 1-2) |
| T-07-25 | Tampering (stale capture overwrite) | sheet draft state | high | mitigate | Sheets submit only through setNotify / the targeted CRUD calls with single-field semantics — never a captured full-object write (prohibition 2); the e2e concurrent-change scenario stages a mid-edit server change and asserts the submit body carries only the edited field |
| T-07-26 | Tampering (destructive misuse) | target delete | medium | mitigate | deleteOffsiteTarget rides useConfirm -> ConfirmSheet with the existing outcome-naming offsite.targets.* copy; no default-focused destructive control |
| T-07-27 | Tampering (desktop regression) | NotifyCard/OffsiteTargetsSection gates | medium | mitigate | Both gates are internal; desktop forms stay the desktop presentation (existing Settings dom tests pass unchanged); Task 3's dual-direction asserts no sheets at >=48rem |
| T-07-SC | Tampering (supply chain) | npm installs | low | accept | Zero package installs this phase (locked decision; Package Legitimacy Audit table: none) |
</threat_model>

<verification>
- `cd web && npx vitest run src/pages/Settings` green (desktop identity across the Settings suite)
- `cd web && npx playwright test e2e/settings-editors.spec.ts` green (manual webServer; never pipe output)
- `cd web && npx tsc --noEmit -p tsconfig.json` green; eslint clean on all touched files
- Source checks: no new write functions beyond setNotify + the four CRUD fns; no second derivation helper; secret fields ride RevealInput/has*-flag semantics
</verification>

<success_criteria>
- Notifications and off-site replication are fully editable on mobile via fullHeight sheet editors riding the existing chains (FLOW-02, D-05/D-07)
- Write-only secrets survive first open, save, and reopen cycles, machine-asserted in e2e
- Every schedule-bearing invoking surface previews from the shared server-derived source (FLOW-01 complete across the phase)
- Desktop >=48rem keeps the inline forms byte-identical, proven in both directions
</success_criteria>

<output>
Create `.planning/phases/07-remaining-destinations-operational-parity/07-07-SUMMARY.md` when done
</output>
