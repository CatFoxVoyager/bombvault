---
phase: 07-remaining-destinations-operational-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/platform.ts
  - web/src/lib/platform.test.ts
  - web/src/index.css
  - web/src/main.tsx
  - web/src/components/mobile/Fab.tsx
  - web/src/components/mobile/Fab.dom.test.tsx
  - web/src/app/mobileShellSource.test.ts
  - web/e2e/platform-chrome.spec.ts
autonomous: true
requirements: [PLAT-01]
user_setup: []

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "web/src/lib/platform.ts resolves a closed 'material' | 'cupertino' union (shape.ts model verbatim: closed union array, isPlatform coercion fn, PLATFORM_STORAGE_KEY \"bv-platform\", DEFAULT_PLATFORM \"material\") and applyPlatform() is the ONE application choke point — it alone writes document.documentElement.setAttribute(\"data-platform\", s) with the stored value coerced against the union (never trusted raw), exactly as applyShape does at shape.ts:51-53"
    - "Default is 'material' with a persisted user override and NO OS detection: navigator.userAgent and navigator.userAgentData appear NOWHERE in src/ (grep-clean slate stays clean); the choice persists to the bv-platform localStorage key and re-applies on boot AND on the display-prefs ADOPTED_EVENT listener in main.tsx (same call-order discipline as applyStoredShape/applyStoredMotionIntensity at main.tsx:17-36)"
    - "index.css gains [data-platform] blocks redefining ONLY custom properties in ONE place with the bible citation inline (the :root[data-shape] block at index.css:592-613 precedent): material gets --mob-fab-radius: 18px; cupertino gets --mob-title-size: 2rem, --mob-title-weight: 700, --mob-check-radius: 999px; --mob-chip-radius: 999px is defined for BOTH (chips are pills in both languages). The --mob-* properties are consumed ONLY by mobile-block classes/components below md — desktop styles keep their literal tokens, so >=48rem renders byte-identically"
    - "Fab (web/src/components/mobile/Fab.tsx) is the material-chrome primary action: 52px height, radius from var(--mob-fab-radius), bg-accent fill with text-accentContrast ink (accent reservation 9 — ONE per surface, the surface's primary action only), label/icon passed via props from the consuming surface's EXISTING CTA key; it renders null under cupertino (the sanctioned STRUCTURAL usePlatform() switch — the cupertino equivalent is the surface's existing filled pinned button, never both)"
    - "The FAB is in-flow or sticky-in-flow inside the page column — NEVER position:fixed or position:absolute against the viewport; the component carries the mandatory why-comment documenting the recorded deviation from the design bible's .fab { position:absolute; right:18px; bottom:88px } (maquette artboard positioning rejected: the app is a scrolling document under the phase 5 no-fixed contract; only the first three visual properties transfer)"
    - "Flipping bv-platform to cupertino and re-applying re-skins the chrome (title/check/chip custom properties) with NO page remount and NO page-component branching beyond the one sanctioned Fab switch — attribute-CSS only (Pitfall 5: no usePlatform() conditionals inside page blocks)"
  artifacts:
    - path: "web/src/lib/platform.ts"
      provides: "Platform union ('material'|'cupertino'), PLATFORMS, PLATFORM_STORAGE_KEY, DEFAULT_PLATFORM, isPlatform coercion, applyPlatform (single choke point), applyStoredPlatform, usePlatform (structural switches only)"
      contains: "data-platform"
    - path: "web/src/lib/platform.test.ts"
      provides: "resolve/coerce invalid stored values to default; attribute set; persistence round-trip; usePlatform structural read"
      contains: "applyPlatform"
    - path: "web/src/index.css"
      provides: "[data-platform] custom-property variant blocks (--mob-fab-radius, --mob-title-size, --mob-title-weight, --mob-check-radius, --mob-chip-radius) with the design-bible citation in one block"
      contains: "--mob-fab-radius"
    - path: "web/src/main.tsx"
      provides: "applyStoredPlatform() at boot (after applyStoredLabelModes) and inside the ADOPTED_EVENT re-apply listener"
      contains: "applyStoredPlatform"
    - path: "web/src/components/mobile/Fab.tsx"
      provides: "material-only primary-action FAB: 52px, var(--mob-fab-radius), accent fill + accentContrast ink, in-flow/sticky-in-flow with the recorded deviation comment"
      contains: "--mob-fab-radius"
    - path: "web/src/app/mobileShellSource.test.ts"
      provides: "UA-identifier needles: navigator.userAgent / userAgentData absent from src/ (extended in this plan, not forked)"
      contains: "userAgentData"
    - path: "web/e2e/platform-chrome.spec.ts"
      provides: "mobile-project e2e: data-platform present + persisted + coerced on <html>; custom properties resolve under both attribute values; desktop project unaffected"
      contains: "data-platform"
  key_links:
    - from: "web/src/main.tsx boot sequence"
      to: "web/src/lib/platform.ts applyStoredPlatform"
      via: "the shape/motion boot discipline verbatim — applied before first render and re-applied on ADOPTED_EVENT, registered BEFORE the display-prefs sync that can fire it"
      pattern: "applyStoredPlatform"
    - from: "web/src/components/mobile/Fab.tsx"
      to: "web/src/lib/platform.ts usePlatform"
      via: "the ONE sanctioned structural switch (material renders the FAB, cupertino renders null) — every other platform difference is attribute-CSS"
      pattern: "usePlatform"
    - from: "web/src/index.css [data-platform] blocks"
      to: "mobile-block consumers (plans 07-03..07-07)"
      via: "custom properties ONLY; consumers use var(--mob-*) through existing token-style utilities; desktop never reads a --mob-* property"
      pattern: "--mob-"
  prohibitions:
    - statement: "No navigator.userAgent or navigator.userAgentData read anywhere in src/ — a web page cannot honestly distinguish Android from iOS without UA sniffing, which D-12 bans; platform is the closed union + persisted preference only"
      status: enforced
      verification: explicit
    - statement: "No position:fixed and no viewport-anchored position:absolute in Fab — the design bible's .fab positioning is maquette-artboard coordinates, not app spec (Pitfall 4)"
      status: enforced
      verification: explicit
    - statement: "No new colors and no new npm dependencies — the platform layer reshapes chrome (radii, sizes, weights), it never recolors; accent stays on the reservations list"
      status: enforced
      verification: backstop
    - statement: "No new i18n keys in this plan — i18n.ts + locales/ are single-owner in Wave 1 (07-02 owns the pre-seed sweep); the FAB's label and aria-label arrive via props from the consuming surface's existing CTA key, so the parity sweep never has two writers"
      status: enforced
      verification: backstop

---

<objective>
Build the PLAT-01 platform-adaptive chrome layer: a `data-platform` root attribute (`material` | `cupertino`) resolved/applied/persisted by a new `web/src/lib/platform.ts` (the shape.ts discipline verbatim), the `[data-platform]` custom-property variant blocks in index.css, and the `Fab` component — the M3 primary action the phase 6 Home trigger deliberately deferred (CONTEXT.md D-12). Desktop above 48rem is untouched: the `--mob-*` properties are consumed only by mobile-block code.

Purpose: every later plan in this phase composes its mobile blocks on this layer — the FAB on destination pages, tonal chips, the cupertino title/check treatments. It must exist first (Wave 1) so the page plans consume one mechanism instead of growing per-component branching.

Output: platform.ts + tests, index.css variants, main.tsx wiring, Fab + dom tests, UA-identifier guard needles, platform-chrome e2e.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-remaining-destinations-operational-parity/07-CONTEXT.md
@.planning/phases/07-remaining-destinations-operational-parity/07-RESEARCH.md
@.planning/phases/07-remaining-destinations-operational-parity/07-PATTERNS.md
@.planning/phases/07-remaining-destinations-operational-parity/07-UI-SPEC.md
@web/src/lib/shape.ts
@web/src/lib/theme.ts
@web/src/main.tsx
@web/src/index.css
@web/src/app/mobileShellSource.test.ts
</context>

<artifacts_section>
## Artifacts this phase produces

Phase-wide symbol inventory (for the plan-review-convergence source-grounding pass — these symbols are NEW in phase 7 and must never be flagged as drift):

- `web/src/lib/platform.ts`: `Platform` type (`"material" | "cupertino"`), `PLATFORMS`, `PLATFORM_STORAGE_KEY` (`"bv-platform"`), `DEFAULT_PLATFORM` (`"material"`), `isPlatform()`, `applyPlatform()`, `applyStoredPlatform()`, `usePlatform()`
- `web/src/lib/platform.test.ts`, `web/src/components/mobile/Fab.tsx` (`Fab`), `web/src/components/mobile/Fab.dom.test.tsx`
- `web/src/lib/useLoadMore.ts` (`loadMoreWindow()` pure fn, `useLoadMore()` hook), `web/src/lib/useLoadMore.test.ts`
- `web/src/components/mobile/ListToolbar.tsx` (`ListToolbar`), `web/src/components/mobile/MobileSectionLabel.tsx` (`MobileSectionLabel`, promoted from Dashboard.tsx)
- `web/src/index.css` `[data-platform]` blocks + `--mob-fab-radius`, `--mob-title-size`, `--mob-title-weight`, `--mob-check-radius`, `--mob-chip-radius`
- `web/src/main.tsx` `applyStoredPlatform()` wiring
- i18n keys: `common.loadMore`, `common.search`, `settings.tabsNavigation`, `config.restoreChain.title`, `config.restoreChain.step1`..`step5`; `home.newBackupConfirm` copy alignment
- Mobile blocks in `VMs.tsx`, `Flash.tsx`, `Config.tsx`, `Receiver.tsx`, `Fleet.tsx`, `Settings.tsx`
- e2e specs: `platform-chrome.spec.ts`, `destination-vms-flash.spec.ts`, `destination-config-receiver-fleet.spec.ts`, `destination-settings.spec.ts`, `list-ergonomics.spec.ts`, `settings-editors.spec.ts`
- Guard/battery extensions: `mobileShellSource.test.ts` (UA needles + phase-7 spacing/weight sweep), `desktop-untouched.spec.ts` (six-route leakage needles), `narrow-viewport.spec.ts` (phase-7 sweeps)

This plan creates: platform.ts (+test), index.css `[data-platform]` blocks, main.tsx wiring, Fab (+dom test), mobileShellSource UA needles, platform-chrome.spec.ts.
</artifacts_section>

<flagged_assumptions>
## Flagged assumptions (edge-coverage fallback — never silently dropped)

- **PLAT-01 unclassified edge (orchestrator probe):** the engine classified no edge predicate for this requirement. The planner owns its coverage by derivation from the UI-SPEC Platform Chrome Contract: the edge behaviors are (a) an invalid/absent stored value must coerce to `material` (authored as truth 1 + platform.test.ts case), (b) the attribute flip must not remount pages (authored as truth 6), (c) desktop must be unaffected by the attribute's presence (authored as truth 3 + e2e desktop assertion). Recorded here as an explicit planner assumption: these three derived predicates are the phase's PLAT-01 edge coverage; if the real-device pass (Phase 8) invalidates the material default, the fix is the one-line DEFAULT_PLATFORM constant.
</flagged_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: data-platform end-to-end — resolve, apply, persist, CSS variant, boot wiring</name>
  <files>web/src/lib/platform.ts, web/src/lib/platform.test.ts, web/src/index.css, web/src/main.tsx, web/src/app/mobileShellSource.test.ts, web/e2e/platform-chrome.spec.ts</files>
  <read_first>
    web/src/lib/shape.ts (WHOLE file — the exact model to clone: closed union, STORAGE_KEY, DEFAULT, isPlatform coercion, applyShape choke point at :51-53, applyStoredShape, the armShapeTransitions pattern if the hook needs a subscriber shape)
    web/src/lib/theme.ts lines 50-70 (paint() single-choke-point discipline — the why-comment style to echo)
    web/src/main.tsx lines 1-40 (the boot call order and the ADOPTED_EVENT listener block — applyStoredPlatform slots after applyStoredLabelModes() in BOTH places)
    web/src/index.css lines 515-615 ([data-motion] and :root[data-shape] blocks — the custom-property variant precedent, values + citation in one block)
    web/src/lib/useMediaQuery.ts (the second, independent media axis — DESKTOP_QUERY stays the only width literal; platform is an ATTRIBUTE, never a media query)
    web/src/app/mobileShellSource.test.ts (the node-env readFileSync source-assert suite this plan extends with UA needles — extend, never fork)
    .planning/phases/07-remaining-destinations-operational-parity/07-UI-SPEC.md Platform Chrome Contract section (the mechanism contract + the recorded platform-selection decision)
  </read_first>
  <action>
    Create web/src/lib/platform.ts cloning shape.ts structure exactly: `export type Platform = "material" | "cupertino"`; `export const PLATFORMS: Platform[]`; `const PLATFORM_STORAGE_KEY = "bv-platform"` (house bv-* key convention); `const DEFAULT_PLATFORM: Platform = "material"`; `isPlatform(v: unknown): v is Platform` coercion; `applyPlatform(platform)` as the ONE choke point that coerces then `document.documentElement.setAttribute("data-platform", s)`; `applyStoredPlatform()` reading localStorage through the coercion; `usePlatform()` for the structural switches (clone whatever reader hook shape.ts/motion.ts expose — if none exists, useSyncExternalStore on the attribute, model: useMediaQuery's subscribe shape). Carry the why-comment: default material with persisted override, no OS detection (UI-SPEC recorded decision — a web page cannot honestly distinguish Android from iOS without banned UA sniffing; Phase 8's real-device pass is the venue to revisit, an explicit Settings control rides a later polish pass).

    index.css: add the `[data-platform]` variant block(s) adjacent to the data-shape block, custom properties ONLY, one block, bible citation inline: material `--mob-fab-radius: 18px` (android.html .fab border-radius); cupertino `--mob-title-size: 2rem; --mob-title-weight: 700` (ios.html .navbar h1 32px/700 letter-spacing -0.5px — the ONE sanctioned 700, living only here) and `--mob-check-radius: 999px`; `--mob-chip-radius: 999px` for BOTH (chips are pills in both languages). Add the consumption-contract comment: --mob-* properties are read ONLY by mobile-block code below md; desktop styles keep literal tokens (this is what keeps >=48rem byte-identical). Do NOT wire any consumer yet — later plans consume.

    main.tsx: add `applyStoredPlatform()` after `applyStoredLabelModes()` in the boot block AND inside the ADOPTED_EVENT listener callback (same position in both — the #191 call-order comment covers it).

    mobileShellSource.test.ts: extend (never fork) with a describe asserting `navigator.userAgent` and `userAgentData` appear in NO file under web/src (readFileSync walk or the suite's existing file-list mechanism — model: the existing banned-viewport-literal FILE-WIDE bans). Include the self-guard test style the suite already uses (assert the guard finds files at all).

    platform.test.ts: coercion of invalid/absent stored values to material; applyPlatform sets the attribute; persistence round-trip (set key, applyStoredPlatform, attribute matches); default when key missing. e2e/platform-chrome.spec.ts (mobile projects): goto /dashboard, assert `html[data-platform]` exists and equals "material" on a fresh profile; seed localStorage bv-platform="cupertino" before goto, assert attribute flips and `getComputedStyle(document.documentElement).getPropertyValue("--mob-title-size")` resolves non-empty; desktop-1280 project: one assertion that the desktop shell is unchanged with the attribute present (the Sidebar renders, BottomNav absent — the desktop-untouched pattern).
  </action>
  <verify>
    <automated>cd web && npx vitest run src/lib/platform.test.ts src/app/mobileShellSource.test.ts && npx playwright test e2e/platform-chrome.spec.ts --project=mobile-android --project=desktop-1280</automated>
  </verify>
  <acceptance_criteria>
    - platform.ts exists with the closed union, "bv-platform" storage key, "material" default, isPlatform coercion, and applyPlatform as the only setAttribute("data-platform", ...) call site in src/
    - platform.test.ts passes: invalid stored value coerces to material; attribute set; round-trip persistence
    - main.tsx calls applyStoredPlatform() exactly twice (boot block + ADOPTED_EVENT listener), both after applyStoredLabelModes()
    - index.css contains the [data-platform] blocks defining --mob-fab-radius, --mob-title-size, --mob-title-weight, --mob-check-radius, --mob-chip-radius with a design-bible citation comment; no component file references --mob-* yet
    - mobileShellSource.test.ts UA-needle describe passes with zero navigator.userAgent/userAgentData occurrences in web/src
    - platform-chrome.spec.ts passes on mobile-android (attribute + flip + custom property) and desktop-1280 (shell unchanged)
  </acceptance_criteria>
  <done>The platform attribute layer works end-to-end: persisted, coerced, applied at boot and on display-prefs adoption, translated to CSS custom properties, guarded against UA sniffing, with desktop verified unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Fab — the material primary action with the recorded placement deviation</name>
  <files>web/src/components/mobile/Fab.tsx, web/src/components/mobile/Fab.dom.test.tsx</files>
  <read_first>
    web/src/components/mobile/StickyActionBar.tsx lines 1-24 (the in-flow/sticky-in-flow discipline and its why-comment — the exact comment shape the Fab's deviation comment must echo)
    web/src/components/Button.tsx (TONE_TABLE closed-prop discipline — Fab is props-closed the same way: label, icon, onClick, an optional aria-label passthrough; no style-override escape hatches)
    web/src/lib/platform.ts (from Task 1 — usePlatform() is the structural gate)
    web/src/index.css (accent tokens: --accent fill, --accentContrast ink — accent reservation 9 in the UI-SPEC Color section)
    .planning/phases/07-remaining-destinations-operational-parity/07-UI-SPEC.md (Platform Chrome Contract: FAB row + FAB surface resolution — which surfaces get the FAB)
  </read_first>
  <action>
    Create Fab in web/src/components/mobile/Fab.tsx: a props-closed button (label: string, icon: ReactNode, onClick, aria-label?: string) rendering ONLY when usePlatform() resolves "material" (return null otherwise — the sanctioned structural switch; the cupertino equivalent is the consuming surface's existing filled pinned button, never both). Visuals: height 52px, border-radius var(--mob-fab-radius), bg-accent with text-accentContrast ink, icon + label row, min 44px touch target satisfied by the 52px height. Placement contract: the component renders as an in-flow block or sticky-in-flow element inside the page column — it must NOT set position:fixed and must NOT position:absolute against the viewport; the mandatory why-comment documents the recorded deviation (design bible android.html .fab is position:absolute right:18px bottom:88px against a fixed-height artboard; the app is a scrolling document under the phase 5 no-fixed contract, so only the first three visual properties transfer — CONTEXT.md D-12 sanctions exactly this recorded rationale). Surface resolution (UI-SPEC, auto-mode decision): the FAB hosts the primary trigger on the NEW destination surfaces (plans 07-03/07-05 consume it); Home keeps its phase 6 sticky block with its own recorded-why; Config/Receiver/Fleet primary actions are sheet-opening rows, not FABs.

    Fab.dom.test.tsx (jsdom — platform is attribute-based, so dom tests work here unlike width-gated surfaces): renders label + icon and fires onClick under material; renders null after applyPlatform("cupertino"); asserts the className contains no fixed/absolute positioning utility (source-level string assert on the component is acceptable per the suite's source-assert style — pick one mechanism and say which in the test comment).
  </action>
  <verify>
    <automated>cd web && npx vitest run src/components/mobile/Fab.dom.test.tsx src/lib/platform.test.ts && npx eslint src/components/mobile/Fab.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Fab.tsx exists, uses usePlatform() as its only platform read, and returns null under cupertino
    - Fab carries the deviation why-comment citing the maquette artboard vs scrolling-document rationale (D-12)
    - Fab height is 52px with radius var(--mob-fab-radius), bg-accent + text-accentContrast; no position:fixed/absolute utility in its className
    - Dom tests pass: renders under material, null under cupertino, onClick fires, label renders
  </acceptance_criteria>
  <done>The M3 primary action exists as a props-closed, platform-gated, in-flow component with its placement deviation recorded — ready for the destination-page plans to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage -> html attribute | The stored bv-platform value crosses into the DOM as a root attribute; an unvalidated string would be an injection surface for attribute/CSS state |
| display-prefs adoption -> re-apply | The server-driven ADOPTED_EVENT re-runs the appliers; a hostile prefs payload must not smuggle a platform value outside the union |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering (attribute injection) | platform.ts stored-value read | medium | mitigate | isPlatform coercion against the closed union before any setAttribute — invalid stored values fall back to DEFAULT_PLATFORM (platform.test.ts case); mirrors applyShape's coercion exactly |
| T-07-02 | Tampering (desktop regression via attribute) | index.css [data-platform] blocks | medium | mitigate | The blocks redefine ONLY --mob-* custom properties, consumed exclusively by mobile-block code below md; desktop styles keep literal tokens; platform-chrome.spec.ts desktop-1280 asserts the shell unchanged |
| T-07-03 | Information disclosure | Fab / platform layer | low | accept | The layer carries no user data — chrome constants and a two-value preference; nothing to disclose |
| T-07-SC | Tampering (supply chain) | npm installs | low | accept | Zero package installs this phase (locked decision; Package Legitimacy Audit table: none) |
</threat_model>

<verification>
- `cd web && npx vitest run src/lib/platform.test.ts src/components/mobile/Fab.dom.test.tsx src/app/mobileShellSource.test.ts` green
- `cd web && npx playwright test e2e/platform-chrome.spec.ts --project=mobile-android --project=desktop-1280` green (manual webServer per the Windows discipline; never buffer output through a pipe)
- `cd web && npx eslint src/lib/platform.ts src/components/mobile/Fab.tsx` clean
- `cd web && npm run build` green (web/dist refresh happens at the phase gate plan 07-08)
</verification>

<success_criteria>
- data-platform resolves material|cupertino from a coerced persisted preference, applies at boot and on adoption, and drives CSS custom properties only
- The FAB exists as the material primary action with the deviation recorded; cupertino renders the null branch
- Desktop >=48rem renders byte-identically with the attribute present
- Zero UA sniffing, zero new dependencies, zero new i18n keys
</success_criteria>

<output>
Create `.planning/phases/07-remaining-destinations-operational-parity/07-01-SUMMARY.md` when done
</output>
