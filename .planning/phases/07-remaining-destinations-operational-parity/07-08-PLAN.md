---
phase: 07-remaining-destinations-operational-parity
plan: 08
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-03", "07-04", "07-05", "07-06", "07-07"]
files_modified:
  - web/src/components/mobile/RunDetailSheet.tsx
  - web/src/components/SelectionTree.tsx
  - web/src/app/mobileShellSource.test.ts
  - web/e2e/desktop-untouched.spec.ts
  - web/e2e/narrow-viewport.spec.ts
autonomous: true
requirements: [MORE-01, MORE-02, FLOW-01, FLOW-02, LISTS-01, PLAT-01]
user_setup: []

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Playwright bounding-box assertions hold across the phase surfaces: FAB 52px, list rows >=44px, filter/tonal chips >=44px, sheet close control >=44px, Load-more control >=44px (backstop verification — the geometry battery in narrow-viewport/list specs)"
      verification: backstop
    - "desktop-untouched.spec.ts battery is extended to the six phase routes (/vms /flash /config /receiver /fleet /settings): desktop markers present and mobile chrome absent at >=48rem on desktop-1280 and desktop-768, with the inverse asserted per route on the mobile projects — in both directions (backstop verification)"
      verification: backstop
    - "The carried 06-UI-REVIEW fixes are landed: RunDetailSheet's stat triad (mobile/RunDetailSheet.tsx:500,505,512) renders text-heading font-semibold tabular-nums, every font-medium in the phase 6 files is gone (weights 400/600 only), and SelectionTree's chevron aria-label takes the {path} expand/collapse form (SelectionTree.tsx:638)"
    - "The phase-7 spacing/weight guard is live in mobileShellSource.test.ts: the sweep asserts absence of the 12px stops (gap-3|px-3|py-3|pt-3|pb-3|pl-3|pr-3|m[trblxy]?-3|p-3), the .5 stops (gap-1\\.5|gap-2\\.5|py-2\\.5|px-2\\.5), and font-medium across the explicit phase-7 file list — file-wide on wholly-phase-7 files, marker-delimited (with an anti-shrink existence assert on both markers) on the six pages and retrofitted components whose desktop halves legitimately carry legacy values; a future exception means editing the guard with a written why, never a silent new value"
    - "The de/fr narrow-viewport sweeps (320-360px) pass over the phase surfaces: no horizontal page scroll, no clipped controls, single-line chips/bars (the Files header fix and the Settings chip strip are the named regressions), and every phase-7 string renders in both languages"
    - "The phase closes green end-to-end: the full vitest suite (incl. i18n parity + orphans), the four-project Playwright run, `cd web && npm run build` (tsc --noEmit + vite build) with web/dist committed, and the Go chain (`just check`) all pass — the SPA embed is fresh, not stale"
  artifacts:
    - path: "web/src/components/mobile/RunDetailSheet.tsx"
      provides: "the carried stat-triad fix (text-heading font-semibold tabular-nums) + the file's font-medium purge"
      contains: "text-heading"
    - path: "web/src/components/SelectionTree.tsx"
      provides: "the carried chevron aria-label fix ({path} expand/collapse form)"
      contains: "expand/collapse"
    - path: "web/src/app/mobileShellSource.test.ts"
      provides: "the phase-7 spacing/weight sweep (explicit file list, marker-delimited regions for mixed files, anti-shrink marker asserts)"
      contains: "font-medium"
    - path: "web/e2e/desktop-untouched.spec.ts"
      provides: "the six-route battery extension with dual-direction mobile-chrome-absence needles"
      contains: "max-md"
    - path: "web/e2e/narrow-viewport.spec.ts"
      provides: "the de/fr 320-360px sweeps extended to the phase surfaces (Files header, Settings strip, destination cards)"
      contains: "narrow"
    - path: "web/dist"
      provides: "the fresh SPA build committed at phase close (the binary embeds it)"
      contains: "index.html"
  key_links:
    - from: "web/src/app/mobileShellSource.test.ts sweep"
      to: "every plan's touched files (07-01..07-07 files_modified)"
      via: "the explicit phase-7 file list — the discipline lock that keeps the 8/16 + 400/600 rules machine-enforced after the phase ships"
      pattern: "mobileShellSource"
    - from: "web/e2e/desktop-untouched.spec.ts"
      to: "the six destination pages"
      via: "dual-direction leakage needles — the D-01 double gate proven from both sides at >=48rem"
      pattern: "desktop-untouched"
    - from: "web/e2e/narrow-viewport.spec.ts"
      to: "the phase surfaces + the carried Files fix"
      via: "de/fr 320-360px — the overflow/long-text considerations proven at real narrow widths"
      pattern: "narrow-viewport"
    - from: "web/dist commit"
      to: "web/embed.go (all:dist)"
      via: "the binary embeds the built SPA — a stale dist would ship a stale interface"
      pattern: "web/dist"
  prohibitions:
    - statement: "No silent guard edits: the spacing/weight sweep's file list and markers change only with a written why-comment in the guard itself — never deleting a needle to make a failing file pass"
      status: enforced
      verification: explicit
    - statement: "No new user-visible strings in this plan — the carried fixes and guards are copy-neutral; any string discovered missing surfaces as a follow-up, never a key authored outside the 42-table binding rule"
      status: enforced
      verification: explicit

---

<objective>
Close the phase: land the carried 06-UI-REVIEW fixes the plans deferred to the close (stat-triad typography, font-medium purge, chevron aria-label), convert the spacing/weight discipline into the failing source-assert guard over every phase-7 file, extend the desktop-untouched battery to the six routes in both directions, run the de/fr narrow sweeps, and take the full green gate (vitest + i18n parity + four-project Playwright + web build with dist committed + `just check`).

Purpose: everything the earlier plans composed must hold together at the phase boundary — the guards make the discipline permanent, the battery makes desktop identity provable, and the gate is what `/gsd-verify-phase` will stand on.

Output: carried fixes, extended guards + battery + narrow sweeps, fresh web/dist, all suites green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-remaining-destinations-operational-parity/07-CONTEXT.md
@.planning/phases/07-remaining-destinations-operational-parity/07-RESEARCH.md
@.planning/phases/07-remaining-destinations-operational-parity/07-PATTERNS.md
@.planning/phases/07-remaining-destinations-operational-parity/07-UI-SPEC.md
@web/src/app/mobileShellSource.test.ts
@web/e2e/desktop-untouched.spec.ts
@web/e2e/narrow-viewport.spec.ts
@web/src/components/mobile/RunDetailSheet.tsx
</context>

<artifacts_section>
## Artifacts this phase produces

See 07-01-PLAN.md for the phase-wide symbol inventory. This plan creates: no new symbols — it lands the carried fixes (RunDetailSheet stat triad, SelectionTree chevron label), extends the existing guards (`mobileShellSource.test.ts` sweep, `desktop-untouched.spec.ts` battery, `narrow-viewport.spec.ts` sweeps), and commits the fresh `web/dist` build. Every file it touches already exists.
</artifacts_section>

<tasks>

<task type="auto">
  <name>Task 1: Carried fixes — stat triad, font-medium purge, chevron aria-label</name>
  <files>web/src/components/mobile/RunDetailSheet.tsx, web/src/components/SelectionTree.tsx</files>
  <read_first>
    <!-- planner-discipline-allow: font-medium -->
    web/src/components/mobile/RunDetailSheet.tsx lines 495-520 (the stat triad at :500/:505/:512 — text-sm font-medium tabular-nums to become text-heading font-semibold tabular-nums) and line 285 (the row-control font-medium to move to 400/600) and any other font-medium in the file (grep first)
    web/src/components/SelectionTree.tsx around :638 (the chevron aria-label — rework to the {path} expand/collapse form; note the file's touch-variant conventions from phase 6)
    .planning/phases/07-remaining-destinations-operational-parity/07-UI-SPEC.md (Carried Phase 6 Review Fixes table — fixes 1 and 3b contracts)
    web/src/components/mobile/RunDetailSheet.dom.test.tsx (the existing assertions the typography change must keep green — extend where the triad is asserted)
  </read_first>
  <action>
    Fix 1: in RunDetailSheet.tsx change the three stat tiles at :500/:505/:512 from text-sm font-medium tabular-nums to text-heading font-semibold tabular-nums (the typography contract: heading role for stat values, weight 600, tabular numerals). Then purge the rest of the file's font-medium uses (e.g. :285) to weight 400 or 600 per their role — controls/labels 400, emphasis/stat 600 — and grep the OTHER phase 6 files for font-medium (the phase 6 component set: mobile/ BottomSheet/StickyActionBar/TapPopover/InfoBubble/SelectionTree, run-detail consumers) moving each to 400/600 the same way; record every file touched in the task summary. Extend RunDetailSheet.dom.test.tsx where the stat triad is asserted so the new class signature is pinned.

    Fix 3b: in SelectionTree.tsx (:638) rework the chevron toggle's aria-label to the {path} expand/collapse form — the accessible name carries the row's path plus the expand/collapse action words (compose from the existing label pieces the row already has; keep the bv-convention-exception marker for the 44px tap target intact). Keep both changes copy-neutral in every other respect.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/components/mobile/RunDetailSheet.dom.test.tsx && npx eslint src/components/mobile/RunDetailSheet.tsx src/components/SelectionTree.tsx && npx tsc --noEmit -p tsconfig.json && ! grep -q "font-medium" src/components/mobile/RunDetailSheet.tsx src/components/SelectionTree.tsx</automated>
  </verify>
  <acceptance_criteria>
    - RunDetailSheet.tsx:500/:505/:512 render text-heading font-semibold tabular-nums; the file contains zero font-medium occurrences
    - The phase 6 file sweep's touched files contain zero font-medium occurrences (each listed in the task summary)
    - SelectionTree.tsx:638's aria-label composes the path with the expand/collapse action words
    - RunDetailSheet.dom.test.tsx passes with the pinned new classes; tsc --noEmit and eslint clean
  </acceptance_criteria>
  <done>The two carried review findings are fixed at their sources and pinned by tests — the weight rule now has no phase 6 stragglers.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-7 guard sweep + desktop battery extension to the six routes</name>
  <files>web/src/app/mobileShellSource.test.ts, web/e2e/desktop-untouched.spec.ts</files>
  <read_first>
    web/src/app/mobileShellSource.test.ts (WHOLE file — the node-env readFileSync source-assert suite: existing needles, the file-list mechanism, the DESKTOP_QUERY sole-width-literal guard, the FOUC byte pin — extend, never fork)
    web/e2e/desktop-untouched.spec.ts lines 44-98 (DESKTOP_PROJECTS, ROUTES, the phase 6 MAQUETTE_ROUTES leakage loop at :77-98 — the exact template to extend)
    .planning/phases/07-remaining-destinations-operational-parity/07-UI-SPEC.md (Spacing Scale — the phase 7 scale decision and the sweep's needle list; Carried Fixes fix 2)
    .planning/phases/07-remaining-destinations-operational-parity/07-RESEARCH.md (Pitfall on guard self-validation; the guarded-file inventory)
    The phase plans' files_modified lists (07-01..07-07) — the union is the sweep's file list
  </read_first>
  <action>
    Extend mobileShellSource.test.ts with the phase-7 sweep describe: (1) the explicit file list = the union of the phase plans' touched files (platform.ts, Fab.tsx, useLoadMore.ts, ListToolbar.tsx, MobileSectionLabel.tsx, the six pages, NotifyCard.tsx, OffsiteTargetsSection.tsx, ActivityLog.tsx, Dashboard.tsx, the new e2e specs) — assert each listed file exists (the list rots loudly if a file moves); (2) the spacing needles asserting ABSENCE of gap-3|px-3|py-3|pt-3|pb-3|pl-3|pr-3|mt-3|mb-3|ml-3|mr-3|mx-3|my-3|p-3 and of gap-1\\.5|gap-2\\.5|py-2\\.5|px-2\\.5, and the weight needle asserting absence of font-medium; (3) scope: file-wide for wholly-phase-7 files (the new libs/components/specs); for the six pages + ActivityLog + NotifyCard + OffsiteTargetsSection + Dashboard (files whose desktop halves legitimately carry legacy 12px), scope the sweep to the MOBILE BLOCK region between the recorded mount markers — the D-01 mount-discipline comments are the markers — AND assert both markers exist in each scoped file (an anti-shrink guard: a deleted marker fails the suite, so the region cannot quietly shrink to dodge the gate); write the why-comment explaining the region scoping in the guard itself; (4) keep the existing needles intact (DESKTOP_QUERY guard, FOUC pin, UA needles from 07-01).

    First run the sweep honestly: files that FAIL the needles get their phase 7 mobile regions normalized to the 8/16 stops + 400/600 weights in THIS task (the plans should have left them clean; a failure is a real finding — fix it here, never by weakening the needle).

    Extend desktop-untouched.spec.ts: add the six phase routes to the battery loop (the ROUTES list already contains them — extend the per-route NEEDLES with the mobile-chrome-absence assertions per the MAQUETTE_ROUTES loop template: no Fab, no mobile card stack, no ListToolbar aria/placeholder signatures, no Load-more button, no BottomSheet chrome at >=48rem) and add the inverse mobile-project assertions per route (desktop markers absent, mobile markers present) so the gate holds in BOTH directions; keep the fresh-DB chrome-not-data contract.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/app/mobileShellSource.test.ts && npx playwright test e2e/desktop-untouched.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - The sweep describe lists the explicit phase-7 file set, fails loudly on a missing file, and asserts the spacing/.5-stop/font-medium needles
    - Region-scoped files assert BOTH markers exist (anti-shrink guard) — deleting a marker fails the suite
    - Files failing the needles were normalized in-task (or the failure was a real pre-existing desktop value correctly excluded by scoping — recorded per file)
    - desktop-untouched covers the six routes in both directions; the run passes on desktop-1280, desktop-768, and the mobile projects
  </acceptance_criteria>
  <done>The discipline is machine-enforced: 12px/.5 stops and font-medium cannot re-enter phase-7 mobile code, and desktop identity is asserted on all six routes from both sides.</done>
</task>

<task type="auto">
  <name>Task 3: Full phase gate — narrow sweeps, four-project run, vitest, build + dist, just check</name>
  <files>web/e2e/narrow-viewport.spec.ts, web/dist</files>
  <read_first>
    web/e2e/narrow-viewport.spec.ts (the existing de/fr 320-360px sweep structure — the extension points for the phase surfaces)
    .planning/phases/07-remaining-destinations-operational-parity/07-UI-SPEC.md (Verification table — all rows; the overflow row naming the Files fix and de/fr sweeps)
    .planning/STATE.md Accumulated Context (the Windows Playwright wedges: manual webServer, orphaned node cleanup, never pipe output through tail)
    CLAUDE.md (the build chain: cd web && npm ci && npm run build, commit web/dist; just check = the Go chain; restic PATH caveat on Windows)
    web/playwright.config.ts (the four projects + reuseExistingServer: !CI)
  </read_first>
  <action>
    (1) Extend narrow-viewport.spec.ts with the phase surfaces at 320px and 360px in de and fr: /vms /flash /config /receiver /fleet /settings plus the Dashboard mobile block with the log section — assert no horizontal page scroll (document.scrollWidth <= viewport), no control clipped outside the viewport, the Settings chip strip single-line + scrollable, and the Files editor header wrapping with nothing unreachable (the carried fix regression). (2) Run the FULL Playwright suite across all four projects with the Windows discipline: start the webServer manually (absolute path to the bombvault binary per the phase 5 note), run the suite WITHOUT piping output, clean up orphaned node/bombvault processes if a run wedges, and re-run any wedge-affected spec individually to distinguish flake from failure. (3) Run the full vitest suite (npx vitest run) — including the i18n parity + orphan tests proving all 42 tables agree. (4) cd web && npm run build (tsc --noEmit && vite build) and COMMIT web/dist (the binary embeds it — the phase is not closed on a stale embed). (5) Run just check (the Go chain) — on Windows the reduced suite runs per the repo caveat; note any skip in the summary. (6) Record every gate's outcome in the task summary; a red gate is a stop-and-fix, never a summary note.
  </action>
  <verify>
    <automated>cd web && npx vitest run && npx playwright test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - narrow-viewport sweeps pass for all six routes + the Dashboard log section at 320/360px in de and fr with no horizontal scroll or clipped control
    - The full four-project Playwright suite passes under the manual-webServer Windows discipline
    - The full vitest suite passes including i18n parity + orphans
    - npm run build succeeds and web/dist is committed fresh; just check passes (Windows reduced-suite skips noted)
  </acceptance_criteria>
  <done>The phase is green end-to-end and sealed: guards live, batteries extended, narrow sweeps passing, the SPA embed fresh, and every suite recorded green in the summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web/dist build artifact | The committed build is what the Go binary embeds and serves; a stale or tampered dist would ship the wrong interface to users |
| guard suites as CI gates | The source-assert needles gate future contributions; a weakened needle silently re-opens a banned pattern |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-07-28 | Tampering (stale embed) | web/dist at phase close | high | mitigate | Task 3 runs npm run build and commits the fresh dist as a closing acceptance criterion — the binary ships this phase's interface, not a leftover |
| T-07-29 | Tampering (guard erosion) | mobileShellSource sweep | medium | mitigate | The guard's needles/file-list change only with a written why (prohibition 1); the anti-shrink marker asserts prevent region games; the suite runs in the full vitest gate |
| T-07-30 | Tampering (desktop regression at the boundary) | desktop-untouched battery | medium | mitigate | The six-route dual-direction extension asserts both directions at >=48rem; the battery runs in the closing full-suite pass |
| T-07-31 | Repudiation (flaky green) | Windows Playwright runs | low | accept | The known wedge modes are handled by the recorded discipline (manual webServer, no piping, orphan cleanup, individual re-runs); wedge-vs-failure is distinguished per the task instructions |
| T-07-SC | Tampering (supply chain) | npm installs | low | accept | Zero package installs this phase (locked decision; Package Legitimacy Audit table: none) |
</threat_model>

<verification>
- `cd web && npx vitest run` fully green (guards, dom suites, i18n parity + orphans)
- `cd web && npx playwright test` fully green across all four projects (manual webServer; never pipe output)
- `cd web && npm run build` green with web/dist committed; `just check` green (Windows reduced-suite skips noted)
- `grep -c font-medium` returns 0 on RunDetailSheet.tsx, SelectionTree.tsx, and every file the Task 1 purge touched
</verification>

<success_criteria>
- The carried 06-UI-REVIEW findings are fixed and test-pinned (MORE-01/02 presentation debt closed)
- The spacing/weight guard and the six-route dual-direction battery are live and green (LISTS-01/PLAT-01 enforcement)
- de/fr narrow sweeps pass over every phase surface (VERIFY-03 readiness for phase 8)
- The phase closes with a fresh committed web/dist and every suite green (all six requirements sealed)
</success_criteria>

<output>
Create `.planning/phases/07-remaining-destinations-operational-parity/07-08-SUMMARY.md` when done
</output>
