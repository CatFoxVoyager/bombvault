---
phase: 07-remaining-destinations-operational-parity
plan: 05
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - web/src/pages/Settings.tsx
  - web/e2e/destination-settings.spec.ts
autonomous: true
requirements: [MORE-02, PLAT-01]
user_setup: []

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Below md the 7-tab Selector strip becomes a horizontally scrollable row of tonal chips in TAB_ORDER order (Settings.tsx:847): the active chip is accent-tonal (accentSoft bg + accentText text — accent reservation 10), inactive chips bg-carbon-surface2 + text-carbon-textMuted (NOT accent), every chip >=44px, the strip carries the pre-seeded settings.tabsNavigation aria-label, and it never wraps"
    - "The chip strip and the desktop Selector strip bind the SAME tab state (the TabKey useState at Settings.tsx:1111) — one state, two presentations: the desktop strip stays mounted-hidden in the max-md:hidden half, tapping a mobile chip switches tab and the stacked cards update, and there is no second mobile tab state"
    - "The active tab's sections render as stacked full-width cards reusing the settings/ tab-card components where self-contained (NotifyCard, ThemeCard, LanguageCard, AccentCard, AboutCard class) and translating section wrappers into cards — each section keeps the desktop information hierarchy (D-08), on the PAGE_SHELL_RESPONSIVE rhythm while desktop keeps PAGE_SHELL_TABBED byte-identical (every existing Settings.*.dom.test.tsx keeps passing)"
    - "Dark mode / language / accent pickers work exactly as today inside the new presentation (D-09): ColorPickerPopover rides TapPopover, language rides flag chips, no picker is re-authored — the phase 6 tap primitives carry over untouched"
    - "Where a tab's section carries a primary add-style action it renders via the Fab component under material chrome; sections without one render no FAB — never a decorative or duplicate FAB (UI-SPEC FAB surface resolution)"
    - "No desktop-only setting remains in the strip's tabs (FLOW-02 surface check): every control visible in the desktop strip's sections is reachable and operable in the stacked presentation; sections whose editors are operational sheets (notifications, off-site targets) render their entry rows here and defer the sheet work to plan 07-07 without losing reachability"
    - "State coverage rides the re-hosted desktop rows (E6): zero-item sections render their existing empty copy, partial data degrades to the existing muted treatments (never blank cells), load/save busy states reuse the desktop treatments, failed saves toast AND shake, and long labels truncate with title — desktop >=48rem renders byte-identically"
  artifacts:
    - path: "web/src/pages/Settings.tsx"
      provides: "the Settings mobile block — TAB_ORDER tonal chip strip (same tab state, settings.tabsNavigation label) + stacked full-width tab cards reusing the settings/ components; pickers as-today; conditional FAB"
      contains: "TAB_ORDER"
    - path: "web/e2e/destination-settings.spec.ts"
      provides: "staged mobile e2e: chip strip switching, stacked-card rendering per tab, pickers operable, desktop dual-direction"
      contains: "display-prefs"
  key_links:
    - from: "web/src/pages/Settings.tsx mobile chip strip"
      to: "web/src/pages/Settings.tsx:1111 tab state + :847 TAB_ORDER"
      via: "one state, two presentations — the mobile strip is a VIEW of the desktop tab state, never a fork"
      pattern: "TAB_ORDER"
    - from: "web/src/pages/Settings.tsx stacked cards"
      to: "web/src/pages/settings/ tab-card components"
      via: "reuse where self-contained; wrapper translation only — no re-authored section logic"
      pattern: "settings/"
    - from: "web/src/pages/Settings.tsx mobile pickers"
      to: "web/src/components/ColorPickerPopover.tsx / TapPopover (phase 6 primitives)"
      via: "D-09 as-today: the same popover pickers render inside the stacked cards"
      pattern: "ColorPickerPopover"
    - from: "web/src/pages/Settings.tsx (conditional FAB)"
      to: "web/src/components/mobile/Fab.tsx"
      via: "accent reservation 9 — only where a section has a genuine primary add-style action"
      pattern: "Fab"
  prohibitions:
    - statement: "No second tab state and no second settings write path — the chip strip sets the existing TabKey state and the stacked cards keep using the page's existing save flows (queueSettingsWrite + savedBaseline); 07-07 owns the only NEW editors, never a parallel write mechanism here"
      status: enforced
      verification: explicit
    - statement: "No accent on inactive chips and no decorative FAB — accent on the strip is the active chip only (reservation 10); a FAB renders only for a genuine primary add-style action (reservation 9)"
      status: enforced
      verification: explicit
    - statement: "No position:fixed on the chip strip — it is sticky-in-flow inside the page column (the StickyActionBar discipline) and scrolls horizontally, never wrapping"
      status: enforced
      verification: explicit

---

<objective>
Give Settings its mobile treatment (MORE-02): the 7-tab Selector strip becomes a tonal chip strip bound to the SAME tab state, and the active tab's sections render as stacked full-width cards reusing the existing settings/ tab components — dark mode, language, and accent working exactly as today via the phase 6 popover primitives (D-08/D-09). Desktop above 48rem keeps PAGE_SHELL_TABBED byte-identical.

Purpose: Settings is the shell around the operational editors — the strip + stacked-cards presentation must exist (and prove no desktop-only setting remains in it) before plan 07-07 hosts the notify/offsite editors as fullHeight sheets on top of it.

Output: Settings mobile block (chip strip + stacked cards), destination-settings.spec.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-remaining-destinations-operational-parity/07-CONTEXT.md
@.planning/phases/07-remaining-destinations-operational-parity/07-RESEARCH.md
@.planning/phases/07-remaining-destinations-operational-parity/07-PATTERNS.md
@.planning/phases/07-remaining-destinations-operational-parity/07-UI-SPEC.md
@web/src/pages/Settings.tsx
@web/src/lib/pageShell.ts
</context>

<artifacts_section>
## Artifacts this phase produces

See 07-01-PLAN.md for the phase-wide symbol inventory. This plan creates: the Settings mobile block (chip strip on the shared TabKey state + stacked tab cards) and `web/e2e/destination-settings.spec.ts`. No new shared components (strip is composed from existing chip/badge classes + MobileSectionLabel; any add-action rides Fab), no new i18n keys (settings.tabsNavigation is pre-seeded), no settings/ tab-card rewrites — reuse and wrapper translation only.
</artifacts_section>

<flagged_assumptions>
## Flagged assumptions (edge-coverage fallback — never silently dropped)

- **MORE-02 unclassified edge (orchestrator probe):** the engine classified no edge predicate for this requirement. The planner owns its coverage by derivation from the UI-SPEC Screen Contract MORE-02 + the UI Considerations E6 row: the edge behaviors are (a) a section with zero items renders its existing empty copy — never blank (truth 7), (b) the chip strip at 7 tabs + de labels must stay single-line/horizontally-scrollable at 320px — never wrap (truth 1 + prohibition 3; the de/fr 320-360px sweep in plan 07-08 proves it on-device widths), (c) tab switching must not remount or lose unsaved in-card state beyond what the desktop strip already loses (the same state — truth 2; covered by the e2e switching scenario). Recorded here as an explicit planner assumption.
</flagged_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: Settings mobile block — chip strip + stacked cards for the tab set</name>
  <files>web/src/pages/Settings.tsx</files>
  <read_first>
    web/src/pages/Settings.tsx lines 820-870 (TabKey union at :833, TAB_ORDER at :847) and lines 1100-1130 (the tab state at :1111) and the page root structure (PAGE_SHELL_TABBED vs PAGE_SHELL_RESPONSIVE — pageShell.ts:105) and the desktop Selector strip markup (the max-md:hidden half's future tenant)
    web/src/pages/settings/ directory listing + one self-contained card (e.g. ThemeCard) and one wrapper-style section (the two reuse classes the contract names)
    web/src/pages/Dashboard.tsx lines 2090-2172 (mount-discipline comment + MobileSectionLabel — the same gate shape for this page)
    .planning/phases/07-remaining-destinations-operational-parity/07-UI-SPEC.md (Screen Contract MORE-02; Color section reservation 10; Typography caption row for chip text)
    .planning/phases/07-remaining-destinations-operational-parity/07-PATTERNS.md (Settings page structure section)
    web/src/components/mobile/{MobileSectionLabel,Fab}.tsx, web/src/lib/platform.ts (conditional FAB gate)
  </read_first>
  <action>
    Apply the D-01 double gate to Settings.tsx: the desktop keeps its PAGE_SHELL_TABBED root and Selector strip inside the max-md:hidden half; the mobile block rides PAGE_SHELL_RESPONSIVE rhythm behind !isDesktop. Mobile block: (1) the chip strip — one horizontally scrollable row (never wrapping, sticky-in-flow per the StickyActionBar discipline, never position:fixed) of one chip per TAB_ORDER entry: active chip accentSoft bg + accentText text, inactive bg-carbon-surface2 + text-carbon-textMuted, every chip a >=44px tap target with 11px caption text, strip aria-label = t("settings.tabsNavigation"), chips set the EXISTING tab state (setTab — the same one the desktop strip sets); (2) below the strip, the ACTIVE tab's sections rendered as stacked full-width cards (gap-4/p-4 card language): reuse the settings/ tab-card components where self-contained; for wrapper-style sections translate the section wrapper into a MobileSectionLabel + card while keeping the inner content JSX untouched; (3) audit TAB_ORDER for add-style primary actions — render Fab for a section that genuinely has one (hosting that section's existing action), no FAB otherwise; (4) pickers as-today: ThemeCard/LanguageCard/AccentCard render their existing controls — ColorPickerPopover on TapPopover, language flag chips — unmodified (D-09).

    The notify and off-site sections render their ENTRY rows/cards here (reachability for the no-desktop-only-setting truth) but their full-screen editor sheets are plan 07-07's work — leave their existing inline forms working as they are today so nothing loses reachability in the interim; 07-07 re-hosts them.

    Guard: every existing Settings.*.dom.test.tsx must keep passing unchanged (jsdom = desktop = byte-identical presentation). Do not add mobile-DOM dom tests (jsdom answers desktop) — Task 3's Playwright spec owns mobile assertions.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/pages/Settings.settingsWrites.dom.test.tsx src/pages/Settings.themeCard.dom.test.tsx src/pages/Settings.languageCard.dom.test.tsx src/pages/Settings.accentCard.dom.test.tsx && npx eslint src/pages/Settings.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Settings.tsx carries both gates; the mobile strip renders one chip per TAB_ORDER entry bound to the same tab state (no second useState)
    - The active chip uses accentSoft/accentText; inactive chips use carbon-surface2/textMuted with no accent; every chip is a >=44px target; the strip has the settings.tabsNavigation aria-label
    - Stacked cards reuse the settings/ components (no re-authored section logic); pickers are the unmodified phase 6 primitives
    - All existing Settings dom tests pass unchanged; tsc --noEmit and eslint clean
  </acceptance_criteria>
  <done>Settings presents as chip strip + stacked cards below the breakpoint with the desktop presentation byte-identical above it — the shell 07-07's editors mount into.</done>
</task>

<task type="auto">
  <name>Task 2: stacked-presentation operability sweep — no desktop-only setting remains</name>
  <files>web/src/pages/Settings.tsx</files>
  <read_first>
    web/src/pages/Settings.tsx (the stacked presentation from Task 1 — walk every TAB_ORDER section's interactive controls)
    web/src/pages/settings/ (each tab-card component — confirm each control's operability in the stacked context: no hover-only affordance, no desktop-width assumption)
    .planning/phases/07-remaining-destinations-operational-parity/07-UI-SPEC.md (UI Considerations E6 row + the zero-one-many/overflow resolutions; VERIFY-04 touch-target + hover rules)
    .planning/phases/07-remaining-destinations-operational-parity/07-CONTEXT.md (D-07 no desktop-only settings; D-08 stacked cards)
  </read_first>
  <action>
    Walk every section reachable from TAB_ORDER in the stacked presentation and close the parity gaps: (1) every interactive control is operable without hover (Tailwind v4 gates hover: behind (hover: hover) — nothing may depend on it) and is a >=44px target below the breakpoint (grow tap areas with padding/min-h, never visual size inflation); (2) sections with list-shaped content render zero-item empty copy (existing keys) rather than blank space, and partial data degrades to the existing muted treatments; (3) long labels/values truncate with title (break-all for mono values); (4) any section control that genuinely cannot work in a stacked card (audit, expect none) gets recorded in the plan summary as a deviation with its 07-07 disposition instead of being silently dropped; (5) confirm the FAB/entry-row split from Task 1 still leaves every setting reachable.

    This is a sweep task: it EDITS Settings.tsx and settings/ components only where a gap is real, and records the per-section audit outcome (pass or fix applied) in the task summary so the executor's report is the evidence trail for the no-desktop-only-setting truth.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/pages/Settings && npx eslint src/pages/Settings.tsx src/pages/settings && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Every TAB_ORDER section's controls are non-hover-dependent and >=44px in the stacked presentation (sweep recorded per section in the task summary)
    - Zero-item and partial-data states render existing empty/muted treatments, never blank cells
    - No control was dropped or made desktop-only by the restructure; any genuine deviation is recorded with its 07-07 disposition
    - Full Settings dom test set, eslint, and tsc pass
  </acceptance_criteria>
  <done>The stacked presentation is proven complete: nothing the desktop strip can do is unreachable on mobile, with the audit trail recorded.</done>
</task>

<task type="auto">
  <name>Task 3: destination-settings.spec.ts — strip interaction, stacked cards, desktop dual-direction</name>
  <files>web/e2e/destination-settings.spec.ts</files>
  <read_first>
    web/e2e/maquette-screens.spec.ts (staging half — route fixtures + display-prefs abort)
    web/e2e/desktop-untouched.spec.ts lines 44-98 (dual-direction template for /settings)
    .planning/phases/07-remaining-destinations-operational-parity/07-UI-SPEC.md (Interaction Contract rows: Settings chip tap, Desktop >=48rem; Verification table Destination pages row)
    web/playwright.config.ts (project names + the manual-webServer Windows note)
  </read_first>
  <action>
    Create web/e2e/destination-settings.spec.ts: (1) mobile project, /settings staged: the strip renders TAB_ORDER.length chips with the tabsNavigation aria-label; tapping each chip switches the stacked cards (assert a per-tab landmark appears/disappears); the active chip carries the accent classes and inactive chips do not; (2) pickers: open the theme/accent picker control, assert the phase 6 popover surface appears and a selection applies (staged where a server round-trip is involved); (3) dark-mode toggle works from the stacked card; (4) horizontal-scroll: with a narrow viewport the strip scrolls (assert scrollWidth > clientWidth on the strip) and no chip is clipped out of reach; (5) desktop dual-direction (desktop-1280 + desktop-768): /settings renders the desktop Selector strip + tabbed shell markers and NONE of the mobile chrome (no chip strip aria-label, no stacked full-width cards) — the desktop-untouched style. Windows discipline: manual webServer, no output piping, orphan cleanup on wedge.
  </action>
  <verify>
    <automated>cd web && npx playwright test e2e/destination-settings.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - All scenarios pass on the mobile project: strip switching, active/inactive accent classes, pickers, dark mode, horizontal scroll
    - Desktop dual-direction passes on desktop-1280 and desktop-768: desktop markers present, mobile chrome absent
    - Fixtures are route-staged (no dependence on server-side settings state beyond the API contract)
  </acceptance_criteria>
  <done>Settings is e2e-proven in both directions: the strip drives the shared tab state, stacked cards render every tab, and desktop identity holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings writes from stacked cards | The mobile presentation submits to the same settings row the desktop edits; a divergent write path would clobber or fork state |
| display-prefs adoption -> Settings re-render | Adopted prefs re-apply theme/language/accent; the stacked cards must not capture stale pref state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-07-16 | Tampering (settings fork) | chip strip / stacked cards | high | mitigate | The strip sets the EXISTING TabKey state and the cards keep the page's existing save flows (prohibition 1 + Task 1 acceptance criteria) — no second state, no second write path |
| T-07-17 | Tampering (desktop regression) | Settings double gate | medium | mitigate | Desktop keeps PAGE_SHELL_TABBED inside max-md:hidden; every existing Settings dom test must pass unchanged; Task 3's dual-direction asserts no mobile chrome at >=48rem |
| T-07-18 | Misrepresentation (accent drift) | chip strip active state | low | mitigate | Accent on the strip is the active chip only (reservation 10), asserted in e2e; inactive chips carbon-surface2 |
| T-07-19 | Information disclosure | stacked settings cards | low | accept | The presentation re-renders data the desktop already renders for the same user; no new data exposure (secret-bearing fields arrive in 07-07 under the write-only contract) |
| T-07-SC | Tampering (supply chain) | npm installs | low | accept | Zero package installs this phase (locked decision; Package Legitimacy Audit table: none) |
</threat_model>

<verification>
- `cd web && npx vitest run src/pages/Settings` green (full Settings dom set — desktop identity)
- `cd web && npx playwright test e2e/destination-settings.spec.ts` green (manual webServer; never pipe output)
- `cd web && npx tsc --noEmit -p tsconfig.json` green; eslint clean on Settings.tsx + settings/
- Source checks: single tab state (no second TabKey useState), strip uses settings.tabsNavigation, no accent classes on inactive chips
</verification>

<success_criteria>
- Settings presents as tonal chip strip + stacked cards below 48rem, bound to the one shared tab state (MORE-02)
- Dark mode / language / accent work exactly as today via the unmodified phase 6 primitives (D-09)
- No desktop-only setting remains reachable-only above 48rem; editors' entry rows keep notify/offsite reachable for 07-07
- Desktop >=48rem stays byte-identical, proven in both directions
</success_criteria>

<output>
Create `.planning/phases/07-remaining-destinations-operational-parity/07-05-SUMMARY.md` when done
</output>
