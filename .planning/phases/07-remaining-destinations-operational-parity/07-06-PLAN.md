---
phase: 07-remaining-destinations-operational-parity
plan: 06
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - web/src/pages/Containers.tsx
  - web/src/pages/Files.tsx
  - web/src/components/ActivityLog.tsx
  - web/src/pages/Dashboard.tsx
  - web/e2e/list-ergonomics.spec.ts
autonomous: true
requirements: [LISTS-01]
user_setup: []

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "List rows are >= 44px tall; adjacent rows never overlap or collapse into each other — a min-height floor on every row in the Containers card list, the Files set rows, and the ActivityLog lines (LISTS-01 adjacency verdict, lifted)"
    - "Zero-match/empty lists render an explicit empty state (never blank): Containers' no-match copy, the Files sets empty state, ActivityLog's existing empty language — and the Load-more control is absent when no further items exist on every list (LISTS-01 empty verdict, lifted)"
    - "Load-more appends the next page after the existing rows, preserving the server-provided order across page boundaries — no re-sort, no duplicate keys, on all three lists (LISTS-01 ordering verdict, lifted); visible slicing is the shared useLoadMore only"
    - "The Containers mobile list gains the ListToolbar bound to the EXISTING desktop search state (Containers.tsx:3564 useState + the :4065-4103 FilterPopover controls) and its existing filterKey chips — one state, two presentations; typing filters the mobile cards and resets the visible slice"
    - "The Files sets list gains the shared search (common.search pre-seeded key — the sets list has no domain placeholder) + load-more, and the carried 06-UI-REVIEW fix lands here: the Files editor header row (Files.tsx:1661-1689) gains flex-wrap so the 360px de/fr overflow is gone"
    - "ActivityLog renders its mobile list variant behind a double gate INSIDE the component (desktop markup byte-identical — Dashboard's desktop mount is untouched) and the Dashboard mobile block mounts it below MobileRepoHealthCard reusing the same logDayFilter props, so logs are reachable below 48rem without a new route or a second merge/filter implementation (buildLogLines/filterLogLines stay the only logic)"
    - "Toolbar/chips are sticky-in-flow inside the page column (never position:fixed), chip strips scroll horizontally and never wrap, lists scroll in the page scroller (no nested internal scroll on mobile — ActivityLog's desktop auto-follow scroller is a desktop-only behavior), long text breaks/truncates per the house pattern, and zero rows renders the empty state rather than a bare toolbar"
  artifacts:
    - path: "web/src/pages/Containers.tsx"
      provides: "Containers mobile list retrofit — ListToolbar on the shared search/filterKey state + useLoadMore over the card list"
      contains: "ListToolbar"
    - path: "web/src/pages/Files.tsx"
      provides: "Files sets list retrofit — common.search toolbar + useLoadMore + the carried header flex-wrap fix (Files.tsx:1661-1689)"
      contains: "flex-wrap"
    - path: "web/src/components/ActivityLog.tsx"
      provides: "internal double gate: mobile list variant (toolbar with the existing day/domain/type filters + >=44px rows + useLoadMore, no auto-follow scroller); desktop variant unchanged"
      contains: "isDesktop"
    - path: "web/src/pages/Dashboard.tsx"
      provides: "the Dashboard mobile block gains the ActivityLog mount below MobileRepoHealthCard (same logDayFilter props as the desktop block)"
      contains: "ActivityLog"
    - path: "web/e2e/list-ergonomics.spec.ts"
      provides: "mobile e2e across the three retrofits: search filters shared state, Load more appends 20 with the button absent at exhaustion, no auto-load, >=44px rows, empty states"
      contains: "display-prefs"
  key_links:
    - from: "web/src/pages/Containers.tsx mobile toolbar"
      to: "web/src/pages/Containers.tsx:3564 search state + web/src/components/mobile/ListToolbar.tsx"
      via: "one state, two presentations — the desktop FilterPopover's state, a mobile view"
      pattern: "ListToolbar"
    - from: "all three lists"
      to: "web/src/lib/useLoadMore.ts"
      via: "THE pagination mechanism — visible slicing, identity reset on filter change, hasMore-gated button"
      pattern: "useLoadMore"
    - from: "web/src/pages/Dashboard.tsx mobile block"
      to: "web/src/components/ActivityLog.tsx"
      via: "the same self-contained component, mounted with the same props the desktop block passes — one log surface, two presentations"
      pattern: "ActivityLog"
    - from: "web/src/components/ActivityLog.tsx filters"
      to: "web/src/lib/activityLog.ts filterLogLines (:764)"
      via: "the ONE filter/merge logic — the mobile toolbar binds the existing filter state, no second implementation"
      pattern: "filterLogLines"
  prohibitions:
    - statement: "No parallel mobile filter state anywhere in this plan — toolbars bind the desktop-owned state (Containers search/filterKey, ActivityLog's existing day/domain/type filter state); the Files toolbar introduces the one shared common.search placeholder instead of per-domain forks"
      status: enforced
      verification: explicit
    - statement: "No hand-rolled pagination and no auto-load: no IntersectionObserver, no scroll listeners, no sentinel refs, no per-list slicing — only the shared useLoadMore; Dashboard's MobileRecentRunsCard stays exempt (top-4, the phase 6 glanceability contract)"
      status: enforced
      verification: explicit
    - statement: "No position:fixed on any toolbar and no desktop markup changes — the ActivityLog gate is internal to the component and the desktop variants of all three lists render byte-identically (existing dom tests keep passing)"
      status: enforced
      verification: explicit

---

<objective>
Retrofit LISTS-01 onto the existing long lists: Containers' mobile card list, the Files sets list, and the ActivityLog (reaching mobile for the first time via the Dashboard mobile block) all gain the sticky-in-flow search + filter chips toolbar and constant-threshold load-more from the Wave-1 foundations — plus the carried Files header flex-wrap fix. Desktop above 48rem is byte-identical everywhere.

Purpose: the foundations (useLoadMore, ListToolbar) exist after 07-02; this plan applies them to the lists the milestone already ships, so the ergonomics contract (search/chips/load-more/44px rows) holds across ALL long lists, not just the new destinations.

Output: three retrofitted lists, the Dashboard mobile log mount, list-ergonomics.spec.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-remaining-destinations-operational-parity/07-CONTEXT.md
@.planning/phases/07-remaining-destinations-operational-parity/07-RESEARCH.md
@.planning/phases/07-remaining-destinations-operational-parity/07-PATTERNS.md
@.planning/phases/07-remaining-destinations-operational-parity/07-UI-SPEC.md
@web/src/pages/Containers.tsx
@web/src/pages/Files.tsx
@web/src/components/ActivityLog.tsx
@web/src/lib/useLoadMore.ts
@web/src/components/mobile/ListToolbar.tsx
</context>

<artifacts_section>
## Artifacts this phase produces

See 07-01-PLAN.md for the phase-wide symbol inventory. This plan creates: the Containers/Files/ActivityLog retrofits (ListToolbar + useLoadMore consumption only — no new shared primitives), the Dashboard mobile ActivityLog mount, and `web/e2e/list-ergonomics.spec.ts`. No new i18n keys (common.search is pre-seeded; every other string reuses existing keys), no api.ts changes (client-side pagination only).
</artifacts_section>

<tasks>

<task type="tracer">
  <name>Task 1: Containers mobile list — shared-state toolbar + load-more</name>
  <files>web/src/pages/Containers.tsx, web/e2e/list-ergonomics.spec.ts</files>
  <read_first>
    web/src/pages/Containers.tsx lines 3550-3580 (the search state at :3564 + filter state), lines 4060-4110 (the desktop FilterPopover controls), lines 4195-4260 (the phase 6 mobile card list — summary line, filterKey chips, MobileContainerCard map)
    web/src/components/mobile/ListToolbar.tsx + web/src/lib/useLoadMore.ts (the Wave-1 APIs — props-closed composite, identity reset semantics)
    .planning/phases/07-remaining-destinations-operational-parity/07-UI-SPEC.md (LISTS-01 pattern section — toolbar + load-more + rows; Dashboard recent-runs exemption)
    .planning/phases/07-remaining-destinations-operational-parity/07-PATTERNS.md (List ergonomics section)
    web/e2e/maquette-screens.spec.ts (staging half — this task CREATES list-ergonomics.spec.ts with the Containers scenarios)
    web/src/lib/i18n.ts (verify containers.searchPlaceholder + filter.* keys exist — reuse, never re-author)
  </read_first>
  <action>
    In Containers.tsx's mobile list block (NOT the desktop half): insert the ListToolbar above the card list bound to the EXISTING search state (setSearch — the same one the desktop FilterPopover input at :4069 sets) with placeholder containers.searchPlaceholder, and host the existing filterKey chips (installed/notInstalled) as its children slot — same state, two presentations. Paginate the rendered card list through useLoadMore over the FILTERED card array (identity reset when search/filterKey changes), appending the not-installed section's cards under the same visible window contract; render the common.loadMore button (>=44px) only when hasMore. Rows/cards: enforce the >=44px floor (MobileContainerCard already exceeds it — verify; grow anything short with padding, never visual shrink). No-match state: when the filtered list is empty render the existing no-match/empty copy as a card (the existing filter.noMatch key family — verify the exact key in i18n.ts and reuse it), never a blank column.

    Create web/e2e/list-ergonomics.spec.ts with the Containers scenarios (staged container-domain fixtures per the maquette model): (a) the toolbar input filters the SAME state — typing drops the card count to the fixture's matching subset; (b) stage >20 cards, assert initial window 20, press Load more once -> 40, exhaust -> button absent; (c) scroll without pressing -> no auto-load (count stays 20); (d) the toolbar is sticky-in-flow (assert it remains visible/positioned in-flow — no position:fixed computed style) and chip taps switch the visible section; (e) >=44px row floor via bounding boxes on the rendered cards. Windows Playwright discipline: manual webServer, no output piping.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/pages/Containers.tree.dom.test.tsx src/pages/Containers.excludesAssistant.dom.test.tsx && npx eslint src/pages/Containers.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - The mobile toolbar sets the same search state the desktop FilterPopover input sets (no second useState); filterKey chips render in the toolbar's children slot
    - Cards paginate via useLoadMore (20/20 constant); the Load-more button is absent at zero rows and when exhausted; no auto-load exists
    - The no-match empty state renders the existing key's copy; rows are >=44px
    - Existing Containers dom tests pass unchanged (desktop byte-identical); tsc --noEmit and eslint clean
  </acceptance_criteria>
  <done>Containers proves the retrofit shape: desktop-owned state, mobile toolbar, constant-threshold pagination, honest empty states — the pattern Tasks 2-3 repeat.</done>
</task>

<task type="auto">
  <name>Task 2: Files sets list — shared search + load-more + carried header fix</name>
  <files>web/src/pages/Files.tsx</files>
  <read_first>
    web/src/pages/Files.tsx lines 1650-1700 (the editor header row at :1661-1689 — the carried 06-UI-REVIEW overflow fix target) and lines 2180-2290 (the mobile sets list block — !isDesktop set rows, expandedSet detail gate)
    web/src/pages/Files.tsx desktop sets controls (the desktop search/filter presentation — what the mobile toolbar mirrors; the sets list has NO domain search placeholder today, hence common.search)
    .planning/phases/07-remaining-destinations-operational-parity/07-UI-SPEC.md (Copywriting Contract search-placeholder row; Carried Fixes table — Files header row; LISTS-01 pattern)
    web/src/lib/useLoadMore.ts + web/src/components/mobile/ListToolbar.tsx (foundation APIs)
    web/e2e/list-ergonomics.spec.ts (extend with the Files scenarios)
  </read_first>
  <action>
    Two changes in Files.tsx: (1) the carried fix — the editor header row at :1661-1689 gains flex-wrap so its controls wrap instead of overflowing at 360px (the de/fr narrow sweep in 07-08 asserts the regression stays dead; keep the row's desktop layout visually equivalent when space allows). (2) The mobile sets list: add a ListToolbar above the set rows with placeholder common.search (the pre-seeded shared key — do NOT create a per-domain key) driving a NEW set-name filter state only if the desktop sets list has none to bind; if the desktop page already filters sets by a search-like state, bind THAT state instead (one state, two presentations — audit first, bind what exists). Paginate the set rows through useLoadMore (identity reset when the filter changes); Load-more button >=44px, only when hasMore; empty/zero-match renders the existing sets empty copy, never blank; rows >=44px (the existing py-1.5 row precedent — verify and grow if short).

    Extend list-ergonomics.spec.ts with the Files scenarios: staged set fixtures — search filters the rows (shared-state assertion), Load more appends exactly one threshold and disappears at exhaustion, no auto-load on scroll, the sets empty state renders for a zero-match filter, and the editor header at 360px wraps with no clipped control (the carried-fix regression assertion).
  </action>
  <verify>
    <automated>cd web && npx vitest run src/pages/Files.tree.dom.test.tsx && npx eslint src/pages/Files.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - The header row at Files.tsx:1661-1689 carries flex-wrap and no control clips at 360px (e2e assertion passes)
    - The sets list toolbar binds an existing filter state where one exists, or adds the single new local filter bound ONLY to the mobile toolbar via common.search (no per-domain key created)
    - Set rows paginate via useLoadMore with the hasMore-gated button; zero-match renders the existing empty copy; rows >=44px
    - Files.tree.dom.test.tsx passes unchanged; tsc --noEmit and eslint clean
  </acceptance_criteria>
  <done>The sets list meets the ergonomics contract and the carried 360px overflow fix is landed where the regression is cheapest to assert.</done>
</task>

<task type="auto">
  <name>Task 3: ActivityLog mobile variant + Dashboard mobile mount</name>
  <files>web/src/components/ActivityLog.tsx, web/src/pages/Dashboard.tsx, web/e2e/list-ergonomics.spec.ts</files>
  <read_first>
    web/src/components/ActivityLog.tsx (WHOLE file — the self-contained card chrome + heading, the internal auto-follow scroller with BOTTOM_THRESHOLD_PX, the existing day/domain/type filter state, buildLogLines/filterLogLines usage, row markup)
    web/src/pages/Dashboard.tsx lines 2929-2941 (the mobile Home block: MobileNextRunCard/MobileRecentRunsCard/MobileRepoHealthCard — the ActivityLog mount joins as the next child) and lines 2628-2641 (the desktop mount's props: dayFilter/onClearDayFilter/hueIndex)
    web/src/pages/Dashboard.tsx lines 2175-2218 (MobileRecentRunsCard — the EXEMPT top-4 surface; do not paginate it)
    .planning/phases/07-remaining-destinations-operational-parity/07-UI-SPEC.md (LISTS-01 pattern; Interaction Contract load-more row; overflow row — lists scroll in the page scroller)
    .planning/phases/07-remaining-destinations-operational-parity/07-PATTERNS.md (List ergonomics — Logs: reuse filterLogLines verbatim)
    web/e2e/list-ergonomics.spec.ts (extend with the log scenarios)
  </read_first>
  <action>
    Retrofit ActivityLog.tsx with an INTERNAL double gate: the desktop rendering (card chrome, heading, internal auto-follow scroller, existing filter row) stays byte-identical when isDesktop (existing Dashboard mount + dom tests unchanged); when !isDesktop the component renders the mobile list variant: MobileSectionLabel header (activityLog.title), a ListToolbar hosting the EXISTING day/domain/type filter controls (same state — the chips/popovers the desktop row already uses, re-presented >=44px), and the merged log lines as full-width rows (>=44px floor, divide-y hairlines, existing glyph + text language, tabular timestamps) paginated via useLoadMore over the FILTERED lines (identity reset when filters change; button only when hasMore; empty state = the existing empty language). The desktop auto-follow scroller is desktop-only: the mobile list scrolls in the PAGE scroller (no nested scroll container, no auto-follow) — gate the scroller wrapper exactly like the markup. Zero new merge/filter logic: buildLogLines/filterLogLines stay the only pipeline.

    In Dashboard.tsx's mobile block (the {!isDesktop} column at :2933-2941), mount <ActivityLog> below MobileRepoHealthCard passing the SAME props the desktop block passes (logDayFilter/onClearDayFilter/hueIndex discipline — reuse the page-level state; the day chip's clear action keeps working on mobile).

    Extend list-ergonomics.spec.ts with the log scenarios (Dashboard, mobile project): the log section renders below repo health; the toolbar's day/domain/type chips filter the merged lines (staged runs fixtures); Load more appends one threshold and disappears at exhaustion; the >=44px floor holds on log rows; and the desktop-1280 project still shows the DESKTOP log (auto-follow scroller present, mobile toolbar absent) — a one-scenario desktop sanity check for this component only (the full six-route battery extension is 07-08's).
  </action>
  <verify>
    <automated>cd web && npx vitest run src/components && npx eslint src/components/ActivityLog.tsx src/pages/Dashboard.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - ActivityLog.tsx contains the internal gate: desktop variant markup unchanged (existing component dom tests pass), mobile variant with toolbar + >=44px rows + useLoadMore and no nested auto-follow scroller
    - The mobile toolbar binds the EXISTING filter state (no second filter state); filterLogLines remains the only filtering implementation
    - Dashboard's mobile block mounts ActivityLog with the same props as the desktop block; MobileRecentRunsCard remains unpaginated (top-4 exempt)
    - tsc --noEmit and eslint clean; the component suite passes
  </acceptance_criteria>
  <done>Logs reach mobile through the same component with the same filter logic — toolbar, 44px rows, and constant-threshold pagination inside an internally-gated, desktop-identical file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staged e2e fixtures | Route-mocked list payloads cross into the retrofitted lists; fixtures mirror the Go wire shapes and are test-only |
| filter state sharing | The mobile toolbars write to state the desktop controls also own; a divergent state would make the two presentations disagree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-07-20 | Tampering (state fork) | mobile toolbars | medium | mitigate | Toolbars bind the desktop-owned states (prohibition 1); e2e asserts a mobile filter change is the same state the desktop controls read (the Containers search assertion) |
| T-07-21 | Tampering (desktop regression) | all three retrofits | medium | mitigate | Desktop variants are byte-identical — existing dom tests (Containers/Files/ActivityLog) must pass unchanged; the ActivityLog gate is internal; 07-08's battery extension re-proves all routes |
| T-07-22 | Denial of service (runaway render) | useLoadMore consumption | low | accept | The window math is bounded (20/step 20, client-side arrays already fetched); the no-auto-load prohibition prevents unbounded growth by interaction |
| T-07-23 | Repudiation (log fidelity) | ActivityLog mobile variant | low | accept | The mobile variant renders the same buildLogLines output through the same glyphs/colors — presentation only, no data transformation |
| T-07-SC | Tampering (supply chain) | npm installs | low | accept | Zero package installs this phase (locked decision; Package Legitimacy Audit table: none) |
</threat_model>

<verification>
- `cd web && npx vitest run src/pages/Containers.tree.dom.test.tsx src/pages/Containers.excludesAssistant.dom.test.tsx src/pages/Files.tree.dom.test.tsx src/components` green (desktop identity everywhere)
- `cd web && npx playwright test e2e/list-ergonomics.spec.ts` green (manual webServer; never pipe output)
- `cd web && npx tsc --noEmit -p tsconfig.json` green; eslint clean on all touched files
- Source checks: no IntersectionObserver/scroll listeners added; no second filter state; no position:fixed in the toolbars; the Files header row carries flex-wrap
</verification>

<success_criteria>
- All three long lists (Containers cards, Files sets, ActivityLog on mobile) meet LISTS-01: shared-state sticky-in-flow toolbars, constant-threshold load-more with an honest empty state, >=44px rows
- The carried Files header overflow fix is landed and regression-asserted at 360px
- Logs are reachable below 48rem through the same self-contained component, desktop byte-identical above it
- Zero auto-load, zero pagination forks, zero new filter states
</success_criteria>

<output>
Create `.planning/phases/07-remaining-destinations-operational-parity/07-06-SUMMARY.md` when done
</output>
