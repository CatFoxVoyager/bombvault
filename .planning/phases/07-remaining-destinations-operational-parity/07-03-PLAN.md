---
phase: 07-remaining-destinations-operational-parity
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - web/src/pages/VMs.tsx
  - web/src/pages/Flash.tsx
  - web/src/pages/VMs.test.tsx
  - web/src/pages/Flash.desktop.dom.test.tsx
  - web/e2e/destination-vms-flash.spec.ts
autonomous: true
requirements: [MORE-01, FLOW-01, LISTS-01, PLAT-01]
user_setup: []

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every destination list in this plan's surfaces renders exactly one card per item, keyed by the item's unique identity (VM name — libvirt/store-unique by construction; Flash is a singleton domain) — two entries never merge into one card (MORE-01 adjacency verdict, lifted)"
    - "A destination with zero items renders its honest empty/gated state — settings-gate off shows the desktop gate copy as a MobileSectionLabel + outline card pointing at Settings (e2e covers gate-off, not only gate-on fixtures); zero VMs shows the existing vms.empty copy, zero flash snapshots shows flash.none; never a blank section — and a single-item list renders one full-size card (MORE-01 empty verdict, lifted)"
    - "VMs.tsx and Flash.tsx each carry the D-01 double gate in ONE file: the desktop JSX wraps in max-md:hidden (always rendered, desktop byte-identical — VMs.test.tsx keeps passing unchanged) and the mobile block renders {!isDesktop && (...)} so no mobile markup exists in the desktop DOM (Dashboard.tsx:2090-2104 mount-discipline shape verbatim)"
    - "The VMs mobile search + filter chips bind the SAME state the desktop controls own — the existing search/scheduleFilter/backupFilter useState at VMs.tsx:1546-1548 and their composed predicate — through the shared ListToolbar; there is NO parallel mobile filter state, NO mobile-side re-filter, and typing or toggling resets the visible slice via useLoadMore's identity reset"
    - "A per-VM trigger row fires the EXISTING backup semantics with useBackupWatch correlation, and the correlated run deep-links into a component-local RunDetailSheet via the FLOW-03 onRun passthrough with the dismissal latch — the desktop page's trigger behavior is not forked (phase 6 BackupButton onRunCorrelated pattern)"
    - "The schedule entry row opens a fullHeight BottomSheet hosting the SAME CadenceBuilder the desktop renders (never a fork — TimePicker arrives via CadenceBuilder's single call site); after the sheet closes the card's accentText effective-preview chip refreshes from getScheduleNext() (api.ts:1964, ScheduleNext filtered by domain) — never from client-side cadence math or the client clock (D-05 + D-06 + FLOW-01)"
    - "Restore entries are secondary/tonal rows — NEVER accent (the bible's restore-is-deliberate rule): the VM restore entry rides the existing RestoreAction surface, and Flash's restore/download entries reuse flash.download/flash.restoreNote copy; flash restore is presented as a download, never a destructive in-place action"
    - "The Flash hero card renders status Badge + last-backup line + offsite indicator + the existing flash.backupNow trigger (FAB under material hosts the same action); below it the existing snapshot rows (>=44px, the py-1.5 row precedent) paginate through the shared useLoadMore — the Load-more button renders ONLY when hasMore is true and never auto-loads"
    - "The Flash zip-export toggle edits through the sheet-editor pattern riding queueSettingsWrite + savedBaseline (Settings.tsx:1190-1223 model) — a re-fetch then merge-ONLY-the-changed-field write, never a captured full-object PUT and never a second write path"
    - "Cards render only fields the wire carries (frozen-API honesty): four-status Badges always carry text labels, offsite blue appears exactly where the desktop surface already speaks offsite (never as reachability), loading reuses the existing vms.*/flash.backingUp busy treatments, load errors reuse vms.loadFailed/flash.loadBackupsFailed with a >=44px retry row, backend error text renders verbatim, long text truncates with title (never layout-break) — and desktop >=48rem renders byte-identically on both routes"
  artifacts:
    - path: "web/src/pages/VMs.tsx"
      provides: "the VMs mobile block — summary meta, ListToolbar on shared state, VM cards, trigger row + RunDetailSheet deep-link, fullHeight CadenceBuilder sheet + preview chip, restore entry, snapshot rows with load-more, FAB, gate-off card"
      contains: "isDesktop"
    - path: "web/src/pages/Flash.tsx"
      provides: "the Flash mobile block — hero card, snapshot rows with load-more, secondary download/restore entries, zip-export toggle sheet on queueSettingsWrite"
      contains: "isDesktop"
    - path: "web/src/pages/VMs.test.tsx"
      provides: "extended desktop-identity coverage: the desktop page renders unchanged with the max-md:hidden wrapper present (jsdom answers desktop)"
      contains: "max-md:hidden"
    - path: "web/src/pages/Flash.desktop.dom.test.tsx"
      provides: "new desktop-identity dom test for Flash mirroring the VMs one (desktop renders, mobile gate closed in jsdom)"
      contains: "isDesktop"
    - path: "web/e2e/destination-vms-flash.spec.ts"
      provides: "staged-fixture mobile e2e: cards/badges/offsite lines, gate-off, trigger deep-link, schedule sheet + preview-chip refresh, Load-more count, desktop dual-direction"
      contains: "display-prefs"
  key_links:
    - from: "web/src/pages/VMs.tsx mobile toolbar"
      to: "web/src/pages/VMs.tsx:1546-1548 useState + web/src/components/mobile/ListToolbar.tsx"
      via: "one state, two presentations — the mobile toolbar is a VIEW of the desktop filter state, never a fork"
      pattern: "ListToolbar"
    - from: "web/src/pages/VMs.tsx schedule sheet"
      to: "web/src/components/CadenceBuilder.tsx (single TimePicker call site at :442)"
      via: "the SAME inline component hosted in a fullHeight BottomSheet — never a fork"
      pattern: "CadenceBuilder"
    - from: "web/src/pages/VMs.tsx preview chip"
      to: "web/src/lib/api.ts getScheduleNext (ScheduleNext[])"
      via: "server-derived schedule preview (D-06) — refreshed after the sheet closes"
      pattern: "getScheduleNext"
    - from: "web/src/pages/Flash.tsx zip-export sheet"
      to: "web/src/pages/Settings.tsx:1190-1223 queueSettingsWrite + savedBaseline"
      via: "the ONE settings write chain — merge-only-the-changed-field"
      pattern: "queueSettingsWrite"
    - from: "web/src/pages/VMs.tsx + Flash.tsx primary actions"
      to: "web/src/components/mobile/Fab.tsx"
      via: "the FAB hosts the surface's primary trigger under material chrome (accent reservation 9); the cupertino pinned-button equivalent renders instead"
      pattern: "Fab"
  prohibitions:
    - statement: "Restore entries never render accent — secondary/tonal rows always (VM restore, flash download/restore); accent stays on the exhaustive reservations list"
      status: enforced
      verification: explicit
    - statement: "No client-side schedule derivation: preview chips render ONLY from getScheduleNext() payload fields — no cadence-string math, no Date.now() countdown logic anywhere in the mobile blocks"
      status: enforced
      verification: explicit
    - statement: "No second settings write path and no captured full-object PUT — the Flash zip-export toggle goes through queueSettingsWrite + savedBaseline with a merge of only the changed field"
      status: enforced
      verification: explicit
    - statement: "No parallel mobile filter state and no IntersectionObserver auto-load — the toolbar binds the desktop-owned search/scheduleFilter/backupFilter state, and pagination is the shared useLoadMore only (constant threshold, button gated on hasMore)"
      status: enforced
      verification: explicit

---

<objective>
Bring VMs and Flash into the mobile card language (MORE-01a): block-level VM cards with status/last-run/offsite, per-card trigger + schedule entry points, restore entries, snapshot rows with load-more — and the Flash hero card with snapshot pagination and a zip-export toggle riding the existing settings write chain. Schedule editing parity lands here for these domains: fullHeight CadenceBuilder sheets with the effective-preview chip on the invoking card (FLOW-01, D-05/D-06).

Purpose: these are the two simplest destination surfaces — the tracer proves the full card + sheet + pagination + FAB stack composed from the Wave-1 foundations before the harder Config/Receiver/Fleet plan repeats the shape.

Output: VMs + Flash mobile blocks behind the D-01 double gate, desktop-identity dom tests, destination-vms-flash.spec.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-remaining-destinations-operational-parity/07-CONTEXT.md
@.planning/phases/07-remaining-destinations-operational-parity/07-RESEARCH.md
@.planning/phases/07-remaining-destinations-operational-parity/07-PATTERNS.md
@.planning/phases/07-remaining-destinations-operational-parity/07-UI-SPEC.md
@web/src/pages/VMs.tsx
@web/src/pages/Flash.tsx
@web/src/pages/Dashboard.tsx
@web/src/components/EffectiveScheduleLine.tsx
</context>

<artifacts_section>
## Artifacts this phase produces

See 07-01-PLAN.md for the phase-wide symbol inventory. This plan creates: the VMs and Flash mobile blocks (D-01 double gate, ListToolbar/CadenceBuilder-sheet/RunDetailSheet/Fab composition, useLoadMore consumption), `Flash.desktop.dom.test.tsx`, the extended `VMs.test.tsx` desktop-identity coverage, and `web/e2e/destination-vms-flash.spec.ts`. No new shared components, no new i18n keys (Wave-1 pre-seed + existing keys cover every string this plan renders), no new API calls (api.ts frozen).
</artifacts_section>

<flagged_assumptions>
## Flagged assumptions (edge-coverage fallback — never silently dropped)

- **FLOW-01 unclassified edge (orchestrator probe):** the engine classified no edge predicate for this requirement. The planner owns its coverage by derivation from the UI-SPEC Interaction Contract: the edge behaviors are (a) the preview chip renders from server payload only (truth 6 + the P3 prohibition), (b) a domain with NO registered schedule entry renders no preview chip rather than a fabricated one (ScheduleNext filtered by domain returns empty — covered by the unavailable-data truth 10), (c) closing the sheet without applying must not change the chip (the sheet's cancel path leaves state untouched — covered by the destination e2e). Recorded here as an explicit planner assumption; the remaining FLOW-01 surfaces (Settings/offsite/notify schedule-adjacent editors) complete in plans 07-05/07-07.
</flagged_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: VMs mobile block — cards, shared-state toolbar, trigger deep-link, schedule sheet, load-more</name>
  <files>web/src/pages/VMs.tsx, web/src/pages/VMs.test.tsx</files>
  <read_first>
    web/src/pages/VMs.tsx lines 1530-1650 (the state block: search/scheduleFilter/backupFilter at :1546-1548, the composed filter predicate, page-level error) and lines 1800-1900 (the desktop FilterPopover controls at :1825-1859 — input classes, ChipFilter options) and the desktop VM list/rows + trigger handlers (the semantics the mobile cards re-present)
    web/src/pages/Dashboard.tsx lines 2090-2230 (the mount-discipline comment, MobileSectionLabel usage, MobileNextRunCard anatomy, four-status row anatomy — the exact card/row language to extend)
    web/src/pages/Containers.tsx lines 3970-4000 + 4200-4220 (the mobile detail double-gate shape and mobile list mount — the second precedent)
    .planning/phases/07-remaining-destinations-operational-parity/07-UI-SPEC.md (Screen Contract MORE-01a VMs — the 7-part block order; LISTS-01 pattern; Copywriting Contract)
    .planning/phases/07-remaining-destinations-operational-parity/07-PATTERNS.md (Card anatomy, Sheet editors skeleton, List ergonomics section)
    web/src/lib/backupWatch.ts (useBackupWatch onRun passthrough — the FLOW-03 seam), the RunDetailSheet host pattern in the phase 6 consumers (grep onRunCorrelated in src/pages + src/components)
    web/src/components/CadenceBuilder.tsx (the inline component to host; TimePicker single call site at :442)
    web/src/components/mobile/{BottomSheet,StickyActionBar,ListToolbar}.tsx, web/src/lib/useLoadMore.ts, web/src/components/mobile/{Fab,MobileSectionLabel}.tsx (the Wave-1 foundation APIs)
    web/src/pages/VMs.test.tsx (existing coverage — extend, never fork)
  </read_first>
  <action>
    Restructure VMs.tsx into the D-01 double gate (Desktop mount-discipline comment verbatim adapted): wrap the existing desktop JSX in max-md:hidden and add the mobile block behind {!isDesktop && (...)}. Mobile block order per the UI-SPEC MORE-01a contract: (1) summary meta line derived from the SAME counts the desktop header shows; (2) ListToolbar bound to the EXISTING search/scheduleFilter/backupFilter state (placeholder: vms.searchPlaceholder; chips: the same persisted ChipFilter options) — one state, two presentations; (3) one card per VM from the FILTERED list (identity: VM name): monogram avatar (bg-carbon-surface2), name (text-body font-semibold), four-status Badge with text label, schedule-method + include-state meta line, last-run line (relative time + tabular), offsite indicator where the desktop surface speaks offsite; (4) per-card trigger row with the existing backup semantics — useBackupWatch with the onRun passthrough deep-linking a component-local RunDetailSheet with the dismissal latch (copy the phase 6 Home/consumer host pattern; desktop omits onRun and is unchanged); (5) schedule entry row opening a fullHeight BottomSheet hosting CadenceBuilder with StickyActionBar apply/cancel — apply rides the SAME save path the desktop schedule control uses; on close, refresh the schedule preview from getScheduleNext() filtered to the vms domain and render the accentText preview chip on the card (absent when the domain has no registered entry); (6) restore entry per VM via the existing RestoreAction surface as a secondary/tonal row (ConfirmSheet via useConfirm carries vms.restoreSelectedConfirm); (7) snapshot disclosure rows per VM (>=44px) paginated by the shared useLoadMore.

    FAB: render Fab hosting the surface's primary backup trigger under material (the per-VM trigger stays on the card — the FAB mirrors the desktop's primary CTA semantics). Gate-off honesty: when the settings gate is off, render MobileSectionLabel + an outline card with the desktop gate copy pointing at Settings (both gate states honest; use the desktop's existing gate copy key). Zero-VM state: the existing vms.empty copy as an empty-state card; load errors: the existing vms.loadFailed key + a >=44px retry row; backend error text verbatim.

    Extend VMs.test.tsx ONLY with desktop-identity assertions (jsdom answers desktop): the desktop page still renders its existing landmarks and the desktop list/controls remain in the DOM inside the max-md:hidden wrapper; the mobile block is absent. No mobile-DOM assertions here (useless under the jsdom stub) — those live in Task 3's Playwright spec.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/pages/VMs.test.tsx && npx eslint src/pages/VMs.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - VMs.tsx contains both gates: the desktop JSX inside a max-md:hidden wrapper and the mobile block behind !isDesktop, in the same file
    - The mobile toolbar sets the same search/scheduleFilter/backupFilter state the desktop FilterPopover controls set (no second useState for either)
    - The trigger row's run deep-links into RunDetailSheet via onRun; the desktop trigger path is unchanged (existing tests green)
    - The schedule sheet hosts CadenceBuilder (no re-implemented cadence UI); the preview chip reads getScheduleNext() data and renders nothing when the domain entry is absent
    - Restore entry is a tonal/secondary row through RestoreAction + useConfirm; snapshot rows paginate via useLoadMore with the button only when hasMore
    - npx vitest run src/pages/VMs.test.tsx passes; tsc --noEmit and eslint clean
  </acceptance_criteria>
  <done>VMs is the proven tracer of the full stack: double gate, shared-state toolbar, card language, trigger deep-link, CadenceBuilder sheet, server-derived preview chip, tonal restore, load-more, FAB, honest gate-off — composed entirely from Wave-1 foundations.</done>
</task>

<task type="auto">
  <name>Task 2: Flash mobile block — hero card, snapshot pagination, zip-export sheet</name>
  <files>web/src/pages/Flash.tsx, web/src/pages/Flash.desktop.dom.test.tsx</files>
  <read_first>
    web/src/pages/Flash.tsx (WHOLE file — the desktop surface: hero status/last-backup data source, the py-1.5 snapshot rows, the zip-export toggle's current write path, flash.download/restoreNote usage, flash.loadBackupsFailed)
    web/src/pages/Settings.tsx lines 1180-1250 + 1700-1745 (queueSettingsWrite + savedBaseline + sendSettingsPatch — the write chain the zip toggle rides; re-fetch then merge-only-the-changed-field)
    web/src/pages/Dashboard.tsx lines 2139-2172 (MobileNextRunCard — the hero-card anatomy Flash's mirrors)
    .planning/phases/07-remaining-destinations-operational-parity/07-UI-SPEC.md (Screen Contract MORE-01a Flash; Copywriting Contract rows for flash.*; Color section anti-list — restore/download never accent)
    web/src/components/mobile/{BottomSheet,ListToolbar}.tsx, web/src/lib/useLoadMore.ts, web/src/components/mobile/Fab.tsx (foundation APIs)
  </read_first>
  <action>
    Apply the same D-01 double gate to Flash.tsx. Mobile block: (1) the hero card — flash status Badge (text label), last-backup line from the existing flash.* data, offsite indicator, and the existing flash.backupNow trigger with its current busy treatment (flash.backingUp); the FAB hosts the same primary action under material. (2) Snapshot rows — the existing rows re-presented at >=44px with the shared useLoadMore (button only when hasMore; identity-keyed; order preserved). (3) Secondary entries — flash.download and the restore note per the existing copy, rendered as tonal rows (flash restore is a download, never destructive); any destructive confirm goes through useConfirm -> ConfirmSheet. (4) The zip-export toggle opens a small fullHeight BottomSheet (title: the existing section key) hosting the SAME toggle control the desktop renders; on apply it writes via queueSettingsWrite + savedBaseline — re-fetch current settings, merge ONLY the zip field, send the patch, success toast per the standing rule, failure toast AND shake.

    Empty/error honesty: no snapshots -> the existing flash.none copy as an empty-state card; load failure -> the existing flash.loadBackupsFailed key + a >=44px retry row; backend error text verbatim. Gate-off (if Flash is gate-bound like the other domains — follow the desktop page's actual gating): the desktop gate copy as an outline card pointing at Settings.

    Create Flash.desktop.dom.test.tsx mirroring the VMs desktop-identity test: jsdom renders the desktop surface with its landmarks intact inside the max-md:hidden wrapper; the mobile block is absent; the existing desktop toggle behavior still fires through the desktop control.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/pages/Flash.desktop.dom.test.tsx && npx eslint src/pages/Flash.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Flash.tsx carries both gates in the same file; the mobile hero card, snapshot rows, and secondary entries render only below the breakpoint
    - Snapshot rows use the shared useLoadMore; no hand-rolled slicing; the Load-more button is absent at zero rows
    - The zip-export toggle write path goes through queueSettingsWrite + savedBaseline with a merge of only the changed field (no full-object PUT, no direct setSettings PUT)
    - Download/restore entries render tonal/secondary (no bg-accent/text-accentText on them); destructive confirms ride useConfirm
    - Flash.desktop.dom.test.tsx passes; tsc --noEmit and eslint clean
  </acceptance_criteria>
  <done>Flash reaches parity with the tracer: hero card, honest pagination, secondary restore/download language, and the settings write chain reused — not a second write path.</done>
</task>

<task type="auto">
  <name>Task 3: destination-vms-flash.spec.ts — staged mobile e2e with desktop dual-direction</name>
  <files>web/e2e/destination-vms-flash.spec.ts</files>
  <read_first>
    web/e2e/maquette-screens.spec.ts (the staging half: stageContainerDomain route mocks with Go-JSON fixtures, **/api/display-prefs* route.abort for localized scenarios, fresh-DB chrome-not-data contract)
    web/e2e/desktop-untouched.spec.ts lines 44-98 (the DESKTOP_PROJECTS set, ROUTES loop, desktop-marker + mobile-chrome-absence assertion style — the dual-direction pattern to mirror per route)
    web/e2e/mobile-shell.spec.ts (the mobile-project boot discipline: viewport, manual webServer note in playwright.config)
    .planning/phases/07-remaining-destinations-operational-parity/07-UI-SPEC.md (Verification table — Destination pages row; Interaction Contract rows for trigger/schedule/load-more)
    web/src/lib/api.ts (the wire shapes the fixtures must mirror field-for-field: ScheduleNext at :1949, the VMs/flash payloads)
  </read_first>
  <action>
    Create web/e2e/destination-vms-flash.spec.ts on the maquette staging model (route-level Go-JSON fixtures; NO real Docker/backend state — fresh DB can never hold a VM). Scenarios, all on a mobile project (and the dual-direction pair where noted):
    (1) /vms populated fixture: exactly one card per staged VM (count == fixture length), four-status badge text visible, offsite line where the fixture carries it; typing in the ListToolbar search filters the SAME state (card count drops) and the visible slice resets (stage >20 rows, assert initial window 20, press common.loadMore once, assert 40, no further button when exhausted).
    (2) Trigger deep-link: press a card trigger row, assert the async-start then RunDetailSheet opens when the correlated run reports (fixture the run payload), dismiss, assert no re-open on record refresh.
    (3) Schedule sheet: press the schedule row, assert the fullHeight sheet with the CadenceBuilder landmarks; apply, assert the preview chip text matches the staged getScheduleNext fixture (server-derived), and with an EMPTY schedule fixture assert no chip renders.
    (4) Gate-off: stage the gate-off settings payload on /vms, assert the gate copy outline card pointing at Settings and no trigger rows.
    (5) /flash: hero card fields from the fixture, snapshot rows paginate via Load more exactly as (1), the download entry renders (tonal — assert no accent class on it).
    (6) Desktop dual-direction (desktop-1280 + desktop-768): /vms and /flash render the desktop markers and NONE of the mobile chrome (no FAB, no mobile card stack, no Load more button) — the desktop-untouched assertion style per route.
    Windows discipline: manual webServer start (never webServer-managed on this machine), never pipe Playwright output through tail, clean up orphaned node processes if the run wedges.
  </action>
  <verify>
    <automated>cd web && npx playwright test e2e/destination-vms-flash.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - All six scenario groups pass on the mobile project(s) and the desktop dual-direction assertions pass on desktop-1280 and desktop-768
    - The spec stages every fixture at the route layer (no dependence on a real Docker/libvirt host) and aborts display-prefs for localized scenarios
    - Load-more assertions prove the constant threshold (20 -> 40) and the button's absence when exhausted; no auto-load occurs while scrolling
    - The gate-off scenario renders the outline card and no trigger rows
  </acceptance_criteria>
  <done>Both surfaces are e2e-proven in both directions: mobile cards/trigger/schedule/pagination/gate-off from staged fixtures, desktop byte-identity asserted per route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staged e2e fixtures | Route-mocked JSON crosses into the UI; fixtures mirror the Go wire shapes field-for-field but are test-only input |
| settings write path (Flash zip toggle) | A mobile editor writes to the shared settings row; a wrong merge shape would clobber concurrent desktop edits |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-07-07 | Tampering (settings clobber) | Flash zip-export toggle write | high | mitigate | The toggle rides queueSettingsWrite + savedBaseline with re-fetch then merge-ONLY-the-changed-field — the phase-6-save-bar contract that makes concurrent writes serialize; never a captured full-object PUT (acceptance criterion in Task 2) |
| T-07-08 | Tampering (desktop regression) | VMs/Flash double gate | medium | mitigate | Desktop JSX stays rendered inside max-md:hidden; existing VMs.test.tsx landmarks still pass; Task 3's desktop dual-direction asserts no mobile chrome at >=48rem |
| T-07-09 | Repudiation (unintended trigger) | per-card backup trigger rows | low | accept | Triggers reuse the existing BackupButton semantics including any consequence confirm the desktop already applies; no new destructive surface is introduced |
| T-07-10 | Information disclosure | card meta lines | low | accept | Cards render only fields the wire already carries to the desktop page (frozen-API honesty truth); no new data leaves the server |
| T-07-SC | Tampering (supply chain) | npm installs | low | accept | Zero package installs this phase (locked decision; Package Legitimacy Audit table: none) |
</threat_model>

<verification>
- `cd web && npx vitest run src/pages/VMs.test.tsx src/pages/Flash.desktop.dom.test.tsx` green
- `cd web && npx playwright test e2e/destination-vms-flash.spec.ts` green (manual webServer per the Windows discipline; never buffer output through a pipe)
- `cd web && npx tsc --noEmit -p tsconfig.json` green; eslint clean on both pages
- Source checks: no IntersectionObserver in either page; no second settings write path; restore/download entries carry no accent classes
</verification>

<success_criteria>
- VMs and Flash render the D-02 card language below 48rem behind the double gate, with trigger/schedule/restore entry points per MORE-01a
- Schedule previews are server-derived and refresh after the CadenceBuilder sheet closes (FLOW-01 for these domains)
- Lists paginate through the shared useLoadMore bound to the desktop-owned filter state (LISTS-01)
- Desktop >=48rem is byte-identical on both routes, proven in both directions
</success_criteria>

<output>
Create `.planning/phases/07-remaining-destinations-operational-parity/07-03-SUMMARY.md` when done
</output>
