---
phase: 07-remaining-destinations-operational-parity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/mobile/MobileSectionLabel.tsx
  - web/src/pages/Dashboard.tsx
  - web/src/lib/useLoadMore.ts
  - web/src/lib/useLoadMore.test.ts
  - web/src/components/mobile/ListToolbar.tsx
  - web/src/lib/i18n.ts
  - web/src/lib/locales/
autonomous: true
requirements: [LISTS-01]
user_setup: []

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "MobileSectionLabel lives in web/src/components/mobile/MobileSectionLabel.tsx as the single source (verbatim move of the Dashboard.tsx:2106-2115 function: h2 px-0.5 text-xs font-semibold uppercase tracking-[0.09em] text-carbon-textMuted, { t, labelKey } props with labelKey typed TranslationKey); Dashboard.tsx imports it back and every existing Dashboard usage renders identically — no second copy of the label markup exists anywhere"
    - "useLoadMore (web/src/lib/useLoadMore.ts) exposes loadMoreWindow(total, visible, threshold) as a pure function plus a useLoadMore hook returning { visible, showMore, hasMore, reset }: initial window 20, step 20, CONSTANT threshold (D-11 — never a growing or viewport-derived step); showMore appends exactly one threshold; the button is rendered by consumers ONLY when hasMore is true; NO IntersectionObserver, NO scroll listeners, NO auto-load anywhere in the implementation"
    - "Typing in a bound search or toggling a filter chip RESETS the visible slice to the initial window (UI-SPEC interaction contract) — the hook resets when the passed items array identity changes, and consumers pass the FILTERED array so slice and filter can never disagree"
    - "ListToolbar (web/src/components/mobile/ListToolbar.tsx) is the sticky-in-flow search + filter-chips composite (D-10 — sticky means sticky-in-flow, never position:fixed): a >=44px search input (placeholder via prop, existing domain key or pre-seeded common.search) plus a children slot for filter chips riding the existing FilterPopover/TapPopover primitives; it is sticky-in-flow inside the page column like StickyActionBar — NEVER position:fixed — and carries the why-comment echoing StickyActionBar.tsx:1-24"
    - "The i18n pre-seed lands EVERY new key this phase's plans need in ONE commit owned by this plan: common.loadMore (en \"Load more\" / de \"Mehr laden\"), common.search (en \"Search\" / de \"Suchen\"), settings.tabsNavigation (en \"Settings sections\" / de \"Einstellungsbereiche\"), config.restoreChain.title + config.restoreChain.step1..step5 (en copy verbatim from the UI-SPEC copywriting contract; de faithful translations in the house vocabulary, em dashes banned) — bound in en+de inline i18n.ts AND all 40 locale modules, parity + orphan tests green"
    - "The carried 06-UI-REVIEW fix for home.newBackupConfirm lands here (this plan owns i18n.ts in Wave 1): en aligns to the contracted sentence \"...stopped and restarted one at a time while their backup runs.\" and de to its faithful equivalent — the string moves to the contract, not the contract to the string (UI-SPEC Carried Fixes 3a); Dashboard.tsx also absorbs its 12px spacing normalization (gap-3/px-3/py-3 -> gap-2/gap-4/px-4/px-2) so the file is guard-ready when 07-08 extends the spacing sweep"
  artifacts:
    - path: "web/src/components/mobile/MobileSectionLabel.tsx"
      provides: "the promoted section-label component — single source for all six page mobile blocks (Dashboard imports it back)"
      contains: "MobileSectionLabel"
    - path: "web/src/lib/useLoadMore.ts"
      provides: "loadMoreWindow pure fn + useLoadMore hook: constant 20/20 window, hasMore, reset-on-filter"
      contains: "loadMoreWindow"
    - path: "web/src/lib/useLoadMore.test.ts"
      provides: "unit tests: initial window, step-20 append, hasMore boundaries at 0/1/n, reset on items identity change, never negative"
      contains: "loadMoreWindow"
    - path: "web/src/components/mobile/ListToolbar.tsx"
      provides: "sticky-in-flow search + chips composite composing FilterPopover/TapPopover; >=44px input; no fixed positioning"
      contains: "ListToolbar"
    - path: "web/src/lib/i18n.ts"
      provides: "the 10 pre-seeded keys in en+de inline + home.newBackupConfirm alignment; the wave's single i18n write point"
      contains: "common.loadMore"
    - path: "web/src/lib/locales/"
      provides: "the same 10 keys in all 40 locale modules (codemod approach, nav.mobileNavigation precedent) + home.newBackupConfirm alignment"
      contains: "loadMore"
    - path: "web/src/pages/Dashboard.tsx"
      provides: "MobileSectionLabel import-back (single source) + 12px spacing normalization in the existing mobile block"
      contains: "MobileSectionLabel"
  key_links:
    - from: "web/src/components/mobile/MobileSectionLabel.tsx"
      to: "web/src/pages/Dashboard.tsx"
      via: "verbatim extraction with import-back — behavior identical, one source (the phase 5 navModel single-registry discipline applied to a component)"
      pattern: "MobileSectionLabel"
    - from: "web/src/lib/useLoadMore.ts"
      to: "consumer plans 07-03/07-04/07-05/07-06"
      via: "THE one pagination mechanism — consumers render visible + the hasMore-gated button; no list in the phase may hand-roll slicing"
      pattern: "useLoadMore"
    - from: "web/src/components/mobile/ListToolbar.tsx"
      to: "web/src/components/FilterPopover.tsx + web/src/lib/filterLogLines consumers"
      via: "the composite hosts existing filter primitives and existing state — it owns no filter logic of its own"
      pattern: "FilterPopover"
    - from: "web/src/lib/i18n.ts (pre-seed)"
      to: "consumer plans 07-03..07-07"
      via: "Wave-1 single-owner i18n (phase 6 precedent): page plans consume pre-seeded + existing keys and declare the no-new-keys prohibition"
      pattern: "config.restoreChain"
  prohibitions:
    - statement: "No infinite scroll and no auto-load: no IntersectionObserver, no scroll-position listeners, no sentinel refs anywhere in useLoadMore or ListToolbar (D-11; REQUIREMENTS.md Out of Scope)"
      status: enforced
      verification: explicit
    - statement: "No position:fixed in ListToolbar — sticky-in-flow inside the page column only (phase 5/6 standing contract)"
      status: enforced
      verification: backstop
    - statement: "No second MobileSectionLabel implementation: the Dashboard copy is moved, not copied; after this plan a repo-wide search finds exactly one definition"
      status: enforced
      verification: explicit
    - statement: "Page plans (07-03..07-07) must not add i18n keys — this plan is the wave's single i18n writer; a consumer that genuinely lacks a key surfaces it in its summary rather than editing i18n.ts in parallel"
      status: enforced
      verification: backstop

---

<objective>
Build the shared mobile-block foundations every later plan composes: promote `MobileSectionLabel` out of Dashboard.tsx into components/mobile/ (single source for six page blocks), build the constant-threshold `useLoadMore` pagination primitive with its unit tests, build the sticky-in-flow `ListToolbar` composite, and land the phase's complete i18n pre-seed (10 new keys across all 42 tables) so the four parallel Wave-2 page plans can be key-neutral — the phase 6 single-owner pattern. Also absorb the Dashboard-carried 06-UI-REVIEW fixes (home.newBackupConfirm copy, Dashboard 12px spacing) while the file is open.

Purpose: LISTS-01's mechanism half and the D-01 card-language scaffolding exist before the page plans; the i18n pre-seed is what makes Wave 2 parallelizable at all.

Output: MobileSectionLabel promotion, useLoadMore + tests, ListToolbar, 10 pre-seeded keys x 42 tables, Dashboard fixes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-remaining-destinations-operational-parity/07-CONTEXT.md
@.planning/phases/07-remaining-destinations-operational-parity/07-RESEARCH.md
@.planning/phases/07-remaining-destinations-operational-parity/07-PATTERNS.md
@.planning/phases/07-remaining-destinations-operational-parity/07-UI-SPEC.md
@web/src/pages/Dashboard.tsx
@web/src/components/mobile/StickyActionBar.tsx
@web/src/components/FilterPopover.tsx
@web/src/lib/i18n.ts
</context>

<artifacts_section>
## Artifacts this phase produces

See 07-01-PLAN.md for the phase-wide symbol inventory. This plan creates: `MobileSectionLabel.tsx` (promoted), `useLoadMore.ts` + `useLoadMore.test.ts` (`loadMoreWindow`, `useLoadMore`), `ListToolbar.tsx` (`ListToolbar`), the i18n pre-seed keys (`common.loadMore`, `common.search`, `settings.tabsNavigation`, `config.restoreChain.title`, `config.restoreChain.step1`..`step5`), and the `home.newBackupConfirm` alignment.
</artifacts_section>

<tasks>

<task type="tracer">
  <name>Task 1: MobileSectionLabel promotion + Dashboard carried fixes</name>
  <files>web/src/components/mobile/MobileSectionLabel.tsx, web/src/pages/Dashboard.tsx</files>
  <read_first>
    web/src/pages/Dashboard.tsx lines 2090-2120 (the mobile-block mount-discipline comment + the MobileSectionLabel function to promote verbatim — note the props shape { t, labelKey } and the TranslationKey typing)
    web/src/pages/Dashboard.tsx (WHOLE file skim for: every MobileSectionLabel call site, the home.newBackupConfirm trigger copy usage, and the 12px spacing values (gap-3/px-3/py-3 family) in the mobile block that must normalize to the 8/16 stops)
    .planning/phases/07-remaining-destinations-operational-parity/07-PATTERNS.md (Section label to promote — the verbatim source block)
    .planning/phases/07-remaining-destinations-operational-parity/07-UI-SPEC.md (Carried Phase 6 Review Fixes table — fixes 2 and 3a contracts)
    web/src/lib/i18n.ts (home.newBackupConfirm en+de current strings — Task 3 owns the edit, this task verifies the Dashboard usage needs no call-site change)
  </read_first>
  <action>
    Promote MobileSectionLabel: create web/src/components/mobile/MobileSectionLabel.tsx exporting the function VERBATIM from Dashboard.tsx:2106-2115 (same props, same TranslationKey import path adjusted to the component's location, same classes). Edit Dashboard.tsx to import it from ../components/mobile/MobileSectionLabel (or the correct relative path) and delete the local definition — the phase 5 navModel documented-exception pattern (components import for a single source) applies; carry a one-line comment noting the promotion. All existing call sites keep working unchanged (props unchanged).

    While Dashboard.tsx is open, absorb its two carried fixes (CONTEXT.md discretion, UI-SPEC Carried Fixes table): (1) normalize the 12px spacing stops in the mobile block — every gap-3/px-3/py-3/pt-3/pb-3/p-3 becomes the nearest 8/16 stop per the UI-SPEC scale decision (in-card element spacing -> gap-2/p-2; card padding and card gaps -> p-4/gap-4); desktop JSX is NOT touched (desktop byte-identity is guarded by the e2e battery). (2) Confirm the home.newBackupConfirm usage needs no call-site change for the Task 3 copy alignment (the trigger renders t("home.newBackupConfirm") verbatim — it does; note this in the task summary so Task 3's key edit is provably safe). Do NOT touch font weights here beyond what normalization requires — the weight sweep is 07-08's guard over RunDetailSheet.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/pages/Dashboard.protectionCard.dom.test.tsx src/pages/Dashboard.ransomwareCard.dom.test.tsx && npx eslint src/components/mobile/MobileSectionLabel.tsx src/pages/Dashboard.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - MobileSectionLabel.tsx exists in web/src/components/mobile/ and Dashboard.tsx has no local MobileSectionLabel definition (exactly one definition repo-wide)
    - Dashboard's mobile block renders identically (existing Dashboard dom tests green; the promotion is a move, not a rewrite)
    - The Dashboard mobile block contains no gap-3/px-3/py-3/pt-3/pb-3/p-3 utility classes (normalized to the 8/16 stops)
    - tsc --noEmit and eslint clean on both files
  </acceptance_criteria>
  <done>The section label is a single shared component; the Dashboard carried fixes are absorbed; every page plan can now compose the card language from components/mobile/.</done>
</task>

<task type="auto">
  <name>Task 2: useLoadMore constant-threshold pagination + ListToolbar composite</name>
  <files>web/src/lib/useLoadMore.ts, web/src/lib/useLoadMore.test.ts, web/src/components/mobile/ListToolbar.tsx</files>
  <read_first>
    web/src/lib/activityLog.ts (filterLogLines at :764 and the file's extractable-pure-fn style with its test conventions — the house model for a lib pure fn + node-env tests)
    web/src/components/mobile/StickyActionBar.tsx lines 1-24 (sticky-in-flow discipline + why-comment to echo)
    web/src/pages/VMs.tsx lines 1825-1859 (the desktop search + chips controls this toolbar mirrors — input classes glim-field-focus, ChipFilter usage, FilterPopover props)
    web/src/components/FilterPopover.tsx + web/src/components/mobile/TapPopover.tsx (the primitives ListToolbar composes — it owns no filter logic)
    .planning/phases/07-remaining-destinations-operational-parity/07-UI-SPEC.md (LISTS-01 pattern: toolbar + load-more contract, Dashboard recent-runs exemption)
  </read_first>
  <action>
    useLoadMore.ts: export the pure fn `loadMoreWindow(total: number, visible: number, threshold: number): number` returning the next visible count (min(total, visible + threshold), clamped at >= 0) and the hook `useLoadMore<T>(items: T[], threshold = 20)` returning `{ visible: T[], showMore: () => void, hasMore: boolean, reset: () => void }`. Initial visible count = threshold (20); showMore adds exactly one threshold via the pure fn; hasMore = visible.length < items.length; the hook resets the visible count to the initial window when the items array IDENTITY changes (consumers pass the filtered array, so a new filter result resets the slice — the UI-SPEC interaction contract). Implementation notes in the why-comment: constant threshold per D-11, client-side only because listRuns() takes no params and api.ts is frozen, no IntersectionObserver/scroll listener ever (Out of Scope). useLoadMore.test.ts (node-env, model: activityLog test style): initial window, append semantics, hasMore at 0/1/exactly-n/over-n, reset on identity change, empty array, threshold boundary math via loadMoreWindow directly.

    ListToolbar.tsx: props-closed composite — `{ search, onSearch, placeholder, children }` (search value + handler lifted; placeholder is a translation KEY string from the consumer; children = the filter chips slot). Renders a sticky-in-flow row (the StickyActionBar positioning discipline — sticky within the page column, a direct child of the page scroller structure, NEVER position:fixed) holding a >=44px search input (existing field classes: rounded-control bg-carbon-surface2 text-carbon-text text-sm px-3→px-2/py adjusted to the 8/16 stops, glim-field-focus — copy VMs.tsx:1831-1833's input styling normalized to the spacing stops) and the children chips row (gap-2, horizontally scrollable if overflow, never wrapping). Why-comment echoes StickyActionBar.tsx:1-24. No i18n keys of its own (placeholder arrives via prop). A small dom test is optional here — the consumers' e2e in 07-03/07-06 exercises it on real surfaces; if you add one, assert the input forwards typing and the chips slot renders children.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/lib/useLoadMore.test.ts && npx eslint src/lib/useLoadMore.ts src/components/mobile/ListToolbar.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - loadMoreWindow is pure and exported; useLoadMore returns visible/showMore/hasMore/reset with initial 20, step 20
    - Unit tests pass covering append, boundaries, reset-on-identity-change, empty input
    - No IntersectionObserver, scroll listener, or sentinel ref exists in useLoadMore.ts or ListToolbar.tsx
    - ListToolbar input is >=44px tall, uses glim-field-focus + semantic tokens, and renders the children slot; no position:fixed utility in the file
  </acceptance_criteria>
  <done>The pagination primitive and the sticky toolbar exist with the window math unit-proven — lists across plans 07-03..07-06 compose them instead of re-deriving.</done>
</task>

<task type="auto">
  <name>Task 3: i18n pre-seed — the phase's complete new-key set across all 42 tables</name>
  <files>web/src/lib/i18n.ts, web/src/lib/locales/</files>
  <read_first>
    web/src/lib/i18n.ts (en + de inline table structure; the home.newBackupConfirm current strings; the common.* section layout; grep the de table for the existing vocabulary of "snapshot", "restore", "settings" to mirror house translations)
    web/src/lib/locales/ (one module to model the sweep on, e.g. de's siblings — the nav.mobileNavigation codemod precedent from phase 6: one-shot addition of the same keys to all 40 modules)
    .planning/phases/07-remaining-destinations-operational-parity/07-UI-SPEC.md (Copywriting Contract — the verbatim en copy for every new key incl. the five restore-chain steps)
    web/src/app/ or src/lib parity/orphan test files (the 42-table parity + orphan tests that fence this sweep — run them as the gate)
  </read_first>
  <action>
    Add EXACTLY these keys (the UI-SPEC Component Inventory's complete new-key list) to the en table in i18n.ts, then the de table with faithful translations, then all 40 locale modules in web/src/lib/locales/ (one-shot codemod in the nav.mobileNavigation style; em dashes banned in ALL user text; do not leave English untranslated in any module):

    - common.loadMore: en "Load more" / de "Mehr laden" (invariant — no plural fork)
    - common.search: en "Search" / de "Suchen" (shared list-search placeholder for lists without a domain key)
    - settings.tabsNavigation: en "Settings sections" / de "Einstellungsbereiche" (aria-label for the mobile chip strip)
    - config.restoreChain.title: en "What happens when the config is restored"
    - config.restoreChain.step1: en "Your current settings choice is saved first."
    - config.restoreChain.step2: en "The latest config snapshot is restored from the repository."
    - config.restoreChain.step3: en "If the APP_KEY does not match the snapshot, you are asked before anything else happens."
    - config.restoreChain.step4: en "The restored configuration is checked against the live system."
    - config.restoreChain.step5: en "Enabled apps and services are restarted and the page reloads. With auto-restart off, you restart them yourself."

    Keep the technical token APP_KEY as-is inside the de step3 translation (check how existing de config.*/settings.* strings treat it and mirror that). Then align home.newBackupConfirm (Carried Fix 3a): en becomes the contracted sentence ending "...stopped and restarted one at a time while their backup runs." (keep the existing sentence's opening context intact — the contract governs the divergent clause), de gets the faithful matching translation with house vocabulary; every locale module's home.newBackupConfirm gets the corresponding faithful translation of the corrected sentence. No other keys change. Run the parity + orphan tests as the gate — they fail on any missed table.
  </action>
  <verify>
    <automated>cd web && npx vitest run --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - All 10 new keys exist in en, de, and every module under web/src/lib/locales/ (42 tables total counting the inline en+de pair) with no English left untranslated in non-locale-modules' languages
    - home.newBackupConfirm en matches the contracted clause "stopped and restarted one at a time while their backup runs." and de is its faithful translation
    - No em dashes in any new/edited user-visible string
    - The full vitest suite passes — parity and orphan tests green
  </acceptance_criteria>
  <done>Every i18n key the phase needs is bound across all 42 tables in one commit owned by this plan — the Wave-2 page plans are provably key-neutral and safely parallel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| locale module content | Codemod-written translation strings cross into the rendered UI; malformed entries would render raw or break the parity gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Tampering (i18n drift) | locales/ codemod sweep | medium | mitigate | The parity + orphan tests fence the sweep — a missed or malformed table fails the suite this task runs as its gate; no key ships half-bound |
| T-07-05 | Tampering (desktop regression) | Dashboard spacing normalization | medium | mitigate | Only the mobile block's utility classes change; the desktop grid is untouched and the desktop-untouched battery (extended in 07-08) re-proves byte-identity |
| T-07-06 | Information disclosure | useLoadMore / ListToolbar | low | accept | Pure slicing math and a text input over existing state — no data crosses a boundary the desktop controls didn't already cross |
| T-07-SC | Tampering (supply chain) | npm installs | low | accept | Zero package installs this phase (locked decision) |
</threat_model>

<verification>
- `cd web && npx vitest run src/lib/useLoadMore.test.ts` green; full `npx vitest run --reporter=dot` green (parity/orphan fence)
- `cd web && npx tsc --noEmit -p tsconfig.json` green; eslint clean on every touched file
- Repo-wide: exactly one MobileSectionLabel definition; zero IntersectionObserver/scroll-listener references in the new files
</verification>

<success_criteria>
- The card-language scaffolding (section label, load-more, toolbar) is single-source under components/mobile/ and lib/
- The i18n pre-seed is complete and fenced — Wave 2 runs four parallel page plans with zero i18n write conflicts
- Dashboard carries its two absorbed 06-UI-REVIEW fixes
</success_criteria>

<output>
Create `.planning/phases/07-remaining-destinations-operational-parity/07-02-SUMMARY.md` when done
</output>
