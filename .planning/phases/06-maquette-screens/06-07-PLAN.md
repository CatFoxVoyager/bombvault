---
phase: 06-maquette-screens
plan: 07
type: execute
wave: 5
depends_on: ["06-01", "06-02", "06-03", "06-04", "06-05", "06-06"]
files_modified:
  - web/src/components/mobile/BottomNav.tsx
  - web/src/lib/i18n.ts
  - web/src/lib/locales/
  - web/e2e/desktop-untouched.spec.ts
  - web/dist
autonomous: true
requirements: [SCRN-01, SCRN-02, SCRN-03, SCRN-04, SCRN-05, PRIM-02, PRIM-03, PRIM-04, FLOW-03]
user_setup: []

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The mobile <nav> landmark in BottomNav.tsx carries an aria-label from the new i18n key (nav.mobileNavigation, en \"Mobile navigation\" / de equivalent) — absorbed 05-UI-REVIEW finding 2, one-line i18n addition + attribute"
    - "The desktop-untouched e2e loop gains a max-md leakage pass: at >=48rem the three maquette pages (Dashboard, Containers, Files) assert the ABSENCE of mobile-only chrome — sticky action bars, the stacked container detail, the filled full-width New-backup trigger, and the mobile block restructure — while the existing 10-route loop keeps passing untouched"
    - "Phase close gate: cd web && npm ci && npm run build is green (tsc --noEmit && vite build) and web/dist is committed — the binary embeds web/dist, so a stale build embeds a stale SPA"
    - "Phase close gate: the full vitest suite and the Playwright suite (all four projects, including the phase's five new specs and the de/fr narrow-viewport backstop from Phase 5) are green"
    - "All phase-touched files pass the eslint house rules (8 bombvault/* rules); no disable comments — any legitimate exception is declared in web/eslint.config.js"
  artifacts:
    - path: "web/src/components/mobile/BottomNav.tsx"
      provides: "labeled nav landmark (finding 2 absorbed — the last open 05-UI-REVIEW finding in Phase 6's reuse set; finding 3/Login stays a Phase 5 surface finding by the UI-SPEC's own record)"
      contains: "aria-label"
    - path: "web/e2e/desktop-untouched.spec.ts"
      provides: "the max-md leakage loop for the three maquette pages at >=48rem"
      contains: "max-md"
    - path: "web/dist"
      provides: "the committed SPA build embedded by the Go binary (CLAUDE.md web build gate)"
      contains: "index.html"
    - path: "web/src/lib/i18n.ts"
      provides: "nav.mobileNavigation in en + de + all 40 locale modules"
      contains: "nav.mobileNavigation"
  key_links:
    - from: "web/e2e/desktop-untouched.spec.ts"
      to: "the three maquette pages"
      via: "the >=48rem loop asserts absence of every mobile-only affordance this phase added — the milestone's desktop-untouched contract is enforced per-page"
      pattern: "desktop-untouched"
    - from: "web/dist"
      to: "web/embed.go (go:embed all:dist)"
      via: "commit the built SPA so the binary serves this phase's screens"
      pattern: "embed"
  prohibitions:
    - statement: "Desktop >=48rem renders byte-identically on every surface — any mobile-only markup must be max-md: scoped or useIsDesktop-branched, never leaked"
      status: enforced
      verification: explicit
    - statement: "No eslint disable comments — exceptions go in web/eslint.config.js only (house rule)"
      status: enforced
      verification: backstop
    - statement: "No new npm dependencies (locked presentation-only milestone; npm ci must not change package-lock.json)"
      status: enforced
      verification: backstop
    - statement: "Frozen surfaces untouched through the whole phase: router.tsx, web/src/lib/api.ts, web/src/lib/progress.ts, internal/**"
      status: enforced
      verification: explicit

---

<objective>
Close Phase 6: absorb the last in-scope 05-UI-REVIEW finding (labeled mobile nav landmark), extend the desktop-untouched e2e loop with the max-md leakage pass over the three maquette pages, and run the phase-close gate — full suites green, web build committed to web/dist.

Purpose: the phase's contract is "mobile works, desktop untouched" — this plan is where both halves are proven together and the embedded SPA is refreshed so the binary ships the new screens.

Output: labeled BottomNav nav, max-md leakage e2e, committed web/dist, green full suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-maquette-screens/06-CONTEXT.md
@.planning/phases/06-maquette-screens/06-RESEARCH.md
@.planning/phases/06-maquette-screens/06-UI-SPEC.md
@.planning/phases/05-mobile-shell-navigation-foundation/05-UI-REVIEW.md
@web/e2e/desktop-untouched.spec.ts
@web/src/components/mobile/BottomNav.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Labeled mobile nav landmark (absorbed 05-UI-REVIEW finding 2)</name>
  <files>web/src/components/mobile/BottomNav.tsx, web/src/lib/i18n.ts, web/src/lib/locales/</files>
  <read_first>
    web/src/components/mobile/BottomNav.tsx line ~92 (the <nav> element — finding 2's site)
    .planning/phases/05-mobile-shell-navigation-foundation/05-UI-REVIEW.md finding 2 (the recorded defect)
    web/src/lib/i18n.ts (en/de inline blocks + the parity pipeline)
  </read_first>
  <action>
    Add the nav.mobileNavigation key (en "Mobile navigation", de "Mobile Navigation") to en + de inline blocks and all 40 locale modules, and set aria-label={t("nav.mobileNavigation")} on the BottomNav <nav> element. Nothing else in BottomNav changes.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/lib/i18n.parity.test.ts src/lib/i18n.orphans.test.ts src/components/mobile/ && npx eslint src/components/mobile/BottomNav.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The BottomNav <nav> carries aria-label from nav.mobileNavigation
    - The key exists in en, de, and all 40 locale modules; parity + orphans green
  </acceptance_criteria>
  <done>The mobile navigation landmark is named for assistive technology; the last in-scope 05-UI-REVIEW finding is absorbed.</done>
</task>

<task type="auto">
  <name>Task 2: max-md leakage pass in the desktop-untouched loop</name>
  <files>web/e2e/desktop-untouched.spec.ts</files>
  <read_first>
    web/e2e/desktop-untouched.spec.ts (the existing parameterized 10-route loop — extend, never weaken)
    .planning/phases/06-maquette-screens/06-UI-SPEC.md Verification table (Desktop-untouched row)
  </read_first>
  <action>
    Extend desktop-untouched.spec.ts with a desktop-width (>=48rem) pass over /, /containers, and /files asserting the ABSENCE of Phase 6 mobile chrome: no sticky action bar (StickyActionBar classes), no stacked container detail (no back-row affordance), no full-width filled New-backup trigger, and the dashboard's desktop grid structure intact. Assert by absence of the mobile-only selectors/classes (role/text-based where possible so restyles stay honest). The existing 10-route loop and its assertions remain exactly as they are.
  </action>
  <verify>
    <automated>cd web && npx playwright test e2e/desktop-untouched.spec.ts --project=desktop-1280 --project=desktop-768</automated>
  </verify>
  <acceptance_criteria>
    - The extended loop passes at both desktop projects
    - The pre-existing 10-route assertions are unchanged in the diff
    - A mobile-only affordance is asserted absent on all three maquette pages at >=48rem
  </acceptance_criteria>
  <done>Desktop-untouched is enforced per-page for everything Phase 6 added — a max-md leak fails CI.</done>
</task>

<task type="auto">
  <name>Task 3: Phase close — full suites, web build, commit web/dist</name>
  <files>web/dist</files>
  <read_first>
    CLAUDE.md (Build/test section: web build gate — npm ci && npm run build, commit web/dist; Go chain for the pre-push hook)
    .planning/phases/06-maquette-screens/06-VALIDATION.md (sampling rates: phase gate = full suite green + web/dist committed)
    web/package.json (build = tsc --noEmit && vite build)
  </read_first>
  <action>
    Run the phase gate in order: (1) cd web && npx vitest run (full suite); (2) the full Playwright suite across all four projects — local-run caveat per STATE.md: start the webServer manually on Windows if teardown hangs (CI boots its own via reuseExistingServer: !CI); include the Phase 5 narrow-viewport de/fr backstop spec; (3) cd web && npm ci && npm run build; (4) commit web/dist with the phase-close commit; (5) confirm package-lock.json is unchanged (zero-dependency contract) and no frozen file (router.tsx, api.ts, progress.ts, internal/**) appears in the phase diff. Judge SCRN-05 under the recorded substitution contract, NOT the REQUIREMENTS verbatim wording: the UI-SPEC Frozen-API Data Adaptations table (restated as 06-04's recorded deviation) substitutes honest stat tiles fed only by frozen-Run fields + the buildLogLines activity log for the per-file new/changed/unchanged triad — the gate passes on the substitutes being present and honest, and FAILS only if fabricated per-file counts appear. If any gate fails, fix before closing — do not weaken an assertion to pass.
  </action>
  <verify>
    <automated>cd web && npx vitest run && npm run build && git status --porcelain web/dist | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Full vitest suite green; Playwright suite green on all four projects including e2e/touch-tree.spec.ts, e2e/tap-popovers.spec.ts, e2e/run-detail-visibility.spec.ts, e2e/maquette-screens.spec.ts, e2e/home-trigger.spec.ts, and the extended desktop-untouched loop
    - npm run build green and web/dist committed
    - package-lock.json unchanged; no frozen path in the phase diff
  </acceptance_criteria>
  <done>Phase 6 is closed green: every screen and primitive tested, desktop untouched proven, and the embedded SPA refreshed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate → main | The e2e loop is the regression guard for the desktop-untouched contract |
| web/dist → binary | The embedded build is what users actually run |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering (stale embedded SPA) | web/dist vs web/src | medium | mitigate | The phase-close gate rebuilds and commits web/dist after all source changes (CLAUDE.md web build gate) |
| T-06-SC | Tampering (supply chain) | npm ci | low | accept | Zero new dependencies (locked); package-lock.json must be unchanged — verified in Task 3 |
| T-06-05 | Tampering (regression reintroduction) | eslint house rules | medium | mitigate | Full eslint pass over phase-touched files; exceptions only via web/eslint.config.js, never disable comments |
</threat_model>

<verification>
- `cd web && npx vitest run` green (full suite)
- `cd web && npx playwright test` green (all four projects; manual webServer start on Windows if needed)
- `cd web && npm run build` green; web/dist committed
- `git diff --name-only` over the phase shows no router.tsx / api.ts / progress.ts / internal/** entries and no package-lock.json change
</verification>

<success_criteria>
- Phase 6 seals with: all five screen/primitive e2e specs green, desktop-untouched loop extended and green, nav landmark labeled, web/dist current
- The milestone's desktop-untouched contract is enforced per-page for every surface this phase touched
</success_criteria>

<specless_note>
Phase-level prohibitions carried here as the closing gate (all descriptor-less, enforced): no confirm-in-confirm chains; no infinite scroll; no horizontal-scroll tables below the breakpoint; no position:fixed chrome; no raw design-bible hex (semantic tokens only); no status colors on interactive controls (the sanctioned confirm danger button excepted); every user-visible string through t() with no em dashes in user text; frozen files untouched; zero new npm dependencies; desktop >=48rem byte-identical. UI-SPEC "UI Considerations" backstop rows that resolve at phase level (touch-target geometry via the plan e2e bounding-box suites; visibility reconcile via run-detail-visibility.spec.ts; narrow-viewport de/fr via the Phase 5 sweep staying green) verify here.
</specless_note>

<output>
Create `.planning/phases/06-maquette-screens/06-07-SUMMARY.md` when done
</output>
