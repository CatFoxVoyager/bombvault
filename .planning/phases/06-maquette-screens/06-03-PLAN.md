---
phase: 06-maquette-screens
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/mobile/TapPopover.tsx
  - web/src/components/mobile/TapPopover.dom.test.tsx
  - web/src/components/InfoBubble.tsx
  - web/src/components/FilterPopover.tsx
  - web/src/components/ColorPickerPopover.tsx
  - web/e2e/tap-popovers.spec.ts
autonomous: true
requirements: [PRIM-02]
user_setup: []

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "TapPopover is a hand-rolled primitive in web/src/components/mobile/TapPopover.tsx: the trigger reports aria-expanded + aria-haspopup=\"dialog\" (the FilterPopover/ColorPickerPopover precedent pair, ColorPickerPopover.tsx:386-394) and the panel is role=\"dialog\" with an accessible name, positioned via computeBubblePosition from web/src/lib/bubblePosition.ts (clamp-then-flip, BUBBLE_VIEWPORT_MARGIN = 8) so it flips at viewport edges"
    - "Dismissal has three paths: tap on a transparent full-viewport event-consuming backdrop (pointer events land on the backdrop layer, never click through to content beneath), a document-level Escape keydown, and re-tapping the trigger; there is NO focus trap (D-09: a popover is not a modal)"
    - "Focus handling is light per D-09: focus moves into the panel on open and restores to the trigger element on every dismissal path (BottomSheet capture pattern, capture strictly before the focus move)"
    - "InfoBubble opens on tap below the breakpoint via useTipBubble's existing manual show()/hide() API (useTipBubble.tsx:76-77, 95-101, 190-192); its desktop hover + focus behavior is byte-identical (hover: stays @media (hover: hover)-gated repo-wide)"
    - "FilterPopover and ColorPickerPopover present their panels through TapPopover below the breakpoint (anchored + dismissal/focus parity — their in-flow absolute panels can clip at phone screen edges); their desktop presentation and interaction are unchanged"
    - "Popover triggers are >=44px touch targets below the breakpoint; no NEW hover-dependent affordance is introduced (VERIFY-04)"
  artifacts:
    - path: "web/src/components/mobile/TapPopover.tsx"
      provides: "PRIM-02 anchored tap-popover primitive: aria trigger pair, computeBubblePosition anchoring, transparent consuming backdrop, Escape + outside-tap + re-tap dismissal, light focus handling"
      contains: "computeBubblePosition"
    - path: "web/src/components/mobile/TapPopover.dom.test.tsx"
      provides: "jsdom tests: open on tap, aria state, all three dismissal paths, focus restore to trigger, no tab containment (no trap)"
      contains: "TapPopover"
    - path: "web/src/components/InfoBubble.tsx"
      provides: "tap migration: the (i) bubble opens on touch via the hook's manual API; desktop hover untouched"
      contains: "show"
    - path: "web/src/components/FilterPopover.tsx"
      provides: "anchored mobile presentation through TapPopover below the breakpoint; desktop unchanged"
      contains: "TapPopover"
    - path: "web/src/components/ColorPickerPopover.tsx"
      provides: "anchored mobile presentation through TapPopover below the breakpoint; drag surface + desktop unchanged"
      contains: "TapPopover"
    - path: "web/e2e/tap-popovers.spec.ts"
      provides: "Playwright coverage: tap opens, outside-tap and Escape dismiss, focus restores; desktop project asserts hover behavior intact"
      contains: "tap"
  key_links:
    - from: "web/src/components/mobile/TapPopover.tsx"
      to: "web/src/lib/bubblePosition.ts"
      via: "THE ONE positioning maths (clamp-then-flip over BUBBLE_VIEWPORT_MARGIN 8) — never a second flip/clamp implementation"
      pattern: "computeBubblePosition"
    - from: "web/src/components/mobile/TapPopover.tsx"
      to: "web/src/lib/useTipBubble.tsx + web/src/components/FilterPopover.tsx precedents"
      via: "aria-expanded/hasOwnProperty dialog trigger pair + measure-then-place useLayoutEffect + document-level Escape (useConfirm.tsx:118-149 pattern) — lifted, not re-invented"
      pattern: "aria-haspopup"
    - from: "web/src/components/InfoBubble.tsx"
      to: "web/src/lib/useTipBubble.tsx show()/hide()"
      via: "the hook's documented manual API is the sanctioned tap-migration entry point — no second bubble implementation"
      pattern: "useTipBubble"
  prohibitions:
    - statement: "TapPopover must not focus-trap (no Tab containment cycling) — D-09 light focus handling only; a trap makes Escape/outside-tap semantics modal for no reason"
      status: enforced
      verification: explicit
    - statement: "Desktop hover behavior is untouched: no existing hover: handler is removed or gated off; hover: stays @media (hover: hover)-gated repo-wide"
      status: enforced
      verification: backstop
    - statement: "The backdrop is visually transparent — no scrim tint; a popover is not a modal (D-09)"
      status: enforced
      verification: backstop
    - statement: "No new positioning maths outside computeBubblePosition and no new npm dependencies"
      status: enforced
      verification: backstop
    - statement: "No new i18n keys in this plan — i18n.ts + locales/ are single-owner in Wave 1 (06-01 is adding common.expand/common.collapse to all 40 modules in parallel); the panel's accessible name arrives via the consumer's aria-label prop and every consumer string is an existing key, so the parity sweep never has two writers"
      status: enforced
      verification: backstop

---

<objective>
Build PRIM-02: the anchored tap-popover primitive that gives every hover-dependent affordance a touch path below the breakpoint, and migrate the three named consumers — InfoBubble (dead on touch today), FilterPopover, ColorPickerPopover — while leaving desktop hover behavior byte-identical (D-09).

Purpose: info bubbles, the container filter, and accent-color pickers are unreachable on a phone today (hover has no touch equivalent). One primitive + three migrations closes that gap for the maquette screens without a UI kit and without touching the frozen chrome contract.

Output: TapPopover + dom tests, three consumer migrations, tap-popovers e2e on the mobile projects.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-maquette-screens/06-CONTEXT.md
@.planning/phases/06-maquette-screens/06-RESEARCH.md
@.planning/phases/06-maquette-screens/06-PATTERNS.md
@.planning/phases/06-maquette-screens/06-UI-SPEC.md
@web/src/lib/bubblePosition.ts
@web/src/lib/useTipBubble.tsx
@web/src/components/ColorPickerPopover.tsx
</context>

<tasks>

<task type="tracer">
  <name>Task 1: TapPopover primitive end-to-end — trigger, anchored panel, dismissal, focus restore</name>
  <files>web/src/components/mobile/TapPopover.tsx, web/src/components/mobile/TapPopover.dom.test.tsx</files>
  <read_first>
    web/src/lib/bubblePosition.ts (WHOLE file — computeBubblePosition signature, BUBBLE_VIEWPORT_MARGIN 8, clamp-then-flip contract, its pure/DOM-free test style)
    web/src/components/ColorPickerPopover.tsx lines 380-410 (the trigger aria pair + portal + positioned panel precedent)
    web/src/lib/useTipBubble.tsx lines 76-77, 95-101, 120-154 (manual show()/hide(), measure-then-place useLayoutEffect, Escape + scroll dismissal effect)
    web/src/lib/useConfirm.tsx lines 118-149 (document-level Escape pattern)
    web/src/components/mobile/BottomSheet.tsx lines 100-112 (capture-then-focus open effect — the focus-restore shape to lift, minus the trap)
    .planning/phases/06-maquette-screens/06-RESEARCH.md Pattern 5 (TapPopover anatomy) + anti-patterns (focus-trapping the popover)
  </read_first>
  <action>
    Per D-09: hand-roll TapPopover in web/src/components/mobile/TapPopover.tsx (BottomSheet's directory precedent). Anatomy: (1) the trigger renders as a button carrying aria-expanded={open} and aria-haspopup="dialog"; (2) the panel is role="dialog" with an accessible name (aria-label prop from the consumer), portal-rendered to document.body (ColorPickerPopover portal precedent — escapes ancestor CSS transforms), positioned in a measure-then-place useLayoutEffect via computeBubblePosition(triggerRect, panelRect, viewport, BUBBLE_VIEWPORT_MARGIN) so it flips at viewport edges; (3) a transparent full-viewport backdrop sits UNDER the panel and consumes outside taps (its onClick closes; the target === currentTarget guard pattern from BottomSheet's scrim applies to the backdrop layer) — visually transparent, NOT bg-black/60; (4) document-level Escape closes; (5) re-tapping the trigger toggles closed; (6) light focus: on open capture the trigger, move focus into the panel's first focusable content (or the panel itself with tabIndex -1); on ANY dismissal restore focus to the captured trigger; NO Tab containment cycling. Motion: reuse the existing glim-fade class precedent for the panel entrance; keep it motion-safe-gated like the sheet's transition. Every user-visible string via t(); semantic tokens only. Write TapPopover.dom.test.tsx covering: open on trigger tap with aria-expanded true; panel role dialog with the accessible name; outside-tap closes AND does not deliver the click to background content (assert a background control's click handler does not fire); Escape closes; re-tap toggles; focus restores to the trigger on every dismissal path; Tab is NOT trapped (focus can leave the panel to outside content).
  </action>
  <verify>
    <automated>cd web && npx vitest run src/components/mobile/TapPopover.dom.test.tsx src/lib/bubblePosition.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - TapPopover.tsx exists in web/src/components/mobile/ and calls computeBubblePosition (no inline flip/clamp maths)
    - Trigger carries aria-expanded + aria-haspopup="dialog"; panel is role="dialog" with an accessible name
    - Dom tests pass: open on tap, all three dismissal paths, focus restore, no focus trap, outside-tap consumed by the backdrop layer
  </acceptance_criteria>
  <done>The popover primitive works end-to-end in jsdom: tap-anchored, edge-flipping, three dismissal paths, light focus — with zero new dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: InfoBubble tap migration + FilterPopover adoption</name>
  <files>web/src/components/InfoBubble.tsx, web/src/components/FilterPopover.tsx</files>
  <read_first>
    web/src/components/InfoBubble.tsx (WHOLE file — the hover/focus bubble and its useTipBubble wiring; note the portal + onAccent prop contract)
    web/src/lib/useTipBubble.tsx lines 76-77, 95-101, 190-192 (show()/hide() manual API — "open/close by hand, for a trigger that needs its own extra reason")
    web/src/components/FilterPopover.tsx (WHOLE file — trigger, in-flow absolute panel, existing open/close state, FilterPopover.tsx:53-89 trigger/panel labelling)
    .planning/phases/06-maquette-screens/06-UI-SPEC.md PRIM-02 section (consumers this phase)
  </read_first>
  <action>
    InfoBubble (the MUST per UI-SPEC — hover+focus is dead on touch today): keep the desktop hover + focus-open behavior exactly as is; ADD a tap path below the breakpoint that calls the hook's existing show()/hide() manual API on click of the (i) control (gate with useIsCoarsePointer — capability, not width, consistent with D-11's axis separation). The bubble content, tokens, and positioning stay the hook's own — do not move InfoBubble onto TapPopover; the hook IS the sanctioned migration for this consumer. The (i) trigger becomes a >=44px touch target below the breakpoint.

    FilterPopover: below the breakpoint, render its panel content through TapPopover (anchored at the trigger, dismissal/focus parity) instead of the in-flow absolute panel that clips at phone screen edges; keep the panel CONTENT and all filter state logic untouched. Desktop: the existing presentation renders byte-identically. The trigger keeps its aria-expanded/aria-haspopup pair and gains a >=44px touch target below the breakpoint.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/components/mobile/TapPopover.dom.test.tsx && npx eslint src/components/InfoBubble.tsx src/components/FilterPopover.tsx</automated>
  </verify>
  <acceptance_criteria>
    - InfoBubble opens on tap below the breakpoint via useTipBubble show()/hide(); desktop hover/focus handlers unchanged in the diff
    - FilterPopover renders through TapPopover below the breakpoint; its filter state logic untouched; desktop branch unchanged
    - Both triggers are >=44px targets below the breakpoint; eslint house rules clean
  </acceptance_criteria>
  <done>Info bubbles and the container filter work by tap on a phone; desktop users see no difference.</done>
</task>

<task type="auto">
  <name>Task 3: ColorPickerPopover adoption + tap-popover e2e</name>
  <files>web/src/components/ColorPickerPopover.tsx, web/e2e/tap-popovers.spec.ts</files>
  <read_first>
    web/src/components/ColorPickerPopover.tsx (WHOLE file — trigger at 386-394, portal + positioned panel, drag surface touch handlers at 361-362)
    web/e2e/mobile-shell.spec.ts (MOBILE_PROJECTS pattern + bootWithoutServerLook route-abort)
    .planning/phases/06-maquette-screens/06-RESEARCH.md Pattern 5 + Validation Architecture (PRIM-02 row)
  </read_first>
  <action>
    ColorPickerPopover: same adoption shape as FilterPopover — below the breakpoint the popover panel (the picker surface incl. its existing touch drag handlers, which stay untouched) presents through TapPopover for anchored positioning + dismissal/focus parity; desktop unchanged.

    e2e/tap-popovers.spec.ts scoped to the two MOBILE_PROJECTS: (1) an InfoBubble control opens its bubble on tap and dismisses on outside-tap; (2) Escape dismisses an open popover; (3) focus returns to the trigger after dismissal (assert document.activeElement); (4) the FilterPopover opens on tap with its panel visible inside the viewport (no clipping at the 360px edge). Desktop-1280 project: one assertion that the InfoBubble still opens on hover (desktop behavior intact) — add it as a separate desktop-project describe in this spec.
  </action>
  <verify>
    <automated>cd web && npx playwright test e2e/tap-popovers.spec.ts --project=mobile-android --project=mobile-iphone --project=desktop-1280</automated>
  </verify>
  <acceptance_criteria>
    - ColorPickerPopover presents through TapPopover below the breakpoint; its drag surface handlers untouched
    - e2e passes on both touch projects: tap-open, outside-tap dismiss, Escape dismiss, focus restore, no 360px clipping
    - Desktop-1280 asserts hover-open still works
  </acceptance_criteria>
  <done>All three hover-dependent affordances have a tap path below the breakpoint, pinned by e2e, with desktop hover verified intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tap-outside propagation | A dismissal layer must consume the tap or it leaks through to background controls (mis-trigger risk) |
| popover content | Filter/color state and translated tips (non-sensitive operational metadata) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03 | Tampering (tap-through to background controls) | TapPopover backdrop | medium | mitigate | The transparent backdrop is a real event-consuming full-viewport layer — outside taps land on it, never on content beneath (dom test asserts the background handler does not fire) |
| T-06-04 | Information disclosure (focus-based) | TapPopover focus handling | low | accept | Content is non-sensitive metadata (tips, filters, palette); D-09's light focus handling (in + restore, no trap) is the sanctioned posture — no sensitive data is exposed |
| T-06-05 | Tampering (XSS) | Translated popover strings | medium | mitigate | React escaping + t() pipeline only; no dangerouslySetInnerHTML |
| T-06-SC | Tampering (supply chain) | npm installs | low | accept | Zero package installs this phase (locked) |
</threat_model>

<verification>
- `cd web && npx vitest run src/components/mobile/TapPopover.dom.test.tsx src/lib/bubblePosition.test.ts` green
- `cd web && npx playwright test e2e/tap-popovers.spec.ts --project=mobile-android --project=mobile-iphone --project=desktop-1280` green
- `cd web && npx eslint src/components/mobile/TapPopover.tsx src/components/InfoBubble.tsx src/components/FilterPopover.tsx src/components/ColorPickerPopover.tsx` clean
- `cd web && npm run build` green before plan close (web/dist refresh happens at the phase gate plan 06-07)
</verification>

<success_criteria>
- InfoBubble, FilterPopover, and ColorPickerPopover all open by tap below the breakpoint, dismiss by outside-tap/Escape/re-tap, and restore focus to their triggers
- Desktop hover behavior is byte-identical (hover: still hover-gated; desktop e2e assertion green)
- One positioning maths (computeBubblePosition), no focus trap, no new dependencies
</success_criteria>

<specless_note>
Planner resolution of the fallback coverage report: PRIM-02's "unclassified" probe row is resolved by the explicit truths above sourced from D-09 + the UI-SPEC PRIM-02 contract (anchored, dismissible, light focus, no trap). UI-SPEC "UI Considerations" E7 rows lift here: overflow (explicit — popovers flip at viewport edges via computeBubblePosition), error (explicit — popover content arrives from the host surface, so host error rows apply), partial/long-text backstops remain check-style statements owned by the host surfaces. Nothing dropped.
</specless_note>

<output>
Create `.planning/phases/06-maquette-screens/06-03-SUMMARY.md` when done
</output>
