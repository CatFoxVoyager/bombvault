---
phase: 06-maquette-screens
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/mobile/BottomSheet.tsx
  - web/src/components/mobile/BottomSheet.dom.test.tsx
  - web/src/components/mobile/ConfirmSheet.tsx
  - web/src/components/mobile/ConfirmSheet.dom.test.tsx
  - web/src/lib/useConfirm.tsx
  - web/src/app/Layout.tsx
autonomous: true
requirements: [PRIM-03]
user_setup: []

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "BottomSheet gains an additive fullHeight prop switching the panel from max-h-[85dvh] (line 194) to h-dvh; every existing consumer (MoreSheet) and the existing BottomSheet.dom.test.tsx pass unchanged with the default"
    - "BottomSheet gains an optional footer slot: a flex-none bottom row inside the panel after the scroll body, bg-carbon-sidebar chrome language, padded by var(--safe-area-bottom); absent footer renders exactly today's panel"
    - "Absorbed 05-UI-REVIEW findings at the shared primitive: sheet side padding becomes inset-clamped max(1rem, var(--safe-area-right)) / max(1rem, var(--safe-area-left)) (was fixed px-4 at lines 199/231 — finding 1), and the close button hit box grows to >=44px (was p-2 ~40px at line 222 — finding 6); pb-[var(--safe-area-bottom)] unchanged"
    - "Below the breakpoint every useConfirm confirmation renders as a fail-tone bottom sheet: panel surface from statusFail tokens with a statusFailBorder hairline, the consequence-naming message unchanged, outcome-naming confirm button carrying tone danger (the sanctioned status-on-control exception); at >=48rem the desktop ConfirmDialog renders byte-identically — the swap branches on useIsDesktop inside the existing portal (D-07)"
    - "No destructive control is default-focused: the ConfirmSheet keeps BottomSheet's capture-then-focus-close open behavior (close = the safe outcome); Escape, scrim tap, and the close button ALL settle cancel-safe; the destructive confirm only fires on its own activation"
    - "The useConfirm promise API (confirm / confirmDialog / settle semantics, lines 94-170) is untouched — every existing call site (BackupButton stop-ack, reset-selection, remove-set, restore guards) inherits the mobile sheet with ZERO per-site changes, and no confirm-in-confirm chain is introduced (one sheet, one decision)"
    - "Layout's scroller main padding becomes p-4 md:p-6 (desktop keeps today's p-6 by construction); no other Layout chrome change"
  artifacts:
    - path: "web/src/components/mobile/BottomSheet.tsx"
      provides: "additive fullHeight prop + footer slot + >=44px close hit box + inset-clamped side padding; the shell for RunDetailSheet (06-04) and ConfirmSheet"
      contains: "fullHeight"
    - path: "web/src/components/mobile/ConfirmSheet.tsx"
      provides: "PRIM-03 mobile presentation half: fail-tone BottomSheet body with consequence copy, outcome-naming danger button, safe-default focus, cancel-safe dismissal"
      contains: "ConfirmSheet"
    - path: "web/src/components/mobile/ConfirmSheet.dom.test.tsx"
      provides: "jsdom tests: fail-tone surface, destructive-not-default-focused, all dismissal paths settle cancel-safe, desktop branch renders ConfirmDialog"
      contains: "ConfirmSheet"
    - path: "web/src/lib/useConfirm.tsx"
      provides: "the presentation swap point: portal branches on useIsDesktop between ConfirmDialog (desktop, unchanged) and ConfirmSheet (below breakpoint); promise API untouched"
      contains: "useIsDesktop"
    - path: "web/src/app/Layout.tsx"
      provides: "responsive scroller rhythm: main p-4 md:p-6 (UI-SPEC per-page responsive rhythm, Layout half)"
      contains: "p-4 md:p-6"
  key_links:
    - from: "web/src/lib/useConfirm.tsx portal (lines 155-170)"
      to: "web/src/components/mobile/ConfirmSheet.tsx"
      via: "tone already defaults to fail at the portal; the component inside branches on useIsDesktop — desktop ConfirmDialog untouched, mobile sheet inherited by every call site"
      pattern: "useIsDesktop"
    - from: "web/src/components/mobile/ConfirmSheet.tsx"
      to: "web/src/components/mobile/BottomSheet.tsx"
      via: "reuses the FOCUSABLE_SELECTOR Tab trap, capture-then-restore focus, and the three close paths — never a second modal mechanism (house anti-pattern)"
      pattern: "FOCUSABLE_SELECTOR"
    - from: "web/src/app/Layout.tsx main"
      to: "sticky action bars (plans 06-05, 06-06)"
      via: "main#bv-main stays the overflow-y-auto scroller; p-4 md:p-6 tightens the mobile gutter the sticky bars span"
      pattern: "bv-main"
  prohibitions:
    - statement: "Desktop ConfirmDialog rendering and behavior above 48rem must not change — the existing desktop-untouched e2e loop and confirm flows keep passing"
      status: enforced
      verification: explicit
    - statement: "No confirm-in-confirm modal chains (REQUIREMENTS.md Out of Scope) — one sheet, one decision"
      status: enforced
      verification: backstop
    - statement: "The useConfirm promise API signature is not altered (call sites compile and their existing tests pass unchanged)"
      status: enforced
      verification: explicit
    - statement: "No new npm dependencies (locked presentation-only milestone)"
      status: enforced
      verification: backstop

---

<objective>
Extend the Phase 5 BottomSheet with the three additive capabilities Phase 6 needs (full-height panel for run detail, footer slot for action rows, plus the two absorbed 05-UI-REVIEW fixes: >=44px close button and inset-clamped side padding), and swap the confirmation PRESENTATION below the breakpoint to a fail-tone sheet inside the ONE useConfirm implementation (D-07 / PRIM-03) — every existing confirm call site inherits the mobile sheet with zero per-site changes. Plus the Layout scroller rhythm (main p-4 md:p-6).

Purpose: PRIM-03 is a presentation swap, not a second confirm system — the promise API and desktop dialog stay byte-identical while BackupButton, restore guards, reset-selection, and remove-set all gain consequence-naming fail-tone sheets on a phone. The BottomSheet extensions unblock 06-04 (run detail) and 06-05/06-06 (sticky bars render against the tightened gutter).

Output: extended BottomSheet, new ConfirmSheet + dom tests, useConfirm presentation branch, Layout responsive padding.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-maquette-screens/06-CONTEXT.md
@.planning/phases/06-maquette-screens/06-RESEARCH.md
@.planning/phases/06-maquette-screens/06-PATTERNS.md
@.planning/phases/06-maquette-screens/06-UI-SPEC.md
@.planning/phases/05-mobile-shell-navigation-foundation/05-UI-REVIEW.md
@web/src/components/mobile/BottomSheet.tsx
@web/src/lib/useConfirm.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: BottomSheet additive extensions — fullHeight, footer slot, >=44px close, inset-clamped padding</name>
  <files>web/src/components/mobile/BottomSheet.tsx, web/src/components/mobile/BottomSheet.dom.test.tsx</files>
  <read_first>
    web/src/components/mobile/BottomSheet.tsx (WHOLE file — panel classes at 194, header padding 199, close button 222, body padding 231, open-effect capture-then-focus 100-112, FOCUSABLE_SELECTOR trap)
    web/src/components/mobile/MoreSheet.tsx (the existing consumer that must stay unaffected)
    web/src/components/mobile/BottomSheet.dom.test.tsx (existing assertions to keep green)
    .planning/phases/05-mobile-shell-navigation-foundation/05-UI-REVIEW.md findings 1 and 6 (the absorbed defects)
    .planning/phases/06-maquette-screens/06-UI-SPEC.md SCRN-05 section (footer slot + full-height contract) and the Spacing Scale exceptions table
  </read_first>
  <action>
    Additive props only, default behavior unchanged: (1) `fullHeight?: boolean` — when true the panel renders h-dvh instead of max-h-[85dvh] (D-05 full-screen run-detail variant); (2) `footer?: ReactNode` — when present, render a flex-none row AFTER the scroll body with bg-carbon-sidebar, border-t border-carbon-border, and padding-bottom from var(--safe-area-bottom); the scroll body keeps min-h-0 flex-1 so the footer never scrolls away. Absorb the 05-UI-REVIEW findings in the SAME edit since PRIM-03/SCRN-05 reuse is blocked without them: side padding of header AND body becomes inset-clamped (max(1rem, var(--safe-area-right)) on the start side and max(1rem, var(--safe-area-left)) on the end side — use inline style or arbitrary-value Tailwind per the existing px-4 sites at lines 199/231), and the close button grows from p-2 to a >=44px hit box (e.g. h-11 w-11 with the icon centered) while keeping its aria-label t(common.close) and hover/active treatments. Update BottomSheet.dom.test.tsx additively: close-button hit box >=44, footer slot rendering + safe-area padding, fullHeight panel class, and keep every existing assertion passing (MoreSheet unaffected).
  </action>
  <verify>
    <automated>cd web && npx vitest run src/components/mobile/BottomSheet.dom.test.tsx src/components/mobile/MoreSheet.dom.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - BottomSheet accepts fullHeight and footer; with neither set, the rendered panel matches today's classes and the existing dom tests pass without assertion edits
    - The dom tests assert: fullHeight switches the panel to h-dvh; footer renders after the scroll body with bg-carbon-sidebar and var(--safe-area-bottom) padding; close button hit box >=44px; side padding carries the max(1rem, var(--safe-area-*)) clamp
  </acceptance_criteria>
  <done>The sheet primitive carries every capability Phase 6 sheets need, with the two open 05-UI-REVIEW defects fixed once at the primitive and zero regressions for MoreSheet.</done>
</task>

<task type="auto">
  <name>Task 2: ConfirmSheet + useConfirm presentation swap (D-07 / PRIM-03)</name>
  <files>web/src/components/mobile/ConfirmSheet.tsx, web/src/components/mobile/ConfirmSheet.dom.test.tsx, web/src/lib/useConfirm.tsx</files>
  <read_first>
    web/src/lib/useConfirm.tsx (WHOLE file — promise API confirm/settle 94-113, Escape/Tab-trap effect 118-149, portal + tone default 155-170)
    web/src/components/mobile/BottomSheet.tsx (FOCUSABLE_SELECTOR trap, capture-then-focus close, three close paths)
    web/src/components/ConfirmDialog.tsx (the desktop half that must stay byte-identical — props, tone=danger button treatment, title Badge heading)
    .planning/phases/06-maquette-screens/06-RESEARCH.md Pattern 6 + Pitfall 5
    .planning/phases/06-maquette-screens/06-UI-SPEC.md PRIM-03 section (surface, copy, buttons, focus rules)
  </read_first>
  <action>
    Per D-07: keep useConfirm's promise API, portal, Escape handling, and trigger capture/restore EXACTLY as they are. The only change: the component rendered inside the portal branches on useIsDesktop — desktop renders ConfirmDialog unchanged; below the breakpoint render the new ConfirmSheet. ConfirmSheet wraps a BottomSheet (default height) whose panel/body carry the fail-tone surface: statusFail background token with a statusFailBorder hairline per UI-SPEC; title keeps the heading Badge treatment (reuse t(confirmDialog.title) as today); message = pending.message verbatim (consequence-naming copy is already written by call sites); confirm button = pending.confirmLabel with tone danger (the sanctioned status-on-control exception), cancel button = pending.cancelLabel / common.cancel as today. Focus rule (Pitfall 5): keep BottomSheet's existing open effect (captures the trigger, focuses the close button — close = the safe outcome); never autoFocus the destructive control. Every dismissal path — Escape, scrim tap (target === currentTarget), close button, Cancel — resolves settle(false) cancel-safe; only the destructive button settles(true). No confirm-in-confirm chains: ConfirmSheet must not open another modal. Do NOT edit any call site (BackupButton, restore guards, FoldersEditor reset confirm, Files delete-set confirm) — inheriting unchanged is the point of D-07 and is the acceptance test.

    ConfirmSheet.dom.test.tsx: render useConfirm below the breakpoint (mock useIsDesktop false via the existing jsdom matchMedia stub) and assert (1) the sheet renders with the fail-tone surface tokens and the danger-toned confirm button; (2) initial focus is NOT on the destructive control (it sits on the close control); (3) Escape, scrim click, and close each settle cancel-safe (promise resolves false); (4) activating the destructive button settles true; (5) with useIsDesktop true the portal renders ConfirmDialog instead.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/components/mobile/ConfirmSheet.dom.test.tsx src/lib/useConfirm.test.tsx src/components/BackupButton.dom.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - useConfirm.tsx branches the portal component on useIsDesktop; the confirm/confirmDialog return shape is unchanged (existing call-site tests pass untouched)
    - ConfirmSheet.dom.test.tsx passes: fail-tone surface, danger confirm label, destructive never default-focused, all dismissal paths cancel-safe, desktop branch renders ConfirmDialog
    - No call-site file is modified by this task (BackupButton.tsx and restore guards appear in no diff)
  </acceptance_criteria>
  <done>Every destructive confirmation in the app presents as a fail-tone bottom sheet on a phone and as today's dialog on desktop, from ONE confirm implementation and zero per-site changes.</done>
</task>

<task type="auto">
  <name>Task 3: Layout scroller rhythm — main p-4 md:p-6</name>
  <files>web/src/app/Layout.tsx</files>
  <read_first>
    web/src/app/Layout.tsx lines 280-310 (the main#bv-main scroller and its load-bearing flex/scroll comments — preserve every comment)
    .planning/phases/06-maquette-screens/06-UI-SPEC.md Spacing Scale section (per-page responsive rhythm lands here: main becomes p-4 md:p-6)
    web/e2e/desktop-untouched.spec.ts (the >=48rem guard this must not break)
  </read_first>
  <action>
    Change the main#bv-main className from p-6 to p-4 md:p-6 — the desktop value is today's value, so >=48rem renders identically by construction. Touch nothing else in Layout: the h-dvh shell, visualViewport keyboard mechanism, chrome switch, and bv-main id comments are load-bearing. (Page-root rhythm — gap-6 md:gap-10 on the three maquette pages — lands with each page's own plan: 06-05 Containers/Files, 06-06 Dashboard.)
  </action>
  <verify>
    <automated>cd web && npx vitest run && npx playwright test e2e/desktop-untouched.spec.ts --project=desktop-1280</automated>
  </verify>
  <acceptance_criteria>
    - Layout.tsx main element carries p-4 md:p-6
    - The desktop-untouched e2e loop passes at desktop-1280 (>=48rem layout unchanged)
    - No other Layout.tsx behavior is modified (keyboard mechanism, chrome switch, id, comments intact)
  </acceptance_criteria>
  <done>Mobile gains a tighter page gutter while the desktop scroller renders byte-identically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user tap → destructive outcome | The confirm sheet is the last guard before stop/restart/reset actions cross to the API |
| focus/AT surface | Sheet focus management determines which control a screen-reader or keyboard user activates first |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02 | Tampering (accidental destructive activation) | ConfirmSheet destructive button | high | mitigate | Fail-tone surface + consequence-naming copy + outcome-naming button; destructive control NEVER default-focused (safe close control takes initial focus); every non-button dismissal path settles cancel-safe; desktop keeps the identical ConfirmDialog guard |
| T-06-03 | Tampering (tap-through) | BottomSheet scrim | medium | mitigate | Scrim remains a real event-consuming layer with the target === currentTarget close guard (Phase 5 aria-hidden sibling pattern preserved through the padding/fullHeight edits) |
| T-06-04 | Information disclosure | Sheet content vs cutouts | low | accept | Inset-clamped side padding (finding 1) addresses landscape cutout overlap; content is non-sensitive operational metadata |
| T-06-SC | Tampering (supply chain) | npm installs | low | accept | Zero package installs this phase (locked) |
</threat_model>

<verification>
- `cd web && npx vitest run src/components/mobile/` green (BottomSheet + ConfirmSheet + MoreSheet suites)
- `cd web && npx vitest run src/lib/useConfirm.test.tsx src/components/BackupButton.dom.test.tsx` green (call sites inherit unchanged)
- `cd web && npx playwright test e2e/desktop-untouched.spec.ts --project=desktop-1280` green
- `cd web && npx eslint src/components/mobile/ src/lib/useConfirm.tsx src/app/Layout.tsx` clean
</verification>

<success_criteria>
- A destructive confirm on a phone opens a fail-tone bottom sheet whose destructive button is never the default focus and whose every dismissal is cancel-safe; on desktop nothing changed
- BottomSheet's fullHeight + footer capabilities exist without breaking MoreSheet, unblocking 06-04 (run detail) and the sticky bars
- The two absorbed 05-UI-REVIEW findings (side insets, close hit box) are fixed once at the primitive
</success_criteria>

<specless_note>
Planner resolution of the fallback coverage report: PRIM-03's "unclassified" probe row is resolved by the explicit truths above (fail-tone presentation, safe-default focus, cancel-safe dismissal, desktop byte-identical) sourced from D-07 and the UI-SPEC PRIM-03 contract — not silently dropped. UI-SPEC "UI Considerations" rows for E8 (confirm sheet: loading = trigger's existing busy state; error = backend text verbatim; long-text = outcome-naming buttons wrap never truncate; zero-one-many = one sheet per decision) lift into these truths and the 06-05/06-06 screen truths. The prohibition against confirm-in-confirm chains is carried in must_haves.prohibitions (descriptor-less).
</specless_note>

<output>
Create `.planning/phases/06-maquette-screens/06-02-SUMMARY.md` when done
</output>
