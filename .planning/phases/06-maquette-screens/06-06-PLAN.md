---
phase: 06-maquette-screens
plan: 06
type: execute
wave: 4
depends_on: ["06-02", "06-04", "06-05"]
files_modified:
  - web/src/pages/Dashboard.tsx
  - web/src/lib/i18n.ts
  - web/src/lib/locales/
  - web/e2e/home-trigger.spec.ts
autonomous: true
requirements: [SCRN-01, FLOW-03]
user_setup: []

estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The mobile Dashboard (max-md: restructure; the desktop grid above md: stays byte-identical) presents, in order: an identity header (app wordmark + the instance facts Dashboard already fetches via getSettings/getStatus/getStats — no new endpoints, 12px meta line under the 20px heading), a Next run card (bg-carbon-surface rounded-card p-4; accentSoft icon backdrop; schedule name 14px/600 + when/what/mode 12px meta; accentText time chip — data from the existing getScheduleNext SummaryTier derivation, Dashboard.tsx:2153-2163), a Recent runs card (top 4 runs, one >=44px row each: four-status Badge with text label + target name + relative time and volume meta; tapping a row opens the 06-04 RunDetailSheet for that run), and a Repository health card (repo stats line + offsite-copy age as an offsite-blue chip in the OffsiteIndicator language — the --status-*-offsite tokens are the offsite DOMAIN identity, never a fifth status hue; four-status language for repo state)"
    - "Glanceability (SCRN-01 success criterion): identity + next run + recent runs + repo health fit one phone screenful without scrolling past the trigger — cards p-4, gap-4 between cards, gap-2 inside, page root gap-6 md:gap-10"
    - "The thumb-zone trigger (D-06) is the 06-05 StickyActionBar as the last direct child of the page column: full-width New backup filled button (bg-accent text-accentContrast, >=44px, new key home.newBackup en \"New backup\" / de \"Neues Backup\") — the ONE solid-accent control on the surface; it is NOT a floating FAB (PLAT-01 is Phase 7) and never position:fixed"
    - "Trigger semantics (FLOW-03 / D-08): press opens the home.newBackupConfirm consequence sheet through the EXISTING useConfirm path (D-07 fail-tone presentation below the breakpoint; confirm button label = home.newBackup as the outcome-naming label; cancel = existing common.cancel) then calls the existing backupEverythingNow() with useBackupWatch baseline ids seeded BEFORE firing; on correlation the 06-04 RunDetailSheet opens for the new run (deep-link into the live run); the outcome is never read from the POST response"
    - "Fresh-install/empty treatments are the EXISTING desktop ones restructured into cards (the isFreshInstall gating and existing dashboard copy — no new empty-state copy authored); failed runs in recent runs show the scrubbed runReason text"
    - "home.newBackup + home.newBackupConfirm exist in en, de, and all 40 locale modules (parity + orphans green)"
  artifacts:
    - path: "web/src/pages/Dashboard.tsx"
      provides: "SCRN-01 mobile surface: identity header, next-run card, tappable recent-runs rows, repo-health card with offsite-blue age chip, sticky New-backup zone"
      contains: "home.newBackup"
    - path: "web/e2e/home-trigger.spec.ts"
      provides: "mobile e2e: glanceability (trigger within the first screenful), trigger -> confirm sheet -> run sheet deep-link, recent-run row tap; desktop project asserts the unchanged dashboard grid + no sticky bar"
      contains: "home-trigger"
    - path: "web/src/lib/i18n.ts"
      provides: "home.newBackup + home.newBackupConfirm in en + de + all 40 locale modules"
      contains: "home.newBackup"
  key_links:
    - from: "web/src/pages/Dashboard.tsx (sticky trigger)"
      to: "web/src/lib/backupWatch.ts + backupEverythingNow (api.ts:2235)"
      via: "FLOW-03 verbatim: async start, baseline-id correlation seeded before firing, RunDetailSheet opens on correlation — no new trigger path"
      pattern: "backupEverythingNow"
    - from: "web/src/pages/Dashboard.tsx (recent-run rows)"
      to: "web/src/components/mobile/RunDetailSheet.tsx"
      via: "tap opens the sheet for that run — the deep-link target contract from 06-04"
      pattern: "RunDetailSheet"
    - from: "web/src/pages/Dashboard.tsx (trigger zone)"
      to: "web/src/components/mobile/StickyActionBar.tsx (06-05)"
      via: "the shared sticky-in-flow bar — one primitive, two consumers (tree Save bar, Home trigger)"
      pattern: "StickyActionBar"
    - from: "Dashboard repo-health card"
      to: "web/src/components/OffsiteIndicator.tsx language + --status-*-offsite tokens"
      via: "offsite-copy age renders in offsite blue as DOMAIN identity (SCRN-01) — never a status hue"
      pattern: "offsite"
  prohibitions:
    - statement: "No floating FAB and no position:fixed trigger zone (D-06 locks the sticky-in-flow bar; the FAB expression is PLAT-01 / Phase 7)"
      status: enforced
      verification: backstop
    - statement: "The backup outcome is never read from the POST response — {ok:true,started:true} then useBackupWatch correlation only (house rule)"
      status: enforced
      verification: backstop
    - statement: "No new endpoints and no edits to api.ts / router.tsx / progress.ts / internal/** (frozen); identity/next-run/repo-health data comes from the fetches Dashboard already makes"
      status: enforced
      verification: explicit
    - statement: "Accent is confined to the sanctioned list — the New backup button, next-run card tints, focus rings; status colors live on badges only and offsite blue is domain identity, not a status"
      status: enforced
      verification: backstop
    - statement: "The desktop dashboard grid above md: renders byte-identically (desktop-untouched e2e stays green)"
      status: enforced
      verification: explicit

---

<objective>
Operationalize SCRN-01: the glanceable mobile Home — instance identity, next run, recent runs with four-status badges, repository health with the offsite-blue copy-age chip, all in one screenful — plus the D-06 thumb-zone New-backup trigger whose confirm sheet flows into backupEverythingNow and deep-links into the 06-04 RunDetailSheet.

Purpose: Home is the phone's landing surface and the phase's FLOW-03 capstone — the primary trace (tap trigger -> consequence sheet -> async start -> correlation -> live run sheet) exercises every primitive the phase built. It runs after 06-04/06-05 because it consumes the run sheet and the shared sticky bar.

Output: restructured Dashboard mobile blocks, home.newBackup/home.newBackupConfirm keys, home-trigger e2e.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-maquette-screens/06-CONTEXT.md
@.planning/phases/06-maquette-screens/06-RESEARCH.md
@.planning/phases/06-maquette-screens/06-PATTERNS.md
@.planning/phases/06-maquette-screens/06-UI-SPEC.md
@web/src/pages/Dashboard.tsx
@web/src/components/OffsiteIndicator.tsx
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Mobile Dashboard block order — identity, next run, tappable recent runs, repo health</name>
  <files>web/src/pages/Dashboard.tsx</files>
  <read_first>
    web/src/pages/Dashboard.tsx — Dashboard() at line 2217 (existing fetches: getSettings/getStatus/getStats/listRuns + the getScheduleNext SummaryTier derivation at 2153-2163), StatCardsRow/SummaryTier structure, RunsCard at 1193 (rows + statusLabel/statusTone + runReason usage), the isFreshInstall gating and FreshInstallNudge, PAGE_SHELL usage
    web/src/components/OffsiteIndicator.tsx (the offsite-blue chip language + token usage to reuse)
    web/src/components/mobile/RunDetailSheet.tsx (06-04 — the tap target)
    .planning/phases/06-maquette-screens/06-UI-SPEC.md SCRN-01 section (block order, density numbers, offsite-blue rule) + Color section (accent reserved list)
  </read_first>
  <action>
    Restructure Dashboard BELOW the breakpoint only (max-md: variants; every md: and up class untouched so the desktop grid renders byte-identically). Mobile block order per UI-SPEC SCRN-01: (1) identity header — app wordmark + instance facts already in Dashboard's fetched state (12px meta line under the 20px heading); (2) Next run card — bg-carbon-surface rounded-card p-4, accentSoft icon backdrop, schedule name 14px/600, when/what/mode meta 12px from the existing getScheduleNext derivation (Dashboard.tsx:2153-2163), time chip in accentText; (3) Recent runs card — top 4 runs, each a >=44px row: Badge tone={statusTone(run.status)} with statusLabel text (never color alone), runKindLabel + runTargetText, relative time + humanBytes meta; tapping a row opens the RunDetailSheet (local open-run state hosting the 06-04 component — no route); (4) Repository health card — existing repo stats + offsite-copy age chip in offsite blue via the --status-*-offsite tokens / OffsiteIndicator language (domain identity, never a fifth status hue). Density: cards p-4, gap-4 between, gap-2 inside, page root gap-6 md:gap-10 — sized so the four blocks + trigger fit one phone screenful (UI-SPEC glanceability). Keep the existing isFreshInstall/empty treatments (restructured into the card language, no new copy); failed runs render runReason scrubbed text. Desktop markup untouched above md:.
  </action>
  <verify>
    <automated>cd web && npx vitest run && npx playwright test e2e/desktop-untouched.spec.ts --project=desktop-1280 --project=desktop-768</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard's mobile block order renders identity -> next run -> recent runs (top 4, >=44px rows) -> repo health, per the SCRN-01 contract
    - Recent-run rows open RunDetailSheet via component-local state (no router change)
    - The offsite-copy age chip renders with the offsite token family (never a status hue class)
    - Full vitest suite green; desktop-untouched e2e green at both desktop projects (grid byte-identical)
  </acceptance_criteria>
  <done>Home reads glanceably on a phone from data the dashboard already fetches, and a tap on any recent run opens the run sheet.</done>
</task>

<task type="auto">
  <name>Task 2: Thumb-zone New-backup trigger — consequence sheet, everything-trigger, deep-link</name>
  <files>web/src/pages/Dashboard.tsx, web/src/lib/i18n.ts, web/src/lib/locales/</files>
  <read_first>
    web/src/components/mobile/StickyActionBar.tsx (06-05 — the shared bar)
    web/src/components/BackupButton.tsx (the confirm-then-fire + ack contract shape to reuse)
    web/src/lib/backupWatch.ts lines 218-289 (baseline seeding before firing; correlation)
    web/src/lib/api.ts line 2235 (backupEverythingNow)
    web/src/components/mobile/RunDetailSheet.tsx (06-04 — the deep-link target)
    .planning/phases/06-maquette-screens/06-UI-SPEC.md Copywriting Contract (home.newBackupConfirm wording) + SCRN-01 trigger contract
    .planning/phases/06-maquette-screens/06-RESEARCH.md the primary trace + Pattern 6 (confirm swap already shipped in 06-02)
  </read_first>
  <action>
    Add home.newBackup (en "New backup" / de "Neues Backup") and home.newBackupConfirm (en "Start a backup of every enabled domain now? Containers are stopped and restarted. Restore keeps only what the next run saves." — consequence-naming per the UI-SPEC copy contract; confirm button label = home.newBackup outcome-naming; cancel = existing common.cancel) to en + de + all 40 locale modules in this commit.

    Per D-06: render the 06-05 StickyActionBar as the last direct child of the Dashboard page column below the breakpoint, containing the full-width New backup button — bg-accent text-accentContrast, >=44px, t(home.newBackup) — the ONE solid-accent control on the surface. Per D-08/FLOW-03: press -> the existing useConfirm confirm with home.newBackupConfirm (the 06-02 fail-tone sheet presents it below the breakpoint automatically — no per-site presentation code); on confirm, call backupEverythingNow() through the existing BackupButton semantics: baseline run ids seeded BEFORE firing (useBackupWatch), async {ok:true,started:true} never read for outcomes; on correlation open the RunDetailSheet for the new run (local state — deep-link into the live run). Button shows its busy state while the trigger is in flight. NOT a FAB, never position:fixed, sticky-in-flow only.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/lib/i18n.parity.test.ts src/lib/i18n.orphans.test.ts src/components/BackupButton.dom.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The sticky trigger renders below the breakpoint with the filled accent recipe and >=44px target; no FAB/fixed positioning anywhere in the diff
    - Press opens the home.newBackupConfirm sheet (D-07 presentation); confirm fires backupEverythingNow with baseline ids seeded before firing; correlation opens RunDetailSheet
    - home.newBackup + home.newBackupConfirm exist in en, de, and all 40 locale modules; parity + orphans green
  </acceptance_criteria>
  <done>The thumb-zone trigger starts an everything-backup through desktop-identical semantics and lands the user inside the live run sheet.</done>
</task>

<task type="auto">
  <name>Task 3: Home trigger e2e — glanceability, deep-link trace, desktop unchanged</name>
  <files>web/e2e/home-trigger.spec.ts</files>
  <read_first>
    web/e2e/mobile-shell.spec.ts (MOBILE_PROJECTS pattern, bootWithoutServerLook route-abort, fresh-DB boot)
    web/playwright.config.ts (projects; reuseExistingServer caveat)
    .planning/phases/06-maquette-screens/06-UI-SPEC.md Verification table (Screens row)
  </read_first>
  <action>
    e2e/home-trigger.spec.ts. Mobile projects: (1) glanceability — on the 360x800 viewport assert the New backup trigger is visible within the initial viewport (no scroll) and the four blocks (identity, next run, recent runs, repo health) render above or beside it in order; (2) the primary trace — tap New backup, assert the fail-tone confirm sheet appears with the consequence copy, tap the outcome-naming confirm, assert navigation into the RunDetailSheet for the newly correlated run (live progress visible); (3) tap a recent-run row -> RunDetailSheet opens for that run; (4) repo-health card shows the offsite-age chip. Desktop-1280/768 project: assert the dashboard grid is unchanged and NO sticky trigger bar / mobile block restructure leaks at >=48rem.
  </action>
  <verify>
    <automated>cd web && npx playwright test e2e/home-trigger.spec.ts --project=mobile-android --project=mobile-iphone --project=desktop-1280 --project=desktop-768</automated>
  </verify>
  <acceptance_criteria>
    - Mobile e2e proves glanceability (trigger visible without scrolling), the full trigger->confirm->run-sheet trace, and recent-run row tap
    - Desktop projects assert no mobile chrome leakage at >=48rem
  </acceptance_criteria>
  <done>SCRN-01 and FLOW-03 are pinned end-to-end by CI e2e on all four projects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trigger tap → stop/restart of containers | The everything-backup stops and restarts containers — the confirm sheet is the guard |
| dashboard data rendering | Instance facts, run history, and repo stats render from existing API state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02 | Tampering (accidental backup trigger) | New backup sticky button | high | mitigate | >=44px thumb-zone target with the consequence-aware home.newBackupConfirm sheet (D-07 fail-tone presentation, safe-default focus, outcome-naming button) before any POST; deep-link only after server correlation |
| T-06-01 | Tampering (stale run state) | Recent runs + live deep-link | medium | mitigate | 06-04's visibility gate + listRuns refetch reconciliation carries over to the Home surface wiring (consumers conditionally render on the gate per PRIM-04) |
| T-06-05 | Tampering (XSS) | Identity/meta lines | medium | mitigate | React escaping + t() only; instance strings are settings-derived and escaped like all rendered text |
| T-06-SC | Tampering (supply chain) | npm installs | low | accept | Zero package installs (locked) |
</threat_model>

<verification>
- `cd web && npx vitest run` green
- `cd web && npx vitest run src/lib/i18n.parity.test.ts src/lib/i18n.orphans.test.ts` green
- `cd web && npx playwright test e2e/home-trigger.spec.ts --project=mobile-android --project=mobile-iphone --project=desktop-1280 --project=desktop-768` green
- `git diff --name-only` shows no api.ts / router.tsx / progress.ts / internal/** entries
</verification>

<success_criteria>
- A phone user reads instance identity, next run, recent runs with four-status text badges, and repository health (offsite age in offsite blue) in one glance — and starts a backup from the thumb zone with a consequence-aware confirm that deep-links into the live run
- Triggering from Home is byte-identical BackupButton/everything-trigger semantics; desktop renders unchanged
</success_criteria>

<specless_note>
Planner resolution of the fallback coverage report: SCRN-01's "unclassified" probe row resolves to the explicit truths above (glanceable block order, one-screenful density, thumb-zone trigger, consequence-aware confirm, deep-link) sourced from the SCRN-01 requirement + D-06/D-07/D-08 and the UI-SPEC SCRN-01 contract — surfaced here, not dropped. FLOW-03's "unclassified" probe row resolves to the trigger-semantics truths (shared with 06-05). UI-SPEC "UI Considerations" E1 rows (empty = existing fresh-install treatments; loading = existing SummaryTier checking rows; partial/overflow backstops) lift into these truths + the e2e. Nothing dropped.
</specless_note>

<output>
Create `.planning/phases/06-maquette-screens/06-06-SUMMARY.md` when done
</output>
