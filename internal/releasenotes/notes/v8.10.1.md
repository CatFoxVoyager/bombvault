The page entrance no longer scales or tilts. **It did not fix the green flash.**

> **Correction, added after release.** This text said the flash was "fixed at its cause". The reporter re-tested on this build and "Wild" behaves as before, so the mechanism below was wrong: it was inferred from the code and never measured. [#228](https://github.com/junkerderprovinz/bombvault/issues/228) is open again. The change itself still stands on its own terms, one spatial dial instead of three.

## 🐛 Fixed

- **The page entrance no longer scales or tilts** ([#228](https://github.com/junkerderprovinz/bombvault/issues/228)). v8.10.0 moved the default down to "Subtle", where neither happens. That kept a new install away from the symptom and left it untouched for anybody who had chosen "Wild", because a changed default never reaches a setting you picked yourself.
- **What was believed at the time, and is not established.** The reasoning was that a page which scales or rotates cannot be drawn independently of its contents, so the browser redraws everything inside it while the cards in there animate too. It does not survive the arithmetic: `#38FF38` is rgb(56, 255, 56), red and blue exactly equal and green clipped, which is the shape of one wrong byte rather than of uninitialised memory. Whether that pixel is produced in the page or in the compositor cannot be decided from the source.
- **"Wild" is still clearly livelier than "Subtle"**: three times the travel, longer, on a curve that overshoots and settles. None of that asks the browser to redraw a page.
- **"Storm" lost the largest values in the file** (3 degrees, 90%). Since it began overriding the system's reduced-motion setting in v8.10.0, it is the one level that animates for somebody who asked for less movement, so it is the last place that should be redrawing a whole page.

## 🔍 Not verified here

- There is still no macOS machine with Firefox on this side. That the flash is gone follows from removing what caused it, and it is a conclusion rather than an observation.
